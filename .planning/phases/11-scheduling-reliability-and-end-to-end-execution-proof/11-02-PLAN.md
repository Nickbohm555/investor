---
phase: 11-scheduling-reliability-and-end-to-end-execution-proof
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/routes.py
  - app/workflows/engine.py
  - app/tools/alpaca.py
  - app/ops/dry_run.py
  - tests/api/test_execution_confirmation.py
  - tests/tools/test_alpaca_orders.py
  - tests/ops/test_dry_run.py
autonomous: true
requirements:
  - SC11-02
must_haves:
  truths:
    - "The same repo-local proof path can drive a scheduled run from trigger to approval to explicit execution confirmation without manual glue."
    - "Execution confirmation submits Alpaca orders through the repo-owned adapter instead of stopping at broker prestage."
    - "The dry-run JSON exposes submitted-order proof that can be checked automatically."
  artifacts:
    - path: "app/api/routes.py"
      provides: "Explicit execution-confirmation HTTP surface"
      contains: "/runs/{run_id}/execute"
    - path: "app/tools/alpaca.py"
      provides: "Order submission adapter"
      contains: "def submit_order"
    - path: "app/ops/dry_run.py"
      provides: "Scheduled-to-submitted proof output"
      contains: "execution_status"
    - path: "tests/api/test_execution_confirmation.py"
      provides: "Route-level confirmation coverage"
      contains: "test_execute_run_submits_orders_and_returns_submitted_status"
  key_links:
    - from: "app/api/routes.py"
      to: "app/workflows/engine.py"
      via: "POST /runs/{run_id}/execute"
      pattern: "advance_run\\(.*execution:confirm"
    - from: "app/workflows/engine.py"
      to: "app/tools/alpaca.py"
      via: "submit_order calls using broker artifact client_order_id"
      pattern: "submit_order\\("
    - from: "app/ops/dry_run.py"
      to: "tests/ops/test_dry_run.py"
      via: "JSON keys for execution proof"
      pattern: "execution_status|submitted_order_count|submitted_client_order_ids"
---

<objective>
Extend the existing repo-local operator proof path so the system does not stop at `broker_prestaged`, but instead performs the explicit confirmation and Alpaca submission step required for the full execution flow.

Purpose: Phase 11 must prove the real approved-execution path, not just the pre-submit draft stage from earlier phases.
Output: Execution-confirmation route coverage, Alpaca order submission adapter coverage, and dry-run proof output that includes submitted-order evidence.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-RESEARCH.md
@.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-VALIDATION.md
@.planning/phases/04-hitl-and-broker-prestage/04-03-SUMMARY.md
@app/api/routes.py
@app/workflows/engine.py
@app/tools/alpaca.py
@app/ops/dry_run.py
@app/services/broker_prestage.py
@app/services/handoff.py
@tests/ops/test_dry_run.py
@tests/integration/test_broker_prestage.py

<interfaces>
From app/api/routes.py:
```python
@router.post("/runs/trigger")
def trigger_run(request: Request) -> dict[str, str]:
    ...

@router.get("/approval/{token}")
def review_token(token: str, request: Request) -> dict:
    ...
```

From app/workflows/engine.py:
```python
def advance_run(self, *, run_id: str, event: str) -> dict:
    if event not in {"approval:approve", "approval:reject"}:
        raise ValueError(...)
```

From app/tools/alpaca.py:
```python
def get_account(self) -> dict: ...
def get_asset(self, symbol: str) -> dict: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing route, adapter, and dry-run tests for explicit execution confirmation</name>
  <files>tests/api/test_execution_confirmation.py, tests/tools/test_alpaca_orders.py, tests/ops/test_dry_run.py, app/api/routes.py, app/workflows/engine.py, app/tools/alpaca.py, app/ops/dry_run.py</files>
  <read_first>.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-RESEARCH.md, app/api/routes.py, app/workflows/engine.py, app/tools/alpaca.py, app/ops/dry_run.py, app/services/broker_prestage.py, app/services/handoff.py, tests/ops/test_dry_run.py, tests/integration/test_broker_prestage.py</read_first>
  <acceptance_criteria>
    - `rg -n "test_execute_run_submits_orders_and_returns_submitted_status|test_execute_run_rejects_non_prestaged_runs|test_execute_run_is_idempotent_for_already_submitted_runs|/runs/\\{run_id\\}/execute" tests/api/test_execution_confirmation.py` returns route tests and the exact execute path.
    - `rg -n "test_submit_order_posts_client_order_id_and_notional|/v2/orders|client_order_id|time_in_force" tests/tools/test_alpaca_orders.py` returns adapter tests for order submission payloads.
    - `rg -n "execution_status|submitted_order_count|submitted_client_order_ids" tests/ops/test_dry_run.py` returns the dry-run JSON proof assertions.
    - `python -m pytest tests/api/test_execution_confirmation.py tests/tools/test_alpaca_orders.py tests/ops/test_dry_run.py -q` fails before implementation because the execute route, submission adapter, and dry-run output do not exist yet.
  </acceptance_criteria>
  <action>Create a new `tests/api/test_execution_confirmation.py` file with failing tests named `test_execute_run_submits_orders_and_returns_submitted_status`, `test_execute_run_rejects_non_prestaged_runs`, and `test_execute_run_is_idempotent_for_already_submitted_runs`. The success-path test must require `POST /runs/{run_id}/execute` to return status code `200` and JSON keys `status`, `run_id`, and `submitted_order_count`, with `status == "submitted"` and `submitted_order_count >= 1`. The invalid-state test must require a `409` response with detail text containing `broker_prestaged`. The idempotency test must require a second POST to return `409` without increasing submitted-order count. Create `tests/tools/test_alpaca_orders.py` with a failing `test_submit_order_posts_client_order_id_and_notional` that requires `AlpacaClient.submit_order(...)` to POST to `/v2/orders` with exact keys `symbol`, `side`, `type`, `time_in_force`, `client_order_id`, and `notional`. Extend `tests/ops/test_dry_run.py` so the dry-run JSON must contain `execution_status == "submitted"`, `submitted_order_count >= 1`, and `submitted_client_order_ids`. Leave the implementation red in this task.</action>
  <verify>
    <automated>python -m pytest tests/api/test_execution_confirmation.py tests/tools/test_alpaca_orders.py tests/ops/test_dry_run.py -q</automated>
  </verify>
  <done>Failing tests exist for the explicit execution-confirmation route, Alpaca order submission adapter, and dry-run submission proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explicit execution confirmation, Alpaca submission, and dry-run submitted-order proof</name>
  <files>app/api/routes.py, app/workflows/engine.py, app/tools/alpaca.py, app/ops/dry_run.py, tests/api/test_execution_confirmation.py, tests/tools/test_alpaca_orders.py, tests/ops/test_dry_run.py</files>
  <read_first>app/api/routes.py, app/workflows/engine.py, app/tools/alpaca.py, app/ops/dry_run.py, tests/api/test_execution_confirmation.py, tests/tools/test_alpaca_orders.py, tests/ops/test_dry_run.py, app/services/broker_prestage.py, app/repositories/broker_artifacts.py, app/db/models.py</read_first>
  <acceptance_criteria>
    - `rg -n "@router.post\\(\"/runs/\\{run_id\\}/execute\"\\)|execution:confirm|submitted_order_count" app/api/routes.py app/workflows/engine.py` returns the new route and engine event wiring.
    - `rg -n "def submit_order\\(|/v2/orders|client_order_id" app/tools/alpaca.py` returns the submission adapter implementation.
    - `rg -n "execution_status|submitted_order_count|submitted_client_order_ids|/runs/.*/execute" app/ops/dry_run.py tests/ops/test_dry_run.py` returns the updated dry-run execution step and JSON keys.
    - `python -m pytest tests/api/test_execution_confirmation.py tests/tools/test_alpaca_orders.py tests/ops/test_dry_run.py -q` exits successfully.
  </acceptance_criteria>
  <action>Implement `POST /runs/{run_id}/execute` in `app/api/routes.py` as the explicit second-step confirmation surface. It must call `request.app.state.workflow_engine.advance_run(run_id=run_id, event="execution:confirm")`, return the engine result keys `status`, `run_id`, and `submitted_order_count`, and map invalid lifecycle transitions to `409`. Extend `WorkflowEngine.advance_run(...)` so it accepts `execution:confirm` in addition to approval events. For that event, require the persisted run state to already be `broker_prestaged`, reuse `handoff["recommendations"]` and the deterministic `client_order_id` convention from broker prestage, call a new `AlpacaClient.submit_order(...)` once per recommendation, and persist `status="submitted"`, `current_step="submitted"`, plus a `submitted_orders` list and integer `submitted_order_count` in `state_payload`. Implement `AlpacaClient.submit_order(...)` in `app/tools/alpaca.py` as a `POST /v2/orders` wrapper that accepts exact keyword arguments `symbol`, `side`, `order_type`, `time_in_force`, `client_order_id`, `notional=None`, and `qty=None`; it must include `type=order_type` in the JSON body and return the parsed JSON response. Update `app/ops/dry_run.py` so after approval it calls `POST /runs/{run_id}/execute`, records `execution_status`, and prints `submitted_order_count` plus `submitted_client_order_ids` in the final JSON. Do not add a separate scheduler path or bypass the existing approval/prestage flow; extend the current repo-owned route and engine seam.</action>
  <verify>
    <automated>python -m pytest tests/api/test_execution_confirmation.py tests/tools/test_alpaca_orders.py tests/ops/test_dry_run.py -q</automated>
  </verify>
  <done>The repo-local proof path now includes an explicit execution-confirmation route that submits orders through the Alpaca adapter and exposes submitted-order evidence in the dry-run JSON.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/api/test_execution_confirmation.py tests/tools/test_alpaca_orders.py tests/ops/test_dry_run.py -q`. Then run `python -m app.ops.dry_run` and confirm the JSON includes `execution_status`, `submitted_order_count`, and `submitted_client_order_ids`.
</verification>

<success_criteria>
The dry-run operator proof no longer stops at prestage. It covers approval, explicit execution confirmation, and Alpaca submission through the repo-owned HTTP and adapter seams.
</success_criteria>

<output>
After completion, create `.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-scheduling-reliability-and-end-to-end-execution-proof-02-SUMMARY.md`
</output>

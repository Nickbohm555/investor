---
phase: 11-scheduling-reliability-and-end-to-end-execution-proof
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
  - 11-02
files_modified:
  - tests/integration/test_scheduled_submission_flow.py
  - tests/integration/test_broker_prestage.py
  - tests/api/test_execution_confirmation.py
  - app/api/routes.py
  - app/workflows/engine.py
  - app/ops/dry_run.py
autonomous: true
requirements:
  - SC11-03
must_haves:
  truths:
    - "Automated integration coverage proves the scheduled trigger, memo delivery, approval callback, explicit execution confirmation, and submission path together."
    - "The scheduled execution flow remains duplicate-safe and restart-safe while reaching submitted status."
    - "Phase 11 can be verified with one integration command that checks the true scheduled-to-submitted behavior."
  artifacts:
    - path: "tests/integration/test_scheduled_submission_flow.py"
      provides: "End-to-end scheduled execution proof"
      contains: "test_scheduled_trigger_to_submitted_order_flow"
    - path: "app/api/routes.py"
      provides: "Final route behavior after integration reconciliation"
      contains: "/runs/trigger/scheduled"
    - path: "app/workflows/engine.py"
      provides: "Restart-safe submitted-state transition"
      contains: "submitted_order_count"
  key_links:
    - from: "tests/integration/test_scheduled_submission_flow.py"
      to: "app/api/routes.py"
      via: "scheduled trigger, approval callback, and execute route"
      pattern: "/runs/trigger/scheduled|/approval/|/runs/.*/execute"
    - from: "app/workflows/engine.py"
      to: "tests/integration/test_scheduled_submission_flow.py"
      via: "persisted state assertions after restart"
      pattern: "submitted_orders|submitted_order_count|status=\"submitted\""
---

<objective>
Close Phase 11 with one deterministic integration proof that the repo-managed scheduled path reaches submitted orders without hidden operator glue or lifecycle regressions.

Purpose: The earlier plans add the config path and submission seam; this plan proves they work together under the same restart-safe and duplicate-safe constraints established in earlier phases.
Output: A new scheduled-to-submitted integration test file plus any small route/engine/dry-run fixes required to make that proof pass cleanly.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-RESEARCH.md
@.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-VALIDATION.md
@.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-01-PLAN.md
@.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-02-PLAN.md
@tests/integration/test_broker_prestage.py
@app/api/routes.py
@app/workflows/engine.py
@app/ops/dry_run.py

<interfaces>
From app/api/routes.py after plan 02:
```python
@router.post("/runs/trigger/scheduled")
def trigger_scheduled_run(...) -> dict[str, object]:
    ...

@router.post("/runs/{run_id}/execute")
def execute_run(run_id: str, request: Request) -> dict[str, object]:
    ...
```

From app/workflows/engine.py after plan 02:
```python
def advance_run(self, *, run_id: str, event: str) -> dict:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing scheduled-to-submitted integration proof</name>
  <files>tests/integration/test_scheduled_submission_flow.py, tests/integration/test_broker_prestage.py, tests/api/test_execution_confirmation.py, app/api/routes.py, app/workflows/engine.py</files>
  <read_first>.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-01-PLAN.md, .planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-02-PLAN.md, tests/integration/test_broker_prestage.py, tests/api/test_execution_confirmation.py, app/api/routes.py, app/workflows/engine.py</read_first>
  <acceptance_criteria>
    - `rg -n "test_scheduled_trigger_to_submitted_order_flow|test_duplicate_scheduled_trigger_does_not_resubmit_orders|/runs/trigger/scheduled|/runs/.*/execute|submitted_order_count" tests/integration/test_scheduled_submission_flow.py` returns the new end-to-end proof cases and exact routes.
    - `python -m pytest tests/integration/test_scheduled_submission_flow.py -q` fails before reconciliation because the full scheduled-to-submitted path is not yet proven end to end.
  </acceptance_criteria>
  <action>Create `tests/integration/test_scheduled_submission_flow.py` with at least two failing tests: `test_scheduled_trigger_to_submitted_order_flow` and `test_duplicate_scheduled_trigger_does_not_resubmit_orders`. Reuse the restart-safe dual-app pattern from `tests/integration/test_broker_prestage.py`: one app triggers `/runs/trigger/scheduled`, extract the approval URL from the sent memo, a second app handles `/approval/{token}`, and then call `POST /runs/{run_id}/execute`. The happy-path test must assert the final execute response is `{"status": "submitted", "run_id": run_id, "submitted_order_count": ...}` and that persisted run state contains `status == "submitted"`, `current_step == "submitted"`, and a non-empty `submitted_orders` list. The duplicate-schedule test must assert a second scheduled trigger for the same market-day window returns `"duplicate"` and does not increase submitted-order count. Keep the tests red until the implementation satisfies the full scheduled path.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_scheduled_submission_flow.py -q</automated>
  </verify>
  <done>A failing integration proof exists for scheduled trigger through submitted order behavior, including duplicate suppression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile lifecycle gaps until the scheduled-to-submitted integration proof passes</name>
  <files>app/api/routes.py, app/workflows/engine.py, app/ops/dry_run.py, tests/integration/test_scheduled_submission_flow.py, tests/integration/test_broker_prestage.py, tests/api/test_execution_confirmation.py</files>
  <read_first>app/api/routes.py, app/workflows/engine.py, app/ops/dry_run.py, tests/integration/test_scheduled_submission_flow.py, tests/integration/test_broker_prestage.py, tests/api/test_execution_confirmation.py</read_first>
  <acceptance_criteria>
    - `rg -n "submitted_order_count|submitted_orders|status\": \"submitted\"|result=duplicate" app/api/routes.py app/workflows/engine.py app/ops/dry_run.py tests/integration/test_scheduled_submission_flow.py` returns the persisted submitted-state and duplicate-path assertions.
    - `python -m pytest tests/integration/test_scheduled_submission_flow.py tests/api/test_execution_confirmation.py tests/ops/test_dry_run.py -q` exits successfully.
  </acceptance_criteria>
  <action>Use the new integration failures to make the smallest necessary adjustments across `app/api/routes.py`, `app/workflows/engine.py`, and `app/ops/dry_run.py`. Preserve the existing duplicate-safe scheduled route contract from Phase 3 and the explicit execution-confirmation seam from plan 02. If lifecycle values are inconsistent, normalize them so the submitted path always persists exact fields `status="submitted"`, `current_step="submitted"`, `submitted_orders`, and `submitted_order_count` before returning success. Keep the scheduled duplicate branch non-submitting and non-replaying by default. Do not introduce shell locking, a second scheduler, or test-only bypasses around approval or submission.</action>
  <verify>
    <automated>python -m pytest tests/integration/test_scheduled_submission_flow.py tests/api/test_execution_confirmation.py tests/ops/test_dry_run.py -q</automated>
  </verify>
  <done>The full scheduled trigger, approval, explicit execution confirmation, and submitted-order path is proven by deterministic integration tests and preserves duplicate-safe behavior.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/integration/test_scheduled_submission_flow.py tests/api/test_execution_confirmation.py tests/ops/test_dry_run.py -q`, then run `python -m pytest -q` as the phase gate.
</verification>

<success_criteria>
Phase 11 is complete when one deterministic integration command proves scheduled trigger through submitted orders and the duplicate-scheduled branch remains safe.
</success_criteria>

<output>
After completion, create `.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-scheduling-reliability-and-end-to-end-execution-proof-03-SUMMARY.md`
</output>

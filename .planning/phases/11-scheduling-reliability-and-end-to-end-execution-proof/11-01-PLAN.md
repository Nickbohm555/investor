---
phase: 11-scheduling-reliability-and-end-to-end-execution-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - .env.example
  - scripts/cron-install.sh
  - scripts/cron-status.sh
  - README.md
  - tests/ops/test_cron_scripts.py
  - tests/ops/test_operational_docs.py
autonomous: true
requirements:
  - SC11-01
must_haves:
  truths:
    - "The repo exposes a concrete 7:00am ET schedule configuration path instead of a hard-coded shell cron line."
    - "Managed cron install and status output show the configured expression, timezone, and log path."
    - "README, .env.example, and ops tests all describe the same schedule-install contract."
  artifacts:
    - path: "app/config.py"
      provides: "Settings keys for the schedule expression and ET timezone contract"
      contains: "schedule_cron_expression"
    - path: "scripts/cron-install.sh"
      provides: "Managed cron block generated from repo config"
      contains: "CRON_TZ="
    - path: "scripts/cron-status.sh"
      provides: "Observable installed cron expression/timezone/log path output"
      contains: "cron_expression="
    - path: "tests/ops/test_cron_scripts.py"
      provides: "Automated proof that install/status use repo-configured 7:00am ET values"
      contains: "0 7 * * 1-5"
  key_links:
    - from: "app/config.py"
      to: "scripts/cron-install.sh"
      via: "INVESTOR_SCHEDULE_CRON_EXPRESSION and INVESTOR_SCHEDULE_TIMEZONE"
      pattern: "INVESTOR_SCHEDULE_(CRON_EXPRESSION|TIMEZONE)"
    - from: "scripts/cron-status.sh"
      to: "tests/ops/test_cron_scripts.py"
      via: "status stdout assertions"
      pattern: "cron_expression=|cron_timezone=|log_path="
---

<objective>
Make the cron install path trustworthy for real market-open use by replacing hard-coded shell scheduling with repo-configured 7:00am ET settings and matching operational proof.

Purpose: Phase 11 starts with schedule reliability because end-to-end execution proof is not meaningful if the installed cadence can drift from repo config.
Output: Settings/env/docs updates plus cron install/status tests that pin the managed 7:00am ET contract.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-RESEARCH.md
@.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-VALIDATION.md
@.planning/phases/03-scheduling-and-email-delivery/03-03-SUMMARY.md
@.planning/phases/05-operational-readiness/05-operational-readiness-02-SUMMARY.md
@app/config.py
@scripts/cron-install.sh
@scripts/cron-status.sh
@tests/ops/test_cron_scripts.py
@tests/ops/test_operational_docs.py
@README.md
@.env.example

<interfaces>
From app/config.py:
```python
class Settings(BaseSettings):
    schedule_cron_expression: str = "30 8 * * 1-5"
    cron_log_path: str = "logs/cron/daily-trigger.log"
```

From scripts/cron-install.sh:
```sh
CRON_LINE="30 8 * * 1-5 cd \"$REPO_ROOT\" && ./scripts/cron-trigger.sh >> ./$LOG_PATH 2>&1"
```

From tests/ops/test_cron_scripts.py:
```python
assert "./scripts/cron-trigger.sh >> ./logs/cron/daily-trigger.log 2>&1" in crontab_text
assert "log_path=logs/cron/daily-trigger.log" in status.stdout
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing cron-config tests for the 7:00am ET install and status contract</name>
  <files>tests/ops/test_cron_scripts.py, tests/ops/test_operational_docs.py, README.md, .env.example, scripts/cron-install.sh, scripts/cron-status.sh, app/config.py</files>
  <read_first>.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-RESEARCH.md, .planning/phases/03-scheduling-and-email-delivery/03-03-SUMMARY.md, .planning/phases/05-operational-readiness/05-operational-readiness-02-SUMMARY.md, tests/ops/test_cron_scripts.py, tests/ops/test_operational_docs.py, scripts/cron-install.sh, scripts/cron-status.sh, app/config.py, README.md, .env.example</read_first>
  <acceptance_criteria>
    - `rg -n "0 7 \\* \\* 1-5|CRON_TZ=America/New_York|cron_expression=0 7 \\* \\* 1-5|cron_timezone=America/New_York" tests/ops/test_cron_scripts.py` returns assertions for the ET install and status outputs.
    - `rg -n "INVESTOR_SCHEDULE_TIMEZONE|7:00am ET|CRON_TZ=America/New_York" tests/ops/test_operational_docs.py README.md .env.example` returns the new docs/env expectations.
    - `python -m pytest tests/ops/test_cron_scripts.py tests/ops/test_operational_docs.py -q` fails before implementation because the repo still uses `30 8 * * 1-5` and does not expose the timezone key.
  </acceptance_criteria>
  <action>Add failing ops coverage that locks the new schedule contract to exact values. Update `tests/ops/test_cron_scripts.py` so install assertions require one managed block containing `CRON_TZ=America/New_York` and `0 7 * * 1-5 cd "$REPO_ROOT" && ./scripts/cron-trigger.sh >> ./logs/cron/daily-trigger.log 2>&1`. Extend the status assertions to require stdout lines `managed=present`, `cron_expression=0 7 * * 1-5`, `cron_timezone=America/New_York`, and `log_path=logs/cron/daily-trigger.log`. Extend `tests/ops/test_operational_docs.py` so `.env.example` and `README.md` must include `INVESTOR_SCHEDULE_TIMEZONE=America/New_York`, the operator-facing phrase `7:00am ET`, and the install/status commands that describe the ET schedule path. Do not implement the shell or settings changes in this task; leave the tests red.</action>
  <verify>
    <automated>python -m pytest tests/ops/test_cron_scripts.py tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>Failing coverage exists for the env-driven 7:00am ET cron install, status output, and matching docs/env contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the repo-configured ET cron path in settings, scripts, and operator docs</name>
  <files>app/config.py, .env.example, scripts/cron-install.sh, scripts/cron-status.sh, README.md, tests/ops/test_cron_scripts.py, tests/ops/test_operational_docs.py</files>
  <read_first>app/config.py, .env.example, scripts/cron-install.sh, scripts/cron-status.sh, README.md, tests/ops/test_cron_scripts.py, tests/ops/test_operational_docs.py</read_first>
  <acceptance_criteria>
    - `rg -n "schedule_cron_expression: str = \"0 7 \\* \\* 1-5\"|schedule_timezone: str = \"America/New_York\"" app/config.py` returns the new defaults.
    - `rg -n "INVESTOR_SCHEDULE_CRON_EXPRESSION=0 7 \\* \\* 1-5|INVESTOR_SCHEDULE_TIMEZONE=America/New_York" .env.example` returns both exact env lines.
    - `rg -n "CRON_TZ=\\$\\{INVESTOR_SCHEDULE_TIMEZONE:-America/New_York\\}|\\$\\{INVESTOR_SCHEDULE_CRON_EXPRESSION:-0 7 \\* \\* 1-5\\}" scripts/cron-install.sh` returns the managed ET block wiring.
    - `rg -n "cron_expression=|cron_timezone=|log_path=" scripts/cron-status.sh` returns the explicit status fields.
    - `rg -n "7:00am ET|INVESTOR_SCHEDULE_TIMEZONE|./scripts/cron-install.sh|./scripts/cron-status.sh" README.md` returns the updated operator instructions.
    - `python -m pytest tests/ops/test_cron_scripts.py tests/ops/test_operational_docs.py -q` exits successfully.
  </acceptance_criteria>
  <action>Update `app/config.py` so `Settings` defaults `schedule_cron_expression` to `0 7 * * 1-5` and adds `schedule_timezone: str = "America/New_York"` exposed as `INVESTOR_SCHEDULE_TIMEZONE`. Update `.env.example` with both exact lines `INVESTOR_SCHEDULE_CRON_EXPRESSION=0 7 * * 1-5` and `INVESTOR_SCHEDULE_TIMEZONE=America/New_York`. Refactor `scripts/cron-install.sh` to derive `CRON_EXPRESSION="${INVESTOR_SCHEDULE_CRON_EXPRESSION:-0 7 * * 1-5}"`, `CRON_TIMEZONE="${INVESTOR_SCHEDULE_TIMEZONE:-America/New_York}"`, and emit a managed block containing `CRON_TZ=$CRON_TIMEZONE` followed by the cron line. Refactor `scripts/cron-status.sh` to print `cron_expression=$CRON_EXPRESSION`, `cron_timezone=$CRON_TIMEZONE`, and `log_path=$LOG_PATH` even before install, using the same defaults as `app/config.py`. Update `README.md` so Cron Operations and Go-Live Checklist explicitly describe the repo-managed `7:00am ET` cadence and the new timezone env key. Keep cron as a thin wrapper; do not move dedupe or trigger logic out of the application/database path.</action>
  <verify>
    <automated>python -m pytest tests/ops/test_cron_scripts.py tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>The repo now exposes and tests a concrete ET schedule configuration path that drives cron install, status output, docs, and env templates from the same values.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/ops/test_cron_scripts.py tests/ops/test_operational_docs.py -q`. Then run `./scripts/cron-status.sh` locally and confirm it prints `cron_expression=0 7 * * 1-5`, `cron_timezone=America/New_York`, and `log_path=logs/cron/daily-trigger.log`.
</verification>

<success_criteria>
Phase 11 schedule hardening is in place: the repo config, managed cron scripts, and operator docs all point to the same 7:00am ET install path and the proof is automated.
</success_criteria>

<output>
After completion, create `.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-scheduling-reliability-and-end-to-end-execution-proof-01-SUMMARY.md`
</output>

---
phase: 16-deepen-quiver-signal-coverage-and-agent-evaluation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/agents/quiver_loop.py
  - app/schemas/quiver.py
  - app/services/research_prompt.py
  - app/tools/quiver.py
  - tests/agents/test_quiver_loop.py
  - tests/services/test_research_prompt.py
  - tests/tools/test_quiver_client.py
autonomous: true
requirements:
  - QRA-01
must_haves:
  truths:
    - "The Quiver loop exposes a broader documented follow-up tool surface than the original four-tool implementation."
    - "Trace records persist why a follow-up investigation was chosen, not just which tool executed."
    - "The new Quiver surface is tied to the officially published OpenAPI, not guessed endpoints."
  artifacts:
    - "app/schemas/quiver.py :: documented bill-summary schema added alongside the existing Quiver row models"
    - "app/tools/quiver.py :: client method for the documented bill-summary endpoint"
    - "app/agents/quiver_loop.py :: tool registry generalized for mixed ticker/query tools and trace rationale capture"
    - "tests/agents/test_quiver_loop.py :: regression coverage for mixed tool params and trace rationale persistence"
    - "tests/tools/test_quiver_client.py :: typed client coverage for the new bill-summary endpoint"
  key_links:
    - "app/services/research_prompt.py -> app/agents/quiver_loop.py :: prompt names the tool set the loop can actually execute"
    - "app/tools/quiver.py -> app/schemas/quiver.py :: new endpoint validates into a typed documented response model"
    - "app/agents/quiver_loop.py -> tests/agents/test_quiver_loop.py :: mixed tool args and rationale persistence stay locked by tests"
---

<objective>
Expand the bounded Quiver loop to use a broader documented follow-up surface and persist the reason each follow-up investigation was chosen.

Purpose: Broaden Phase 7's tool loop using the currently published Quiver API docs while keeping the workflow bounded and auditable.
Output: New bill-summary tool support, mixed-parameter tool execution, richer trace rationale, and tests.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/Desktop/Tinkering/investor/.planning/phases/16-deepen-quiver-signal-coverage-and-agent-evaluation/16-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/agents/quiver_loop.py
@app/schemas/quiver.py
@app/services/research_prompt.py
@app/tools/quiver.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for the documented bill-summary tool and richer follow-up trace rationale</name>
  <files>tests/agents/test_quiver_loop.py, tests/tools/test_quiver_client.py, tests/services/test_research_prompt.py, app/agents/quiver_loop.py, app/tools/quiver.py, app/services/research_prompt.py, app/schemas/quiver.py</files>
  <read_first>app/agents/quiver_loop.py, app/tools/quiver.py, app/services/research_prompt.py, app/schemas/quiver.py, .planning/phases/16-deepen-quiver-signal-coverage-and-agent-evaluation/16-RESEARCH.md</read_first>
  <action>Create `tests/agents/test_quiver_loop.py` with failing tests named `test_quiver_loop_executes_bill_summary_queries_without_forcing_ticker_uppercase` and `test_quiver_loop_persists_model_rationale_for_follow_up_tool_calls`. Create `tests/tools/test_quiver_client.py` with a failing test named `test_quiver_client_returns_typed_bill_summaries`. Update `tests/services/test_research_prompt.py` so it requires `build_quiver_loop_system_prompt(...)` to mention `get_live_bill_summaries` and to instruct the model to explain why each follow-up tool call is needed before using it.</action>
  <verify>
    <automated>python -m pytest tests/agents/test_quiver_loop.py tests/tools/test_quiver_client.py tests/services/test_research_prompt.py -q</automated>
  </verify>
  <done>The repo has failing tests that pin the documented bill-summary tool and richer trace rationale behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the new Quiver tool surface and rationale-aware trace logging</name>
  <files>app/schemas/quiver.py, app/tools/quiver.py, app/services/research_prompt.py, app/agents/quiver_loop.py, tests/agents/test_quiver_loop.py, tests/tools/test_quiver_client.py, tests/services/test_research_prompt.py</files>
  <read_first>app/schemas/quiver.py, app/tools/quiver.py, app/services/research_prompt.py, app/agents/quiver_loop.py, tests/agents/test_quiver_loop.py, tests/tools/test_quiver_client.py, tests/services/test_research_prompt.py</read_first>
  <action>Add a typed `BillSummary` model to `app/schemas/quiver.py`. Extend `app/tools/quiver.py` with a generalized request helper and a new `get_live_bill_summaries(query: str | None = None)` method against the documented `/beta/live/billSummaries?page_size=10&summary_limit=5000` endpoint. Refactor `app/agents/quiver_loop.py` to drive execution from a tool registry so `get_live_bill_summaries` can accept `query` while ticker tools keep `ticker`. Preserve `investigated_tickers` only for ticker-based tools. When the model emits content alongside a tool call, store that content as the trace `rationale`; only fall back to the current generic wording when no content exists. Update the loop system prompt to name the bill-summary tool and require a brief explanation before each follow-up tool call.</action>
  <verify>
    <automated>python -m pytest tests/agents/test_quiver_loop.py tests/tools/test_quiver_client.py tests/services/test_research_prompt.py -q</automated>
  </verify>
  <done>The bounded Quiver loop now exposes a broader documented tool surface and persists why follow-up calls were chosen.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/agents/test_quiver_loop.py tests/tools/test_quiver_client.py tests/services/test_research_prompt.py -q`.
</verification>

<success_criteria>
Phase 16 starts with a broader documented Quiver tool surface and rationale-aware traces, giving the agent more ways to investigate while keeping every follow-up auditable.
</success_criteria>

<output>
After completion, create `.planning/phases/16-deepen-quiver-signal-coverage-and-agent-evaluation/16-deepen-quiver-signal-coverage-and-agent-evaluation-01-SUMMARY.md`
</output>

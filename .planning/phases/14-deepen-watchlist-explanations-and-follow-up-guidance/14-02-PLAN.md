---
phase: 14-deepen-watchlist-explanations-and-follow-up-guidance
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - app/services/research_prompt.py
  - app/services/report_builder.py
  - tests/services/test_research_prompt.py
  - tests/services/test_report_builder.py
autonomous: true
requirements:
  - WLG-02
must_haves:
  truths:
    - "The model is explicitly instructed to return structured watchlist guidance fields instead of burying richer explanation in prose."
    - "The report builder maps those watchlist fields deterministically into persisted report data and preserves no-action compatibility."
    - "Watchlist follow-up guidance distinguishes missing evidence, unresolved questions, and next steps instead of collapsing all three into one list."
  artifacts:
    - "app/services/research_prompt.py :: exact JSON contract instructions for enriched watchlist items"
    - "app/services/report_builder.py :: deterministic mapping from watchlist fields into `ResearchNeededItem`"
    - "tests/services/test_research_prompt.py :: prompt-contract regression coverage for the exact required watchlist fields"
    - "tests/services/test_report_builder.py :: builder coverage for prompt-populated watchlist guidance and no-action fallbacks"
  key_links:
    - "app/services/research_prompt.py -> app/schemas/research.py :: prompt instructions match the watchlist schema keys exactly"
    - "app/services/report_builder.py -> app/schemas/reports.py :: builder persists the exact guidance fields instead of deriving them later in templates"
    - "tests/services/test_research_prompt.py -> app/services/research_prompt.py :: prompt drift fails when any required watchlist field drops out of the system instruction"
---

<objective>
Teach the research-output contract and deterministic builder to populate the new watchlist explanation fields so the richer guidance lives in structured data before rendering.

Purpose: Move watchlist depth upstream into the prompt and builder instead of guessing from weak fallback data in templates.
Output: Prompt-contract updates, builder enrichment, and restored tests for prompt and watchlist report semantics.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-RESEARCH.md
@.planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-01-PLAN.md
@app/services/research_prompt.py
@app/services/report_builder.py
@app/schemas/research.py
@app/schemas/reports.py

<interfaces>
From app/services/research_prompt.py:
```python
def build_final_research_payload(
    *,
    run_id: str,
    evidence_bundles: List[TickerEvidenceBundle],
    account_context: Dict[str, str],
    trace_summary: str,
) -> Dict[str, str]:
    ...
```

From app/services/report_builder.py:
```python
def build_strategic_insight_report(
    *,
    run_id: str,
    outcome: ResearchOutcome,
    baseline_run_id: str | None,
    baseline_outcome: ResearchOutcome | None,
) -> StrategicInsightReport:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recreate prompt-contract tests that require explicit watchlist guidance keys</name>
  <files>tests/services/test_research_prompt.py, tests/services/test_report_builder.py, app/services/research_prompt.py, app/services/report_builder.py</files>
  <read_first>app/services/research_prompt.py, app/services/report_builder.py, app/schemas/research.py, app/schemas/reports.py, .planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-RESEARCH.md, .planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-01-PLAN.md</read_first>
  <behavior>
    - Test 1: the final research system prompt explicitly requires the new watchlist fields by exact key name.
    - Test 2: the builder preserves prompt-populated watchlist guidance exactly instead of overwriting it with generic fallbacks.
    - Test 3: the no-action branch remains valid and lighter-weight, using deterministic fallback values only where no watchlist item exists.
  </behavior>
  <action>Create `tests/services/test_research_prompt.py` with failing tests named exactly `test_build_final_research_payload_requires_watchlist_guidance_keys` and `test_build_research_prompt_payload_keeps_json_only_watchlist_contract`. Require the `system` string returned by `build_final_research_payload(...)` to contain the exact substring `Every watchlist item must include watchlist_reason, missing_evidence, unresolved_questions, and next_steps.` and to keep the existing exact outcome list `candidates, watchlist, or no_action.`. Update `tests/services/test_report_builder.py` with failing tests named exactly `test_build_strategic_insight_report_preserves_prompt_supplied_watchlist_guidance` and `test_build_strategic_insight_report_uses_exact_no_action_follow_up_defaults`. In the watchlist builder test, create a `WatchlistCandidate` with exact values `watchlist_reason=\"Conflicting conviction after mixed insider and contract signals.\"`, `missing_evidence=[\"Need two independent supporting filings.\"]`, `unresolved_questions=[\"Did the contract award recur across quarters?\"]`, and `next_steps=[\"Check the next Quiver contracts refresh for repeat awards.\"]`, then require those same exact strings to appear unchanged on `report.research_queue[0]`. In the no-action test, require `next_steps == [\"Wait for materially new Quiver evidence before reconsidering.\"]` and `operator_action == \"Wait for materially new evidence before reconsidering.\"`.</action>
  <acceptance_criteria>
    - `rg -n "test_build_final_research_payload_requires_watchlist_guidance_keys|test_build_research_prompt_payload_keeps_json_only_watchlist_contract" tests/services/test_research_prompt.py` returns both prompt tests.
    - `rg -n "Every watchlist item must include watchlist_reason, missing_evidence, unresolved_questions, and next_steps\\.|candidates, watchlist, or no_action\\." tests/services/test_research_prompt.py` returns the exact required prompt strings.
    - `rg -n "test_build_strategic_insight_report_preserves_prompt_supplied_watchlist_guidance|test_build_strategic_insight_report_uses_exact_no_action_follow_up_defaults" tests/services/test_report_builder.py` returns both new builder tests.
    - `rg -n "Conflicting conviction after mixed insider and contract signals\\.|Need two independent supporting filings\\.|Did the contract award recur across quarters\\?|Check the next Quiver contracts refresh for repeat awards\\.|Wait for materially new Quiver evidence before reconsidering\\.|Wait for materially new evidence before reconsidering\\." tests/services/test_report_builder.py` returns every exact guidance string.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_research_prompt.py tests/services/test_report_builder.py -q</automated>
  </verify>
  <done>Prompt and builder tests now pin the exact watchlist guidance keys and values required for Phase 14.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enrich the prompt contract and builder semantics for explicit watchlist guidance</name>
  <files>app/services/research_prompt.py, app/services/report_builder.py, tests/services/test_research_prompt.py, tests/services/test_report_builder.py</files>
  <read_first>app/services/research_prompt.py, app/services/report_builder.py, tests/services/test_research_prompt.py, tests/services/test_report_builder.py, app/schemas/research.py, app/schemas/reports.py</read_first>
  <behavior>
    - Test 1: every final prompt path tells the model exactly which watchlist keys to return and does not move watchlist richness into free-form prose.
    - Test 2: watchlist builder output uses prompt-supplied `watchlist_reason`, `missing_evidence`, `unresolved_questions`, and `next_steps` whenever present.
    - Test 3: no-action remains lighter than watchlist, with exact deterministic fallback strings rather than invented pseudo-watchlist narratives.
  </behavior>
  <action>Update `build_final_research_payload(...)` in `app/services/research_prompt.py` so the `system` string reads exactly: `Return JSON only. Choose exactly one outcome value from candidates, watchlist, or no_action. Every recommendation-like item must include supporting_evidence, opposing_evidence, risk_notes, and source_summary arrays. Every watchlist item must include watchlist_reason, missing_evidence, unresolved_questions, and next_steps.`. Keep `build_research_prompt_payload(...)` delegating to `build_final_research_payload(...)` so both the legacy JSON path and the loop-agent finalization path share the same watchlist contract. Update `app/services/report_builder.py` so the watchlist branch prefers prompt-supplied structured values with these exact rules: `watchlist_reason=item.watchlist_reason or (item.opposing_evidence[0] if item.opposing_evidence else outcome.summary)`, `missing_evidence=item.missing_evidence or [outcome.summary]`, `unresolved_questions=item.unresolved_questions or item.opposing_evidence or [outcome.summary]`, `next_steps=item.next_steps or ["Check the next Quiver refresh before approval."]`, and `operator_action="Do not approve yet. Resolve the listed evidence gaps first."`. Keep the no-action branch exact and lighter-weight with `missing_evidence=outcome.reasons or [outcome.summary]`, `unresolved_questions=[]`, `next_steps=["Wait for materially new Quiver evidence before reconsidering."]`, and `operator_action="Wait for materially new evidence before reconsidering."`. Do not add a separate watchlist email template or a new outcome branch in this plan.</action>
  <acceptance_criteria>
    - `rg -n "Every watchlist item must include watchlist_reason, missing_evidence, unresolved_questions, and next_steps\\." app/services/research_prompt.py` returns the exact prompt contract sentence.
    - `rg -n "Choose exactly one outcome value from candidates, watchlist, or no_action\\." app/services/research_prompt.py` returns the exact unchanged outcome list sentence.
    - `rg -n "watchlist_reason=item\\.watchlist_reason or \\(item\\.opposing_evidence\\[0\\] if item\\.opposing_evidence else outcome\\.summary\\)|missing_evidence=item\\.missing_evidence or \\[outcome\\.summary\\]|unresolved_questions=item\\.unresolved_questions or item\\.opposing_evidence or \\[outcome\\.summary\\]|next_steps=item\\.next_steps or \\[\"Check the next Quiver refresh before approval\\.\"\\]|operator_action=\"Do not approve yet\\. Resolve the listed evidence gaps first\\.\"" app/services/report_builder.py` returns the exact watchlist mapping rules and operator action.
    - `rg -n "next_steps=\\[\"Wait for materially new Quiver evidence before reconsidering\\.\"\\]|operator_action=\"Wait for materially new evidence before reconsidering\\.\"" app/services/report_builder.py` returns the exact no-action defaults.
    - `python -m pytest tests/services/test_research_prompt.py tests/services/test_report_builder.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_research_prompt.py tests/services/test_report_builder.py -q</automated>
  </verify>
  <done>The research prompt and deterministic builder now populate the enriched watchlist guidance fields without introducing a second report format.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/services/test_research_prompt.py tests/services/test_report_builder.py -q`. Then inspect `app/services/research_prompt.py` and confirm the final system prompt contains the exact watchlist key list and no alternative prose-only watchlist contract.
</verification>

<success_criteria>
Phase 14 now captures richer watchlist guidance at the source: the model is instructed to return explicit watchlist fields and the deterministic builder preserves them into persisted report data.
</success_criteria>

<output>
After completion, create `.planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-deepen-watchlist-explanations-and-follow-up-guidance-02-SUMMARY.md`
</output>

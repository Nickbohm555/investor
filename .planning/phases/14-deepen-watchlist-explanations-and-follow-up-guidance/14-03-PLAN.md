---
phase: 14-deepen-watchlist-explanations-and-follow-up-guidance
plan: 03
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
files_modified:
  - README.md
  - app/templates/reports/strategic_report.txt.j2
  - app/templates/reports/strategic_report.html.j2
  - tests/graph/test_workflow_reporting.py
  - tests/ops/test_operational_docs.py
  - tests/services/test_report_render.py
autonomous: true
requirements:
  - WLG-03
must_haves:
  truths:
    - "Rendered watchlist sections explain why an item stayed off the action list, what evidence is missing, which questions remain, and what to check next."
    - "The same enriched watchlist fields survive workflow persistence and can be inspected from stored strategic report payloads after a run."
    - "README and automated verification describe the richer watchlist report surface so operators know what to expect from watchlist runs."
  artifacts:
    - "app/templates/reports/strategic_report.txt.j2 :: watchlist-aware text rendering with explicit guidance labels"
    - "app/templates/reports/strategic_report.html.j2 :: watchlist-aware HTML rendering with the same guidance labels"
    - "tests/services/test_report_render.py :: rendering coverage for the new watchlist sections"
    - "tests/graph/test_workflow_reporting.py :: workflow persistence coverage for enriched `strategic_report.research_queue` fields"
    - "README.md :: operator-facing description of the richer watchlist explanation surface"
    - "tests/ops/test_operational_docs.py :: doc drift coverage for the README watchlist-report wording"
  key_links:
    - "app/templates/reports/strategic_report.*.j2 -> app/schemas/reports.py :: templates render the named watchlist fields directly"
    - "tests/graph/test_workflow_reporting.py -> app/graph/workflow.py :: persisted `strategic_report` payload proves the richer fields survive workflow state"
    - "README.md -> tests/ops/test_operational_docs.py :: operator docs and verification stay aligned on the enriched watchlist output"
---

<objective>
Render the richer watchlist guidance in operator output, verify it persists through workflow state, and document the new watchlist report surface.

Purpose: Make the enriched watchlist contract visible and durable in the delivered memo and repo docs, not just in internal schema objects.
Output: Updated report templates, rendering/workflow verification, README copy, and doc drift coverage.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-RESEARCH.md
@.planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-02-PLAN.md
@.planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-upgrade-outputs-to-strategic-insight-reports-02-SUMMARY.md
@README.md
@app/templates/reports/strategic_report.txt.j2
@app/templates/reports/strategic_report.html.j2

<interfaces>
From app/templates/reports/strategic_report.txt.j2:
```jinja2
Research Queue
{% for item in report.research_queue %}
- {{ item.ticker }} | {{ item.thesis }}
  Uncertainty: {{ item.uncertainty }}
  Follow-up: {{ item.follow_up_questions | join('; ') }}
  Action: {{ item.operator_action }}
{% endfor %}
```

From app/graph/workflow.py:
```python
"strategic_report": report.model_dump(mode="python"),
"baseline_run_id": report.baseline_run_id,
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recreate rendering, workflow-persistence, and README drift tests for the richer watchlist report</name>
  <files>tests/services/test_report_render.py, tests/graph/test_workflow_reporting.py, tests/ops/test_operational_docs.py, README.md, app/templates/reports/strategic_report.txt.j2, app/templates/reports/strategic_report.html.j2</files>
  <read_first>app/templates/reports/strategic_report.txt.j2, app/templates/reports/strategic_report.html.j2, README.md, app/graph/workflow.py, .planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-RESEARCH.md, .planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-02-PLAN.md</read_first>
  <behavior>
    - Test 1: rendered text and HTML reports show explicit labels for watchlist reason, missing evidence, unresolved questions, and next session checks.
    - Test 2: workflow-level report persistence exposes those same fields under `state_payload["strategic_report"]["research_queue"]`.
    - Test 3: README explains the richer watchlist memo surface and docs drift coverage fails if that wording disappears.
  </behavior>
  <action>Create `tests/services/test_report_render.py` with failing tests named exactly `test_render_report_email_outputs_named_watchlist_guidance_sections` and `test_render_report_email_outputs_watchlist_guidance_in_html`. Require the text template output to contain the exact labels `Why Still Watchlist:`, `Missing Evidence:`, `Open Questions:`, `Next Session Checks:`, and `Action:` under each `report.research_queue` item. Require the HTML output to contain the exact labels `Why Still Watchlist:`, `Missing Evidence:`, `Open Questions:`, and `Next Session Checks:` inside the watchlist list item markup. Create `tests/graph/test_workflow_reporting.py` with a failing test named exactly `test_workflow_persists_named_watchlist_guidance_fields_on_strategic_report` that requires `state["strategic_report"]["research_queue"][0]` to contain keys `watchlist_reason`, `missing_evidence`, `unresolved_questions`, and `next_steps`. Create `tests/ops/test_operational_docs.py` with a failing test named exactly `test_readme_mentions_named_watchlist_guidance_sections` that requires `README.md` to contain the exact sentence `Watchlist items now explain why the idea is not actionable, what evidence is missing, which questions remain unresolved, and what to check on the next session.`. Leave the README and templates unchanged until Task 2 so these tests fail first.</action>
  <acceptance_criteria>
    - `rg -n "test_render_report_email_outputs_named_watchlist_guidance_sections|test_render_report_email_outputs_watchlist_guidance_in_html" tests/services/test_report_render.py` returns both rendering tests.
    - `rg -n "Why Still Watchlist:|Missing Evidence:|Open Questions:|Next Session Checks:|Action:" tests/services/test_report_render.py` returns all required label assertions.
    - `rg -n "test_workflow_persists_named_watchlist_guidance_fields_on_strategic_report|watchlist_reason|missing_evidence|unresolved_questions|next_steps" tests/graph/test_workflow_reporting.py` returns the workflow persistence test and exact required keys.
    - `rg -n "test_readme_mentions_named_watchlist_guidance_sections|Watchlist items now explain why the idea is not actionable, what evidence is missing, which questions remain unresolved, and what to check on the next session\\." tests/ops/test_operational_docs.py` returns the docs test and exact README sentence.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_report_render.py tests/graph/test_workflow_reporting.py tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>Rendering, persistence, and docs coverage now pin the exact richer watchlist surface expected from Phase 14.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the richer watchlist sections, prove persistence, and document the new operator-visible memo shape</name>
  <files>README.md, app/templates/reports/strategic_report.txt.j2, app/templates/reports/strategic_report.html.j2, tests/services/test_report_render.py, tests/graph/test_workflow_reporting.py, tests/ops/test_operational_docs.py</files>
  <read_first>README.md, app/templates/reports/strategic_report.txt.j2, app/templates/reports/strategic_report.html.j2, tests/services/test_report_render.py, tests/graph/test_workflow_reporting.py, tests/ops/test_operational_docs.py, app/services/report_builder.py, app/graph/workflow.py</read_first>
  <behavior>
    - Test 1: both templates render the enriched watchlist fields with exact section labels and empty-state fallbacks still work for non-watchlist runs.
    - Test 2: persisted workflow state can be inspected later for the same named watchlist keys without reconstructing them from email text.
    - Test 3: operator docs describe the richer watchlist memo surface and are locked by an automated README drift test.
  </behavior>
  <action>Update `app/templates/reports/strategic_report.txt.j2` so each `report.research_queue` item renders in this exact order: `- {{ item.ticker }} | {{ item.thesis }}`, `  Why Still Watchlist: {{ item.watchlist_reason }}`, `  Missing Evidence: {{ item.missing_evidence | join('; ') if item.missing_evidence else 'None.' }}`, `  Open Questions: {{ item.unresolved_questions | join('; ') if item.unresolved_questions else 'None.' }}`, `  Next Session Checks: {{ item.next_steps | join('; ') if item.next_steps else 'None.' }}`, and `  Action: {{ item.operator_action }}`. Update `app/templates/reports/strategic_report.html.j2` so each research-queue list item renders the same labels with `<br>` separators and uses `None.` when any of the three list fields are empty. Do not reintroduce `Uncertainty:` or `Follow-up:` labels anywhere in either template. Leave the section heading `Research Queue` unchanged so the existing report layout stays intact. Update `README.md` by appending this exact sentence to the `Dry Run` section paragraph: `Watchlist items now explain why the idea is not actionable, what evidence is missing, which questions remain unresolved, and what to check on the next session.`. Update `tests/graph/test_workflow_reporting.py` so it constructs a watchlist run and asserts the persisted `strategic_report["research_queue"][0]` dictionary includes the four named keys; do not change `app/graph/workflow.py` unless the test reveals persistence is not flowing through `report.model_dump(mode=\"python\")` as expected.</action>
  <acceptance_criteria>
    - `rg -n "Why Still Watchlist: \\{\\{ item\\.watchlist_reason \\}\\}|Missing Evidence: \\{\\{ item\\.missing_evidence \\| join\\('; ' \\) if item\\.missing_evidence else 'None\\.' \\}\\}|Open Questions: \\{\\{ item\\.unresolved_questions \\| join\\('; ' \\) if item\\.unresolved_questions else 'None\\.' \\}\\}|Next Session Checks: \\{\\{ item\\.next_steps \\| join\\('; ' \\) if item\\.next_steps else 'None\\.' \\}\\}|Action: \\{\\{ item\\.operator_action \\}\\}" app/templates/reports/strategic_report.txt.j2` returns the exact text-template watchlist labels.
    - `rg -n "Why Still Watchlist: \\{\\{ item\\.watchlist_reason \\}\\}<br>|Missing Evidence: \\{\\{ item\\.missing_evidence \\| join\\('; ' \\) if item\\.missing_evidence else 'None\\.' \\}\\}<br>|Open Questions: \\{\\{ item\\.unresolved_questions \\| join\\('; ' \\) if item\\.unresolved_questions else 'None\\.' \\}\\}<br>|Next Session Checks: \\{\\{ item\\.next_steps \\| join\\('; ' \\) if item\\.next_steps else 'None\\.' \\}\\}<br>" app/templates/reports/strategic_report.html.j2` returns the exact HTML watchlist labels.
    - `rg -n "Uncertainty:|Follow-up:" app/templates/reports/strategic_report.txt.j2 app/templates/reports/strategic_report.html.j2` returns no matches.
    - `rg -n "Watchlist items now explain why the idea is not actionable, what evidence is missing, which questions remain unresolved, and what to check on the next session\\." README.md` returns the exact README sentence.
    - `python -m pytest tests/services/test_report_render.py tests/graph/test_workflow_reporting.py tests/ops/test_operational_docs.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_report_render.py tests/graph/test_workflow_reporting.py tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>The richer watchlist guidance is visible in rendered reports, survives persisted workflow state, and is documented in the README with drift coverage.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/services/test_report_render.py tests/graph/test_workflow_reporting.py tests/ops/test_operational_docs.py -q`. Then inspect one serialized `strategic_report` fixture in `tests/graph/test_workflow_reporting.py` and confirm the same named watchlist keys appear in both the persisted payload and the rendered text labels.
</verification>

<success_criteria>
Phase 14 is operator-visible and durable: watchlist memos render explicit why-not-now guidance, the same fields persist in workflow state, and the README describes the richer report surface.
</success_criteria>

<output>
After completion, create `.planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-deepen-watchlist-explanations-and-follow-up-guidance-03-SUMMARY.md`
</output>

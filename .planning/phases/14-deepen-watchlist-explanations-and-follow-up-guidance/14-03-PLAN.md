---
phase: 14-deepen-watchlist-explanations-and-follow-up-guidance
plan: 03
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
files_modified:
  - README.md
  - app/templates/reports/strategic_report.txt.j2
  - app/templates/reports/strategic_report.html.j2
  - tests/graph/test_workflow_reporting.py
  - tests/ops/test_operational_docs.py
  - tests/services/test_report_render.py
autonomous: true
requirements:
  - WLG-03
must_haves:
  truths:
    - "Rendered `research_queue` sections explain why a watchlist or no-action idea is not yet actionable, what evidence is missing, which questions remain, and what to check next."
    - "The same enriched `research_queue` fields survive workflow persistence and can be inspected from stored strategic report payloads after either a watchlist or no-action run."
    - "README and automated verification describe the richer shared report surface so operators know what to expect from both watchlist and no-action runs."
  artifacts:
    - "app/templates/reports/strategic_report.txt.j2 :: shared research-queue text rendering with neutral guidance labels"
    - "app/templates/reports/strategic_report.html.j2 :: shared research-queue HTML rendering with the same neutral labels"
    - "tests/services/test_report_render.py :: rendering coverage for shared watchlist and no-action research-queue sections"
    - "tests/graph/test_workflow_reporting.py :: workflow persistence coverage for enriched `strategic_report.research_queue` fields"
    - "README.md :: operator-facing description of the richer shared watchlist/no-action explanation surface"
    - "tests/ops/test_operational_docs.py :: doc drift coverage for the README shared-report wording"
  key_links:
    - "app/templates/reports/strategic_report.*.j2 -> app/schemas/reports.py :: templates render the named shared research-queue fields directly for watchlist and no-action entries"
    - "tests/graph/test_workflow_reporting.py -> app/graph/workflow.py :: persisted `strategic_report` payload proves the richer fields survive workflow state"
    - "README.md -> tests/ops/test_operational_docs.py :: operator docs and verification stay aligned on the enriched shared output"
---

<objective>
Render the richer shared research-queue guidance in operator output, verify it persists through workflow state, and document the watchlist plus no-action report surface.

Purpose: Make the enriched shared contract visible and durable in the delivered memo and repo docs, not just in internal schema objects.
Output: Updated report templates, rendering/workflow verification, README copy, and doc drift coverage.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-RESEARCH.md
@.planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-02-PLAN.md
@.planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-upgrade-outputs-to-strategic-insight-reports-02-SUMMARY.md
@README.md
@app/templates/reports/strategic_report.txt.j2
@app/templates/reports/strategic_report.html.j2

<interfaces>
From app/templates/reports/strategic_report.txt.j2:
```jinja2
Research Queue
{% for item in report.research_queue %}
- {{ item.ticker }} | {{ item.thesis }}
  Uncertainty: {{ item.uncertainty }}
  Follow-up: {{ item.follow_up_questions | join('; ') }}
  Action: {{ item.operator_action }}
{% endfor %}
```

From app/graph/workflow.py:
```python
"strategic_report": report.model_dump(mode="python"),
"baseline_run_id": report.baseline_run_id,
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recreate rendering, workflow-persistence, and README drift tests for the richer watchlist report</name>
  <files>tests/services/test_report_render.py, tests/graph/test_workflow_reporting.py, tests/ops/test_operational_docs.py, README.md, app/templates/reports/strategic_report.txt.j2, app/templates/reports/strategic_report.html.j2</files>
  <read_first>app/templates/reports/strategic_report.txt.j2, app/templates/reports/strategic_report.html.j2, README.md, app/graph/workflow.py, .planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-RESEARCH.md, .planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-02-PLAN.md</read_first>
  <behavior>
    - Test 1: rendered text and HTML reports show neutral labels for why an idea is not actionable yet, what evidence is missing, which questions remain, and what to check next.
    - Test 2: workflow-level report persistence exposes those same fields under `state_payload["strategic_report"]["research_queue"]`.
    - Test 3: README explains the richer shared watchlist/no-action memo surface and docs drift coverage fails if that wording disappears.
  </behavior>
  <action>Create `tests/services/test_report_render.py` with failing tests named exactly `test_render_report_email_outputs_named_research_queue_guidance_sections`, `test_render_report_email_outputs_research_queue_guidance_in_html`, and `test_render_report_email_outputs_no_action_guidance_with_neutral_labels`. Require the text template output to contain the exact labels `Why Not Actionable Yet:`, `Missing Evidence:`, `Open Questions:`, `Next Checks:`, and `Action:` under each `report.research_queue` item. Require the HTML output to contain the exact labels `Why Not Actionable Yet:`, `Missing Evidence:`, `Open Questions:`, and `Next Checks:` inside the shared research-queue list item markup. In the explicit no-action render test, require those same neutral labels to render for a `research_queue` item whose `ticker` is the run id and whose fields come from the no-action branch, proving there is still one shared output format. Create `tests/graph/test_workflow_reporting.py` with a failing test named exactly `test_workflow_persists_named_research_queue_guidance_fields_on_strategic_report` that requires `state["strategic_report"]["research_queue"][0]` to contain keys `watchlist_reason`, `missing_evidence`, `unresolved_questions`, and `next_steps`. Create `tests/ops/test_operational_docs.py` with a failing test named exactly `test_readme_mentions_shared_research_queue_guidance_sections` that requires `README.md` to contain the exact sentence `Watchlist and no-action items now explain why the idea is not actionable, what evidence is missing, which questions remain unresolved, and what to check on the next session.`. Leave the README and templates unchanged until Task 2 so these tests fail first.</action>
  <acceptance_criteria>
    - `rg -n "test_render_report_email_outputs_named_research_queue_guidance_sections|test_render_report_email_outputs_research_queue_guidance_in_html|test_render_report_email_outputs_no_action_guidance_with_neutral_labels" tests/services/test_report_render.py` returns all three rendering tests.
    - `rg -n "Why Not Actionable Yet:|Missing Evidence:|Open Questions:|Next Checks:|Action:" tests/services/test_report_render.py` returns all required neutral label assertions.
    - `rg -n "test_workflow_persists_named_research_queue_guidance_fields_on_strategic_report|watchlist_reason|missing_evidence|unresolved_questions|next_steps" tests/graph/test_workflow_reporting.py` returns the workflow persistence test and exact required keys.
    - `rg -n "test_readme_mentions_shared_research_queue_guidance_sections|Watchlist and no-action items now explain why the idea is not actionable, what evidence is missing, which questions remain unresolved, and what to check on the next session\\." tests/ops/test_operational_docs.py` returns the docs test and exact README sentence.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_report_render.py tests/graph/test_workflow_reporting.py tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>Rendering, persistence, and docs coverage now pin the exact richer watchlist surface expected from Phase 14.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the richer watchlist sections, prove persistence, and document the new operator-visible memo shape</name>
  <files>README.md, app/templates/reports/strategic_report.txt.j2, app/templates/reports/strategic_report.html.j2, tests/services/test_report_render.py, tests/graph/test_workflow_reporting.py, tests/ops/test_operational_docs.py</files>
  <read_first>README.md, app/templates/reports/strategic_report.txt.j2, app/templates/reports/strategic_report.html.j2, tests/services/test_report_render.py, tests/graph/test_workflow_reporting.py, tests/ops/test_operational_docs.py, app/services/report_builder.py, app/graph/workflow.py</read_first>
  <behavior>
    - Test 1: both templates render the enriched research-queue fields with exact neutral section labels and empty-state fallbacks for both watchlist and no-action runs.
    - Test 2: persisted workflow state can be inspected later for the same named watchlist keys without reconstructing them from email text.
    - Test 3: operator docs describe the richer shared watchlist/no-action memo surface and are locked by an automated README drift test.
  </behavior>
  <action>Update `app/templates/reports/strategic_report.txt.j2` so each `report.research_queue` item renders in this exact order: `- {{ item.ticker }} | {{ item.thesis }}`, `  Why Not Actionable Yet: {{ item.watchlist_reason }}`, `  Missing Evidence: {{ item.missing_evidence | join('; ') if item.missing_evidence else 'None.' }}`, `  Open Questions: {{ item.unresolved_questions | join('; ') if item.unresolved_questions else 'None.' }}`, `  Next Checks: {{ item.next_steps | join('; ') if item.next_steps else 'None.' }}`, and `  Action: {{ item.operator_action }}`. Update `app/templates/reports/strategic_report.html.j2` so each research-queue list item renders the same neutral labels with `<br>` separators and uses `None.` when any of the three list fields are empty. Do not reintroduce `Uncertainty:` or `Follow-up:` labels anywhere in either template, and do not branch to a separate no-action template section or second report format. Leave the section heading `Research Queue` unchanged so the existing report layout stays intact. Update `README.md` by appending this exact sentence to the `Dry Run` section paragraph: `Watchlist and no-action items now explain why the idea is not actionable, what evidence is missing, which questions remain unresolved, and what to check on the next session.`. Update `tests/graph/test_workflow_reporting.py` so it constructs both a watchlist run and a no-action run, asserting the persisted `strategic_report["research_queue"][0]` dictionary includes the four named keys in both cases; do not change `app/graph/workflow.py` unless the test reveals persistence is not flowing through `report.model_dump(mode=\"python\")` as expected.</action>
  <acceptance_criteria>
    - `rg -n "Why Not Actionable Yet: \\{\\{ item\\.watchlist_reason \\}\\}|Missing Evidence: \\{\\{ item\\.missing_evidence \\| join\\('; ' \\) if item\\.missing_evidence else 'None\\.' \\}\\}|Open Questions: \\{\\{ item\\.unresolved_questions \\| join\\('; ' \\) if item\\.unresolved_questions else 'None\\.' \\}\\}|Next Checks: \\{\\{ item\\.next_steps \\| join\\('; ' \\) if item\\.next_steps else 'None\\.' \\}\\}|Action: \\{\\{ item\\.operator_action \\}\\}" app/templates/reports/strategic_report.txt.j2` returns the exact text-template neutral labels.
    - `rg -n "Why Not Actionable Yet: \\{\\{ item\\.watchlist_reason \\}\\}<br>|Missing Evidence: \\{\\{ item\\.missing_evidence \\| join\\('; ' \\) if item\\.missing_evidence else 'None\\.' \\}\\}<br>|Open Questions: \\{\\{ item\\.unresolved_questions \\| join\\('; ' \\) if item\\.unresolved_questions else 'None\\.' \\}\\}<br>|Next Checks: \\{\\{ item\\.next_steps \\| join\\('; ' \\) if item\\.next_steps else 'None\\.' \\}\\}<br>" app/templates/reports/strategic_report.html.j2` returns the exact HTML neutral labels.
    - `rg -n "Uncertainty:|Follow-up:" app/templates/reports/strategic_report.txt.j2 app/templates/reports/strategic_report.html.j2` returns no matches.
    - `rg -n "Watchlist and no-action items now explain why the idea is not actionable, what evidence is missing, which questions remain unresolved, and what to check on the next session\\." README.md` returns the exact README sentence.
    - `python -m pytest tests/services/test_report_render.py tests/graph/test_workflow_reporting.py tests/ops/test_operational_docs.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_report_render.py tests/graph/test_workflow_reporting.py tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>The richer watchlist guidance is visible in rendered reports, survives persisted workflow state, and is documented in the README with drift coverage.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/services/test_report_render.py tests/graph/test_workflow_reporting.py tests/ops/test_operational_docs.py -q`. Then inspect serialized watchlist and no-action `strategic_report` fixtures in `tests/graph/test_workflow_reporting.py` and confirm the same named shared research-queue keys appear in both the persisted payload and the rendered text labels.
</verification>

<success_criteria>
Phase 14 is operator-visible and durable: watchlist and no-action memos render explicit why-not-now guidance through one shared research-queue format, the same fields persist in workflow state, and the README describes the richer report surface.
</success_criteria>

<output>
After completion, create `.planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-deepen-watchlist-explanations-and-follow-up-guidance-03-SUMMARY.md`
</output>

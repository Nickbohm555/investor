---
phase: 14-deepen-watchlist-explanations-and-follow-up-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/research.py
  - app/schemas/reports.py
  - app/schemas/workflow.py
  - app/services/report_builder.py
  - tests/services/test_report_builder.py
  - tests/services/test_report_contracts.py
autonomous: true
requirements:
  - WLG-01
must_haves:
  truths:
    - "Watchlist outcomes carry explicit structured guidance for why an idea stayed non-actionable, what evidence is missing, which questions remain unresolved, and what to check next."
    - "The strategic report schema persists watchlist guidance as named fields instead of collapsing everything into `uncertainty` or one generic follow-up list."
    - "The report builder can serialize a valid enriched `research_queue` entry without inventing a second watchlist-only format."
  artifacts:
    - "app/schemas/research.py :: `WatchlistCandidate` and `WatchlistOutcome.items` upgraded to deterministic watchlist-guidance fields"
    - "app/schemas/reports.py :: `ResearchNeededItem` upgraded with explicit persisted watchlist guidance fields"
    - "app/services/report_builder.py :: compatibility mapping from watchlist outcomes into the enriched report contract"
    - "tests/services/test_report_contracts.py :: schema-level regression coverage for the new watchlist/report fields"
    - "tests/services/test_report_builder.py :: builder coverage that proves the enriched `research_queue` shape is emitted"
  key_links:
    - "app/schemas/research.py -> app/services/report_builder.py :: watchlist items expose structured fields the builder can map directly"
    - "app/services/report_builder.py -> app/schemas/reports.py :: builder emits enriched `ResearchNeededItem` entries, not ad hoc dicts"
    - "app/schemas/workflow.py -> app/schemas/reports.py :: workflow and renderer keep importing the report contract through the existing schema export surface"
---

<objective>
Define the enriched watchlist data contract first so Phase 14 deepens explanation quality through deterministic structured fields rather than template-only copy or prompt-only prose.

Purpose: Establish the exact watchlist/report schema seam and restore the missing report test surface before prompt or rendering work depends on it.
Output: Upgraded research/report schemas, compatibility builder mapping, and recreated report-contract tests.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-RESEARCH.md
@.planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-upgrade-outputs-to-strategic-insight-reports-01-SUMMARY.md
@app/schemas/research.py
@app/schemas/reports.py
@app/schemas/workflow.py
@app/services/report_builder.py

<interfaces>
From app/schemas/research.py:
```python
class CandidateRecommendation(BaseModel):
    ticker: str
    action: str
    conviction_score: float = Field(ge=0.0, le=1.0)
    supporting_evidence: List[str] = Field(default_factory=list)
    opposing_evidence: List[str] = Field(default_factory=list)
    risk_notes: List[str] = Field(default_factory=list)
    source_summary: List[str] = Field(default_factory=list)
    broker_eligible: bool = True

class WatchlistOutcome(BaseModel):
    outcome: Literal["watchlist"]
    summary: str
    items: List[CandidateRecommendation]
```

From app/schemas/reports.py:
```python
class ResearchNeededItem(BaseModel):
    bucket: Literal["research"] = "research"
    ticker: str
    thesis: str = ""
    uncertainty: str = ""
    follow_up_questions: List[str] = Field(default_factory=list)
    operator_action: str = ""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recreate the missing report-contract tests and pin the enriched watchlist schema surface</name>
  <files>tests/services/test_report_contracts.py, tests/services/test_report_builder.py, app/schemas/research.py, app/schemas/reports.py, app/schemas/workflow.py, app/services/report_builder.py</files>
  <read_first>app/schemas/research.py, app/schemas/reports.py, app/schemas/workflow.py, app/services/report_builder.py, .planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-RESEARCH.md, .planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-upgrade-outputs-to-strategic-insight-reports-01-SUMMARY.md</read_first>
  <behavior>
    - Test 1: watchlist outcomes require explicit named fields for why the item stayed on the watchlist and what evidence to gather next.
    - Test 2: the report contract exposes persisted watchlist fields that separate missing evidence, unresolved questions, and next session checks.
    - Test 3: the builder emits the enriched `research_queue` keys even before prompt enrichment lands, using compatibility-safe defaults instead of breaking current watchlist parsing.
  </behavior>
  <action>Create `tests/services/test_report_contracts.py` with failing tests named exactly `test_watchlist_outcome_items_expose_explicit_guidance_fields` and `test_research_needed_item_exposes_named_watchlist_guidance_fields`. Require `app/schemas/research.py` to define a new model named exactly `WatchlistCandidate(CandidateRecommendation)` with these additional fields and defaults: `watchlist_reason: str = ""`, `missing_evidence: list[str] = Field(default_factory=list)`, `unresolved_questions: list[str] = Field(default_factory=list)`, and `next_steps: list[str] = Field(default_factory=list)`. Change `WatchlistOutcome.items` to `List[WatchlistCandidate]`. Require `app/schemas/reports.py` to change `ResearchNeededItem` so it exposes exactly `ticker`, `thesis`, `watchlist_reason`, `missing_evidence`, `unresolved_questions`, `next_steps`, and `operator_action`; remove `uncertainty` and `follow_up_questions` from the contract instead of keeping overlapping fields. Update `app/schemas/workflow.py` to re-export `WatchlistCandidate` alongside the existing `ResearchOutcome` exports. Recreate `tests/services/test_report_builder.py` with failing tests named exactly `test_build_strategic_insight_report_emits_named_watchlist_guidance_fields` and `test_build_strategic_insight_report_keeps_no_action_research_queue_valid_without_watchlist_items`. In those tests, require `build_strategic_insight_report(...)` to place keys `watchlist_reason`, `missing_evidence`, `unresolved_questions`, and `next_steps` on `report.research_queue[0]`, and require the no-action branch to keep `report.research_queue[0].ticker == run_id.upper()` while still emitting the same four keys.</action>
  <acceptance_criteria>
    - `rg -n "test_watchlist_outcome_items_expose_explicit_guidance_fields|test_research_needed_item_exposes_named_watchlist_guidance_fields" tests/services/test_report_contracts.py` returns both schema tests.
    - `rg -n "class WatchlistCandidate|watchlist_reason: str = \"\"|missing_evidence: list\\[str\\] = Field\\(default_factory=list\\)|unresolved_questions: list\\[str\\] = Field\\(default_factory=list\\)|next_steps: list\\[str\\] = Field\\(default_factory=list\\)" app/schemas/research.py` returns the exact watchlist schema additions.
    - `rg -n "class ResearchNeededItem|watchlist_reason|missing_evidence|unresolved_questions|next_steps|operator_action" app/schemas/reports.py` returns the enriched report fields.
    - `rg -n "uncertainty|follow_up_questions" app/schemas/reports.py` returns no matches.
    - `rg -n "WatchlistCandidate" app/schemas/workflow.py` returns the new workflow export.
    - `rg -n "test_build_strategic_insight_report_emits_named_watchlist_guidance_fields|test_build_strategic_insight_report_keeps_no_action_research_queue_valid_without_watchlist_items" tests/services/test_report_builder.py` returns both recreated builder tests.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_report_contracts.py tests/services/test_report_builder.py -q</automated>
  </verify>
  <done>Missing report-contract tests exist again and the enriched watchlist/report schema seam is pinned to explicit named fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the enriched watchlist and report contracts with compatibility-safe builder defaults</name>
  <files>app/schemas/research.py, app/schemas/reports.py, app/schemas/workflow.py, app/services/report_builder.py, tests/services/test_report_builder.py, tests/services/test_report_contracts.py</files>
  <read_first>app/schemas/research.py, app/schemas/reports.py, app/schemas/workflow.py, app/services/report_builder.py, tests/services/test_report_contracts.py, tests/services/test_report_builder.py</read_first>
  <behavior>
    - Test 1: watchlist schema validation stays backward-compatible by defaulting new fields instead of requiring prompt changes immediately.
    - Test 2: the report builder emits enriched watchlist entries with deterministic field names and safe fallbacks for existing no-action behavior.
    - Test 3: no-action still uses the existing `ResearchOutcome -> StrategicInsightReport` pipeline and does not create a second watchlist-only report format.
  </behavior>
  <action>Implement `WatchlistCandidate` in `app/schemas/research.py` exactly as the new subclass of `CandidateRecommendation`, keeping all inherited candidate fields plus defaulted fields `watchlist_reason`, `missing_evidence`, `unresolved_questions`, and `next_steps`. Change `WatchlistOutcome.items` to `List[WatchlistCandidate]` and keep the discriminator contract unchanged. Update `app/schemas/reports.py` so `ResearchNeededItem` contains exactly `bucket: Literal["research"] = "research"`, `ticker: str`, `thesis: str = ""`, `watchlist_reason: str = ""`, `missing_evidence: list[str] = Field(default_factory=list)`, `unresolved_questions: list[str] = Field(default_factory=list)`, `next_steps: list[str] = Field(default_factory=list)`, and `operator_action: str = ""`. Update `app/services/report_builder.py` so the watchlist branch builds `ResearchNeededItem` with these exact compatibility defaults until richer prompt data arrives: `watchlist_reason=item.watchlist_reason or (item.opposing_evidence[0] if item.opposing_evidence else outcome.summary)`, `missing_evidence=item.missing_evidence or item.risk_notes or [outcome.summary]`, `unresolved_questions=item.unresolved_questions or item.opposing_evidence or [outcome.summary]`, `next_steps=item.next_steps or ["Review the next Quiver refresh before approval."]`, and `operator_action="Do not approve yet. Resolve the listed evidence gaps first."`. Update the no-action branch to emit one `ResearchNeededItem` with `ticker=run_id.upper()`, `thesis=outcome.summary`, `watchlist_reason=outcome.summary`, `missing_evidence=outcome.reasons or [outcome.summary]`, `unresolved_questions=[]`, `next_steps=["Wait for materially new Quiver evidence before reconsidering."]`, and `operator_action="Wait for materially new evidence before reconsidering."`. Do not change candidate or deferred report bucketing rules in this plan.</action>
  <acceptance_criteria>
    - `rg -n "class WatchlistCandidate\\(CandidateRecommendation\\)|items: List\\[WatchlistCandidate\\]" app/schemas/research.py` returns the exact watchlist type upgrade.
    - `rg -n "watchlist_reason=item\\.watchlist_reason or \\(item\\.opposing_evidence\\[0\\] if item\\.opposing_evidence else outcome\\.summary\\)|missing_evidence=item\\.missing_evidence or item\\.risk_notes or \\[outcome\\.summary\\]|unresolved_questions=item\\.unresolved_questions or item\\.opposing_evidence or \\[outcome\\.summary\\]|next_steps=item\\.next_steps or \\[\"Review the next Quiver refresh before approval\\.\"\\]|operator_action=\"Do not approve yet\\. Resolve the listed evidence gaps first\\.\"" app/services/report_builder.py` returns the exact compatibility mapping for watchlist items.
    - `rg -n "ticker=run_id\\.upper\\(\\)|watchlist_reason=outcome\\.summary|missing_evidence=outcome\\.reasons or \\[outcome\\.summary\\]|next_steps=\\[\"Wait for materially new Quiver evidence before reconsidering\\.\"\\]|operator_action=\"Wait for materially new evidence before reconsidering\\.\"" app/services/report_builder.py` returns the exact no-action mapping.
    - `python -m pytest tests/services/test_report_contracts.py tests/services/test_report_builder.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_report_contracts.py tests/services/test_report_builder.py -q</automated>
  </verify>
  <done>The repo now has an explicit persisted watchlist/report contract with compatibility-safe builder defaults and restored report-contract coverage.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/services/test_report_contracts.py tests/services/test_report_builder.py -q`. Then inspect `app/schemas/reports.py` and confirm `ResearchNeededItem` contains only the named watchlist-guidance fields, with no residual `uncertainty` or `follow_up_questions` fields.
</verification>

<success_criteria>
Phase 14 has a stable contract foundation: watchlist items and `research_queue` entries explicitly encode why an idea is not actionable, what evidence is missing, what questions remain, and what to investigate next.
</success_criteria>

<output>
After completion, create `.planning/phases/14-deepen-watchlist-explanations-and-follow-up-guidance/14-deepen-watchlist-explanations-and-follow-up-guidance-01-SUMMARY.md`
</output>

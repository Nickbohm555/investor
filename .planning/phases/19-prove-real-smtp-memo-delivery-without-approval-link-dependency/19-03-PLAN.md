---
phase: 19-prove-real-smtp-memo-delivery-without-approval-link-dependency
plan: 03
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
files_modified:
  - .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md
autonomous: false
requirements:
  - SMTP-19-01
must_haves:
  truths:
    - "A real scheduled-trigger attempt is made against the shipped runtime and its SMTP memo path."
    - "The proof artifact records whether SMTP delivery succeeded, where the memo landed, which approval host was rendered, and whether the run remained pending because callback execution was intentionally skipped."
    - "If live prerequisites are still missing, the result artifact records the exact blocker instead of silently passing or inventing fallback evidence."
  artifacts:
    - ".planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md :: filled proof record with real command output, logs, and inbox evidence"
  key_links:
    - "app.ops.live_proof -> POST /runs/trigger/scheduled :: real proof uses the shipped route-backed entrypoint"
    - "docker compose logs --no-color app -> 19-LIVE-PROOF-RESULT.md :: operator evidence captures the exact scheduled-trigger and memo-delivery log lines"
    - "delivered memo -> 19-LIVE-PROOF-RESULT.md :: inbox verification records the recipient and rendered approval host without requiring callback execution"
user_setup:
  - service: smtp
    why: "Real inbox delivery requires a working SMTP account and recipient mailbox Claude cannot provision"
    env_vars:
      - name: INVESTOR_SMTP_HOST
        source: "SMTP provider host settings"
      - name: INVESTOR_SMTP_PORT
        source: "SMTP provider port settings"
      - name: INVESTOR_SMTP_USERNAME
        source: "SMTP provider username"
      - name: INVESTOR_SMTP_PASSWORD
        source: "SMTP provider password or app password"
      - name: INVESTOR_SMTP_FROM_EMAIL
        source: "Verified sender address for the SMTP account"
      - name: INVESTOR_DAILY_MEMO_TO_EMAIL
        source: "Target operator inbox address"
  - service: quiver
    why: "The real scheduled route still uses live Quiver data"
    env_vars:
      - name: INVESTOR_QUIVER_API_KEY
        source: "Quiver dashboard or account settings"
  - service: openai-compatible-llm
    why: "The real scheduled route still uses the configured research model"
    env_vars:
      - name: INVESTOR_OPENAI_API_KEY
        source: "Model provider dashboard"
      - name: INVESTOR_OPENAI_BASE_URL
        source: "Model provider API base URL"
      - name: INVESTOR_OPENAI_MODEL
        source: "Model provider model identifier"
---

<objective>
Run the real Phase 19 SMTP proof through the shipped runtime, capture durable evidence, and pause for human inbox verification.

Purpose: The code and docs work only matter if the repo can either complete one real memo delivery or record the exact blocking reason from the real proof path.
Output: A populated Phase 19 proof result artifact plus a human verification checkpoint for inbox delivery and approval-host inspection.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-RESEARCH.md
@.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-VALIDATION.md
@.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RUNBOOK.md
@.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md
@README.md

<interfaces>
From app/ops/live_proof.py:
```python
python -m app.ops.live_proof preflight
python -m app.ops.live_proof trigger-scheduled
python -m app.ops.live_proof inspect-run --run-id <run_id>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the real scheduled-route SMTP proof and record machine-visible evidence</name>
  <files>.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md</files>
  <read_first>.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RUNBOOK.md, .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md, README.md, app/ops/live_proof.py</read_first>
  <acceptance_criteria>
    - `rg -n "^run_id:|^scheduled_trigger_status:|^smtp_ready:|^approval_reachability_ready:|^blocking_failures:|^warnings:|^scheduled_trigger_log_line:|^memo_delivery_log_line:|^current_step:|^approval_status:|^final_run_status:" .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md` returns all proof fields populated.
    - `rg -n "skipped-for-phase-19|memo_delivery result=|scheduled_trigger result=" .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md` returns the callback skip marker and observed log evidence.
  </acceptance_criteria>
  <action>Follow the Phase 19 runbook exactly. Start the runtime with `docker compose up -d --build`, tail readiness with `docker compose logs -f migrate app` long enough to confirm startup, run `python -m app.ops.live_proof preflight`, and record the exact returned values for `smtp_ready`, `approval_reachability_ready`, `blocking_failures`, and `warnings` in `19-LIVE-PROOF-RESULT.md`. If `blocking_failures` is non-empty, stop the proof attempt and write the exact blocker plus any SMTP diagnostic into `final_run_status` and `remaining_manual_steps`. If `blocking_failures` is empty, run `python -m app.ops.live_proof trigger-scheduled`, require `status` to be `started`, copy the returned `run_id`, then run `python -m app.ops.live_proof inspect-run --run-id <run_id>` and capture `current_step` plus `approval_status`. In all cases, collect `docker compose logs --no-color app` and copy the exact matching `scheduled_trigger result=` line and `memo_delivery result=` line into the result file when present. Do not click the approval link or simulate approval callback in this task.</action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/ops/test_live_proof.py tests/ops/test_operational_docs.py tests/services/test_mail_provider.py -q</automated>
  </verify>
  <done>The proof artifact contains the machine-visible preflight, trigger, inspect, and log evidence for a real Phase 19 SMTP proof attempt or its exact blocker.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify inbox delivery and rendered approval host</name>
  <files>.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md</files>
  <read_first>.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RUNBOOK.md, .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md</read_first>
  <acceptance_criteria>
    - The user confirms whether a memo for the recorded `run_id` arrived in `INVESTOR_DAILY_MEMO_TO_EMAIL`.
    - The user confirms whether the approval link host in the delivered memo matches `INVESTOR_EXTERNAL_BASE_URL`.
    - The result artifact is updated after the checkpoint with the user-confirmed recipient and host values or the failure observed.
  </acceptance_criteria>
  <action>Pause after the automated proof attempt and collect human confirmation for the only non-CLI proof surface: inbox delivery and the rendered approval-link host. Treat callback execution as intentionally out of scope for this phase. Capture the user's response exactly enough that Task 3 can update `19-LIVE-PROOF-RESULT.md` with either confirmed delivery details or a concrete mismatch/failure.</action>
  <what-built>Claude attempted the real Phase 19 SMTP proof, captured preflight/trigger/log evidence, and prepared the result artifact for inbox verification.</what-built>
  <how-to-verify>
    1. Open the inbox configured by `INVESTOR_DAILY_MEMO_TO_EMAIL` and find the memo for the recorded `run_id`.
    2. Confirm whether the memo arrived; if it did, copy the recipient address you verified.
    3. Inspect the approval link in the delivered memo and confirm whether its host exactly matches `INVESTOR_EXTERNAL_BASE_URL`.
    4. Do not click the approval link for this phase. Reply with `approved` plus the verified recipient and host, or describe what was missing.
  </how-to-verify>
  <verify>
    <automated>printf "checkpoint: waiting for human inbox verification\n"</automated>
  </verify>
  <done>The executor has either human-confirmed inbox delivery and approval-host verification inputs, or a concrete human-reported failure to record in the proof artifact.</done>
  <resume-signal>Type `approved` plus the verified recipient/host, or describe the inbox or host mismatch.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize the Phase 19 proof artifact after inbox verification</name>
  <files>.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md</files>
  <read_first>.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md, .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RUNBOOK.md</read_first>
  <acceptance_criteria>
    - `rg -n "^memo_delivered_to:|^approval_link_host:|^approval_callback_status: skipped-for-phase-19|^final_run_status:" .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md` returns the finalized human-confirmed fields.
    - `rg -n "remaining_manual_steps:" .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md` returns either `none` or the exact next blockers.
  </acceptance_criteria>
  <action>Update `19-LIVE-PROOF-RESULT.md` with the user-confirmed inbox recipient and approval-link host. Set `approval_callback_status: skipped-for-phase-19` explicitly even when delivery succeeds. If the inbox verification failed, record the exact observed issue in `final_run_status` and add concrete next steps in `remaining_manual_steps`. If the inbox verification succeeded and the host matched, record `final_run_status: smtp-proof-complete` and set `remaining_manual_steps: none` unless other live-provider blockers remain outside SMTP delivery.</action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>The proof artifact ends with either a completed SMTP proof record or an exact, durable blocker description after human inbox verification.</done>
</task>

</tasks>

<verification>
Run `PYTHONPATH=. pytest tests/ops/test_live_proof.py tests/ops/test_operational_docs.py tests/services/test_mail_provider.py -q`. Then confirm `.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md` contains the final recipient, rendered approval host, callback skip marker, and final proof status.
</verification>

<success_criteria>
Phase 19 leaves behind a durable proof record showing one real scheduled-route SMTP delivery attempt, the inbox outcome, the rendered approval host, and whether approval reachability was merely a warning or still a blocker outside this phase's proof scope.
</success_criteria>

<output>
After completion, create `.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-prove-real-smtp-memo-delivery-without-approval-link-dependency-03-SUMMARY.md`
</output>

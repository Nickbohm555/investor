---
phase: 19-prove-real-smtp-memo-delivery-without-approval-link-dependency
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - README.md
  - tests/ops/test_operational_docs.py
  - .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RUNBOOK.md
  - .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md
autonomous: true
requirements:
  - SMTP-19-01
  - SMTP-19-03
must_haves:
  truths:
    - "Operators have one repo-owned Phase 19 runbook that tells them to prove real SMTP delivery through the scheduled route without requiring approval callback execution."
    - "The delivered memo is still expected to contain the configured approval-link host, but the proof contract only requires host-string verification and explicit callback skip recording."
    - "README and docs tests point to the current Phase 19 SMTP-proof workflow instead of the older callback-dependent Phase 15 contract."
  artifacts:
    - "README.md :: current link to the Phase 19 SMTP proof workflow and exact CLI commands"
    - ".planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RUNBOOK.md :: runbook for non-blocking approval reachability proof"
    - ".planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md :: evidence template for real inbox delivery and approval-host verification"
    - "tests/ops/test_operational_docs.py :: drift coverage for the new Phase 19 docs contract"
  key_links:
    - "README.md -> .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RUNBOOK.md :: repo entrypoint points to the current SMTP proof runbook"
    - "tests/ops/test_operational_docs.py -> README.md and Phase 19 proof assets :: docs drift coverage moves with the contract change"
    - "19-LIVE-PROOF-RUNBOOK.md -> app.ops.live_proof :: runbook commands stay on the shipped CLI and scheduled route"
---

<objective>
Move the operator-facing proof assets to a Phase 19 SMTP-only proof contract and lock them with docs tests.

Purpose: Once the executable proof contract changes, the README, runbook, and result template must change with it so operators do not keep following the old callback-dependent instructions.
Output: Updated README/docs tests plus Phase 19 runbook and result template that reflect the non-blocking approval-link reachability contract.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-RESEARCH.md
@.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-VALIDATION.md
@README.md
@tests/ops/test_operational_docs.py
@.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md
@.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md

<interfaces>
From app/ops/live_proof.py:
```python
python -m app.ops.live_proof preflight
python -m app.ops.live_proof trigger-scheduled
python -m app.ops.live_proof inspect-run --run-id <run_id>
```

From tests/ops/test_operational_docs.py:
```python
def test_readme_links_phase_15_live_proof_runbook() -> None:
def test_phase_15_runbook_covers_reachability_preflight_and_persisted_state_checks() -> None:
def test_phase_15_result_template_requires_operator_visible_proof_fields() -> None:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing docs drift tests for the Phase 19 SMTP-proof contract</name>
  <files>tests/ops/test_operational_docs.py, README.md, .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RUNBOOK.md, .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md</files>
  <read_first>tests/ops/test_operational_docs.py, README.md, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md, .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-RESEARCH.md</read_first>
  <behavior>
    - Test 1: README points to the Phase 19 runbook and the existing `app.ops.live_proof` commands.
    - Test 2: The Phase 19 runbook explicitly treats approval reachability as non-blocking and requires only approval-host verification in the delivered memo.
    - Test 3: The Phase 19 result template records inbox delivery, approval-link host verification, and an explicit callback skip status.
  </behavior>
  <action>Update `tests/ops/test_operational_docs.py` by adding failing tests named exactly `test_readme_links_phase_19_live_proof_runbook`, `test_phase_19_runbook_treats_approval_reachability_as_non_blocking`, and `test_phase_19_result_template_records_smtp_proof_without_callback_execution`. Require `README.md` to contain the exact sentence `See \`.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RUNBOOK.md\` for the current SMTP delivery proof workflow.` and to keep the exact commands `python -m app.ops.live_proof preflight`, `python -m app.ops.live_proof trigger-scheduled`, and `python -m app.ops.live_proof inspect-run --run-id <run_id>`. Require the new Phase 19 runbook file to contain the exact headings `## Preflight`, `## Start The Runtime`, `## Trigger The Live Run`, `## Inbox Verification`, `## Approval Host Verification`, `## Persisted State Verification`, `## Observed Logs`, and `## Failure Markers`, plus the exact phrase `approval reachability is a warning for Phase 19, not a blocker`. Require the new result template file to contain the exact field labels `run_id:`, `scheduled_trigger_status:`, `memo_delivered_to:`, `approval_link_host:`, `approval_callback_status: skipped-for-phase-19`, `smtp_ready:`, `approval_reachability_ready:`, `blocking_failures:`, `warnings:`, `scheduled_trigger_log_line:`, `memo_delivery_log_line:`, `current_step:`, `approval_status:`, `final_run_status:`, and `remaining_manual_steps:`. Leave README and the new Phase 19 files absent or unchanged until Task 2 so these assertions fail first.</action>
  <acceptance_criteria>
    - `rg -n "test_readme_links_phase_19_live_proof_runbook|test_phase_19_runbook_treats_approval_reachability_as_non_blocking|test_phase_19_result_template_records_smtp_proof_without_callback_execution" tests/ops/test_operational_docs.py` returns all three Phase 19 docs tests.
    - `rg -n "19-LIVE-PROOF-RUNBOOK.md|approval reachability is a warning for Phase 19, not a blocker|approval_callback_status: skipped-for-phase-19" tests/ops/test_operational_docs.py` returns the exact new contract strings.
    - `test -f .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RUNBOOK.md || true` leaves the runbook absent before implementation.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>Red docs tests pin the README and Phase 19 proof-asset contract before the docs are written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the Phase 19 runbook, result template, and README routing</name>
  <files>README.md, tests/ops/test_operational_docs.py, .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RUNBOOK.md, .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md</files>
  <read_first>README.md, tests/ops/test_operational_docs.py, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md, .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-RESEARCH.md, app/ops/live_proof.py</read_first>
  <behavior>
    - Test 1: Operators are routed from README to the new Phase 19 proof flow.
    - Test 2: The runbook tells operators to attempt the real send even if approval reachability is unavailable, while still recording that status.
    - Test 3: The proof artifact records delivery evidence and callback skip status without pretending callback execution happened.
  </behavior>
  <action>Update `README.md` so the Go-Live or live-proof section contains the exact sentence `See \`.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RUNBOOK.md\` for the current SMTP delivery proof workflow.` and remove the Phase 15 runbook sentence from the active proof guidance. Create `.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RUNBOOK.md` with the exact headings required by Task 1. In `## Preflight`, require `python -m app.ops.live_proof preflight`, instruct the operator to record `smtp_ready`, `approval_reachability_ready`, `blocking_failures`, and `warnings`, and state the exact sentence `approval reachability is a warning for Phase 19, not a blocker`. In `## Start The Runtime`, require `docker compose up -d --build` and `docker compose logs -f migrate app`. In `## Trigger The Live Run`, require `python -m app.ops.live_proof trigger-scheduled` and state that only `status: started` counts as the passing trigger result. In `## Inbox Verification`, require confirming the memo reached `INVESTOR_DAILY_MEMO_TO_EMAIL`. In `## Approval Host Verification`, require checking that the delivered memo contains an approval link whose host matches `INVESTOR_EXTERNAL_BASE_URL`, and require recording `approval_callback_status: skipped-for-phase-19` instead of clicking the link. In `## Persisted State Verification`, require `python -m app.ops.live_proof inspect-run --run-id <run_id>` and require recorded `current_step` and `approval_status` even when no callback occurred yet. In `## Observed Logs`, require capturing exact `scheduled_trigger result=` and `memo_delivery result=` lines from `docker compose logs --no-color app`. Create `.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md` as a fill-in template with the exact field labels from Task 1 and placeholder value `<pending>` on each line except `approval_callback_status: skipped-for-phase-19`. Do not delete the Phase 15 historical files; this task creates the new current contract and reroutes README/tests to it.</action>
  <acceptance_criteria>
    - `rg -n "19-LIVE-PROOF-RUNBOOK.md|python -m app\\.ops\\.live_proof preflight|python -m app\\.ops\\.live_proof trigger-scheduled|python -m app\\.ops\\.live_proof inspect-run --run-id <run_id>" README.md .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RUNBOOK.md` returns the README link and all three CLI commands.
    - `rg -n "approval reachability is a warning for Phase 19, not a blocker|## Approval Host Verification|approval_callback_status: skipped-for-phase-19" .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RUNBOOK.md .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-LIVE-PROOF-RESULT.md` returns the exact non-blocking and skip-status strings.
    - `PYTHONPATH=. pytest tests/ops/test_operational_docs.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>The active proof docs and templates now match the Phase 19 SMTP-only proof contract and are locked by tests.</done>
</task>

</tasks>

<verification>
Run `PYTHONPATH=. pytest tests/ops/test_operational_docs.py -q`. Then read the Phase 19 runbook and confirm it never requires clicking the approval link, but still requires verifying the rendered host string in the delivered memo.
</verification>

<success_criteria>
The current operator guidance points to a Phase 19 runbook and result template that preserve approval-link rendering but narrow success to real SMTP delivery plus approval-host verification.
</success_criteria>

<output>
After completion, create `.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-prove-real-smtp-memo-delivery-without-approval-link-dependency-02-SUMMARY.md`
</output>

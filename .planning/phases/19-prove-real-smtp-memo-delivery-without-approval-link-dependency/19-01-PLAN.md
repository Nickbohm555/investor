---
phase: 19-prove-real-smtp-memo-delivery-without-approval-link-dependency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - app/services/mail_provider.py
  - app/ops/live_proof.py
  - tests/ops/test_live_proof.py
  - tests/services/test_mail_provider.py
autonomous: true
requirements:
  - SMTP-19-02
  - SMTP-19-04
must_haves:
  truths:
    - "Preflight reports SMTP readiness and approval-link reachability as separate statuses, and an unreachable approval host does not block the Phase 19 SMTP proof path."
    - "SMTP transport checks and real memo delivery either support the provider's configured TLS mode or fail with a clear transport-mode diagnostic before a misleading proof attempt."
    - "The repo keeps the existing scheduled-trigger proof path instead of creating a second SMTP-only execution surface."
  artifacts:
    - "app/ops/live_proof.py :: split-status preflight and scheduled-trigger proof helper"
    - "app/services/mail_provider.py :: SMTP transport implementation that supports or diagnoses the configured security mode"
    - "tests/ops/test_live_proof.py :: ops coverage for non-blocking approval reachability and blocking SMTP readiness"
    - "tests/services/test_mail_provider.py :: transport-mode coverage for STARTTLS, implicit TLS, and unsupported configuration"
  key_links:
    - "app/ops/live_proof.py -> app/services/mail_provider.py :: preflight and runtime share one SMTP transport-mode contract instead of drifting"
    - "app/ops/live_proof.py -> app/config.py :: proof status is driven by the existing INVESTOR_* settings, including external_base_url and SMTP fields"
    - "app/ops/live_proof.py -> /runs/trigger/scheduled :: live proof still enters through the shipped route rather than calling workflow internals directly"
---

<objective>
Split the live-proof contract so SMTP delivery readiness is blocking, approval-link reachability is non-blocking, and SMTP transport-mode handling matches real provider behavior.

Purpose: Phase 19 is a contract correction over the existing live-proof flow, not a new mail subsystem. This plan makes the proof tooling and transport seam truthful before any real inbox proof attempt.
Output: Updated SMTP transport behavior or fail-fast diagnostics, plus tests that lock the Phase 19 preflight contract.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-RESEARCH.md
@.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-VALIDATION.md
@.planning/phases/03-scheduling-and-email-delivery/03-02-SUMMARY.md
@.planning/phases/03-scheduling-and-email-delivery/03-03-SUMMARY.md
@.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-prove-the-live-quiver-to-email-workflow-end-to-end-01-SUMMARY.md
@app/config.py
@app/services/mail_provider.py
@app/ops/live_proof.py
@tests/ops/test_live_proof.py

<interfaces>
From app/config.py:
```python
class Settings(BaseSettings):
    smtp_host: str = "smtp.example.com"
    smtp_port: int = 587
    smtp_username: str = "investor-user"
    smtp_password: str = "change-me"
    smtp_from_email: str = "investor@example.com"
    external_base_url: str = "https://investor.example.com"
```

From app/services/mail_provider.py:
```python
class MailProvider(Protocol):
    def send(self, subject: str, text_body: str, html_body: str, recipient: str) -> None:
        ...

class SmtpMailProvider:
    def send(self, subject: str, text_body: str, html_body: str, recipient: str) -> None:
        ...
```

From app/ops/live_proof.py:
```python
def _check_smtp(settings) -> dict[str, object]:
def _check_reachability(settings) -> dict[str, object]:
def _run_preflight(settings):
def _trigger_scheduled(settings):
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for the split preflight contract and SMTP transport modes</name>
  <files>tests/ops/test_live_proof.py, tests/services/test_mail_provider.py, app/ops/live_proof.py, app/services/mail_provider.py</files>
  <read_first>tests/ops/test_live_proof.py, app/ops/live_proof.py, app/services/mail_provider.py, app/config.py, .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-RESEARCH.md, .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-VALIDATION.md</read_first>
  <behavior>
    - Test 1: preflight returns blocking SMTP readiness and non-blocking approval-link reachability as separate fields.
    - Test 2: approval reachability failures remain warnings while SMTP transport failures remain blocking failures.
    - Test 3: SMTP delivery and preflight cover STARTTLS on 587, implicit TLS on 465, and unsupported-mode diagnostics.
  </behavior>
  <action>Extend `tests/ops/test_live_proof.py` with failing tests named exactly `test_preflight_reports_blocking_smtp_status_and_non_blocking_approval_reachability`, `test_preflight_does_not_block_on_unreachable_approval_host`, and `test_preflight_surfaces_smtp_transport_mode_diagnostic`. Require `_run_preflight(settings)` to return the exact top-level keys `quiver_checks`, `llm_check`, `smtp_check`, `external_base_url`, `reachability_check`, `smtp_ready`, `approval_reachability_ready`, `blocking_failures`, and `warnings`. Require `blocking_failures` to contain the exact string `smtp` when SMTP is not ready, and require `warnings` to contain the exact string `approval-link-unreachable` when the approval probe cannot connect. Create `tests/services/test_mail_provider.py` with failing tests named exactly `test_smtp_mail_provider_uses_starttls_on_port_587`, `test_smtp_mail_provider_uses_smtp_ssl_on_port_465`, and `test_smtp_mail_provider_raises_clear_error_for_unsupported_transport_mode`. In those tests, pin one concrete contract: port `587` uses `smtplib.SMTP(...).starttls(...)`, port `465` uses `smtplib.SMTP_SSL(...)`, and any unsupported security configuration raises a `ValueError` whose message contains the exact string `Unsupported SMTP transport mode`. Do not edit README or runbook files in this task; keep the suite red until the runtime code matches the new contract.</action>
  <acceptance_criteria>
    - `rg -n "test_preflight_reports_blocking_smtp_status_and_non_blocking_approval_reachability|test_preflight_does_not_block_on_unreachable_approval_host|test_preflight_surfaces_smtp_transport_mode_diagnostic" tests/ops/test_live_proof.py` returns all three new ops tests.
    - `rg -n "smtp_ready|approval_reachability_ready|blocking_failures|warnings|approval-link-unreachable" tests/ops/test_live_proof.py` returns the split-status assertions.
    - `rg -n "test_smtp_mail_provider_uses_starttls_on_port_587|test_smtp_mail_provider_uses_smtp_ssl_on_port_465|test_smtp_mail_provider_raises_clear_error_for_unsupported_transport_mode" tests/services/test_mail_provider.py` returns all three mail-provider tests.
    - `rg -n "Unsupported SMTP transport mode|SMTP_SSL|starttls" tests/services/test_mail_provider.py` returns the exact transport-mode contract strings.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest tests/ops/test_live_proof.py tests/services/test_mail_provider.py -q</automated>
  </verify>
  <done>Red tests pin the Phase 19 proof-state split and SMTP transport contract before implementation starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement split preflight status and shared SMTP transport handling</name>
  <files>app/config.py, app/services/mail_provider.py, app/ops/live_proof.py, tests/ops/test_live_proof.py, tests/services/test_mail_provider.py</files>
  <read_first>app/config.py, app/services/mail_provider.py, app/ops/live_proof.py, tests/ops/test_live_proof.py, tests/services/test_mail_provider.py, .planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Phase 19 preflight classifies SMTP readiness as blocking and approval reachability as non-blocking.
    - Test 2: The real send path and the preflight path share one SMTP transport-mode decision.
    - Test 3: Implicit TLS providers on 465 work, and unsupported modes fail fast with a transport-specific diagnostic instead of a vague connection error.
  </behavior>
  <action>Update `app/config.py` to add a settings field named exactly `smtp_security` with a `Literal["starttls", "ssl", "auto"]` default of `"auto"`. In `app/services/mail_provider.py`, factor the SMTP connection selection into one shared helper that chooses `smtplib.SMTP` plus `starttls(...)` when the effective mode is STARTTLS, chooses `smtplib.SMTP_SSL` when the effective mode is implicit TLS, and raises `ValueError("Unsupported SMTP transport mode")` when the configured `smtp_security` and `smtp_port` combination cannot be satisfied. The effective defaults must be concrete: `smtp_security="auto"` + port `587` means STARTTLS, and `smtp_security="auto"` + port `465` means implicit TLS. Use the same helper from `SmtpMailProvider.send(...)` and `app.ops.live_proof._check_smtp(settings)` so preflight and delivery cannot drift. In `app/ops/live_proof.py`, keep the existing `POST /runs/trigger/scheduled` proof path, but change `_run_preflight(settings)` to return `smtp_ready` and `approval_reachability_ready` as separate booleans, `blocking_failures` as a list containing only blocking items such as `smtp`, and `warnings` as a list containing only non-blocking items such as `approval-link-unreachable`. Keep `external_base_url` and `reachability_check` in the payload, keep approval-link rendering untouched in `app/graph/workflow.py`, and do not require approval callback execution for this plan's success. If `_check_smtp(settings)` fails because the transport mode is unsupported, include the exact diagnostic message under `smtp_check["error"]` and mark `smtp_ready` false.</action>
  <acceptance_criteria>
    - `rg -n "smtp_security: Literal\\[\"starttls\", \"ssl\", \"auto\"\\] = \"auto\"" app/config.py` returns the new settings field.
    - `rg -n "SMTP_SSL|Unsupported SMTP transport mode|smtp_security" app/services/mail_provider.py app/ops/live_proof.py` returns the shared transport-mode logic and diagnostic string.
    - `rg -n "smtp_ready|approval_reachability_ready|blocking_failures|warnings" app/ops/live_proof.py` returns the split-status preflight fields.
    - `rg -n "\"smtp\"|\"approval-link-unreachable\"" app/ops/live_proof.py tests/ops/test_live_proof.py` returns the exact blocking and warning markers.
    - `PYTHONPATH=. pytest tests/ops/test_live_proof.py tests/services/test_mail_provider.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest tests/ops/test_live_proof.py tests/services/test_mail_provider.py -q</automated>
  </verify>
  <done>The codebase has one tested SMTP transport contract and one tested Phase 19 preflight contract that separates SMTP readiness from approval reachability.</done>
</task>

</tasks>

<verification>
Run `PYTHONPATH=. pytest tests/ops/test_live_proof.py tests/services/test_mail_provider.py -q`. Then run `python -m app.ops.live_proof preflight` with a STARTTLS config and an SSL-on-465 config and confirm both payloads include `smtp_ready`, `approval_reachability_ready`, `blocking_failures`, and `warnings`.
</verification>

<success_criteria>
The repo still proves mail through the scheduled route, but Phase 19 preflight no longer blocks on approval-host reachability, and SMTP transport behavior is explicit enough to support real providers or fail fast with a clear message.
</success_criteria>

<output>
After completion, create `.planning/phases/19-prove-real-smtp-memo-delivery-without-approval-link-dependency/19-prove-real-smtp-memo-delivery-without-approval-link-dependency-01-SUMMARY.md`
</output>

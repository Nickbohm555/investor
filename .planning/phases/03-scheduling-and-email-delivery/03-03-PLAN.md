---
phase: 03-scheduling-and-email-delivery
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - app/api/routes.py
  - app/services/mail_provider.py
  - scripts/cron-trigger.sh
  - tests/api/test_scheduling.py
  - tests/api/test_routes.py
  - tests/ops/test_cron_scripts.py
  - tests/services/test_mail_provider.py
  - README.md
autonomous: true
requirements:
  - SCHD-02
  - SCHD-03
  - MAIL-01
  - MAIL-02
  - MAIL-03
must_haves:
  truths:
    - "Duplicate scheduled triggers do not resend the primary daily memo after the first successful send."
    - "Operators can inspect log output and see whether a scheduled trigger started, duplicated, sent mail, or failed."
    - "The repository documents the exact verification path for cron installation, scheduled trigger behavior, and SMTP-backed delivery."
  artifacts:
    - path: "app/api/routes.py"
      provides: "Operational logging and duplicate-send guard on scheduled trigger flow"
      contains: "scheduled_trigger result="
    - path: "app/services/mail_provider.py"
      provides: "Delivery logging hooks around SMTP sends"
      contains: "memo_delivery result="
    - path: "README.md"
      provides: "Operational verification commands for cron and email delivery"
      contains: "scripts/cron-install.sh"
  key_links:
    - from: "app/api/routes.py"
      to: "app/services/mail_provider.py"
      via: "Scheduled run path sends exactly once on the new-run branch"
      pattern: "duplicate.*False|send\\("
    - from: "scripts/cron-trigger.sh"
      to: "README.md"
      via: "Documented operator command matches the installed wrapper"
      pattern: "cron-trigger.sh"
    - from: "tests/api/test_scheduling.py"
      to: "app/api/routes.py"
      via: "Integration tests assert duplicate scheduled requests do not resend mail"
      pattern: "duplicate|sent_messages"
---

<objective>
Finish Phase 3 by wiring observable delivery behavior and expanding integration verification.

Purpose: Close the operational gap between scheduled triggering and real memo delivery so duplicate schedule windows do not resend mail and operators have concrete logs and docs to validate the workflow.
Output: Operational logging around scheduled trigger and mail send paths, duplicate-send integration coverage, and updated verification docs.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-scheduling-and-email-delivery/03-RESEARCH.md
@.planning/phases/03-scheduling-and-email-delivery/03-01-PLAN.md
@.planning/phases/03-scheduling-and-email-delivery/03-02-PLAN.md
@app/api/routes.py
@app/services/mail_provider.py
@scripts/cron-trigger.sh
@README.md

<interfaces>
From 03-01:
```python
@router.post("/runs/trigger/scheduled")
def trigger_scheduled_run(...) -> dict:
    ...
```

From 03-02:
```python
class MailProvider(Protocol):
    def send(subject: str, text_body: str, html_body: str, recipient: str) -> None:
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add operational logging and duplicate-send protection around scheduled delivery</name>
  <files>app/api/routes.py
app/graph/workflow.py
app/services/mail_provider.py
scripts/cron-trigger.sh
tests/api/test_scheduling.py
tests/services/test_mail_provider.py</files>
  <read_first>.planning/REQUIREMENTS.md
.planning/phases/03-scheduling-and-email-delivery/03-RESEARCH.md
.planning/phases/03-scheduling-and-email-delivery/03-02-PLAN.md
app/api/routes.py
app/graph/workflow.py
app/services/mail_provider.py
scripts/cron-trigger.sh
tests/api/test_scheduling.py</read_first>
  <acceptance_criteria>- `rg -n "scheduled_trigger result=started|scheduled_trigger result=duplicate|scheduled_trigger result=failure" app/api/routes.py scripts/cron-trigger.sh tests/api/test_scheduling.py` returns all three route/wrapper log prefixes.
- `rg -n "memo_delivery result=sent|memo_delivery result=failure" app/services/mail_provider.py tests/services/test_mail_provider.py` returns both delivery log prefixes.
- `rg -n "sent_messages|duplicate.*true|assert .*len\\(sent_messages\\) == 1" tests/api/test_scheduling.py` returns the duplicate-send guard assertions.
- `pytest tests/api/test_scheduling.py tests/services/test_mail_provider.py -q` exits with code 0.</acceptance_criteria>
  <behavior>
    - Test 1: The first scheduled trigger logs `scheduled_trigger result=started` and sends exactly one memo.
    - Test 2: A duplicate scheduled trigger logs `scheduled_trigger result=duplicate` and does not increment the sent-message count.
    - Test 3: SMTP send failures log `memo_delivery result=failure` and propagate a non-success status back to the wrapper.
  </behavior>
  <action>Keep duplicate-send suppression in `app/api/routes.py`, not in the shell wrapper: the scheduled route must decide whether to invoke the workflow/mail path at all. Update `app/api/routes.py` so the scheduled trigger branch logs one of three exact prefixes through the Python logger: `scheduled_trigger result=started`, `scheduled_trigger result=duplicate`, or `scheduled_trigger result=failure`, always including `schedule_key=` and `run_id=` fields in the message. On the duplicate branch, return the existing run immediately before calling `workflow.invoke(...)` or `request.app.state.mail_provider.send(...)`; on the new-run branch, continue into the already-wired Phase 02 mail path exactly once. Update `app/graph/workflow.py` only as needed so the send seam remains provider-driven and observable from the route tests. Update `app/services/mail_provider.py` so successful sends log `memo_delivery result=sent provider=smtp recipient=<email>` and exceptions log `memo_delivery result=failure provider=smtp recipient=<email>`. Update `scripts/cron-trigger.sh` so it mirrors the app outcome in the wrapper log by writing `scheduled_trigger result=<value>` lines rather than generic success/failure text. Extend `tests/api/test_scheduling.py` to track a `sent_messages` spy and assert the count stays at exactly `1` after two same-day scheduled triggers.</action>
  <verify>
    <automated>pytest tests/api/test_scheduling.py tests/services/test_mail_provider.py -q</automated>
  </verify>
  <done>The scheduled path emits explicit operational logs and the duplicate branch is proven not to resend the daily memo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand integration verification and operator docs for the scheduled email path</name>
  <files>tests/api/test_routes.py
tests/api/test_scheduling.py
tests/ops/test_cron_scripts.py
README.md</files>
  <read_first>.planning/REQUIREMENTS.md
README.md
tests/api/test_routes.py
tests/api/test_scheduling.py
tests/ops/test_cron_scripts.py
scripts/cron-install.sh
scripts/cron-trigger.sh</read_first>
  <acceptance_criteria>- `rg -n "scripts/cron-install.sh|scripts/cron-status.sh|scripts/cron-remove.sh|pytest tests/api/test_scheduling.py" README.md` returns the cron commands and test command references.
- `rg -n "external_base_url|/approval/|duplicate|managed=present|scheduled_trigger result=" tests/api/test_routes.py tests/api/test_scheduling.py tests/ops/test_cron_scripts.py` returns all required integration assertions.
- `pytest tests/ops/test_cron_scripts.py tests/api/test_scheduling.py tests/api/test_routes.py -q` exits with code 0.</acceptance_criteria>
  <behavior>
    - Test 1: Route coverage proves approval URLs still resolve through `/approval/{token}` after the Phase 3 memo changes.
    - Test 2: Cron-script coverage proves install/status/remove behavior and wrapper log output remain in sync with the README commands.
    - Test 3: The README gives an operator a complete repo-local verification sequence for cron install, scheduled trigger, and SMTP-backed memo delivery.
  </behavior>
  <action>Broaden `tests/api/test_routes.py` to assert the approval callback still accepts tokens generated from the configured external-base-url memo flow. Expand `tests/ops/test_cron_scripts.py` so it checks `managed=present`, `managed=absent`, and `scheduled_trigger result=` output from the wrapper and status scripts. Update `README.md` verification instructions to include these exact operator commands: `./scripts/cron-install.sh`, `./scripts/cron-status.sh`, `./scripts/cron-trigger.sh`, `pytest tests/api/test_scheduling.py tests/ops/test_cron_scripts.py -q`, and `./scripts/cron-remove.sh`. Add one short SMTP verification note telling the operator to confirm the received message uses the configured `INVESTOR_EXTERNAL_BASE_URL` host in both approval links.</action>
  <verify>
    <automated>pytest tests/ops/test_cron_scripts.py tests/api/test_scheduling.py tests/api/test_routes.py -q</automated>
  </verify>
  <done>The repository now has end-to-end automated coverage and operator-facing docs for cron-managed scheduled delivery, duplicate protection, and public approval-link behavior.</done>
</task>

</tasks>

<verification>
Run `pytest tests/ops/test_cron_scripts.py tests/api/test_scheduling.py tests/api/test_routes.py tests/services/test_mail_provider.py -q` and confirm duplicate scheduled runs do not resend mail, operational logs contain the exact result prefixes, and README commands match the implemented scripts.
</verification>

<success_criteria>
Phase 3 closes with schedule/email integration that is observable, duplicate-safe, and documented: the first scheduled run sends one memo, repeated same-window triggers are logged as duplicates without resending, and the repo contains the exact commands needed to verify the workflow locally.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-and-email-delivery/03-03-SUMMARY.md`
</output>

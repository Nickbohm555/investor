---
phase: 03-scheduling-and-email-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .env.example
  - app/config.py
  - app/services/mail_provider.py
  - app/services/email.py
  - app/services/tokens.py
  - app/graph/workflow.py
  - tests/services/test_mail_provider.py
  - tests/services/test_email.py
  - tests/services/test_tokens.py
  - tests/graph/test_workflow.py
  - tests/conftest.py
autonomous: true
requirements:
  - MAIL-01
  - MAIL-02
  - MAIL-03
user_setup:
  - service: smtp
    why: "Real daily memo delivery"
    env_vars:
      - name: INVESTOR_SMTP_HOST
        source: "SMTP provider hostname"
      - name: INVESTOR_SMTP_PORT
        source: "SMTP provider port"
      - name: INVESTOR_SMTP_USERNAME
        source: "SMTP provider username"
      - name: INVESTOR_SMTP_PASSWORD
        source: "SMTP provider password"
      - name: INVESTOR_SMTP_FROM_EMAIL
        source: "Verified sender address"
      - name: INVESTOR_DAILY_MEMO_TO_EMAIL
        source: "Recipient inbox"
      - name: INVESTOR_EXTERNAL_BASE_URL
        source: "Public base URL that should appear in approval links"
must_haves:
  truths:
    - "Daily memos send through a provider abstraction with SMTP implemented as the first real transport."
    - "Memo content clearly renders ranked candidates, watchlist items, or no-action reasons instead of a single ticker string."
    - "Approval and rejection links are signed, expire on the configured TTL, and always use the configured external base URL."
  artifacts:
    - path: "app/services/mail_provider.py"
      provides: "Mail provider protocol and SMTP adapter"
      contains: "class SmtpMailProvider"
    - path: "app/services/email.py"
      provides: "Branch-aware memo renderer"
      contains: "Ranked Candidates"
    - path: "app/services/tokens.py"
      provides: "Signed approval token helpers scoped to run and decision"
      contains: "salt=\"approval-token\""
  key_links:
    - from: "app/graph/workflow.py"
      to: "app/services/tokens.py"
      via: "Workflow signs approve/reject tokens per run before composing email"
      pattern: "sign_approval_token"
    - from: "app/graph/workflow.py"
      to: "app/services/email.py"
      via: "Workflow passes structured memo content into the renderer"
      pattern: "compose_recommendation_email"
    - from: "app/services/mail_provider.py"
      to: "app/config.py"
      via: "SMTP adapter reads explicit host, port, auth, sender, and recipient settings"
      pattern: "smtp_host|smtp_port|smtp_username|smtp_password"
---

<objective>
Implement the real email delivery and signed-link memo path for Phase 3.

Purpose: Satisfy `MAIL-01`, `MAIL-02`, and `MAIL-03` by replacing console-email placeholders with an SMTP-backed provider abstraction and memo content that uses configured public links.
Output: SMTP settings, a provider abstraction, branch-aware memo rendering, and signed approval/rejection links with tests.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-scheduling-and-email-delivery/03-RESEARCH.md
@.planning/phases/03-scheduling-and-email-delivery/03-01-PLAN.md
@app/config.py
@app/services/email.py
@app/services/tokens.py
@app/graph/workflow.py
@tests/services/test_email.py
@tests/services/test_tokens.py

<interfaces>
From app/services/tokens.py:
```python
def sign_approval_token(run_id: str, decision: str, secret: str, ttl_seconds: int) -> str:
    ...

def verify_approval_token(token: str, secret: str, ttl_seconds: int) -> ApprovalTokenPayload:
    ...
```

From app/services/email.py:
```python
def compose_recommendation_email(
    run_id: str,
    recommendations: list,
    approval_url: str,
    rejection_url: str,
) -> RecommendationEmail:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the mail provider abstraction and SMTP settings</name>
  <files>.env.example
app/config.py
app/services/mail_provider.py
tests/services/test_mail_provider.py
tests/conftest.py</files>
  <read_first>.planning/REQUIREMENTS.md
.planning/phases/03-scheduling-and-email-delivery/03-RESEARCH.md
.env.example
app/config.py
app/services/email.py
tests/conftest.py</read_first>
  <acceptance_criteria>- `rg -n "smtp_host|smtp_port|smtp_username|smtp_password|smtp_from_email|daily_memo_to_email|external_base_url|approval_token_ttl_seconds" .env.example app/config.py` returns all eight setting names.
- `rg -n "class MailProvider|class SmtpMailProvider|def send\\(" app/services/mail_provider.py` returns the provider protocol and SMTP adapter.
- `rg -n "EmailMessage|starttls|send_message" app/services/mail_provider.py tests/services/test_mail_provider.py` returns the stdlib SMTP usage points.
- `pytest tests/services/test_mail_provider.py -q` exits with code 0.</acceptance_criteria>
  <behavior>
    - Test 1: `SmtpMailProvider.send()` builds an `EmailMessage` with plain text and HTML bodies, then calls `starttls`, `login`, and `send_message` when credentials and STARTTLS are configured.
    - Test 2: The provider uses the exact configured sender and recipient values from settings rather than hard-coded addresses.
    - Test 3: Test settings in `tests/conftest.py` populate default SMTP and external-URL env vars so the suite does not depend on a real provider.
  </behavior>
  <action>Extend `.env.example` and `app/config.py` with these exact env-backed settings: `smtp_host`, `smtp_port`, `smtp_username`, `smtp_password`, `smtp_from_email`, `daily_memo_to_email`, `external_base_url`, and `approval_token_ttl_seconds`. Use these exact example values in `.env.example`: `INVESTOR_SMTP_HOST=smtp.example.com`, `INVESTOR_SMTP_PORT=587`, `INVESTOR_SMTP_USERNAME=investor-user`, `INVESTOR_SMTP_PASSWORD=change-me`, `INVESTOR_SMTP_FROM_EMAIL=investor@example.com`, `INVESTOR_DAILY_MEMO_TO_EMAIL=operator@example.com`, `INVESTOR_EXTERNAL_BASE_URL=https://investor.example.com`, and `INVESTOR_APPROVAL_TOKEN_TTL_SECONDS=900`. Create `app/services/mail_provider.py` with a `MailProvider` protocol exposing `send(subject: str, text_body: str, html_body: str, recipient: str) -> None` and an `SmtpMailProvider` implementation that uses `EmailMessage`, `set_content`, `add_alternative`, `smtplib.SMTP`, `starttls`, `login`, and `send_message`. Update `tests/conftest.py` to set non-secret defaults for the new env vars, and create `tests/services/test_mail_provider.py` with a fake SMTP client that asserts the exact sender, recipient, subject, and STARTTLS/login flow.</action>
  <verify>
    <automated>pytest tests/services/test_mail_provider.py -q</automated>
  </verify>
  <done>The codebase has a real provider abstraction for mail delivery, and SMTP is implemented using the Python standard library with env-driven settings instead of a console no-op.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render structured memos and signed approval links from configured public URLs</name>
  <files>app/services/email.py
app/services/tokens.py
app/graph/workflow.py
tests/services/test_email.py
tests/services/test_tokens.py
tests/graph/test_workflow.py</files>
  <read_first>.planning/REQUIREMENTS.md
.planning/phases/03-scheduling-and-email-delivery/03-RESEARCH.md
app/services/email.py
app/services/tokens.py
app/graph/workflow.py
tests/services/test_email.py
tests/services/test_tokens.py</read_first>
  <acceptance_criteria>- `rg -n "Ranked Candidates|Watchlist|No Action|Approve:|Reject:" app/services/email.py tests/services/test_email.py` returns all five memo strings.
- `rg -n "external_base_url|approval_token_ttl_seconds|sign_approval_token|verify_approval_token" app/graph/workflow.py app/services/tokens.py tests/services/test_tokens.py` returns the base-url and TTL wiring points.
- `rg -n "https://investor.example.com/approval/" tests/services/test_email.py tests/graph/test_workflow.py` returns the configured public-host prefix.
- `pytest tests/services/test_email.py tests/services/test_tokens.py tests/graph/test_workflow.py -q` exits with code 0.</acceptance_criteria>
  <behavior>
    - Test 1: Candidate emails render a `Ranked Candidates` section, watchlist emails render `Watchlist`, and empty-result emails render `No Action`.
    - Test 2: Approval and rejection links both begin with `https://investor.example.com/approval/` in tests and the decoded token payload contains the same `run_id` and `decision` values used to generate the link.
    - Test 3: Expired approval tokens still raise `ExpiredApprovalTokenError` when verified with the configured `approval_token_ttl_seconds`.
  </behavior>
  <action>Replace the current ticker-list renderer in `app/services/email.py` with a branch-aware renderer that accepts the finalized Phase 2 outcome object and writes one of three exact section headings: `Ranked Candidates`, `Watchlist`, or `No Action`. Candidate rows must include ticker, action, conviction score, rationale/support text, plus `Approve:` and `Reject:` lines containing the final URLs. Watchlist rows must include ticker and summary text. No-action output must include each reason on its own line under the `No Action` heading. In `app/services/tokens.py`, keep the `approval-token` salt but make the token payload explicitly include `run_id` and `decision`, and rely on `serializer.loads(..., max_age=ttl_seconds, salt="approval-token")` for expiry enforcement. Update `app/graph/workflow.py` so `_compose_email` signs one `approve` token and one `reject` token with `settings.app_secret` and `settings.approval_token_ttl_seconds`, then composes URLs with `settings.external_base_url.rstrip("/") + "/approval/" + token` instead of `http://localhost:8000/...`. Extend the workflow, email, and token tests so they assert the three exact headings, the full external URL prefix, and round-trip token payload scope.</action>
  <verify>
    <automated>pytest tests/services/test_email.py tests/services/test_tokens.py tests/graph/test_workflow.py -q</automated>
  </verify>
  <done>The daily memo path is provider-ready, branch-aware, and generates public approval/rejection links that are signed, scoped to one run, and governed by explicit TTL settings.</done>
</task>

</tasks>

<verification>
Run `pytest tests/services/test_mail_provider.py tests/services/test_email.py tests/services/test_tokens.py tests/graph/test_workflow.py -q` and confirm the daily memo now uses SMTP contracts, renders explicit memo modes, and produces signed public approval links.
</verification>

<success_criteria>
`MAIL-01`, `MAIL-02`, and `MAIL-03` are covered by concrete SMTP transport code, branch-aware memo rendering, and approval links composed from `INVESTOR_EXTERNAL_BASE_URL` with token expiry enforced through `itsdangerous`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-and-email-delivery/03-02-SUMMARY.md`
</output>

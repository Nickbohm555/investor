---
phase: 03-scheduling-and-email-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - app/main.py
  - app/db/models.py
  - app/db/migrations/versions/0002_add_schedule_fields.py
  - app/db/session.py
  - app/services/scheduling.py
  - app/api/routes.py
  - scripts/cron-install.sh
  - scripts/cron-remove.sh
  - scripts/cron-status.sh
  - scripts/cron-trigger.sh
  - tests/api/test_scheduling.py
  - tests/conftest.py
  - tests/ops/test_cron_scripts.py
autonomous: true
requirements:
  - SCHD-01
  - SCHD-02
  - SCHD-03
must_haves:
  truths:
    - "Operator can install, inspect, and remove a repo-managed cron block without hand-editing crontab entries."
    - "A scheduled trigger creates one primary run for a market-day window and returns the existing run on duplicates."
    - "Scheduled execution loads repo environment values and writes timestamped success or failure lines to a cron log file."
  artifacts:
    - path: "app/services/scheduling.py"
      provides: "Schedule-window key generation and duplicate-run protection"
      contains: "def build_schedule_key"
    - path: "app/api/routes.py"
      provides: "Scheduled trigger API path with duplicate detection"
      contains: "/runs/trigger/scheduled"
    - path: "scripts/cron-trigger.sh"
      provides: "Repo-owned cron wrapper that loads .env and logs trigger outcomes"
      contains: "scheduled_trigger"
    - path: "scripts/cron-install.sh"
      provides: "Managed crontab install path"
      contains: ">>> investor daily schedule >>>"
  key_links:
    - from: "scripts/cron-trigger.sh"
      to: "app/api/routes.py"
      via: "HTTP POST to the scheduled trigger endpoint"
      pattern: "POST.*/runs/trigger/scheduled"
    - from: "app/api/routes.py"
      to: "app/services/scheduling.py"
      via: "Schedule-window key and upsert-based duplicate guard"
      pattern: "build_schedule_key|create_or_get_scheduled_run"
    - from: "app/services/scheduling.py"
      to: "app/db/models.py"
      via: "RunRecord schedule_key uniqueness"
      pattern: "schedule_key|uq_runs_schedule_key"
---

<objective>
Build the schedule-trigger and cron-install foundation for Phase 3.

Purpose: Satisfy `SCHD-01`, `SCHD-02`, and `SCHD-03` by making cron installable from the repo and moving duplicate protection into the app/database path instead of shell tricks.
Output: A scheduled trigger API, schedule-key persistence fields, and repo-owned cron scripts with automated tests.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-scheduling-and-email-delivery/03-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@app/config.py
@app/api/routes.py
@app/db/models.py
@tests/api/test_routes.py

<interfaces>
From app/config.py:
```python
class Settings(BaseSettings):
    app_name: str = "investor"
    app_env: str = "development"
    app_secret: str = "change-me"
    database_url: str = "sqlite+pysqlite:///./investor.db"
```

From app/api/routes.py:
```python
@router.post("/runs/trigger", status_code=202)
def trigger_run(request: Request) -> dict[str, str]:
    ...
```

From app/db/models.py:
```python
class RunRecord(Base):
    __tablename__ = "runs"
    run_id: Mapped[str] = mapped_column(String(64), primary_key=True)
    status: Mapped[str] = mapped_column(String(32), nullable=False)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add schedule-window persistence and the scheduled trigger endpoint</name>
  <files>app/config.py
app/main.py
app/db/models.py
app/db/migrations/versions/0002_add_schedule_fields.py
app/db/session.py
app/services/scheduling.py
app/api/routes.py
tests/api/test_scheduling.py
tests/conftest.py</files>
  <read_first>.planning/REQUIREMENTS.md
.planning/phases/03-scheduling-and-email-delivery/03-RESEARCH.md
app/config.py
app/main.py
app/db/models.py
app/db/session.py
app/api/routes.py
tests/api/test_routes.py</read_first>
  <acceptance_criteria>- `rg -n "schedule_cron_expression|schedule_trigger_url|scheduled_trigger_token|cron_log_path" app/config.py` returns all four settings names.
- `rg -n "session_factory|get_session_factory|app.state.session_factory" app/main.py app/api/routes.py app/db/session.py` returns the DB/session wiring used by the scheduled route.
- `rg -n "StaticPool|check_same_thread|Base.metadata.create_all|session_factory override" app/db/session.py tests/conftest.py` returns the shared SQLite test-harness setup.
- `rg -n "schedule_key|trigger_source|replay_of_run_id|uq_runs_schedule_key" app/db/models.py app/db/migrations/versions/0002_add_schedule_fields.py` returns all four persistence identifiers.
- `rg -n "IntegrityError|existing run|schedule_key" app/services/scheduling.py tests/api/test_scheduling.py` returns the dialect-safe duplicate handling seam and assertions.
- `rg -n "/runs/trigger/scheduled|X-Investor-Scheduled-Trigger|duplicate" app/api/routes.py tests/api/test_scheduling.py` returns the scheduled route, auth header, and duplicate response path.
- `pytest tests/api/test_scheduling.py -q` exits with code 0.</acceptance_criteria>
  <behavior>
    - Test 1: `POST /runs/trigger/scheduled` with a valid `X-Investor-Scheduled-Trigger` header creates a run and returns status `started` with a `schedule_key` shaped like `daily:YYYY-MM-DD`.
    - Test 2: Repeating the same scheduled request on the same day returns the original `run_id`, sets `duplicate` to `true`, and does not create a second primary run.
    - Test 3: A bad or missing `X-Investor-Scheduled-Trigger` header returns HTTP 401 without creating a run row.
  </behavior>
  <action>Add four new settings to `app/config.py` with these exact defaults: `schedule_cron_expression="30 8 * * 1-5"`, `schedule_trigger_url="http://127.0.0.1:8000/runs/trigger/scheduled"`, `scheduled_trigger_token="change-me-scheduled-trigger"`, and `cron_log_path="logs/cron/daily-trigger.log"`. Update `app/db/session.py` so `get_engine()` detects `sqlite+pysqlite:///:memory:` and constructs the engine with `connect_args={"check_same_thread": False}` and `poolclass=StaticPool`, then update `app/main.py` so `create_app()` stores `app.state.session_factory = get_session_factory(settings.database_url)` alongside `settings` and `workflow_store`. Update `tests/conftest.py` to build a shared test app by creating the schema with `Base.metadata.create_all(bind=engine)` before `TestClient(create_app())`, and explicitly override `app.state.session_factory` with the shared in-memory SQLite session factory used by the tests. Extend `RunRecord` in `app/db/models.py` with `trigger_source: Mapped[str] = mapped_column(String(16), nullable=False, default="manual")`, `schedule_key: Mapped[str | None] = mapped_column(String(32), nullable=True)`, and `replay_of_run_id: Mapped[str | None] = mapped_column(String(64), nullable=True)`, then add a unique constraint named `uq_runs_schedule_key` on `schedule_key`. Create `app/db/migrations/versions/0002_add_schedule_fields.py` to add those columns and the unique constraint. In `app/services/scheduling.py`, create `build_schedule_key(now: datetime) -> str` that returns `daily:{market_date}` using the America/New_York calendar date, plus `create_or_get_scheduled_run(session, now, run_factory)` that uses a dialect-safe repository seam: create the row with the computed `schedule_key`, `session.add(...)`, and `session.commit()`, then on `sqlalchemy.exc.IntegrityError` roll back and query the existing `RunRecord` by `schedule_key` to return the original run. Update `app/api/routes.py` to open a DB session from `request.app.state.session_factory`, require header `X-Investor-Scheduled-Trigger` to equal `settings.scheduled_trigger_token`, call `create_or_get_scheduled_run(...)`, and return `202 {"status": "started", "run_id": "...", "schedule_key": "...", "duplicate": false}` on a new run or `200 {"status": "duplicate", "run_id": "...", "schedule_key": "...", "duplicate": true}` when the unique constraint already exists. Put the scheduled-route coverage in `tests/api/test_scheduling.py` and keep it SQLite-backed by asserting the duplicate branch through the shared `StaticPool` harness and IntegrityError/query fallback instead of PostgreSQL-only SQL.</action>
  <verify>
    <automated>pytest tests/api/test_scheduling.py -q</automated>
  </verify>
  <done>The app has a schedule-aware trigger path backed by persisted schedule keys, and duplicate scheduled requests no longer create a second primary run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create repo-managed cron scripts for install, remove, status, and trigger</name>
  <files>scripts/cron-install.sh
scripts/cron-remove.sh
scripts/cron-status.sh
scripts/cron-trigger.sh
tests/ops/test_cron_scripts.py</files>
  <read_first>.planning/REQUIREMENTS.md
.planning/phases/03-scheduling-and-email-delivery/03-RESEARCH.md
app/config.py
app/api/routes.py
tests/api/test_scheduling.py</read_first>
  <acceptance_criteria>- `test -f scripts/cron-install.sh && test -f scripts/cron-remove.sh && test -f scripts/cron-status.sh && test -f scripts/cron-trigger.sh` exits with code 0.
- `rg -n ">>> investor daily schedule >>>|<<< investor daily schedule <<<" scripts/cron-install.sh scripts/cron-remove.sh scripts/cron-status.sh` returns both managed block markers.
- `rg -n "INVESTOR_SCHEDULE_TRIGGER_URL|INVESTOR_SCHEDULED_TRIGGER_TOKEN|logs/cron/daily-trigger.log|/runs/trigger/scheduled" scripts/cron-trigger.sh tests/ops/test_cron_scripts.py` returns all required env vars and paths.
- `pytest tests/ops/test_cron_scripts.py -q` exits with code 0.</acceptance_criteria>
  <behavior>
    - Test 1: Running the install script twice keeps exactly one managed crontab block between `>>> investor daily schedule >>>` and `<<< investor daily schedule <<<`.
    - Test 2: The status script reports `managed=present` or `managed=absent` and prints the exact configured log path.
    - Test 3: The trigger wrapper loads `.env`, posts to `/runs/trigger/scheduled`, and appends a log line beginning with `scheduled_trigger` for both success and failure cases.
  </behavior>
  <action>Create a new `scripts/` directory with four POSIX-shell scripts using `#!/bin/sh`. `scripts/cron-install.sh` must read the repo root, create `logs/cron`, remove any existing investor-managed block from `crontab -l`, and append a new managed block bracketed by the exact markers `# >>> investor daily schedule >>>` and `# <<< investor daily schedule <<<`. The cron line must be `30 8 * * 1-5 cd "$REPO_ROOT" && ./scripts/cron-trigger.sh >> ./logs/cron/daily-trigger.log 2>&1`. `scripts/cron-remove.sh` must remove only the managed block. `scripts/cron-status.sh` must print `managed=present` or `managed=absent` and `log_path=logs/cron/daily-trigger.log`. `scripts/cron-trigger.sh` must set `PATH=/usr/bin:/bin:/usr/sbin:/sbin`, `cd` to the repo root, load `.env` with `set -a; . ./.env; set +a`, create the log directory, and call `curl -fsS -X POST "$INVESTOR_SCHEDULE_TRIGGER_URL" -H "X-Investor-Scheduled-Trigger: $INVESTOR_SCHEDULED_TRIGGER_TOKEN" -H "Content-Type: application/json" -d '{"replay":false}'`. On success, append a line starting with `scheduled_trigger status=success`; on failure, append a line starting with `scheduled_trigger status=failure` and exit non-zero. Add `tests/ops/test_cron_scripts.py` that shells these scripts against a temp crontab file fixture instead of the real user crontab.</action>
  <verify>
    <automated>pytest tests/ops/test_cron_scripts.py -q</automated>
  </verify>
  <done>The repository owns the full cron install/remove/status/trigger workflow, and the trigger wrapper loads env state and logs observable outcomes without relying on Linux-only locking tools.</done>
</task>

</tasks>

<verification>
Run `pytest tests/api/test_scheduling.py tests/ops/test_cron_scripts.py -q` and confirm scheduled triggers are idempotent, cron scripts are repo-managed, and wrapper logging is observable.
</verification>

<success_criteria>
`SCHD-01`, `SCHD-02`, and `SCHD-03` are covered by concrete schedule-key persistence, a dedicated scheduled trigger path, and portable cron scripts that manage installation and observable trigger behavior from the repo.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scheduling-and-email-delivery/03-01-SUMMARY.md`
</output>

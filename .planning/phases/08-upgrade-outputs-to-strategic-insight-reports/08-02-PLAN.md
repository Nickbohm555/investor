---
phase: 08-upgrade-outputs-to-strategic-insight-reports
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - pyproject.toml
  - app/services/email.py
  - app/services/report_render.py
  - app/templates/reports/strategic_report.txt.j2
  - app/templates/reports/strategic_report.html.j2
  - tests/services/test_email.py
  - tests/services/test_report_render.py
autonomous: true
requirements:
  - REP-03
must_haves:
  truths:
    - "The operator-facing report text and HTML come from deterministic templates, not inline string concatenation."
    - "Text and HTML versions expose the same bucketed report sections and approval links."
    - "Template or schema drift fails locally through unit tests instead of silently degrading the outgoing email."
  artifacts:
    - "app/services/report_render.py :: Jinja2 environment and strategic report rendering entry points"
    - "app/templates/reports/strategic_report.txt.j2 :: deterministic text template for strategic reports"
    - "app/templates/reports/strategic_report.html.j2 :: deterministic HTML template for strategic reports"
    - "app/services/email.py :: thin wrapper that composes `RecommendationEmail` from a `StrategicInsightReport`"
    - "tests/services/test_report_render.py :: focused rendering coverage"
  key_links:
    - "app/services/email.py -> app/services/report_render.py :: email service delegates rendering instead of building strings inline"
    - "app/services/report_render.py -> app/templates/reports/*.j2 :: one report object renders both text and HTML"
    - "tests/services/test_report_render.py -> app/templates/reports/*.j2 :: automated checks pin exact section headings and approval link placement"
---

<objective>
Add the deterministic rendering layer for strategic reports so outgoing operator emails can be generated from templates and validated with stable tests.

Purpose: Separate presentation from report business logic and replace the flat manual email-string builder with a template-driven renderer.
Output: Jinja2-backed text/HTML templates, rendering service, dependency update, and rendering-focused tests.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-RESEARCH.md
@.planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-01-PLAN.md
@app/schemas/reports.py
@app/services/email.py
@tests/services/test_email.py

<interfaces>
From app/services/email.py:
```python
def compose_recommendation_email(
    run_id: str,
    recommendations: DailyMemoContent,
    approval_url: str,
    rejection_url: str,
) -> RecommendationEmail:
    ...
```

From app/schemas/reports.py:
```python
class StrategicInsightReport(BaseModel):
    run_id: str
    baseline_run_id: str | None
    headline: str
    summary: str
    immediate_actions: list[ImmediateActionItem]
    deferred_actions: list[DeferredActionItem]
    research_queue: list[ResearchNeededItem]
    dropped_tickers: list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing renderer tests and create the template/render-service skeleton</name>
  <files>tests/services/test_report_render.py, tests/services/test_email.py, pyproject.toml, app/services/report_render.py, app/services/email.py, app/templates/reports/strategic_report.txt.j2, app/templates/reports/strategic_report.html.j2</files>
  <read_first>.planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-RESEARCH.md, .planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-01-PLAN.md, pyproject.toml, app/services/email.py, app/schemas/reports.py, tests/services/test_email.py</read_first>
  <behavior>
    - Test 1: the renderer returns text and HTML bodies that share the same strategic-report sections and approval/rejection links.
    - Test 2: the templates render explicit bucket headings and dropped-ticker content from a `StrategicInsightReport`.
    - Test 3: the legacy email service becomes a thin wrapper around the report renderer instead of building the output inline.
  </behavior>
  <action>Create `tests/services/test_report_render.py` with failing tests named `test_render_report_email_outputs_bucket_sections_and_links` and `test_render_report_email_includes_dropped_tickers_and_research_queue`. Require a new service module `app/services/report_render.py` exporting exactly `build_report_environment(template_dir: str | Path | None = None) -> Environment` and `render_report_email(*, report: StrategicInsightReport, approval_url: str, rejection_url: str) -> RecommendationEmail`. The tests must require these exact text headings in the rendered email body: `Strategic Insight Report`, `Immediate Actions`, `Deferred`, `Research Queue`, `Dropped Since Last Delivered Run`, `Approve:`, and `Reject:`. Require the HTML output to contain `<h1>Strategic Insight Report</h1>`, `<h2>Immediate Actions</h2>`, `<h2>Deferred</h2>`, `<h2>Research Queue</h2>`, and anchor tags for both approval links. Update `tests/services/test_email.py` with a failing test named `test_compose_report_email_delegates_to_renderer` that requires `app/services/email.py` to expose exactly `compose_report_email(*, report: StrategicInsightReport, approval_url: str, rejection_url: str) -> RecommendationEmail`. Add the exact dependency string `"Jinja2==3.1.6"` to `pyproject.toml`. Create skeleton files `app/templates/reports/strategic_report.txt.j2` and `app/templates/reports/strategic_report.html.j2` containing the exact heading strings above so the renderer has concrete template targets.</action>
  <acceptance_criteria>
    - `rg -n "test_render_report_email_outputs_bucket_sections_and_links|test_render_report_email_includes_dropped_tickers_and_research_queue" tests/services/test_report_render.py` returns both render tests.
    - `rg -n "Strategic Insight Report|Immediate Actions|Deferred|Research Queue|Dropped Since Last Delivered Run|Approve:|Reject:" tests/services/test_report_render.py app/templates/reports/strategic_report.txt.j2 app/templates/reports/strategic_report.html.j2` returns all required headings.
    - `rg -n "test_compose_report_email_delegates_to_renderer|def compose_report_email\\(\\*, report: StrategicInsightReport, approval_url: str, rejection_url: str\\) -> RecommendationEmail" tests/services/test_email.py app/services/email.py` returns the new wrapper test and exact email-service signature.
    - `rg -n "\"Jinja2==3\\.1\\.6\"" pyproject.toml` returns the exact dependency string.
    - `rg -n "def build_report_environment\\(template_dir: str \\| Path \\| None = None\\) -> Environment|def render_report_email\\(\\*, report: StrategicInsightReport, approval_url: str, rejection_url: str\\) -> RecommendationEmail" app/services/report_render.py` returns both exact renderer entry points.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_report_render.py tests/services/test_email.py -q</automated>
  </verify>
  <done>Failing render tests, renderer entry points, templates, and the Jinja2 dependency target all exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Jinja2-backed report rendering and route email composition through it</name>
  <files>pyproject.toml, app/services/report_render.py, app/services/email.py, app/templates/reports/strategic_report.txt.j2, app/templates/reports/strategic_report.html.j2, tests/services/test_email.py, tests/services/test_report_render.py</files>
  <read_first>pyproject.toml, app/services/report_render.py, app/services/email.py, app/templates/reports/strategic_report.txt.j2, app/templates/reports/strategic_report.html.j2, tests/services/test_report_render.py, tests/services/test_email.py</read_first>
  <behavior>
    - Test 1: the renderer uses one strict Jinja environment for both text and HTML output.
    - Test 2: both templates render deterministic sections for headline, summary, immediate actions, deferred actions, research queue, dropped tickers, and approval links.
    - Test 3: email composition for strategic reports no longer depends on `html.escape` or inline list assembly.
  </behavior>
  <action>Add `"Jinja2==3.1.6"` to `pyproject.toml`. Implement `app/services/report_render.py` with `Environment(loader=FileSystemLoader("app/templates/reports"), autoescape=select_autoescape(["html", "xml"]), undefined=StrictUndefined)` and have `build_report_environment(...)` optionally override the template directory for tests. `render_report_email(...)` must render `strategic_report.txt.j2` and `strategic_report.html.j2` with one payload dictionary named `report=report.model_dump()` plus the separate `approval_url` and `rejection_url` values. Set the returned subject exactly to `Investor strategic insight for {report.run_id}`. Replace manual report-string assembly in `app/services/email.py` with a thin wrapper function `compose_report_email(...)` that calls `render_report_email(...)`. Do not keep business logic or conditionals for `immediate/defer/research` bucketing in `app/services/email.py`; that logic must remain in the builder. Implement the text template so it renders these sections in this exact order: `Strategic Insight Report`, `Run {{ report.run_id }}`, `Headline: {{ report.headline }}`, `Summary: {{ report.summary }}`, `Immediate Actions`, `Deferred`, `Research Queue`, `Dropped Since Last Delivered Run`, `Approve: {{ approval_url }}`, and `Reject: {{ rejection_url }}`. Implement the HTML template with matching section order and exact heading tags `<h1>Strategic Insight Report</h1>`, `<h2>Immediate Actions</h2>`, `<h2>Deferred</h2>`, `<h2>Research Queue</h2>`, and `<h2>Dropped Since Last Delivered Run</h2>`. Render bucket items as unordered lists; when a section is empty, render the exact fallback text `None.` in text and `<p>None.</p>` in HTML.</action>
  <acceptance_criteria>
    - `rg -n "FileSystemLoader\\(\"app/templates/reports\"\\)|StrictUndefined|select_autoescape\\(\\[\"html\", \"xml\"\\]\\)|Investor strategic insight for \\{report.run_id\\}" app/services/report_render.py` returns the exact Jinja environment settings and subject format.
    - `rg -n "Strategic Insight Report|Run \\{\\{ report.run_id \\}\\}|Headline: \\{\\{ report.headline \\}\\}|Summary: \\{\\{ report.summary \\}\\}|Immediate Actions|Deferred|Research Queue|Dropped Since Last Delivered Run|Approve: \\{\\{ approval_url \\}\\}|Reject: \\{\\{ rejection_url \\}\\}|None\\." app/templates/reports/strategic_report.txt.j2` returns the exact text-template headings, link markers, and empty fallback.
    - `rg -n "<h1>Strategic Insight Report</h1>|<h2>Immediate Actions</h2>|<h2>Deferred</h2>|<h2>Research Queue</h2>|<h2>Dropped Since Last Delivered Run</h2>|<p>None\\.</p>" app/templates/reports/strategic_report.html.j2` returns the exact HTML headings and empty fallback.
    - `rg -n "def compose_report_email\\(\\*, report: StrategicInsightReport, approval_url: str, rejection_url: str\\) -> RecommendationEmail|render_report_email\\(" app/services/email.py` returns the exact wrapper signature and delegation call.
    - `python -m pytest tests/services/test_report_render.py tests/services/test_email.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_report_render.py tests/services/test_email.py -q</automated>
  </verify>
  <done>The repo can deterministically render text and HTML strategic reports from templates, and email composition now delegates to the renderer.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/services/test_report_render.py tests/services/test_email.py -q`. Then read `app/services/email.py` and confirm it contains only the new report wrapper plus any necessary imports, with no inline HTML/text concatenation or `escape(...)` calls.
</verification>

<success_criteria>
Phase 8 now has a deterministic renderer: one typed report object can produce stable text and HTML output with explicit strategic-report sections and approval links, and template drift is caught by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-upgrade-outputs-to-strategic-insight-reports-02-SUMMARY.md`
</output>

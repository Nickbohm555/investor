---
phase: 08-upgrade-outputs-to-strategic-insight-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/reports.py
  - app/schemas/workflow.py
  - app/services/report_compare.py
  - app/services/report_builder.py
  - tests/services/test_report_compare.py
  - tests/services/test_report_builder.py
autonomous: true
requirements:
  - REP-01
  - REP-02
must_haves:
  truths:
    - "The report contract records what changed from the previous delivered run instead of hiding change detection inside prose."
    - "Each reported ticker lands in one explicit operator bucket: `immediate`, `defer`, or `research`."
    - "The report builder emits typed data that later rendering code can consume without re-deriving business logic."
  artifacts:
    - "app/schemas/reports.py :: `StrategicInsightReport` and discriminated report item models"
    - "app/services/report_compare.py :: deterministic baseline comparison helpers and dropped-ticker detection"
    - "app/services/report_builder.py :: builder that converts `ResearchOutcome` plus optional baseline into a `StrategicInsightReport`"
    - "tests/services/test_report_compare.py :: coverage for exact change-label and dropped-ticker behavior"
    - "tests/services/test_report_builder.py :: coverage for exact bucket classification and summary counts"
  key_links:
    - "app/services/report_builder.py -> app/services/report_compare.py :: builder uses comparison helpers to populate `change_summary` and `dropped_tickers`"
    - "app/services/report_builder.py -> app/schemas/reports.py :: builder returns validated `StrategicInsightReport`, not a dict"
    - "app/schemas/workflow.py -> app/schemas/reports.py :: workflow/email code can import report types from the existing schema export surface"
---

<objective>
Define the Phase 8 data contract first: a report schema, deterministic comparison layer, and report builder that turn finalized research output into typed strategic insight data.

Purpose: Make "strategic insight" a stable application contract before any rendering or workflow wiring begins.
Output: New report schemas, comparison/build services, and focused unit tests for bucket classification and delta labels.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-RESEARCH.md
@.planning/phases/03-scheduling-and-email-delivery/03-02-SUMMARY.md
@app/schemas/research.py
@app/schemas/workflow.py
@app/db/models.py
@app/graph/workflow.py

<interfaces>
From app/schemas/research.py:
```python
class CandidateRecommendation(BaseModel):
    ticker: str
    action: str
    conviction_score: float
    supporting_evidence: List[str]
    opposing_evidence: List[str]
    risk_notes: List[str]
    source_summary: List[str]
    broker_eligible: bool = True

ResearchOutcome = Annotated[
    Union[CandidateOutcome, WatchlistOutcome, NoActionOutcome],
    Field(discriminator="outcome"),
]
```

From app/db/models.py:
```python
class RunRecord(Base):
    run_id: Mapped[str]
    status: Mapped[str]
    state_payload: Mapped[Optional[dict]]
    created_at: Mapped[datetime]
```

From app/graph/workflow.py:
```python
def _build_memo_content(self, outcome: ResearchOutcome) -> DailyMemoContent:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests and report-contract skeletons for comparison and bucketed report building</name>
  <files>tests/services/test_report_compare.py, tests/services/test_report_builder.py, app/schemas/reports.py, app/schemas/workflow.py, app/services/report_compare.py, app/services/report_builder.py</files>
  <read_first>.planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-RESEARCH.md, app/schemas/research.py, app/schemas/workflow.py, app/db/models.py, app/graph/workflow.py, tests/services/test_email.py</read_first>
  <behavior>
    - Test 1: comparison returns the exact labels `new thesis`, `conviction increased`, `conviction decreased`, `risk profile changed`, and `signal mix unchanged`.
    - Test 2: comparison records dropped baseline tickers when a ticker existed in the baseline run but is absent from the current run.
    - Test 3: the builder classifies current recommendations into `immediate`, `defer`, and `research` with deterministic rules instead of prompt prose.
    - Test 4: the builder emits exact bucket counts, a baseline-aware headline, and a summary string that can be rendered without recomputing logic.
  </behavior>
  <action>Create `tests/services/test_report_compare.py` first with failing tests named `test_classify_change_returns_exact_change_labels` and `test_compare_candidates_returns_dropped_tickers_from_baseline_only`. Require a new service module `app/services/report_compare.py` exporting exactly `classify_change(*, current: CandidateRecommendation, previous: CandidateRecommendation | None) -> str` and `compare_candidates(*, current: list[CandidateRecommendation], previous: list[CandidateRecommendation]) -> tuple[dict[str, str], list[str]]`. The exact change labels must be `new thesis`, `conviction increased`, `conviction decreased`, `risk profile changed`, and `signal mix unchanged`. Make the dropped-ticker test require the returned list to contain `["AMD"]` when baseline candidates include `AMD` and the current run does not. Create `tests/services/test_report_builder.py` with failing tests named `test_build_strategic_insight_report_classifies_immediate_defer_and_research_items` and `test_build_strategic_insight_report_handles_watchlist_and_no_action_without_candidates`. Require a new builder module `app/services/report_builder.py` exporting exactly `build_strategic_insight_report(*, run_id: str, outcome: ResearchOutcome, baseline_run_id: str | None, baseline_outcome: ResearchOutcome | None) -> StrategicInsightReport`. The builder must classify a candidate as `immediate` only when `action == "buy"`, `broker_eligible is True`, and `conviction_score >= 0.75`; classify it as `defer` when `action == "buy"` but either `broker_eligible is False` or `conviction_score < 0.75`; and classify watchlist or no-action follow-ups as `research`. Require the builder to emit the exact operator actions `Review for approval and paper-order prestage.` for `immediate`, `Re-check on the next market session.` for `defer`, and `Collect more Quiver evidence before approval.` for `research`. Create `app/schemas/reports.py` with skeleton models named exactly `ImmediateActionItem`, `DeferredActionItem`, `ResearchNeededItem`, `ReportItem`, and `StrategicInsightReport`. `StrategicInsightReport` must expose exactly `run_id`, `baseline_run_id`, `headline`, `summary`, `immediate_actions`, `deferred_actions`, `research_queue`, and `dropped_tickers`. Update `app/schemas/workflow.py` to re-export `StrategicInsightReport` from `app.schemas.reports` so later workflow code can import from the existing schema surface.</action>
  <acceptance_criteria>
    - `rg -n "test_classify_change_returns_exact_change_labels|test_compare_candidates_returns_dropped_tickers_from_baseline_only" tests/services/test_report_compare.py` returns both comparison tests.
    - `rg -n "new thesis|conviction increased|conviction decreased|risk profile changed|signal mix unchanged|\\[\"AMD\"\\]" tests/services/test_report_compare.py` returns every exact label and the dropped-ticker expectation.
    - `rg -n "test_build_strategic_insight_report_classifies_immediate_defer_and_research_items|test_build_strategic_insight_report_handles_watchlist_and_no_action_without_candidates" tests/services/test_report_builder.py` returns both builder tests.
    - `rg -n "Review for approval and paper-order prestage\\.|Re-check on the next market session\\.|Collect more Quiver evidence before approval\\.|conviction_score >= 0\\.75|broker_eligible" tests/services/test_report_builder.py` returns the exact bucket rules and action strings.
    - `rg -n "class ImmediateActionItem|class DeferredActionItem|class ResearchNeededItem|class StrategicInsightReport|ReportItem =" app/schemas/reports.py` returns all required schema names.
    - `rg -n "StrategicInsightReport" app/schemas/workflow.py` returns the report export in the workflow schema module.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_report_compare.py tests/services/test_report_builder.py -q</automated>
  </verify>
  <done>Failing tests and concrete report-model entry points exist for Phase 8 comparison and builder work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic comparison and report-building logic on the new typed contract</name>
  <files>app/schemas/reports.py, app/schemas/workflow.py, app/services/report_compare.py, app/services/report_builder.py, tests/services/test_report_compare.py, tests/services/test_report_builder.py</files>
  <read_first>app/schemas/reports.py, app/schemas/workflow.py, app/services/report_compare.py, app/services/report_builder.py, tests/services/test_report_compare.py, tests/services/test_report_builder.py, app/schemas/research.py</read_first>
  <behavior>
    - Test 1: comparison helpers return deterministic change labels and dropped tickers for baseline-versus-current candidate sets.
    - Test 2: the report builder returns validated report models with bucket-specific fields and a summary that reflects exact bucket counts.
    - Test 3: watchlist and no-action outcomes still produce a valid report with empty `immediate_actions` and populated `research_queue`.
  </behavior>
  <action>Implement `app/schemas/reports.py` as strict Pydantic models. `ImmediateActionItem` must include `ticker`, `thesis`, `change_summary`, `why_now`, and `operator_action`. `DeferredActionItem` must include `ticker`, `thesis`, `change_summary`, `defer_reason`, and `operator_action`. `ResearchNeededItem` must include `ticker`, `thesis`, `uncertainty`, `follow_up_questions`, and `operator_action`. `StrategicInsightReport` must use `baseline_run_id: str | None = None`, plus default factories for `immediate_actions`, `deferred_actions`, `research_queue`, and `dropped_tickers`. Implement `classify_change(...)` in `app/services/report_compare.py` with this exact decision order: return `new thesis` when `previous is None`; return `conviction increased` when `current.conviction_score > previous.conviction_score`; return `conviction decreased` when `current.conviction_score < previous.conviction_score`; return `risk profile changed` when `current.risk_notes != previous.risk_notes`; otherwise return `signal mix unchanged`. Implement `compare_candidates(...)` by mapping tickers in uppercase and returning a `(change_map, dropped_tickers)` tuple where `dropped_tickers` is the sorted list of baseline tickers missing from the current set. Implement `build_strategic_insight_report(...)` so `CandidateOutcome` recommendations are sorted by descending `conviction_score`, compared against baseline candidates, and bucketed with these exact deterministic rules: `immediate` when `action == "buy"` and `broker_eligible` and `conviction_score >= 0.75`; `defer` when `action == "buy"` but either `broker_eligible is False` or `conviction_score < 0.75`; `research` for every watchlist or no-action item. Build `thesis` from `source_summary[0]` when present, otherwise from the first supporting evidence string, otherwise from `action`. For `why_now`, join `supporting_evidence` with `; ` and fall back to `source_summary`. For `defer_reason`, use `Broker eligibility is false.` when `broker_eligible is False` and `Conviction below immediate threshold of 0.75.` when the score is below threshold. For `uncertainty`, use `outcome.summary` for watchlist/no-action branches when item-level opposing evidence is absent. Set the report `headline` exactly to `{immediate_count} immediate | {defer_count} defer | {research_count} research` and the report `summary` exactly to `Compared against {baseline_label}; dropped tickers: {dropped_label}.` where `baseline_label` is `baseline run {baseline_run_id}` or `no prior delivered run`, and `dropped_label` is a comma-separated ticker list or `none`. Update `app/schemas/workflow.py` to import and export `StrategicInsightReport` without removing the existing workflow email models.</action>
  <acceptance_criteria>
    - `rg -n "def classify_change\\(\\*, current: CandidateRecommendation, previous: CandidateRecommendation \\| None\\) -> str|def compare_candidates\\(\\*, current: list\\[CandidateRecommendation\\], previous: list\\[CandidateRecommendation\\]\\) -> tuple\\[dict\\[str, str\\], list\\[str\\]\\]" app/services/report_compare.py` returns both exact comparison signatures.
    - `rg -n "new thesis|conviction increased|conviction decreased|risk profile changed|signal mix unchanged|sorted\\(" app/services/report_compare.py` returns the exact label strings and sorted dropped-ticker behavior.
    - `rg -n "action == \"buy\"|conviction_score >= 0\\.75|Review for approval and paper-order prestage\\.|Re-check on the next market session\\.|Collect more Quiver evidence before approval\\.|\\{immediate_count\\} immediate \\| \\{defer_count\\} defer \\| \\{research_count\\} research" app/services/report_builder.py` returns the exact bucket rules, action strings, and headline format.
    - `rg -n "Compared against \\{baseline_label\\}; dropped tickers: \\{dropped_label\\}\\.|Broker eligibility is false\\.|Conviction below immediate threshold of 0\\.75\\." app/services/report_builder.py` returns the exact summary and defer-reason strings.
    - `python -m pytest tests/services/test_report_compare.py tests/services/test_report_builder.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_report_compare.py tests/services/test_report_builder.py -q</automated>
  </verify>
  <done>The repo can build a validated strategic report object with deterministic change labels, bucketing, and dropped-ticker metadata.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/services/test_report_compare.py tests/services/test_report_builder.py -q`. Then read `app/services/report_builder.py` and confirm the only bucket outputs are `immediate`, `defer`, and `research`, with no template- or LLM-owned branching logic left in the builder.
</verification>

<success_criteria>
Phase 8 has a stable typed foundation: current research outcomes can be compared to a prior baseline and converted into deterministic operator buckets with explicit change reasons, dropped tickers, and report summary fields.
</success_criteria>

<output>
After completion, create `.planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-upgrade-outputs-to-strategic-insight-reports-01-SUMMARY.md`
</output>

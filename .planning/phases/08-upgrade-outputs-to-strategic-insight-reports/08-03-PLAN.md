---
phase: 08-upgrade-outputs-to-strategic-insight-reports
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - app/api/routes.py
  - app/graph/workflow.py
  - app/repositories/run_repository.py
  - app/services/run_service.py
  - tests/api/test_routes.py
  - tests/graph/test_workflow.py
  - tests/graph/test_workflow_reporting.py
autonomous: true
requirements:
  - REP-01
  - REP-02
  - REP-03
must_haves:
  truths:
    - "A triggered run produces a strategic insight report from the current outcome and the most recent prior delivered run when one exists."
    - "The delivered email body shows report sections and change-aware rationale instead of the old flat ranked-candidates/watchlist/no-action memo."
    - "The report object is persisted with the run state so later review and local verification use the same structured source of truth."
  artifacts:
    - "app/services/run_service.py :: baseline lookup for the most recent prior run that already has a persisted strategic report or finalized outcome"
    - "app/repositories/run_repository.py :: query helper for baseline run selection"
    - "app/graph/workflow.py :: report build/render integration replacing `_build_memo_content()`"
    - "tests/graph/test_workflow_reporting.py :: workflow-level proof that strategic report generation uses baseline data and new renderer output"
    - "tests/api/test_routes.py :: route-level proof that persisted state now stores `strategic_report` and baseline metadata"
  key_links:
    - "app/api/routes.py -> app/services/run_service.py :: trigger routes load the baseline state before invoking the workflow"
    - "app/graph/workflow.py -> app/services/report_builder.py and app/services/email.py :: workflow builds the report, renders the email, and persists the report payload in state"
    - "app/services/run_service.py -> app/repositories/run_repository.py :: baseline selection uses persisted prior runs, not rendered email text"
---

<objective>
Wire the new strategic report path into live workflow execution and persistence so manual and scheduled runs deliver change-aware operator reports from real run history.

Purpose: Replace the old flat memo path with the new builder-plus-renderer seam and persist the report as the workflow’s source of truth.
Output: Workflow integration, baseline lookup, persisted strategic report payloads, and end-to-end report-focused tests.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-RESEARCH.md
@.planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-01-PLAN.md
@.planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-02-PLAN.md
@app/api/routes.py
@app/graph/workflow.py
@app/repositories/run_repository.py
@app/services/run_service.py
@tests/api/test_routes.py
@tests/graph/test_workflow.py

<interfaces>
From app/services/run_service.py:
```python
class RunService:
    def store_state_payload(self, run_id: str, state: dict) -> RunRecord:
        ...

    def get_run(self, run_id: str) -> Optional[RunRecord]:
        ...
```

From app/api/routes.py:
```python
@router.post("/runs/trigger")
def trigger_run(request: Request) -> dict[str, str]:
    ...

@router.post("/runs/trigger/scheduled")
def trigger_scheduled_run(...) -> dict[str, object]:
    ...
```

From app/graph/workflow.py:
```python
def invoke(self, state: dict) -> dict:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing workflow and route tests for baseline-aware report generation and persisted report state</name>
  <files>tests/graph/test_workflow_reporting.py, tests/graph/test_workflow.py, tests/api/test_routes.py, app/services/run_service.py, app/repositories/run_repository.py, app/graph/workflow.py, app/api/routes.py</files>
  <read_first>.planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-01-PLAN.md, .planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-02-PLAN.md, app/api/routes.py, app/graph/workflow.py, app/services/run_service.py, app/repositories/run_repository.py, tests/graph/test_workflow.py, tests/api/test_routes.py</read_first>
  <behavior>
    - Test 1: workflow invoke builds a strategic report, renders the new email body, and stores the report payload in paused workflow state.
    - Test 2: when a prior delivered run exists, the new report includes `baseline_run_id` and dropped-ticker metadata from that baseline.
    - Test 3: manual and scheduled trigger routes persist `state_payload["strategic_report"]` and `state_payload["baseline_run_id"]` instead of only the old flat memo content.
  </behavior>
  <action>Create `tests/graph/test_workflow_reporting.py` with failing tests named `test_workflow_invoke_builds_report_and_persists_strategic_report_payload` and `test_workflow_invoke_marks_dropped_tickers_from_baseline_run`. Require `app/graph/workflow.py` to keep exporting `compile_workflow(...)`, but have `invoke(...)` place a serialized `strategic_report` object and `baseline_run_id` onto the returned state. The first workflow test must require the paused state to include `strategic_report["headline"]`, `strategic_report["summary"]`, and the text heading `Strategic Insight Report` inside `email_body`. The second workflow test must require `baseline_run_id == "run-prev"` and `strategic_report["dropped_tickers"] == ["AMD"]` when the baseline outcome contains `AMD` and the current outcome does not. Update `tests/graph/test_workflow.py` with a failing assertion that `mail_provider.calls[0]["text_body"]` contains `Immediate Actions` instead of `Ranked Candidates`. Update `tests/api/test_routes.py` with a failing test named `test_trigger_persists_strategic_report_and_baseline_run_id` that seeds one prior run with `state_payload={"strategic_report": {"run_id": "run-prev"}, "finalized_outcome": {...}}`, triggers a new run, and then requires the stored `RunRecord.state_payload` for the new run to contain keys `strategic_report`, `baseline_run_id`, and `email_body`. Add explicit skeleton entry points to `app/services/run_service.py` and `app/repositories/run_repository.py` named exactly `get_latest_report_baseline(self, *, exclude_run_id: str) -> dict | None` and `get_latest_reportable_run(self, *, exclude_run_id: str) -> RunRecord | None` so the tests have concrete lookup seams.</action>
  <acceptance_criteria>
    - `rg -n "test_workflow_invoke_builds_report_and_persists_strategic_report_payload|test_workflow_invoke_marks_dropped_tickers_from_baseline_run" tests/graph/test_workflow_reporting.py` returns both new workflow-reporting tests.
    - `rg -n "strategic_report\\[\"headline\"\\]|strategic_report\\[\"summary\"\\]|Strategic Insight Report|baseline_run_id == \"run-prev\"|\\[\"AMD\"\\]" tests/graph/test_workflow_reporting.py` returns the required persisted-report and baseline expectations.
    - `rg -n "Immediate Actions" tests/graph/test_workflow.py` returns the updated workflow email assertion.
    - `rg -n "test_trigger_persists_strategic_report_and_baseline_run_id|strategic_report|baseline_run_id|email_body" tests/api/test_routes.py` returns the new route persistence test and required keys.
    - `rg -n "def get_latest_report_baseline\\(self, \\*, exclude_run_id: str\\) -> dict \\| None|def get_latest_reportable_run\\(self, \\*, exclude_run_id: str\\) -> RunRecord \\| None" app/services/run_service.py app/repositories/run_repository.py` returns both exact baseline lookup seams.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/graph/test_workflow_reporting.py tests/graph/test_workflow.py tests/api/test_routes.py -q</automated>
  </verify>
  <done>Failing workflow and route coverage exists for baseline-aware strategic report generation and persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace flat memo generation with persisted strategic reports and baseline-aware workflow wiring</name>
  <files>app/api/routes.py, app/graph/workflow.py, app/repositories/run_repository.py, app/services/run_service.py, tests/api/test_routes.py, tests/graph/test_workflow.py, tests/graph/test_workflow_reporting.py</files>
  <read_first>app/api/routes.py, app/graph/workflow.py, app/repositories/run_repository.py, app/services/run_service.py, tests/graph/test_workflow_reporting.py, tests/graph/test_workflow.py, tests/api/test_routes.py, app/services/report_builder.py, app/services/email.py</read_first>
  <behavior>
    - Test 1: trigger routes look up the most recent prior reportable run before workflow invoke and pass the baseline into workflow state.
    - Test 2: workflow invoke builds a strategic report from `finalized_outcome` plus the baseline outcome, renders the email through `compose_report_email(...)`, and persists the serialized report on the paused state.
    - Test 3: persisted state can be inspected later without reconstructing the report from rendered email text.
  </behavior>
  <action>Implement `RunRepository.get_latest_reportable_run(...)` to return the most recent `RunRecord` ordered by `created_at.desc()` where `run_id != exclude_run_id` and `state_payload` is not null. Implement `RunService.get_latest_report_baseline(...)` to call that repository method and return `None` unless the selected run has both `state_payload["finalized_outcome"]` and either `state_payload["strategic_report"]` or `state_payload["email_body"]`. When a baseline exists, return a dict with exact keys `run_id`, `finalized_outcome`, and `strategic_report`. Update both trigger routes in `app/api/routes.py` so they call `request.app.state.run_service.get_latest_report_baseline(exclude_run_id=run_id_or_new_run.run_id)` before `runtime.start_run(...)` and pass the returned object as `baseline_report` in the state dict. Refactor `app/graph/workflow.py` to stop calling `_build_memo_content()` and `compose_recommendation_email(...)`. Instead, after `finalize_research_outcome(...)`, read `baseline_report = state.get("baseline_report")`, validate `baseline_report["finalized_outcome"]` through `ResearchOutcome`, call `build_strategic_insight_report(run_id=state["run_id"], outcome=finalized_outcome, baseline_run_id=baseline_report["run_id"] if baseline_report else None, baseline_outcome=validated_baseline_outcome if baseline_report else None)`, then call `compose_report_email(report=report, approval_url=self._build_approval_url(run_id, "approve"), rejection_url=self._build_approval_url(run_id, "reject"))`. Persist `strategic_report=report.model_dump(mode="python")` and `baseline_run_id=report.baseline_run_id` on the paused state returned from `_await_human_review(...)`; keep `finalized_outcome` and `recommendations` for downstream approval and broker-prestage compatibility. Delete `_build_memo_content()` entirely from `app/graph/workflow.py`. Ensure `tests/graph/test_workflow.py` and `tests/api/test_routes.py` now assert the new text headings `Strategic Insight Report` and `Immediate Actions` instead of `Ranked Candidates`. Do not compare or derive baseline changes from previous email bodies anywhere in this implementation.</action>
  <acceptance_criteria>
    - `rg -n "def get_latest_reportable_run\\(self, \\*, exclude_run_id: str\\) -> RunRecord \\| None|created_at\\.desc\\(\\)|state_payload is not null|state_payload\\[\"finalized_outcome\"\\]" app/repositories/run_repository.py app/services/run_service.py` returns the baseline lookup methods and baseline state requirements.
    - `rg -n "get_latest_report_baseline\\(exclude_run_id=|baseline_report" app/api/routes.py` returns the route-level baseline lookup and state wiring.
    - `rg -n "build_strategic_insight_report\\(|compose_report_email\\(|strategic_report|baseline_run_id|del _build_memo_content|def _build_memo_content" app/graph/workflow.py` returns the new builder/render calls and persisted report fields, and `def _build_memo_content` returns no matches.
    - `rg -n "Strategic Insight Report|Immediate Actions|strategic_report|baseline_run_id" tests/graph/test_workflow.py tests/graph/test_workflow_reporting.py tests/api/test_routes.py` returns the new output and persistence assertions.
    - `python -m pytest tests/graph/test_workflow_reporting.py tests/graph/test_workflow.py tests/api/test_routes.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/graph/test_workflow_reporting.py tests/graph/test_workflow.py tests/api/test_routes.py -q</automated>
  </verify>
  <done>Workflow execution now produces baseline-aware strategic reports, renders the new operator email, and persists the report as structured run state.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/graph/test_workflow_reporting.py tests/graph/test_workflow.py tests/api/test_routes.py -q`. Then trigger one local test run through the route harness and confirm the stored `RunRecord.state_payload` includes `strategic_report`, `baseline_run_id`, `finalized_outcome`, and `email_body`.
</verification>

<success_criteria>
Phase 8 is fully wired: new runs build strategic insight reports against the latest prior delivered baseline, outgoing emails render the report sections deterministically, and the structured report is persisted for later review and local verification.
</success_criteria>

<output>
After completion, create `.planning/phases/08-upgrade-outputs-to-strategic-insight-reports/08-upgrade-outputs-to-strategic-insight-reports-03-SUMMARY.md`
</output>

---
phase: 06-replace-langgraph-with-a-custom-workflow-engine
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - pyproject.toml
  - app/config.py
  - app/ops/dry_run.py
  - README.md
  - .env.example
  - tests/ops/test_operational_docs.py
  - tests/ops/test_dry_run.py
autonomous: true
requirements: [SC-01, SC-03]
must_haves:
  truths:
    - "The repo no longer declares LangGraph packages, settings, env vars, or docs language."
    - "The dry-run and operator-doc tests still prove the trigger-to-approval-to-broker-prestage path after the engine cutover."
  artifacts:
    - path: "pyproject.toml"
      provides: "Dependency list without LangGraph packages"
      contains: "dependencies = ["
    - path: "app/config.py"
      provides: "Settings surface without langgraph_checkpointer_url"
      min_lines: 20
    - path: "tests/ops/test_operational_docs.py"
      provides: "Doc drift checks for the post-LangGraph operator contract"
      min_lines: 30
  key_links:
    - from: "README.md"
      to: "tests/ops/test_operational_docs.py"
      via: "doc-proof command and checklist assertions"
      pattern: "README_COMMANDS"
    - from: "app/ops/dry_run.py"
      to: "tests/ops/test_dry_run.py"
      via: "JSON output assertions"
      pattern: "approval_status"
---

<objective>
Remove the remaining LangGraph dependency, config, and documentation surface after the runtime cutover is complete.

Purpose: Finish the Phase 6 replacement so the repository itself presents one coherent app-owned workflow system to operators, tests, and future phases.
Output: Clean dependency/config surface, updated env/docs, and green dry-run/doc-proof coverage for the new workflow engine.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-RESEARCH.md
@.planning/phases/05-operational-readiness/05-operational-readiness-02-SUMMARY.md
@pyproject.toml
@app/config.py
@app/ops/dry_run.py
@README.md
@.env.example
@tests/ops/test_operational_docs.py
@tests/ops/test_dry_run.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove LangGraph from dependencies, settings, and dry-run configuration</name>
  <files>pyproject.toml, app/config.py, app/ops/dry_run.py, .env.example, tests/ops/test_dry_run.py</files>
  <read_first>pyproject.toml, app/config.py, app/ops/dry_run.py, .env.example, tests/ops/test_dry_run.py, .planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-RESEARCH.md, .planning/phases/05-operational-readiness/05-operational-readiness-01-SUMMARY.md</read_first>
  <action>Remove the `langgraph` and `langgraph-checkpoint-postgres` dependency lines from `pyproject.toml`. Remove the `langgraph_checkpointer_url` setting from `app/config.py` and remove `INVESTOR_LANGGRAPH_CHECKPOINTER_URL=` from `.env.example`. Update `app/ops/dry_run.py` so it no longer reads, injects, or documents any checkpointer URL; the dry run should rely only on `database_url`, repo-owned doubles, and the app-owned workflow engine from Plan 02. Update `tests/ops/test_dry_run.py` only as needed to keep the exact JSON contract intact: `trigger_status`, `approval_status`, `run_id`, `approval_url`, `artifact_count`, and `log_lines` must still be present, and `approval_status` must still equal `broker_prestaged`.</action>
  <acceptance_criteria>`pyproject.toml` contains no `langgraph` or `langgraph-checkpoint-postgres` strings; `app/config.py` contains no `langgraph_checkpointer_url`; `.env.example` contains no `INVESTOR_LANGGRAPH_CHECKPOINTER_URL`; `app/ops/dry_run.py` contains no `checkpointer` string; `tests/ops/test_dry_run.py` still asserts `approval_status` and `artifact_count`.</acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ops/test_dry_run.py -q</automated>
  </verify>
  <done>The repo's declared runtime no longer depends on LangGraph packages or env variables, and the dry-run command still proves the end-to-end local workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite operator-facing docs and doc-proof tests around the app-owned workflow engine</name>
  <files>README.md, tests/ops/test_operational_docs.py, .env.example</files>
  <read_first>README.md, tests/ops/test_operational_docs.py, .env.example, .planning/phases/05-operational-readiness/05-operational-readiness-02-SUMMARY.md, .planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-RESEARCH.md</read_first>
  <action>Rewrite the README opening line to `Local-first Python service for an app-owned investing workflow.` Remove any mention of `LangGraph-driven` wording and keep the operator command surface unchanged: `pip install -e .`, `cp .env.example .env`, `docker compose up -d postgres`, `alembic upgrade head`, `python -m app.ops.dry_run`, `uvicorn app.main:app --reload`, and the four cron scripts must still be documented. Update `tests/ops/test_operational_docs.py` so `ENV_KEYS` no longer lists `INVESTOR_LANGGRAPH_CHECKPOINTER_URL`, and keep the README command and checklist assertions aligned with the new wording. Keep the Phase 5 pattern intact: any doc-string change that matters to operators must be locked by `tests/ops/test_operational_docs.py`.</action>
  <acceptance_criteria>`README.md` contains the exact line `Local-first Python service for an app-owned investing workflow.`; `README.md` contains no `LangGraph-driven` string; `tests/ops/test_operational_docs.py` contains no `INVESTOR_LANGGRAPH_CHECKPOINTER_URL`; `tests/ops/test_operational_docs.py` still contains the commands `python -m app.ops.dry_run` and `uvicorn app.main:app --reload`.</acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ops/test_operational_docs.py tests/ops/test_dry_run.py -q</automated>
  </verify>
  <done>Operator docs, env template, and doc-proof tests describe only the app-owned workflow engine path, with no LangGraph terminology or setup steps left behind.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/ops/test_operational_docs.py tests/ops/test_dry_run.py -q`, then run `python -m pytest -q` if the quick slice is green. Also use `rg -n "langgraph|checkpointer" README.md .env.example app/config.py app/ops/dry_run.py pyproject.toml` to confirm no leftover Phase 6 terminology survives outside historical planning files.
</verification>

<success_criteria>
The repository surface, operator docs, and verification paths all reflect one app-owned workflow engine. There are no remaining runtime dependencies or operator-facing references to LangGraph or checkpointers.
</success_criteria>

<output>
After completion, create `.planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-03-SUMMARY.md`
</output>

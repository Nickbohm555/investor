---
phase: 06-replace-langgraph-with-a-custom-workflow-engine
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - app/services/approvals.py
  - app/main.py
  - app/api/routes.py
  - tests/api/test_routes.py
  - tests/integration/test_hitl_resume.py
  - tests/integration/test_broker_prestage.py
autonomous: true
requirements: [SC-02, SC-03]
must_haves:
  truths:
    - "Trigger, approval, and rejection flows run through the persisted workflow engine instead of runtime resume semantics."
    - "Approval and rejection produce the same broker-prestage and reject outcomes after an app restart."
    - "The HTTP contract no longer exposes `thread_id` or runtime-resume vocabulary."
    - "An approval result that returns broker `handoff` data is consumed to create or persist broker artifacts before the run is finalized as `broker_prestaged`."
  artifacts:
    - path: "app/services/approvals.py"
      provides: "Approval service that consumes engine handoff results and persists broker artifacts"
      min_lines: 80
    - path: "app/main.py"
      provides: "App-state composition for workflow_engine"
      min_lines: 40
    - path: "app/api/routes.py"
      provides: "Trigger and approval routes wired to the persisted engine"
      min_lines: 120
  key_links:
    - from: "app/api/routes.py"
      to: "app/main.py"
      via: "request.app.state.workflow_engine"
      pattern: "workflow_engine"
    - from: "app/services/approvals.py"
      to: "app/workflows/engine.py"
      via: "advance_run on approval event and consumption of returned handoff"
      pattern: "advance_run"
    - from: "app/services/approvals.py"
      to: "tests/integration/test_broker_prestage.py"
      via: "broker artifact creation and restart-safe persistence assertions"
      pattern: "artifact"
    - from: "tests/integration/test_hitl_resume.py"
      to: "app/services/approvals.py"
      via: "persisted approval flow assertions"
      pattern: "ApprovalService"
---

<objective>
Wire the persisted workflow engine into the FastAPI trigger and approval paths and preserve restart-safe approval-to-broker behavior.

Purpose: Separate the HTTP and integration cutover from the schema/engine-core work so the riskiest behavioral changes remain small enough to verify quickly.
Output: Route and app-state rewiring, approval-service cutover to `workflow_engine.advance_run`, and refreshed API/integration coverage for trigger, approval, rejection, and broker prestage.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-RESEARCH.md
@.planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-02-PLAN.md
@.planning/phases/04-hitl-and-broker-prestage/04-03-SUMMARY.md
@app/services/approvals.py
@app/main.py
@app/api/routes.py
@app/workflows/engine.py
@tests/api/test_routes.py
@tests/integration/test_hitl_resume.py
@tests/integration/test_broker_prestage.py

<interfaces>
Workflow engine API provided by Plan 02:
```python
class WorkflowEngine:
    def start_run(self, *, run_id: str, quiver_client: object) -> dict: ...
    def advance_run(self, *, run_id: str, event: str) -> dict: ...
```

HTTP-visible status vocabulary to preserve:
```python
"awaiting_review"
"rejected"
"broker_prestaged"
```

Approval result contract to consume in this plan:
```python
{
    "status": "broker_prestaged",
    "current_step": "broker_prestaged",
    "handoff": {...}
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire app composition and routes onto the persisted workflow engine</name>
  <files>app/main.py, app/api/routes.py, tests/api/test_routes.py</files>
  <read_first>app/main.py, app/api/routes.py, app/workflows/engine.py, tests/api/test_routes.py, .planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-02-PLAN.md</read_first>
  <action>Update `app/main.py` so the app composes and stores `app.state.workflow_engine` instead of `app.state.runtime`. Update both trigger routes in `app/api/routes.py` to call `workflow_engine.start_run(run_id=..., quiver_client=quiver_client)`, remove all `thread_id` generation entirely, and keep persisted recommendation/state storage aligned with the engine result. Update `tests/api/test_routes.py` so the route contract asserts no `thread_id` in responses or stored payload expectations, and that trigger responses still return `status == "started"` while persisted runs end at `awaiting_review` until approval occurs.</action>
  <acceptance_criteria>`app/main.py` contains `app.state.workflow_engine`; `rg -n "app\\.state\\.runtime|InvestorRuntime" app/main.py app/api/routes.py` returns no matches; `app/api/routes.py` contains no `thread_id =` line; `tests/api/test_routes.py` contains no `thread_id` assertion and contains `awaiting_review`.</acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/api/test_routes.py -q</automated>
  </verify>
  <done>The FastAPI trigger path uses the persisted workflow engine directly, and the API contract no longer leaks thread-id or runtime-resume concepts.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire approval handling to persisted event advancement and preserve restart-safe integration behavior</name>
  <files>app/services/approvals.py, app/api/routes.py, tests/integration/test_hitl_resume.py, tests/integration/test_broker_prestage.py</files>
  <read_first>app/services/approvals.py, app/api/routes.py, app/workflows/engine.py, tests/integration/test_hitl_resume.py, tests/integration/test_broker_prestage.py, .planning/phases/04-hitl-and-broker-prestage/04-03-SUMMARY.md</read_first>
  <action>Update `ApprovalService` so it records the approval event, validates duplicate/stale/not-awaiting-review cases, and then calls `workflow_engine.advance_run(run_id=payload.run_id, event=f"approval:{payload.decision}")`; do not inject `thread_id` back into `state_payload`, and do not call `runtime.resume_run`. On the approve branch, the service must explicitly consume the returned `handoff` payload and pass it into the existing broker-prestage persistence path so broker artifacts are created or updated before the run is finalized as `broker_prestaged`; reject must not call the broker-prestage path. Keep the restart-safe behavior from Phase 4: integration tests must recreate the app against the same database before the callback fires. Update `tests/integration/test_hitl_resume.py` and `tests/integration/test_broker_prestage.py` so they assert the exact persisted transitions `awaiting_review -> rejected` on reject and `awaiting_review -> broker_prestaged` on approve, and on the approve path they also assert broker artifacts exist after the second app instance handles the approval event, with no `resuming` step and no `thread_id` expectations.</action>
  <acceptance_criteria>`app/services/approvals.py` contains `advance_run(run_id=payload.run_id, event=f"approval:{payload.decision}")` and contains `handoff`; `rg -n "runtime\\.resume_run|thread_id" app/services/approvals.py tests/integration/test_hitl_resume.py tests/integration/test_broker_prestage.py` returns no matches outside fixture data setup; `tests/integration/test_hitl_resume.py` contains `broker_prestaged` and does not contain `resuming`; `tests/integration/test_broker_prestage.py` contains assertions for broker artifact creation after restart on approve.</acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/integration/test_hitl_resume.py tests/integration/test_broker_prestage.py -q</automated>
  </verify>
  <done>Approval handling advances stored workflow steps directly, consumes the engine `handoff` to create or persist broker artifacts on approve, and restart-safe integration coverage proves both the status transition and artifact creation after the app is recreated.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/api/test_routes.py tests/integration/test_hitl_resume.py tests/integration/test_broker_prestage.py -q`, then run `python -m pytest -q` at the end of the wave. This plan is only complete if HTTP entrypoints and restart-safe approval flows both use the persisted workflow engine.
</verification>

<success_criteria>
The FastAPI layer and approval service both use the app-owned workflow engine, and restart-safe trigger/approval/broker-prestage coverage remains intact after the behavioral cutover.
</success_criteria>

<output>
After completion, create `.planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-03-SUMMARY.md`
</output>

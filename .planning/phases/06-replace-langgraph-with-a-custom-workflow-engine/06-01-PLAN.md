---
phase: 06-replace-langgraph-with-a-custom-workflow-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/workflows/__init__.py
  - app/workflows/engine.py
  - app/workflows/state.py
  - app/graph/workflow.py
  - tests/graph/test_workflow.py
  - app/db/migrations/versions/0003_merge_phase5_heads.py
  - tests/services/test_persistence.py
autonomous: true
requirements: [SC-01, SC-02]
must_haves:
  truths:
    - "The repository has one app-owned workflow engine entrypoint for starting runs and applying approval events."
    - "Workflow results use persisted-step vocabulary that approval and broker-prestage flows can share in later plans."
    - "Alembic has a single merged head above the two existing 0002 revisions before the runtime cutover begins."
  artifacts:
    - path: "app/workflows/engine.py"
      provides: "WorkflowEngine interface for persisted run start and event advancement"
      min_lines: 40
    - path: "app/workflows/state.py"
      provides: "Serializable workflow result and event types"
      min_lines: 20
    - path: "app/db/migrations/versions/0003_merge_phase5_heads.py"
      provides: "Single-head Alembic merge revision"
      contains: "down_revision = (\"0002_add_schedule_fields\", \"0002_create_broker_artifacts\")"
  key_links:
    - from: "tests/graph/test_workflow.py"
      to: "app/workflows/engine.py"
      via: "direct WorkflowEngine imports and assertions"
      pattern: "from app\\.workflows\\.engine import WorkflowEngine"
    - from: "tests/services/test_persistence.py"
      to: "app/db/migrations/versions/0003_merge_phase5_heads.py"
      via: "migration-head assertion"
      pattern: "0003_merge_phase5_heads"
---

<objective>
Establish the Phase 6 foundation before the runtime cutover by introducing the app-owned workflow engine interface and reconciling the existing Alembic branch split.

Purpose: Lock the final engine naming and step vocabulary first so downstream plans build against one stable app-owned contract instead of changing APIs mid-phase.
Output: New workflow engine contract modules, updated engine-focused tests, a merged Alembic head, and persistence tests that prove the branch merge.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-RESEARCH.md
@.planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-VALIDATION.md
@.planning/phases/04-hitl-and-broker-prestage/04-01-SUMMARY.md
@.planning/phases/04-hitl-and-broker-prestage/04-03-SUMMARY.md
@app/graph/workflow.py
@app/graph/runtime.py
@app/services/approvals.py
@app/db/migrations/versions/0002_add_schedule_fields.py
@app/db/migrations/versions/0002_create_broker_artifacts.py
@tests/graph/test_workflow.py
@tests/services/test_persistence.py

<interfaces>
From app/graph/workflow.py:
```python
class CompiledInvestorWorkflow:
    def invoke(self, state: dict) -> dict: ...
    def resume(self, state: dict, decision: str) -> dict: ...
```

From app/graph/runtime.py:
```python
class InvestorRuntime:
    def start_run(self, *, run_id: str, thread_id: str, research_node, quiver_client, workflow=None) -> dict: ...
    def resume_run(self, state: dict, *, decision: str, research_node) -> dict: ...
```

Target contract for this phase:
```python
class WorkflowEngine:
    def start_run(self, *, run_id: str, quiver_client: object) -> dict: ...
    def advance_run(self, *, run_id: str, event: str) -> dict: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce the app-owned workflow engine contract and update unit coverage</name>
  <files>app/workflows/__init__.py, app/workflows/engine.py, app/workflows/state.py, app/graph/workflow.py, tests/graph/test_workflow.py</files>
  <read_first>app/workflows/engine.py, app/workflows/state.py, app/graph/workflow.py, app/graph/runtime.py, app/services/approvals.py, tests/graph/test_workflow.py, .planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-RESEARCH.md, .planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-VALIDATION.md</read_first>
  <action>Create `app/workflows/state.py` with serializable types for the final phase contract: a `WorkflowEvent` literal that includes exactly `approval:approve` and `approval:reject`, and a `WorkflowResult` model or dataclass that carries `status`, `current_step`, `state_payload`, and optional `handoff`. Create `app/workflows/engine.py` with `class WorkflowEngine` exposing exactly two public methods for the rest of Phase 6: `start_run(self, *, run_id: str, quiver_client: object) -> dict` and `advance_run(self, *, run_id: str, event: str) -> dict`. In this plan the class can remain a thin adapter around the existing workflow code and does not need to own database writes yet, but it must lock the final public method names and event vocabulary. Update `app/graph/workflow.py` so approve returns `status="broker_prestaged"` and `current_step="broker_prestaged"` with `handoff`, and reject returns `status="rejected"` and `current_step="rejected"` with no `resume_command`. Update `tests/graph/test_workflow.py` to import `WorkflowEngine`, stop asserting `resume_command`, stop asserting `InvestorRuntime.start_run` / `resume_run`, and assert the exact phase-wide result vocabulary: `start_run(...)` produces `status == "awaiting_review"` and `current_step == "awaiting_review"`, `advance_run(..., event="approval:approve")` produces `status == "broker_prestaged"` and `current_step == "broker_prestaged"`, and `advance_run(..., event="approval:reject")` produces `status == "rejected"` and `current_step == "rejected"`.</action>
  <acceptance_criteria>`app/workflows/engine.py` contains `class WorkflowEngine`, `def start_run(self, *, run_id: str, quiver_client: object) -> dict`, and `def advance_run(self, *, run_id: str, event: str) -> dict`; `app/workflows/state.py` contains the exact strings `approval:approve` and `approval:reject`; `tests/graph/test_workflow.py` imports `WorkflowEngine`; `rg -n "resume_command" tests/graph/test_workflow.py` returns no matches; `rg -n "InvestorRuntime|start_run|resume_run" tests/graph/test_workflow.py` returns no matches for runtime usage and only matches the new `WorkflowEngine.start_run` assertions.</acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/graph/test_workflow.py -q</automated>
  </verify>
  <done>Graph-level workflow coverage proves the final app-owned engine API and final step/status vocabulary that all later Phase 6 plans will preserve.</done>
</task>

<task type="auto">
  <name>Task 2: Merge the duplicate Alembic heads and prove the branch shape in persistence tests</name>
  <files>app/db/migrations/versions/0003_merge_phase5_heads.py, tests/services/test_persistence.py</files>
  <read_first>app/db/migrations/versions/0001_create_run_tables.py, app/db/migrations/versions/0002_add_schedule_fields.py, app/db/migrations/versions/0002_create_broker_artifacts.py, tests/services/test_persistence.py, .planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-RESEARCH.md</read_first>
  <action>Create `app/db/migrations/versions/0003_merge_phase5_heads.py` as an Alembic merge revision with `revision = "0003_merge_phase5_heads"` and `down_revision = ("0002_add_schedule_fields", "0002_create_broker_artifacts")`. Keep `upgrade()` and `downgrade()` empty because this revision only merges heads. Update `tests/services/test_persistence.py` to add migration-path coverage that explicitly imports the three existing revision modules plus the new merge revision, asserts the new merge revision is the only head after the split, and verifies the merged history still yields the `runs`, `recommendations`, `approval_events`, `state_transitions`, and `broker_artifacts` tables. Keep the test SQLite-only and deterministic; do not add a live Postgres dependency in this plan.</action>
  <acceptance_criteria>`app/db/migrations/versions/0003_merge_phase5_heads.py` contains `revision = "0003_merge_phase5_heads"` and `down_revision = ("0002_add_schedule_fields", "0002_create_broker_artifacts")`; `tests/services/test_persistence.py` contains a test named `test_alembic_branch_merge_produces_single_head`; that test references the exact revision ids `0002_add_schedule_fields`, `0002_create_broker_artifacts`, and `0003_merge_phase5_heads`.</acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_persistence.py -q</automated>
  </verify>
  <done>The migration history is explicitly merged in-repo, and persistence coverage fails if the Phase 5 branch split reappears or if the merged history drops any existing runtime tables.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/graph/test_workflow.py tests/services/test_persistence.py -q` after both tasks. The repo should now have one merged Alembic head and a unit-tested engine contract ready for the persisted-runtime cutover.
</verification>

<success_criteria>
Phase 6 has a stable engine API, stable step/status vocabulary, and a reconciled migration history. Later plans can cut over runtime wiring without redefining the public workflow contract.
</success_criteria>

<output>
After completion, create `.planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-01-SUMMARY.md`
</output>

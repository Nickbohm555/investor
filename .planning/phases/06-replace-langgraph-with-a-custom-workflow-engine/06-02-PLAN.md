---
phase: 06-replace-langgraph-with-a-custom-workflow-engine
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - app/db/models.py
  - app/db/migrations/versions/0004_phase6_workflow_engine_schema.py
  - app/repositories/runs.py
  - app/services/run_service.py
  - app/workflows/engine.py
  - app/workflows/state.py
  - app/graph/workflow.py
  - tests/graph/test_workflow.py
  - tests/services/test_persistence.py
autonomous: true
requirements: [SC-01, SC-02]
must_haves:
  truths:
    - "Persisted run records no longer expose `thread_id`; workflow progress is represented by `current_step` and `state_payload`."
    - "The workflow engine can load a stored run and compute the next persisted step using the same `start_run` and `advance_run` API defined in Plan 01."
    - "The Phase 6 schema no longer exposes `thread_id`; the run cursor is represented by `current_step` and persisted `state_payload`."
  artifacts:
    - path: "app/workflows/engine.py"
      provides: "Persisted workflow dispatcher used by later route and approval wiring"
      min_lines: 80
    - path: "app/db/migrations/versions/0004_phase6_workflow_engine_schema.py"
      provides: "Schema migration that removes `thread_id` and keeps `state_payload`"
      contains: "drop_column(\"thread_id\")"
    - path: "app/db/models.py"
      provides: "RunRecord schema without `thread_id` and with persisted `state_payload` as the workflow cursor payload"
      contains: "state_payload"
  key_links:
    - from: "app/workflows/engine.py"
      to: "app/db/models.py"
      via: "persisted run loading and step advancement"
      pattern: "RunRecord"
    - from: "tests/graph/test_workflow.py"
      to: "app/workflows/engine.py"
      via: "engine method assertions"
      pattern: "advance_run"
    - from: "tests/services/test_persistence.py"
      to: "app/db/migrations/versions/0004_phase6_workflow_engine_schema.py"
      via: "schema assertions"
      pattern: "0004_phase6_workflow_engine_schema"
---

<objective>
Replace the persisted runtime foundation beneath the existing routes by removing `thread_id` and teaching the workflow engine to operate on stored runs.

Purpose: Shrink the cutover into a focused persistence-and-engine plan so the later route/approval wiring plan can build on a finished schema and engine core.
Output: Updated schema/model surface, a persisted `WorkflowEngine`, and refreshed unit/persistence coverage for the new engine core.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-RESEARCH.md
@.planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-VALIDATION.md
@.planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-01-PLAN.md
@app/db/models.py
@app/repositories/runs.py
@app/services/run_service.py
@app/workflows/engine.py
@app/graph/workflow.py
@tests/services/test_persistence.py

<interfaces>
Persisted run shape to preserve:
```python
class RunRecord(Base):
    run_id: Mapped[str]
    status: Mapped[str]
    trigger_source: Mapped[str]
    approval_status: Mapped[str]
    current_step: Mapped[str]
    state_payload: Mapped[Optional[dict]]
```

Final engine entry points for this plan:
```python
class WorkflowEngine:
    def start_run(self, *, run_id: str, quiver_client: object) -> dict: ...
    def advance_run(self, *, run_id: str, event: str) -> dict: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the runtime persistence contract with app-owned workflow fields and migration support</name>
  <files>app/db/models.py, app/db/migrations/versions/0004_phase6_workflow_engine_schema.py, app/repositories/runs.py, app/services/run_service.py, tests/services/test_persistence.py</files>
  <read_first>app/db/models.py, app/repositories/runs.py, app/services/run_service.py, app/db/migrations/versions/0001_create_run_tables.py, app/db/migrations/versions/0002_add_schedule_fields.py, app/db/migrations/versions/0002_create_broker_artifacts.py, app/db/migrations/versions/0003_merge_phase5_heads.py, tests/services/test_persistence.py, .planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-RESEARCH.md</read_first>
  <action>Update `RunRecord` so the `runs` table no longer defines `thread_id`; keep `run_id`, `status`, `trigger_source`, `schedule_key`, `replay_of_run_id`, `approval_status`, `current_step`, `state_payload`, `created_at`, and `updated_at`. Create `app/db/migrations/versions/0004_phase6_workflow_engine_schema.py` with `down_revision = "0003_merge_phase5_heads"` and an `upgrade()` that uses `op.batch_alter_table("runs")` to add `state_payload` when missing and drop `thread_id`; keep `broker_artifacts` untouched. Update `RunService.create_pending_run()` to remove the `thread_id` parameter entirely, update `apply_approval_decision()` and any returned payloads so they no longer include `thread_id`, and keep `current_step` as the only persisted workflow cursor. Update `RunsRepository` helpers if needed so transition and lookup code no longer references `thread_id`. Expand `tests/services/test_persistence.py` to assert the `runs` inspector contains `state_payload` and does not contain `thread_id`, and to assert `RunService` can create pending runs, store state, and apply approval decisions without any thread id field.</action>
  <acceptance_criteria>`app/db/models.py` contains `state_payload` and has no `thread_id` field on `RunRecord`; `app/db/migrations/versions/0004_phase6_workflow_engine_schema.py` contains `down_revision = "0003_merge_phase5_heads"` and `drop_column("thread_id")`; `app/services/run_service.py` contains `def create_pending_run(` but no `thread_id:` parameter; `rg -n "thread_id" app/db/models.py app/repositories/runs.py app/services/run_service.py` returns no matches outside migration downgrade code and test fixtures.</acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_persistence.py -q</automated>
  </verify>
  <done>The database/model contract is now neutral to LangGraph, and persistence tests prove the app can create and mutate runs without a thread-id concept.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the persisted workflow engine core against the new schema</name>
  <files>app/workflows/engine.py, app/workflows/state.py, app/graph/workflow.py, tests/graph/test_workflow.py, tests/services/test_persistence.py</files>
  <read_first>app/workflows/engine.py, app/workflows/state.py, app/graph/workflow.py, app/db/models.py, app/repositories/runs.py, tests/graph/test_workflow.py, tests/services/test_persistence.py, .planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-01-PLAN.md, .planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-RESEARCH.md</read_first>
  <action>Implement the persisted engine API in `app/workflows/engine.py` using the exact method names locked in Plan 01. `start_run(run_id: str, *, quiver_client)` must load the run record, execute the research and memo branch, serialize `evidence_bundles`, `finalized_outcome`, `recommendations`, and `email_body` into `run.state_payload`, and return a result with `status="awaiting_review"` and `current_step="awaiting_review"`. `advance_run(run_id: str, *, event: str)` must load the stored run and dispatch by `current_step` and event vocabulary only: `approval:reject` returns `status="rejected"` and `current_step="rejected"`; `approval:approve` returns `status="broker_prestaged"` and `current_step="broker_prestaged"` plus `handoff`, but this plan must not wire broker persistence or FastAPI routes yet. Update `tests/graph/test_workflow.py` and `tests/services/test_persistence.py` so they assert the engine operates from persisted run state, uses the exact `start_run` / `advance_run` methods, and preserves the final step/status vocabulary from Plan 01.</action>
  <acceptance_criteria>`app/workflows/engine.py` contains `def start_run(self, *, run_id: str, quiver_client)` and `def advance_run(self, *, run_id: str, event: str)`; `rg -n "broker_prestaged|awaiting_review|rejected" tests/graph/test_workflow.py tests/services/test_persistence.py` returns matches; `rg -n "thread_id" app/workflows/engine.py tests/graph/test_workflow.py tests/services/test_persistence.py` returns no matches outside historical fixture setup.</acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/graph/test_workflow.py tests/services/test_persistence.py -q</automated>
  </verify>
  <done>The workflow engine now operates on persisted run data and final step/status vocabulary, ready for a separate plan to wire FastAPI and approval flows onto it.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/services/test_persistence.py tests/graph/test_workflow.py -q`. This plan is only complete if the schema and engine core are stable enough that the next plan can wire routes and approvals without changing method names or step/status vocabulary.
</verification>

<success_criteria>
Runtime persistence and engine core are app-owned in code and schema. The next plan can wire HTTP and approval execution to this engine without reopening the migration or contract design.
</success_criteria>

<output>
After completion, create `.planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-02-SUMMARY.md`
</output>

---
phase: 06-replace-langgraph-with-a-custom-workflow-engine
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - app/db/models.py
  - app/db/migrations/versions/0004_phase6_workflow_engine_schema.py
  - app/repositories/runs.py
  - app/services/run_service.py
  - app/services/approvals.py
  - app/workflows/engine.py
  - app/workflows/state.py
  - app/graph/workflow.py
  - app/main.py
  - app/api/routes.py
  - tests/api/test_routes.py
  - tests/integration/test_hitl_resume.py
  - tests/integration/test_broker_prestage.py
  - tests/graph/test_workflow.py
  - tests/services/test_persistence.py
autonomous: true
requirements: [SC-01, SC-02, SC-03]
must_haves:
  truths:
    - "Trigger, approval, and rejection flows advance app-owned workflow steps without thread ids, checkpointers, or runtime resume calls."
    - "Approval and rejection operate from persisted run state and produce the same broker-prestage and reject outcomes after an app restart."
    - "The Phase 6 schema no longer exposes `thread_id`; the run cursor is represented by `current_step` and persisted `state_payload`."
  artifacts:
    - path: "app/workflows/engine.py"
      provides: "Persisted workflow dispatcher used by routes and approvals"
      min_lines: 80
    - path: "app/db/migrations/versions/0004_phase6_workflow_engine_schema.py"
      provides: "Schema migration that removes `thread_id` and keeps `state_payload`"
      contains: "drop_column(\"thread_id\")"
    - path: "app/services/approvals.py"
      provides: "Approval service that calls workflow_engine.advance_run"
      min_lines: 80
  key_links:
    - from: "app/api/routes.py"
      to: "app/workflows/engine.py"
      via: "request.app.state.workflow_engine"
      pattern: "workflow_engine"
    - from: "app/services/approvals.py"
      to: "app/workflows/engine.py"
      via: "advance_run on approval event"
      pattern: "advance_run"
    - from: "tests/integration/test_hitl_resume.py"
      to: "app/services/approvals.py"
      via: "persisted approval flow assertions"
      pattern: "ApprovalService"
---

<objective>
Cut the live runtime over from LangGraph-shaped orchestration to app-owned persisted workflow steps.

Purpose: Make the application itself responsible for step dispatch, approval event handling, and restart-safe transitions so the Phase 6 goal is true in runtime behavior, not just naming.
Output: Updated schema/model surface, a persisted `WorkflowEngine`, route and approval rewiring, and refreshed integration coverage for trigger, approval, rejection, and broker prestage.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-RESEARCH.md
@.planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-VALIDATION.md
@.planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-01-PLAN.md
@app/db/models.py
@app/repositories/runs.py
@app/services/run_service.py
@app/services/approvals.py
@app/workflows/engine.py
@app/graph/workflow.py
@app/main.py
@app/api/routes.py
@tests/api/test_routes.py
@tests/integration/test_hitl_resume.py
@tests/integration/test_broker_prestage.py
@tests/services/test_persistence.py

<interfaces>
Persisted run shape to preserve:
```python
class RunRecord(Base):
    run_id: Mapped[str]
    status: Mapped[str]
    trigger_source: Mapped[str]
    approval_status: Mapped[str]
    current_step: Mapped[str]
    state_payload: Mapped[Optional[dict]]
```

Final engine entry points for this plan:
```python
class WorkflowEngine:
    def start_run(self, *, run_id: str, quiver_client: object) -> dict: ...
    def advance_run(self, *, run_id: str, event: str | None = None) -> dict: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the runtime persistence contract with app-owned workflow fields and migration support</name>
  <files>app/db/models.py, app/db/migrations/versions/0004_phase6_workflow_engine_schema.py, app/repositories/runs.py, app/services/run_service.py, tests/services/test_persistence.py</files>
  <read_first>app/db/models.py, app/repositories/runs.py, app/services/run_service.py, app/db/migrations/versions/0001_create_run_tables.py, app/db/migrations/versions/0002_add_schedule_fields.py, app/db/migrations/versions/0002_create_broker_artifacts.py, app/db/migrations/versions/0003_merge_phase5_heads.py, tests/services/test_persistence.py, .planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-RESEARCH.md</read_first>
  <action>Update `RunRecord` so the `runs` table no longer defines `thread_id`; keep `run_id`, `status`, `trigger_source`, `schedule_key`, `replay_of_run_id`, `approval_status`, `current_step`, `state_payload`, `created_at`, and `updated_at`. Create `app/db/migrations/versions/0004_phase6_workflow_engine_schema.py` with `down_revision = "0003_merge_phase5_heads"` and an `upgrade()` that uses `op.batch_alter_table("runs")` to add `state_payload` when missing and drop `thread_id`; keep `broker_artifacts` untouched. Update `RunService.create_pending_run()` to remove the `thread_id` parameter entirely, update `apply_approval_decision()` and any returned payloads so they no longer include `thread_id`, and keep `current_step` as the only persisted workflow cursor. Update `RunsRepository` helpers if needed so transition and lookup code no longer references `thread_id`. Expand `tests/services/test_persistence.py` to assert the `runs` inspector contains `state_payload` and does not contain `thread_id`, and to assert `RunService` can create pending runs, store state, and apply approval decisions without any thread id field.</action>
  <acceptance_criteria>`app/db/models.py` contains `state_payload` and has no `thread_id` field on `RunRecord`; `app/db/migrations/versions/0004_phase6_workflow_engine_schema.py` contains `down_revision = "0003_merge_phase5_heads"` and `drop_column("thread_id")`; `app/services/run_service.py` contains `def create_pending_run(` but no `thread_id:` parameter; `rg -n "thread_id" app/db/models.py app/repositories/runs.py app/services/run_service.py` returns no matches outside migration downgrade code and test fixtures.</acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_persistence.py -q</automated>
  </verify>
  <done>The database/model contract is now neutral to LangGraph, and persistence tests prove the app can create and mutate runs without a thread-id concept.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire trigger and approval flows onto the persisted workflow engine</name>
  <files>app/workflows/engine.py, app/workflows/state.py, app/graph/workflow.py, app/services/approvals.py, app/main.py, app/api/routes.py, tests/graph/test_workflow.py, tests/api/test_routes.py, tests/integration/test_hitl_resume.py, tests/integration/test_broker_prestage.py</files>
  <read_first>app/workflows/engine.py, app/workflows/state.py, app/graph/workflow.py, app/services/approvals.py, app/main.py, app/api/routes.py, tests/graph/test_workflow.py, tests/api/test_routes.py, tests/integration/test_hitl_resume.py, tests/integration/test_broker_prestage.py, .planning/phases/04-hitl-and-broker-prestage/04-03-SUMMARY.md, .planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-RESEARCH.md</read_first>
  <action>Implement the final persisted engine API in `app/workflows/engine.py`: `start_run(run_id: str, *, quiver_client)` must load the run from the database, execute the research/memo branch, persist serialized `evidence_bundles`, `finalized_outcome`, `recommendations`, and `email_body` into `run.state_payload`, then transition the run to `status="awaiting_review"` and `current_step="awaiting_review"`. `advance_run(run_id: str, *, event: str | None = None)` must load the stored run and dispatch by `current_step`: on `current_step == "awaiting_review"` and `event == "approval:reject"` it must transition directly to `status="rejected"` and `current_step="rejected"` with no broker side effects; on `current_step == "awaiting_review"` and `event == "approval:approve"` it must rebuild `CandidateRecommendation` objects from `run.state_payload["recommendations"]`, call `BrokerPrestageService.prestage_approved_recommendations(run_id=run_id, recommendation_ids=[...], broker_mode=settings.broker_mode)`, and transition the run to `status="broker_prestaged"` and `current_step="broker_prestaged"`. Update `app/main.py` so the app composes and stores `app.state.workflow_engine` instead of `app.state.runtime`. Update both trigger routes in `app/api/routes.py` to call `workflow_engine.start_run(...)`, remove `thread_id` generation entirely, and keep persisted recommendation/state storage aligned with the engine result. Update `ApprovalService` so it records the approval event, validates duplicate/stale/not-awaiting-review cases, and then calls `workflow_engine.advance_run(run_id=payload.run_id, event=f"approval:{payload.decision}")`; do not inject `thread_id` back into `state_payload`, and do not call `runtime.resume_run`. Update graph, API, and integration tests so they assert the new behavior exactly: no `thread_id` in API responses, approve transitions are `awaiting_review -> broker_prestaged`, reject transitions are `awaiting_review -> rejected`, and restart-safe integration tests recreate the app against the same database before the callback fires.</action>
  <acceptance_criteria>`app/workflows/engine.py` contains `def start_run(self, *, run_id: str, quiver_client)` and `def advance_run(self, *, run_id: str, event: str | None = None)`; `app/main.py` contains `app.state.workflow_engine` and `rg -n "app\\.state\\.runtime|InvestorRuntime" app/main.py app/api/routes.py app/services/approvals.py` returns no matches; `app/api/routes.py` contains no `thread_id =` line; `app/services/approvals.py` contains `advance_run(run_id=payload.run_id, event=f"approval:{payload.decision}")`; `tests/integration/test_hitl_resume.py` contains `broker_prestaged` and does not assert `resuming`; `tests/api/test_routes.py` contains no `thread_id` assertion.</acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/graph/test_workflow.py tests/api/test_routes.py tests/integration/test_hitl_resume.py tests/integration/test_broker_prestage.py -q</automated>
  </verify>
  <done>The live trigger and approval paths use the app-owned workflow engine end to end, restart-safe integration tests remain green, and the runtime contract no longer exposes LangGraph-style resume concepts.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/services/test_persistence.py tests/graph/test_workflow.py tests/api/test_routes.py tests/integration/test_hitl_resume.py tests/integration/test_broker_prestage.py -q`, then run `python -m pytest -q` if the quick slice is green. This plan is only complete if the app still proves restart-safe trigger, approval, rejection, and broker-prestage behavior after the cutover.
</verification>

<success_criteria>
Runtime orchestration is app-owned in both code and persistence. Approval no longer replays a paused payload through runtime resume semantics, and Phase 4/5 restart-safety remains covered after the schema and engine changes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-replace-langgraph-with-a-custom-workflow-engine/06-02-SUMMARY.md`
</output>

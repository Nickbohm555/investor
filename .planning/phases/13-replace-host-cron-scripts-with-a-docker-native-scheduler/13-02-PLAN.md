---
phase: 13-replace-host-cron-scripts-with-a-docker-native-scheduler
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - docker-compose.yml
  - .env.example
  - Dockerfile
  - ops/docker/start-app-runtime.sh
  - ops/scheduler/start-supercronic.sh
  - ops/scheduler/trigger-scheduled.sh
  - tests/conftest.py
  - tests/api/test_scheduling.py
  - tests/integration/test_scheduled_submission_flow.py
  - app/api/routes.py
  - app/services/scheduling.py
  - app/ops/dry_run.py
autonomous: true
requirements:
  - TBD-13-01
  - TBD-13-02
must_haves:
  truths:
    - "The shipped Compose stack runs Postgres, migrations, and one app container that also owns the scheduler process."
    - "The scheduler still reaches the existing `/runs/trigger/scheduled` route instead of creating a second execution path."
    - "Same-day duplicate scheduled triggers remain deduped by the app/database contract after the Docker migration."
  artifacts:
    - path: "docker-compose.yml"
      provides: "Compose-native runtime with postgres, migrate, and app services only"
      contains: "start-app-runtime.sh"
    - path: "tests/api/test_scheduling.py"
      provides: "Route-level duplicate-safety proof for scheduled triggers"
      contains: "duplicate"
    - path: "tests/integration/test_scheduled_submission_flow.py"
      provides: "Submission-path proof that duplicate scheduled triggers do not resubmit"
      contains: "submitted_order_count"
  key_links:
    - from: "docker-compose.yml"
      to: "ops/docker/start-app-runtime.sh"
      via: "app service command or default image entrypoint"
      pattern: "app:|start-app-runtime.sh|service_completed_successfully"
    - from: "ops/scheduler/trigger-scheduled.sh"
      to: "app/api/routes.py"
      via: "in-container POST using X-Investor-Scheduled-Trigger"
      pattern: "INVESTOR_SCHEDULE_TRIGGER_URL|/runs/trigger/scheduled|X-Investor-Scheduled-Trigger"
    - from: "tests/integration/test_scheduled_submission_flow.py"
      to: "app/services/scheduling.py"
      via: "same-day ET schedule-key dedupe assertions"
      pattern: "daily:|submitted_order_count|duplicate"
---

<objective>
Wire the actual Compose runtime so the app container owns both the FastAPI service and the repo-managed scheduler process while preserving the current route and database contracts.

Purpose: Phase 13 should migrate operations, not redesign scheduling logic; this plan turns the base assets from Plan 01 into the real runtime path while preserving the existing scheduled route and Postgres dedupe semantics.
Output: A three-service Compose runtime, Compose-aligned environment defaults, and green route/integration tests for duplicate-safe scheduled execution.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-replace-host-cron-scripts-with-a-docker-native-scheduler/13-RESEARCH.md
@.planning/phases/13-replace-host-cron-scripts-with-a-docker-native-scheduler/13-VALIDATION.md
@.planning/phases/13-replace-host-cron-scripts-with-a-docker-native-scheduler/13-01-PLAN.md
@.planning/phases/03-scheduling-and-email-delivery/03-03-SUMMARY.md
@.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-03-SUMMARY.md
@CLAUDE.md
@docker-compose.yml
@.env.example
@Dockerfile
@ops/docker/start-app-runtime.sh
@ops/scheduler/start-supercronic.sh
@ops/scheduler/trigger-scheduled.sh
@app/api/routes.py
@app/services/scheduling.py
@app/ops/dry_run.py

<interfaces>
From app/api/routes.py:
```python
return {
    "status": "duplicate",
    "run_id": run.run_id,
    "schedule_key": run.schedule_key or "",
    "duplicate": True,
}
```

From app/services/scheduling.py:
```python
def create_or_get_scheduled_run(...) -> tuple[RunRecord, bool]:
    ...
```

From CLAUDE.md:
```text
no separate always-on scheduler service
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the single-service Compose file with the app-managed scheduler runtime</name>
  <files>docker-compose.yml, .env.example, Dockerfile, ops/docker/start-app-runtime.sh, ops/scheduler/start-supercronic.sh, ops/scheduler/trigger-scheduled.sh</files>
  <read_first>docker-compose.yml, .env.example, Dockerfile, ops/docker/start-app-runtime.sh, ops/scheduler/start-supercronic.sh, ops/scheduler/trigger-scheduled.sh, app/config.py, app/main.py, tests/ops/test_docker_scheduler.py, CLAUDE.md</read_first>
  <behavior>
    - Test 1: `docker-compose.yml` defines `postgres`, `migrate`, and `app`, with the app gated on migration completion and no `scheduler` service.
    - Test 2: The app runtime owns the scheduler process through `ops/docker/start-app-runtime.sh`.
    - Test 3: Compose configuration resolves successfully without requiring host crontab setup.
  </behavior>
  <action>Rewrite `docker-compose.yml` into the actual Docker-native runtime. Keep `postgres` on image `postgres:16` with `POSTGRES_DB=investor`, `POSTGRES_USER=investor`, and `POSTGRES_PASSWORD=investor`, plus a healthcheck using `pg_isready -U investor -d investor`. Add a one-shot `migrate` service that builds from `Dockerfile`, uses `env_file: .env`, runs `alembic upgrade head`, depends on `postgres` with `condition: service_healthy`, and sets `restart: "no"`. Add an `app` service built from `Dockerfile`, using `env_file: .env`, publishing `8000:8000`, depending on `postgres` healthy and `migrate` completed successfully, and declaring a healthcheck `curl -fsS http://localhost:8000/health || exit 1`. Do not define a `scheduler` service anywhere in `docker-compose.yml`; the `app` image already starts `ops/docker/start-app-runtime.sh` and that script owns the scheduler process per `CLAUDE.md`. Update `.env.example` so `INVESTOR_DATABASE_URL=postgresql://investor:investor@postgres:5432/investor` and `INVESTOR_SCHEDULE_TRIGGER_URL=http://127.0.0.1:8000/runs/trigger/scheduled`, keeping `INVESTOR_SCHEDULE_CRON_EXPRESSION=0 7 * * 1-5` and `INVESTOR_SCHEDULE_TIMEZONE=America/New_York`. Update `ops/scheduler/start-supercronic.sh` so it fails fast when `INVESTOR_SCHEDULED_TRIGGER_TOKEN` is unset. Keep `ops/scheduler/trigger-scheduled.sh` pointed at `${INVESTOR_SCHEDULE_TRIGGER_URL:-http://127.0.0.1:8000/runs/trigger/scheduled}` so the request stays inside the app container by default. Do not add Docker socket access, in-container `crontab -l` management, or a separate scheduler service.</action>
  <acceptance_criteria>
    - `rg -n "postgres:16|migrate:|app:|condition: service_healthy|condition: service_completed_successfully|8000:8000|start-app-runtime.sh" docker-compose.yml` returns the three-service runtime and health-gated dependencies.
    - `! rg -n "^\\s*scheduler:" docker-compose.yml` succeeds.
    - `rg -n "postgres:5432/investor|http://127.0.0.1:8000/runs/trigger/scheduled|INVESTOR_SCHEDULE_CRON_EXPRESSION=0 7 \\* \\* 1-5|INVESTOR_SCHEDULE_TIMEZONE=America/New_York" .env.example ops/scheduler/trigger-scheduled.sh` returns the Docker-native env defaults and local in-container trigger URL.
    - `docker compose config >/dev/null` exits successfully.
    - `python -m pytest tests/ops/test_docker_scheduler.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ops/test_docker_scheduler.py -q && docker compose config >/dev/null</automated>
  </verify>
  <done>The Compose runtime now contains postgres, migrate, and app services only, and the app container owns the scheduler process without violating the repo constraint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bring the restored scheduling tests green without changing the dedupe ownership boundary</name>
  <files>tests/conftest.py, tests/api/test_scheduling.py, tests/integration/test_scheduled_submission_flow.py, app/api/routes.py, app/services/scheduling.py, app/ops/dry_run.py</files>
  <read_first>tests/conftest.py, tests/api/test_scheduling.py, tests/integration/test_scheduled_submission_flow.py, app/api/routes.py, app/services/scheduling.py, app/ops/dry_run.py, .planning/phases/03-scheduling-and-email-delivery/03-03-SUMMARY.md, .planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-03-SUMMARY.md</read_first>
  <behavior>
    - Test 1: Duplicate same-day scheduled triggers still return the original run and `duplicate=True`.
    - Test 2: A scheduled run that already submitted orders does not submit more orders when the duplicate branch is hit.
    - Test 3: Log/result strings remain `scheduled_trigger result=started`, `scheduled_trigger result=duplicate`, and `scheduled_trigger result=failure`.
  </behavior>
  <action>Use the restored tests to reconcile any missing fixtures or runtime mismatches while preserving the Phase 3 and Phase 11 contracts. If `tests/conftest.py` still lacks helpers, add the exact fixture seams needed to build deterministic app instances, capture sent mail, and inspect persisted run state using temporary SQLite or the existing session factory helpers from `app/ops/dry_run.py`. Only change `app/api/routes.py`, `app/services/scheduling.py`, or `app/ops/dry_run.py` if the recreated tests reveal an actual mismatch. If code changes are required, preserve the exact duplicate response shape `{"status": "duplicate", "run_id": ..., "schedule_key": ..., "duplicate": True}`, preserve the log prefixes `scheduled_trigger result=started|duplicate|failure`, and preserve the schedule key format `daily:YYYY-MM-DD`. Do not move dedupe into Docker runtime scripts, do not bypass the token-guarded route in tests, and do not create a scheduler-owned code path separate from `/runs/trigger/scheduled`.</action>
  <acceptance_criteria>
    - `rg -n "duplicate\": True|daily:|submitted_order_count|scheduled_trigger result=duplicate|scheduled_trigger result=started|scheduled_trigger result=failure" tests/api/test_scheduling.py tests/integration/test_scheduled_submission_flow.py app/api/routes.py app/services/scheduling.py app/ops/dry_run.py` returns the expected dedupe and logging contract strings.
    - `python -m pytest tests/api/test_scheduling.py tests/integration/test_scheduled_submission_flow.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/api/test_scheduling.py tests/integration/test_scheduled_submission_flow.py -q</automated>
  </verify>
  <done>The Docker-native runtime keeps the existing app/database dedupe behavior intact, and the recreated route/integration tests are green.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/ops/test_docker_scheduler.py tests/api/test_scheduling.py tests/integration/test_scheduled_submission_flow.py -q`, then run `docker compose config >/dev/null`.
</verification>

<success_criteria>
The repo now has one real scheduler runtime: Compose starts postgres, migrations, and one app container that owns both the HTTP service and the repo-managed scheduler process while duplicate protection remains app-owned.
</success_criteria>

<output>
After completion, create `.planning/phases/13-replace-host-cron-scripts-with-a-docker-native-scheduler/13-replace-host-cron-scripts-with-a-docker-native-scheduler-02-SUMMARY.md`
</output>

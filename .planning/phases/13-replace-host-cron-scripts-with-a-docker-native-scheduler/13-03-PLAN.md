---
phase: 13-replace-host-cron-scripts-with-a-docker-native-scheduler
plan: 03
type: execute
wave: 3
depends_on:
  - 13-02
files_modified:
  - README.md
  - tests/ops/test_operational_docs.py
  - scripts/cron-install.sh
  - scripts/cron-status.sh
  - scripts/cron-trigger.sh
  - scripts/cron-remove.sh
autonomous: true
requirements:
  - TBD-13-03
must_haves:
  truths:
    - "The primary operator workflow is now Docker-native, not host `crontab` scripts."
    - "Scheduler observability happens through Compose logs for `migrate` and `app`, not a separate scheduler service."
    - "The repo no longer presents two real scheduling systems to operators."
  artifacts:
    - path: "README.md"
      provides: "Docker-native setup, runtime, and verification instructions"
      contains: "docker compose up -d --build"
    - path: "tests/ops/test_operational_docs.py"
      provides: "Docs drift coverage for the Docker-native scheduler contract"
      contains: "docker compose logs -f migrate app"
    - path: "scripts/cron-install.sh"
      provides: "Removed host-cron path"
      contains: ""
  key_links:
    - from: "README.md"
      to: "docker-compose.yml"
      via: "documented runtime commands and service names"
      pattern: "docker compose up -d --build|docker compose logs -f migrate app|docker compose down -v"
    - from: "tests/ops/test_operational_docs.py"
      to: "README.md"
      via: "assertions that Docker-native commands replace host cron instructions"
      pattern: "docker compose up -d --build|cron-install.sh|cron-status.sh"
---

<objective>
Finish the operational migration by removing the host-cron operator contract and locking the README and docs tests to the Docker-native app-managed scheduler path.

Purpose: Phase 13 is only complete when the repo presents one scheduler path to operators. Leaving the old cron scripts as first-class guidance would keep two operational systems alive.
Output: Updated README and docs tests for the app-managed Docker scheduler contract, plus removal of the host cron helper scripts.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-replace-host-cron-scripts-with-a-docker-native-scheduler/13-RESEARCH.md
@.planning/phases/13-replace-host-cron-scripts-with-a-docker-native-scheduler/13-VALIDATION.md
@.planning/phases/13-replace-host-cron-scripts-with-a-docker-native-scheduler/13-02-PLAN.md
@.planning/phases/05-operational-readiness/05-02-SUMMARY.md
@.planning/phases/12-system-diagram-and-readme-architecture-capture/12-system-diagram-and-readme-architecture-capture-02-SUMMARY.md
@CLAUDE.md
@README.md
@tests/ops/test_operational_docs.py
@docker-compose.yml
@ops/docker/start-app-runtime.sh
@ops/scheduler/trigger-scheduled.sh
@scripts/cron-install.sh
@scripts/cron-status.sh
@scripts/cron-trigger.sh
@scripts/cron-remove.sh

<interfaces>
From README.md today:
```markdown
## Cron Operations

./scripts/cron-install.sh
./scripts/cron-status.sh
./scripts/cron-trigger.sh
./scripts/cron-remove.sh
```

From CLAUDE.md:
```text
no separate always-on scheduler service
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing docs assertions for the app-managed Docker scheduler contract</name>
  <files>tests/ops/test_operational_docs.py, README.md, scripts/cron-install.sh, scripts/cron-status.sh, scripts/cron-trigger.sh, scripts/cron-remove.sh</files>
  <read_first>tests/ops/test_operational_docs.py, README.md, docker-compose.yml, ops/docker/start-app-runtime.sh, ops/scheduler/trigger-scheduled.sh, CLAUDE.md, scripts/cron-install.sh, scripts/cron-status.sh, scripts/cron-trigger.sh, scripts/cron-remove.sh</read_first>
  <behavior>
    - Test 1: README requires the exact Docker-native commands `docker compose up -d --build`, `docker compose logs -f migrate app`, and `docker compose down -v`.
    - Test 2: README fails if it still presents `./scripts/cron-install.sh`, `./scripts/cron-status.sh`, `./scripts/cron-trigger.sh`, or `./scripts/cron-remove.sh` as the primary scheduling path.
    - Test 3: Docs drift coverage checks that the app container owns the scheduler process and that no separate `scheduler` service is documented.
  </behavior>
  <action>Extend `tests/ops/test_operational_docs.py` before touching the README. Add one test that requires `README.md` to contain the exact command strings `docker compose up -d --build`, `docker compose logs -f migrate app`, and `docker compose down -v`. Add a second test that fails if the README still contains any of `./scripts/cron-install.sh`, `./scripts/cron-status.sh`, `./scripts/cron-trigger.sh`, `./scripts/cron-remove.sh`, `logs/cron/daily-trigger.log`, or the phrase `scheduler service` as the documented primary scheduler workflow. Add an assertion that the README must describe the `app` container as owning the scheduler process. Leave the repo red after this task; do not update the README or remove the scripts yet.</action>
  <acceptance_criteria>
    - `rg -n "docker compose up -d --build|docker compose logs -f migrate app|docker compose down -v|cron-install.sh|cron-status.sh|scheduler service|app container owns the scheduler process" tests/ops/test_operational_docs.py` returns the new positive and negative assertions.
    - `python -m pytest tests/ops/test_operational_docs.py -q` fails before the README and script cleanup land.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>The docs suite now expresses the final app-managed Docker scheduler contract and fails while the repo still documents host cron.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the README cron workflow with Docker commands and remove the host cron scripts</name>
  <files>README.md, tests/ops/test_operational_docs.py, scripts/cron-install.sh, scripts/cron-status.sh, scripts/cron-trigger.sh, scripts/cron-remove.sh</files>
  <read_first>README.md, tests/ops/test_operational_docs.py, docker-compose.yml, .env.example, ops/docker/start-app-runtime.sh, ops/scheduler/trigger-scheduled.sh, CLAUDE.md, scripts/cron-install.sh, scripts/cron-status.sh, scripts/cron-trigger.sh, scripts/cron-remove.sh</read_first>
  <behavior>
    - Test 1: Setup and runtime instructions use Docker Compose as the default operator path.
    - Test 2: The repo no longer ships the old host cron helper scripts.
    - Test 3: Docs drift coverage passes against the new app-managed Docker scheduler README contract.
  </behavior>
  <action>Update `README.md` so the primary setup and runtime workflow is Docker-native and truthful to the revised plan shape. In `## Setup`, replace the old `docker compose up -d postgres` plus host `alembic` step with one Docker-first flow centered on `docker compose up -d --build`. Add a short operator section that tells the user to inspect scheduling activity with `docker compose logs -f migrate app` and to tear down with `docker compose down -v`. Remove the `## Cron Operations` section entirely, remove any recommendation to inspect `logs/cron/daily-trigger.log`, and state plainly that the `app` container owns the weekday scheduler process through the repo-managed scripts in `ops/scheduler/`. Delete `scripts/cron-install.sh`, `scripts/cron-status.sh`, `scripts/cron-trigger.sh`, and `scripts/cron-remove.sh` instead of repurposing them as wrappers, so the repo exposes one real scheduler path. If `tests/ops/test_operational_docs.py` uses command lists or checklist constants, update them to the exact Docker strings above.</action>
  <acceptance_criteria>
    - `rg -n "docker compose up -d --build|docker compose logs -f migrate app|docker compose down -v|app container owns the weekday scheduler process" README.md` returns the new Docker-native operator instructions.
    - `! test -e scripts/cron-install.sh && ! test -e scripts/cron-status.sh && ! test -e scripts/cron-trigger.sh && ! test -e scripts/cron-remove.sh` succeeds.
    - `python -m pytest tests/ops/test_operational_docs.py tests/ops/test_docker_scheduler.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ops/test_operational_docs.py tests/ops/test_docker_scheduler.py -q</automated>
  </verify>
  <done>The README and docs tests now point exclusively to the app-managed Docker scheduler workflow, and the host cron scripts are gone.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/ops/test_operational_docs.py tests/ops/test_docker_scheduler.py -q`, then run `docker compose up -d --build`, `docker compose logs --no-color migrate app | tail -n 100`, and `docker compose down -v` to confirm the documented operator path works end to end without host cron.
</verification>

<success_criteria>
Phase 13 is operationally complete when the repo documents only the app-managed Docker scheduler runtime, the old host cron helper scripts are removed, and the documented smoke path shows scheduler activity through `app` container logs.
</success_criteria>

<output>
After completion, create `.planning/phases/13-replace-host-cron-scripts-with-a-docker-native-scheduler/13-replace-host-cron-scripts-with-a-docker-native-scheduler-03-SUMMARY.md`
</output>

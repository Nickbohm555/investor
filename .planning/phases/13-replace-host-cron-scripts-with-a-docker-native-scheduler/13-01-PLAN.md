---
phase: 13-replace-host-cron-scripts-with-a-docker-native-scheduler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/api/test_scheduling.py
  - tests/integration/test_scheduled_submission_flow.py
  - tests/ops/test_docker_scheduler.py
  - .dockerignore
  - Dockerfile
  - ops/docker/start-app-runtime.sh
  - ops/scheduler/crontab
  - ops/scheduler/start-supercronic.sh
  - ops/scheduler/trigger-scheduled.sh
autonomous: true
requirements:
  - TBD-13-01
  - TBD-13-02
must_haves:
  truths:
    - "Phase 13 has runnable source tests again instead of depending on `.pyc` artifacts or missing scheduler test files."
    - "The repo contains explicit Docker runtime assets for an app-container-managed scheduler path, not a separate always-on scheduler service."
    - "The scheduler contract is pinned to the existing token-guarded `/runs/trigger/scheduled` route and the app-owned ET/Postgres dedupe boundary."
  artifacts:
    - path: "tests/ops/test_docker_scheduler.py"
      provides: "Ops-level contract coverage for the app-container-managed Docker scheduler runtime"
      contains: "test_compose_stack_defines_app_managed_scheduler_runtime"
    - path: "ops/docker/start-app-runtime.sh"
      provides: "Combined app container entrypoint that starts the scheduler process and uvicorn together"
      contains: "supercronic"
    - path: "ops/scheduler/trigger-scheduled.sh"
      provides: "Repo-owned scheduler wrapper that hits the existing scheduled route"
      contains: "/runs/trigger/scheduled"
  key_links:
    - from: "ops/docker/start-app-runtime.sh"
      to: "ops/scheduler/start-supercronic.sh"
      via: "same-container scheduler bootstrap"
      pattern: "start-supercronic.sh|uvicorn|trap"
    - from: "ops/scheduler/trigger-scheduled.sh"
      to: "app/api/routes.py"
      via: "HTTP POST to the existing scheduled trigger route"
      pattern: "/runs/trigger/scheduled|X-Investor-Scheduled-Trigger"
    - from: "tests/integration/test_scheduled_submission_flow.py"
      to: "app/services/scheduling.py"
      via: "duplicate-safe ET schedule-key assertions"
      pattern: "daily:|submitted_order_count|duplicate"
---

<objective>
Restore the missing Phase 13 test harness and create the base Docker runtime assets for a repo-managed scheduler that runs inside the app container.

Purpose: The current workspace has only compiled scheduler-related test artifacts, and the revised phase shape must honor the repo rule that scheduling stays inside repo-managed artifacts without introducing a separate always-on scheduler service.
Output: Restored pytest source files for scheduling verification, plus `.dockerignore`, one app Docker image, a combined app-runtime entrypoint, and repo-owned scheduler scripts under `ops/`.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-replace-host-cron-scripts-with-a-docker-native-scheduler/13-RESEARCH.md
@.planning/phases/13-replace-host-cron-scripts-with-a-docker-native-scheduler/13-VALIDATION.md
@.planning/phases/03-scheduling-and-email-delivery/03-03-SUMMARY.md
@.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-03-SUMMARY.md
@CLAUDE.md
@docker-compose.yml
@app/api/routes.py
@app/services/scheduling.py
@app/main.py
@app/config.py
@app/ops/dry_run.py
@scripts/cron-trigger.sh

<interfaces>
From app/api/routes.py:
```python
@router.post("/runs/trigger/scheduled")
def trigger_scheduled_run(
    request: Request,
    response: Response,
    scheduled_trigger: Optional[str] = Header(default=None, alias="X-Investor-Scheduled-Trigger"),
) -> dict[str, object]:
    ...
```

From app/services/scheduling.py:
```python
def build_schedule_key(now: datetime) -> str:
    market_date = now.astimezone(NEW_YORK).date().isoformat()
    return f"daily:{market_date}"
```

From CLAUDE.md:
```text
Scheduling: Cron must be managed inside the repo as local/server installable artifacts — no separate always-on scheduler service
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recreate the missing scheduler verification test sources without docs drift work</name>
  <files>tests/conftest.py, tests/api/test_scheduling.py, tests/integration/test_scheduled_submission_flow.py, tests/ops/test_docker_scheduler.py</files>
  <read_first>app/api/routes.py, app/services/scheduling.py, app/main.py, app/config.py, app/ops/dry_run.py, docker-compose.yml, CLAUDE.md</read_first>
  <behavior>
    - Test 1: Duplicate scheduled triggers in the same ET market-day window return `{"status": "duplicate", "duplicate": true}` and preserve the original `schedule_key`.
    - Test 2: A scheduled run that reaches submission does not resubmit orders when the same-day scheduled trigger is called again.
    - Test 3: The Docker runtime contract uses only `postgres`, `migrate`, and `app`, and the `app` service is responsible for the scheduler process inside the container.
  </behavior>
  <action>Recreate the missing pytest source files needed for Phase 13 verification. In `tests/conftest.py`, add the minimum shared fixtures required to build deterministic FastAPI apps against a temporary SQLite database using the same local doubles already visible in `app/ops/dry_run.py` for mail, research, and Alpaca. In `tests/api/test_scheduling.py`, add `test_duplicate_scheduled_trigger_returns_duplicate_without_second_primary_run` that posts twice to `/runs/trigger/scheduled` with header `X-Investor-Scheduled-Trigger`, asserts the first payload reports `"status": "started"` and `"duplicate": False`, and asserts the second payload reports `"status": "duplicate"`, `"duplicate": True`, and the same `schedule_key` value in `daily:YYYY-MM-DD` format. In `tests/integration/test_scheduled_submission_flow.py`, add `test_duplicate_scheduled_trigger_does_not_resubmit_orders` that runs scheduled trigger -> approval callback -> execute, then proves a duplicate same-day scheduled trigger leaves `submitted_order_count` unchanged. In `tests/ops/test_docker_scheduler.py`, add `test_compose_stack_defines_app_managed_scheduler_runtime` and `test_scheduler_runtime_targets_existing_route` that require `docker-compose.yml` to define the exact services `postgres`, `migrate`, and `app`, require `app` to use `ops/docker/start-app-runtime.sh`, and require `ops/scheduler/crontab` to contain `CRON_TZ=America/New_York` plus `/app/ops/scheduler/trigger-scheduled.sh`. Keep these tests red until the missing Docker assets land; do not add README assertions in this plan.</action>
  <acceptance_criteria>
    - `rg -n "test_duplicate_scheduled_trigger_returns_duplicate_without_second_primary_run|test_duplicate_scheduled_trigger_does_not_resubmit_orders|test_compose_stack_defines_app_managed_scheduler_runtime|ops/docker/start-app-runtime.sh" tests` returns the new source tests.
    - `python -m pytest tests/ops/test_docker_scheduler.py tests/api/test_scheduling.py tests/integration/test_scheduled_submission_flow.py -q` fails because the Docker scheduler assets and app-managed runtime contract do not exist yet.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ops/test_docker_scheduler.py tests/api/test_scheduling.py tests/integration/test_scheduled_submission_flow.py -q</automated>
  </verify>
  <done>Phase 13 has real source tests for Docker scheduler runtime and dedupe behavior, and those tests are red for the missing app-container scheduler assets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the base app-container scheduler assets around Supercronic</name>
  <files>.dockerignore, Dockerfile, ops/docker/start-app-runtime.sh, ops/scheduler/crontab, ops/scheduler/start-supercronic.sh, ops/scheduler/trigger-scheduled.sh</files>
  <read_first>docker-compose.yml, .env.example, app/config.py, app/main.py, app/api/routes.py, tests/ops/test_docker_scheduler.py, scripts/cron-trigger.sh, CLAUDE.md</read_first>
  <behavior>
    - Test 1: The repo exposes one app image that includes `uvicorn`, `curl`, `tzdata`, `gettext-base`, and Supercronic `v0.2.43`.
    - Test 2: The app runtime entrypoint starts the scheduler bootstrap and `uvicorn` in the same container with signal handling.
    - Test 3: The trigger wrapper posts to the existing scheduled route and logs to stdout instead of file paths.
  </behavior>
  <action>Create `.dockerignore` with exact exclusions for `.git`, `.planning`, `.venv`, `.pytest_cache`, `__pycache__/`, `tests/__pycache__/`, `logs/`, and `investor.db`. Create `Dockerfile` based on `python:3.12-slim` that installs `curl`, `gettext-base`, and `tzdata`, downloads Supercronic `v0.2.43` to `/usr/local/bin/supercronic`, sets `PYTHONUNBUFFERED=1`, uses `WORKDIR /app`, copies `pyproject.toml`, `README.md`, `app/`, and `ops/`, runs `pip install --no-cache-dir -e .`, exposes port `8000`, sets `HEALTHCHECK CMD curl -fsS http://localhost:8000/health || exit 1`, and defaults to `/app/ops/docker/start-app-runtime.sh`. Create `ops/docker/start-app-runtime.sh` that starts `/app/ops/scheduler/start-supercronic.sh` in the background, starts `uvicorn app.main:app --host 0.0.0.0 --port 8000` in the foreground, traps `TERM` and `INT`, and stops both child processes cleanly. Create `ops/scheduler/crontab` with exactly two lines: `CRON_TZ=America/New_York` and `${INVESTOR_SCHEDULE_CRON_EXPRESSION:-0 7 * * 1-5} /app/ops/scheduler/trigger-scheduled.sh`. Create `ops/scheduler/start-supercronic.sh` that fails if `INVESTOR_SCHEDULED_TRIGGER_TOKEN` is empty, renders `ops/scheduler/crontab` to `/tmp/investor.crontab` with `envsubst`, prints the rendered file to stdout, and then `exec /usr/local/bin/supercronic /tmp/investor.crontab`. Create `ops/scheduler/trigger-scheduled.sh` that POSTs `{"replay":false}` to `${INVESTOR_SCHEDULE_TRIGGER_URL:-http://127.0.0.1:8000/runs/trigger/scheduled}` with header `X-Investor-Scheduled-Trigger: ${INVESTOR_SCHEDULED_TRIGGER_TOKEN}`, then prints one of the exact stdout lines `scheduled_trigger result=started`, `scheduled_trigger result=duplicate`, or `scheduled_trigger result=failure`. This combined runtime is required by `CLAUDE.md`; do not introduce a `scheduler` Compose service, Docker socket access, or host-cron assumptions.</action>
  <acceptance_criteria>
    - `rg -n "python:3.12-slim|HEALTHCHECK|curl -fsS http://localhost:8000/health|supercronic|v0.2.43|gettext-base|start-app-runtime.sh" Dockerfile ops/docker/start-app-runtime.sh` returns the expected base-image, healthcheck, Supercronic install, and combined runtime entrypoint lines.
    - `rg -n "CRON_TZ=America/New_York|INVESTOR_SCHEDULE_CRON_EXPRESSION|/app/ops/scheduler/trigger-scheduled.sh|/runs/trigger/scheduled|scheduled_trigger result=started" ops/scheduler/crontab ops/scheduler/start-supercronic.sh ops/scheduler/trigger-scheduled.sh` returns the exact scheduler contract strings.
    - `python -m pytest tests/ops/test_docker_scheduler.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ops/test_docker_scheduler.py -q</automated>
  </verify>
  <done>The repo now contains the base Docker-native scheduler assets for an app-container-managed runtime and the ops scheduler tests pass against those files.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/ops/test_docker_scheduler.py -q` after the Docker assets land. Keep docs work for Plan 03 so the red/green sequencing stays crisp.
</verification>

<success_criteria>
Phase 13 has a real execution foundation: missing scheduler-related test sources are restored, and the repo contains explicit Docker runtime assets that keep scheduling inside repo-managed app-container artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/13-replace-host-cron-scripts-with-a-docker-native-scheduler/13-replace-host-cron-scripts-with-a-docker-native-scheduler-01-SUMMARY.md`
</output>

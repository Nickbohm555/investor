---
phase: 13-replace-host-cron-scripts-with-a-docker-native-scheduler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/api/test_scheduling.py
  - tests/integration/test_scheduled_submission_flow.py
  - tests/ops/test_operational_docs.py
  - tests/ops/test_docker_scheduler.py
  - .dockerignore
  - Dockerfile
  - Dockerfile.scheduler
  - ops/scheduler/crontab
  - ops/scheduler/start-supercronic.sh
  - ops/scheduler/trigger-scheduled.sh
autonomous: true
requirements:
  - TBD-13-01
  - TBD-13-03
must_haves:
  truths:
    - "Phase 13 has runnable source tests again instead of depending on `.pyc` artifacts or undocumented behavior."
    - "The repo contains explicit container and scheduler assets for a dedicated Supercronic-based scheduler path."
    - "The scheduler contract is pinned to the existing token-guarded `/runs/trigger/scheduled` route and Compose-visible stdout logging."
  artifacts:
    - path: "tests/ops/test_docker_scheduler.py"
      provides: "Ops-level contract coverage for the Compose scheduler stack"
      contains: "test_compose_stack_defines_scheduler_runtime"
    - path: "Dockerfile.scheduler"
      provides: "Dedicated scheduler image with Supercronic"
      contains: "supercronic"
    - path: "ops/scheduler/trigger-scheduled.sh"
      provides: "Compose-network scheduler trigger wrapper"
      contains: "http://app:8000/runs/trigger/scheduled"
  key_links:
    - from: "ops/scheduler/trigger-scheduled.sh"
      to: "app/api/routes.py"
      via: "HTTP POST to the existing scheduled trigger route"
      pattern: "/runs/trigger/scheduled|X-Investor-Scheduled-Trigger"
    - from: "ops/scheduler/start-supercronic.sh"
      to: "ops/scheduler/crontab"
      via: "env-rendered Supercronic startup"
      pattern: "supercronic|INVESTOR_SCHEDULE_CRON_EXPRESSION|CRON_TZ=America/New_York"
    - from: "tests/ops/test_docker_scheduler.py"
      to: "docker-compose.yml"
      via: "assertions for service names, health gating, and scheduler command paths"
      pattern: "postgres|migrate|app|scheduler|service_healthy"
---

<objective>
Restore the missing Phase 13 test harness and create the base container assets that make a Docker-native scheduler implementation verifiable.

Purpose: The current workspace has only compiled test artifacts under `tests/`, so Phase 13 needs real source tests and explicit container files before any scheduler migration work can be trusted.
Output: Restored pytest source files for scheduling/docs verification, plus `.dockerignore`, app and scheduler Dockerfiles, and repo-owned scheduler scripts under `ops/scheduler/`.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-replace-host-cron-scripts-with-a-docker-native-scheduler/13-RESEARCH.md
@.planning/phases/13-replace-host-cron-scripts-with-a-docker-native-scheduler/13-VALIDATION.md
@.planning/phases/03-scheduling-and-email-delivery/03-03-SUMMARY.md
@.planning/phases/11-scheduling-reliability-and-end-to-end-execution-proof/11-03-SUMMARY.md
@README.md
@docker-compose.yml
@app/api/routes.py
@app/services/scheduling.py
@app/main.py
@app/config.py
@app/ops/dry_run.py
@scripts/cron-trigger.sh

<interfaces>
From app/api/routes.py:
```python
@router.post("/runs/trigger/scheduled")
def trigger_scheduled_run(
    request: Request,
    response: Response,
    scheduled_trigger: Optional[str] = Header(default=None, alias="X-Investor-Scheduled-Trigger"),
) -> dict[str, object]:
    ...
```

From app/services/scheduling.py:
```python
def build_schedule_key(now: datetime) -> str:
    market_date = now.astimezone(NEW_YORK).date().isoformat()
    return f"daily:{market_date}"
```

From app/config.py:
```python
schedule_cron_expression: str = "0 7 * * 1-5"
schedule_timezone: str = "America/New_York"
schedule_trigger_url: str = "http://127.0.0.1:8000/runs/trigger/scheduled"
scheduled_trigger_token: str = "change-me-scheduled-trigger"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recreate the missing scheduler verification test sources and leave the repo red on purpose</name>
  <files>tests/conftest.py, tests/api/test_scheduling.py, tests/integration/test_scheduled_submission_flow.py, tests/ops/test_operational_docs.py, tests/ops/test_docker_scheduler.py</files>
  <read_first>app/api/routes.py, app/services/scheduling.py, app/main.py, app/config.py, app/ops/dry_run.py, README.md, docker-compose.yml</read_first>
  <behavior>
    - Test 1: Duplicate scheduled triggers in the same ET market-day window return `{"status": "duplicate", "duplicate": true}` and preserve the original `schedule_key`.
    - Test 2: A scheduled run that reaches submission does not resubmit orders when the same-day scheduled trigger is called again.
    - Test 3: The docs contract moves to Compose commands and requires `postgres`, `migrate`, `app`, and `scheduler` in `docker-compose.yml`.
  </behavior>
  <action>Recreate the missing pytest source files needed for Phase 13 verification. In `tests/conftest.py`, add the minimum shared fixtures required to build deterministic FastAPI apps against a temporary SQLite database using the same local doubles already visible in `app/ops/dry_run.py` for mail, research, and Alpaca. In `tests/api/test_scheduling.py`, add `test_duplicate_scheduled_trigger_returns_duplicate_without_second_primary_run` that posts twice to `/runs/trigger/scheduled` with header `X-Investor-Scheduled-Trigger`, asserts the first payload reports `"status": "started"` and `"duplicate": False`, and asserts the second payload reports `"status": "duplicate"`, `"duplicate": True`, and the same `schedule_key` value in `daily:YYYY-MM-DD` format. In `tests/integration/test_scheduled_submission_flow.py`, add `test_duplicate_scheduled_trigger_does_not_resubmit_orders` that runs scheduled trigger -> approval callback -> execute, then proves a duplicate same-day scheduled trigger leaves `submitted_order_count` unchanged. In `tests/ops/test_operational_docs.py`, add assertions that `README.md` contains the exact Compose commands `docker compose up -d --build` and `docker compose logs -f migrate app scheduler`, and add negative assertions that the README primary operator path does not rely on `./scripts/cron-install.sh` or `./scripts/cron-status.sh`. In `tests/ops/test_docker_scheduler.py`, add `test_compose_stack_defines_scheduler_runtime` and `test_scheduler_crontab_targets_existing_route` that require `docker-compose.yml` to define the exact services `postgres`, `migrate`, `app`, and `scheduler`, require the scheduler to depend on `app` with `condition: service_healthy`, and require `ops/scheduler/crontab` to contain `CRON_TZ=America/New_York` plus `/app/ops/scheduler/trigger-scheduled.sh`. Keep these tests red until the missing assets land; do not paper over the failures with skipped tests.</action>
  <acceptance_criteria>
    - `rg -n "test_duplicate_scheduled_trigger_returns_duplicate_without_second_primary_run|test_duplicate_scheduled_trigger_does_not_resubmit_orders|docker compose up -d --build|docker compose logs -f migrate app scheduler|test_compose_stack_defines_scheduler_runtime" tests` returns the new source tests.
    - `python -m pytest tests/ops/test_docker_scheduler.py tests/api/test_scheduling.py tests/integration/test_scheduled_submission_flow.py tests/ops/test_operational_docs.py -q` fails because the Docker scheduler assets and Compose docs contract do not exist yet.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ops/test_docker_scheduler.py tests/api/test_scheduling.py tests/integration/test_scheduled_submission_flow.py tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>Phase 13 has real source tests for scheduler runtime, dedupe, and docs behavior, and those tests are red for the missing Docker-native scheduler assets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the base app and scheduler container assets around Supercronic</name>
  <files>.dockerignore, Dockerfile, Dockerfile.scheduler, ops/scheduler/crontab, ops/scheduler/start-supercronic.sh, ops/scheduler/trigger-scheduled.sh</files>
  <read_first>docker-compose.yml, .env.example, app/config.py, app/main.py, app/api/routes.py, tests/ops/test_docker_scheduler.py, scripts/cron-trigger.sh</read_first>
  <behavior>
    - Test 1: The repo exposes a dedicated scheduler image that includes Supercronic `v0.2.43`.
    - Test 2: The scheduler startup script renders the repo-owned crontab and executes Supercronic against it.
    - Test 3: The trigger wrapper posts to the existing scheduled route on the Compose network and logs to stdout instead of file paths.
  </behavior>
  <action>Create `.dockerignore` with exact exclusions for `.git`, `.planning`, `.venv`, `.pytest_cache`, `__pycache__/`, `tests/__pycache__/`, `logs/`, and `investor.db`. Create `Dockerfile` based on `python:3.12-slim` that installs `curl` and `tzdata`, sets `PYTHONUNBUFFERED=1`, uses `WORKDIR /app`, copies `pyproject.toml`, `README.md`, and `app/`, runs `pip install --no-cache-dir -e .`, exposes port `8000`, sets `HEALTHCHECK CMD curl -fsS http://localhost:8000/health || exit 1`, and defaults to `uvicorn app.main:app --host 0.0.0.0 --port 8000`. Create `Dockerfile.scheduler` based on `python:3.12-slim` that installs `curl`, `gettext-base`, and `tzdata`, downloads Supercronic `v0.2.43` from GitHub releases to `/usr/local/bin/supercronic`, copies `ops/scheduler/`, makes the scripts executable, and uses `/app/ops/scheduler/start-supercronic.sh` as the entrypoint. Create `ops/scheduler/crontab` with exactly two lines: `CRON_TZ=America/New_York` and `${INVESTOR_SCHEDULE_CRON_EXPRESSION:-0 7 * * 1-5} /app/ops/scheduler/trigger-scheduled.sh`. Create `ops/scheduler/start-supercronic.sh` that fails if `INVESTOR_SCHEDULED_TRIGGER_TOKEN` is empty, renders `ops/scheduler/crontab` to `/tmp/investor.crontab` with `envsubst`, prints the rendered file to stdout, and then `exec /usr/local/bin/supercronic /tmp/investor.crontab`. Create `ops/scheduler/trigger-scheduled.sh` that POSTs `{"replay":false}` to `http://app:8000/runs/trigger/scheduled` with header `X-Investor-Scheduled-Trigger: ${INVESTOR_SCHEDULED_TRIGGER_TOKEN}`, then prints one of the exact stdout lines `scheduled_trigger result=started`, `scheduled_trigger result=duplicate`, or `scheduled_trigger result=failure`. Do not reuse `logs/cron/daily-trigger.log`, `crontab -l`, or host-only path assumptions in these assets.</action>
  <acceptance_criteria>
    - `rg -n "python:3.12-slim|HEALTHCHECK|curl -fsS http://localhost:8000/health|supercronic|v0.2.43|gettext-base" Dockerfile Dockerfile.scheduler` returns the expected base-image, healthcheck, and Supercronic install lines.
    - `rg -n "CRON_TZ=America/New_York|INVESTOR_SCHEDULE_CRON_EXPRESSION|/app/ops/scheduler/trigger-scheduled.sh|http://app:8000/runs/trigger/scheduled|scheduled_trigger result=started" ops/scheduler/crontab ops/scheduler/start-supercronic.sh ops/scheduler/trigger-scheduled.sh` returns the exact scheduler contract strings.
    - `python -m pytest tests/ops/test_docker_scheduler.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ops/test_docker_scheduler.py -q</automated>
  </verify>
  <done>The repo now contains the base Docker-native scheduler assets and the ops scheduler tests pass against those concrete files.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/ops/test_docker_scheduler.py -q` after the container assets land. Keep the broader Phase 13 tests red until later plans wire Compose and docs fully.
</verification>

<success_criteria>
Phase 13 has a real execution foundation: missing scheduler-related test sources are restored, and the repo contains explicit app and scheduler container assets built around Supercronic and the existing scheduled route.
</success_criteria>

<output>
After completion, create `.planning/phases/13-replace-host-cron-scripts-with-a-docker-native-scheduler/13-replace-host-cron-scripts-with-a-docker-native-scheduler-01-SUMMARY.md`
</output>

---
phase: 20-direct-post-triggered-research-to-email-run-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/routes.py
  - tests/api/test_manual_trigger.py
  - tests/integration/test_manual_trigger_email_flow.py
autonomous: true
requirements:
  - PH20-01
must_haves:
  truths:
    - "A direct POST to /runs/trigger returns 202 with a run_id when the manual workflow starts successfully."
    - "The persisted run created by the manual route records trigger_source=manual and reaches awaiting_review after the memo is sent."
    - "If workflow startup fails after run creation, the API response still identifies the created run_id so the operator can inspect the partial run."
  artifacts:
    - path: "app/api/routes.py"
      provides: "Manual trigger route with explicit success and failure response semantics"
      min_lines: 20
    - path: "tests/api/test_manual_trigger.py"
      provides: "Route-level regression coverage for POST /runs/trigger"
      min_lines: 40
    - path: "tests/integration/test_manual_trigger_email_flow.py"
      provides: "Manual trigger to persisted run/email integration proof with doubles"
      min_lines: 40
  key_links:
    - from: "app/api/routes.py"
      to: "app/workflows/engine.py"
      via: "runtime.workflow_engine.start_run(...)"
      pattern: "start_run\\("
    - from: "app/api/routes.py"
      to: "app/services/run_service.py"
      via: "runtime.run_service.create_pending_run(...)"
      pattern: "create_pending_run\\("
---

<objective>
Define and lock the manual POST route contract that Phase 20 depends on.

Purpose: Reuse the existing `/runs/trigger` path per D-01, D-02, and D-03 instead of building a second orchestration seam.
Output: Manual-route regression tests plus route-level observability and failure semantics that expose the created `run_id`.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-direct-post-triggered-research-to-email-run-path/20-CONTEXT.md
@.planning/phases/20-direct-post-triggered-research-to-email-run-path/20-RESEARCH.md
@.planning/phases/20-direct-post-triggered-research-to-email-run-path/20-VALIDATION.md
@app/api/routes.py
@app/workflows/engine.py
@app/graph/workflow.py
@app/services/run_service.py
@tests/conftest.py
@tests/integration/test_scheduled_submission_flow.py

<interfaces>
From `app/api/routes.py`:

```python
@router.post("/runs/trigger", status_code=202)
def trigger_run(request: Request) -> dict[str, str]:
```

From `app/workflows/engine.py`:

```python
def start_run(self, *, run_id: str, quiver_client: object, baseline_report: dict | None = None) -> dict:
```

From `app/graph/workflow.py`:

```python
self._mail_provider.send(
    subject=email.subject,
    text_body=email.body,
    html_body=email.html_body,
    recipient=self._settings.daily_memo_to_email,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create red tests for the direct manual trigger contract</name>
  <files>tests/api/test_manual_trigger.py, tests/integration/test_manual_trigger_email_flow.py</files>
  <read_first>tests/conftest.py, app/api/routes.py, app/workflows/engine.py, app/graph/workflow.py, tests/integration/test_scheduled_submission_flow.py</read_first>
  <acceptance_criteria>
    - `tests/api/test_manual_trigger.py` contains `client.post("/runs/trigger")`.
    - `tests/api/test_manual_trigger.py` asserts `response.status_code == 202`.
    - `tests/api/test_manual_trigger.py` asserts `payload["status"] == "started"`.
    - `tests/api/test_manual_trigger.py` asserts `stored_run.trigger_source == "manual"`.
    - `tests/api/test_manual_trigger.py` asserts a 500 payload includes `detail["run_id"]` for workflow-start failures.
    - `tests/integration/test_manual_trigger_email_flow.py` asserts exactly one memo is sent after one manual POST.
    - `tests/integration/test_manual_trigger_email_flow.py` asserts persisted run `status == "awaiting_review"` and `current_step == "awaiting_review"`.
  </acceptance_criteria>
  <action>Write failing tests for PH20-01 per D-01 and D-03. In `tests/api/test_manual_trigger.py`, cover two cases: (1) `POST /runs/trigger` returns HTTP 202 with JSON keys `status` and `run_id`, and the stored `RunRecord` has `trigger_source="manual"`; (2) when `runtime.workflow_engine.start_run(...)` raises `RuntimeError("manual-start failed")` after `create_pending_run(...)`, the response is HTTP 500 and `response.json()["detail"]["run_id"]` equals the created run id. In `tests/integration/test_manual_trigger_email_flow.py`, reuse the existing `app_with_runtime` fixture pattern to prove one direct `POST /runs/trigger` produces one sent memo, stores the persisted workflow payload, and leaves the run at `awaiting_review` without going through the scheduled route.</action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/api/test_manual_trigger.py tests/integration/test_manual_trigger_email_flow.py -q</automated>
  </verify>
  <done>Both new test files fail first for the missing manual-route contract and describe the exact success and failure semantics the implementation must satisfy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement manual route observability and failure response details</name>
  <files>app/api/routes.py, tests/api/test_manual_trigger.py, tests/integration/test_manual_trigger_email_flow.py</files>
  <read_first>app/api/routes.py, app/services/run_service.py, app/workflows/engine.py, tests/api/test_manual_trigger.py, tests/integration/test_manual_trigger_email_flow.py</read_first>
  <behavior>
    - Test 1: Successful manual POST returns `{"status": "started", "run_id": "<run-id>"}` with HTTP 202 and persists `trigger_source="manual"`.
    - Test 2: Manual workflow startup failure returns HTTP 500 with `detail["message"] == "Manual trigger failed"` and `detail["run_id"]` set to the created run id.
    - Test 3: Successful manual POST sends one memo and persists `status="awaiting_review"` plus `current_step="awaiting_review"`.
  </behavior>
  <acceptance_criteria>
    - `app/api/routes.py` logs `manual_trigger result=started run_id=%s`.
    - `app/api/routes.py` logs `manual_trigger result=failure run_id=%s`.
    - `app/api/routes.py` raises `HTTPException(status_code=500, detail={"message": "Manual trigger failed", "run_id": run_id})` on workflow startup failure.
    - `PYTHONPATH=. pytest tests/api/test_manual_trigger.py tests/integration/test_manual_trigger_email_flow.py -q` passes.
  </acceptance_criteria>
  <action>Update `trigger_run(...)` in `app/api/routes.py` without introducing a new route per D-01 and D-02. Keep `run_id = f"run-{uuid4().hex[:8]}"`, `trigger_source="manual"`, and the existing `baseline_report` lookup. Wrap `runtime.workflow_engine.start_run(...)` and `runtime.run_service.store_recommendations(...)` in `try/except Exception as exc:`. On success, log `manual_trigger result=started run_id=%s` and return the existing HTTP 202 payload `{"status": "started", "run_id": run_id}`. On failure, log `manual_trigger result=failure run_id=%s`, preserve the created run record, and raise `HTTPException(status_code=500, detail={"message": "Manual trigger failed", "run_id": run_id})` so operators and the Phase 20 helper can inspect the partial run directly.</action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/api/test_manual_trigger.py tests/integration/test_manual_trigger_email_flow.py -q</automated>
  </verify>
  <done>`POST /runs/trigger` is now a tested, observable manual entrypoint that either reaches awaiting review or returns a failure payload containing the created run id.</done>
</task>

</tasks>

<verification>
Run `PYTHONPATH=. pytest tests/api/test_manual_trigger.py tests/integration/test_manual_trigger_email_flow.py -q` and confirm both the success path and failure-with-run-id contract pass.
</verification>

<success_criteria>
- Direct manual POST flow is covered by dedicated route and integration tests.
- The manual route keeps the existing workflow engine path and does not reuse scheduled headers or scheduled helpers.
- Failure responses expose the created `run_id` for follow-up inspection instead of hiding partial-run state.
</success_criteria>

<output>
After completion, create `.planning/phases/20-direct-post-triggered-research-to-email-run-path/20-direct-post-triggered-research-to-email-run-path-01-SUMMARY.md`
</output>

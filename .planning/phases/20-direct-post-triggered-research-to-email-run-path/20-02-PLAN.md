---
phase: 20-direct-post-triggered-research-to-email-run-path
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - app/ops/live_proof.py
  - tests/ops/test_live_proof.py
  - tests/ops/test_manual_post_proof.py
autonomous: true
requirements:
  - PH20-02
must_haves:
  truths:
    - "The operator can run one repo-owned CLI surface that checks direct-run prerequisites in operator order before attempting a live POST."
    - "The proof helper can trigger the direct manual route and report the HTTP status plus returned run metadata."
    - "When live prerequisites are missing, the preflight output names the first blocking failure instead of failing later inside Quiver, LLM, or SMTP calls."
  artifacts:
    - path: "app/ops/live_proof.py"
      provides: "Manual-post preflight and direct-trigger helper commands"
      min_lines: 80
    - path: "tests/ops/test_manual_post_proof.py"
      provides: "Dedicated regression coverage for the manual-proof helper"
      min_lines: 40
    - path: "app/config.py"
      provides: "Direct manual trigger URL setting for the Phase 20 helper"
      contains: "manual_trigger_url"
  key_links:
    - from: "app/ops/live_proof.py"
      to: "app.ops.readiness.collect_readiness_errors"
      via: "manual preflight stage ordering"
      pattern: "collect_readiness_errors"
    - from: "app/ops/live_proof.py"
      to: "/runs/trigger"
      via: "httpx.post(settings.manual_trigger_url"
      pattern: "manual_trigger_url"
---

<objective>
Extend the repo-owned live-proof CLI so it can preflight and trigger the direct manual POST path.

Purpose: Deliver D-04, D-05, D-07, D-08, D-11, and D-12 through one existing operator surface instead of a second helper stack.
Output: A staged preflight payload plus a `trigger-manual` command that preserves the direct route response body.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-direct-post-triggered-research-to-email-run-path/20-CONTEXT.md
@.planning/phases/20-direct-post-triggered-research-to-email-run-path/20-RESEARCH.md
@.planning/phases/20-direct-post-triggered-research-to-email-run-path/20-VALIDATION.md
@app/ops/live_proof.py
@app/ops/readiness.py
@app/config.py
@tests/ops/test_live_proof.py

<interfaces>
From `app/config.py`:

```python
schedule_trigger_url: str = "http://127.0.0.1:8000/runs/trigger/scheduled"
external_base_url: str = "https://investor.example.com"
```

From `app/ops/live_proof.py`:

```python
subparsers.add_parser("preflight")
subparsers.add_parser("trigger-scheduled")
inspect_run = subparsers.add_parser("inspect-run")
```

From `app/ops/readiness.py`:

```python
def collect_readiness_errors(settings: Settings) -> list[str]:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create red tests for staged manual-post preflight and trigger helper behavior</name>
  <files>tests/ops/test_live_proof.py, tests/ops/test_manual_post_proof.py</files>
  <read_first>tests/ops/test_live_proof.py, app/ops/live_proof.py, app/ops/readiness.py, app/config.py</read_first>
  <acceptance_criteria>
    - `tests/ops/test_manual_post_proof.py` contains `trigger-manual`.
    - `tests/ops/test_manual_post_proof.py` asserts the helper posts to `/runs/trigger`.
    - `tests/ops/test_manual_post_proof.py` asserts `result["status_code"] == 202` on success.
    - `tests/ops/test_manual_post_proof.py` asserts `result["payload"]["run_id"]` is preserved from the route response.
    - `tests/ops/test_live_proof.py` or `tests/ops/test_manual_post_proof.py` asserts `blocking_failures[0]` is the exact readiness error string for a placeholder env, not a downstream HTTP failure.
    - `tests/ops/test_manual_post_proof.py` asserts Quiver and LLM probes are skipped when readiness errors already exist.
  </acceptance_criteria>
  <action>Create failing ops tests for PH20-02. Add `tests/ops/test_manual_post_proof.py` for the new direct-run helper contract. Cover: (1) parser includes a `trigger-manual` subcommand; (2) `_trigger_manual(settings)` posts to `settings.manual_trigger_url`, returns a dict with keys `status_code`, `ok`, and `payload`, and preserves the manual route JSON body on both 202 and 500 responses; (3) manual preflight reports the first blocking failure as the exact readiness error string such as `INVESTOR_QUIVER_BASE_URL must not equal https://example.test`; (4) when readiness errors exist, Quiver and LLM calls are skipped rather than attempted. Update `tests/ops/test_live_proof.py` only where the shared `preflight` payload changes must stay backward-compatible with the existing Phase 19 assertions.</action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/ops/test_live_proof.py tests/ops/test_manual_post_proof.py -q</automated>
  </verify>
  <done>The new ops tests fail first and describe an exact staged-preflight payload plus the direct manual trigger helper response shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement staged preflight ordering and a direct manual trigger command</name>
  <files>app/config.py, app/ops/live_proof.py, tests/ops/test_live_proof.py, tests/ops/test_manual_post_proof.py</files>
  <read_first>app/config.py, app/ops/live_proof.py, app/ops/readiness.py, tests/ops/test_live_proof.py, tests/ops/test_manual_post_proof.py</read_first>
  <behavior>
    - Test 1: `preflight` reports readiness-derived blocking failures before any Quiver or LLM network probe is attempted.
    - Test 2: `trigger-manual` posts to `http://127.0.0.1:8000/runs/trigger` by default and returns the route body plus HTTP status.
    - Test 3: Approval reachability remains warning-only while the manual proof still surfaces the truthful rendered host.
  </behavior>
  <acceptance_criteria>
    - `app/config.py` contains `manual_trigger_url: str = "http://127.0.0.1:8000/runs/trigger"`.
    - `app/ops/live_proof.py` imports `collect_readiness_errors`.
    - `app/ops/live_proof.py` defines a `trigger-manual` subcommand.
    - `app/ops/live_proof.py` returns `first_blocking_failure` in the preflight payload.
    - `app/ops/live_proof.py` keeps `smtp_ready`, `approval_reachability_ready`, `blocking_failures`, and `warnings` keys in the payload.
    - `PYTHONPATH=. pytest tests/ops/test_live_proof.py tests/ops/test_manual_post_proof.py -q` passes.
  </acceptance_criteria>
  <action>Extend the existing CLI in `app/ops/live_proof.py` instead of creating a second helper surface, per D-02 and the research recommendation. In `app/config.py`, add `manual_trigger_url: str = "http://127.0.0.1:8000/runs/trigger"`. In `app/ops/live_proof.py`, import `collect_readiness_errors` from `app.ops.readiness`. Update `_run_preflight(settings)` to execute fixed stages in this order: `readiness`, `smtp`, `quiver`, `llm`, `approval_reachability`. If `collect_readiness_errors(settings)` returns any items, set `blocking_failures` to that exact list, set `first_blocking_failure` to the first string, preserve `external_base_url`, keep `approval_reachability_ready` and `warnings`, and skip Quiver plus LLM probes entirely. If readiness passes, run SMTP inspection before Quiver and LLM. Preserve the existing keys `smtp_check`, `smtp_ready`, `quiver_checks`, `llm_check`, `reachability_check`, `blocking_failures`, and `warnings`; add `manual_trigger_url` and `first_blocking_failure` without removing the older keys so Phase 19 coverage stays intact. Add `_trigger_manual(settings)` that performs `httpx.post(settings.manual_trigger_url, timeout=30)` with no special headers, decodes JSON when present, and returns `{"status_code": response.status_code, "ok": response.is_success, "payload": decoded_payload}`. Add `trigger-manual` to the argparse subcommands and route `main(...)` to the new helper.</action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/ops/test_live_proof.py tests/ops/test_manual_post_proof.py -q</automated>
  </verify>
  <done>The live-proof CLI now supports a truthful direct manual POST workflow: staged preflight first, one manual trigger command second, and persisted run inspection afterward.</done>
</task>

</tasks>

<verification>
Run `PYTHONPATH=. pytest tests/ops/test_live_proof.py tests/ops/test_manual_post_proof.py -q` and confirm the CLI exposes `trigger-manual`, ordered blocking failures, and backward-compatible preflight keys.
</verification>

<success_criteria>
- One repo-owned CLI can preflight the manual POST path, trigger it, and inspect the resulting run.
- Missing env values are surfaced as exact readiness strings before any Quiver or LLM call is attempted.
- The helper preserves manual route response bodies so a failure response still exposes `run_id` to the operator.
</success_criteria>

<output>
After completion, create `.planning/phases/20-direct-post-triggered-research-to-email-run-path/20-direct-post-triggered-research-to-email-run-path-02-SUMMARY.md`
</output>

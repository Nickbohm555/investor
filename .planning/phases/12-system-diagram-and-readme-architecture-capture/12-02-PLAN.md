---
phase: 12-system-diagram-and-readme-architecture-capture
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - docs/architecture/system-diagram.png
  - README.md
  - tests/ops/test_operational_docs.py
autonomous: false
requirements:
  - ARCH-03
must_haves:
  truths:
    - "README shows the architecture diagram from the repo entry point instead of burying architecture details in deeper docs."
    - "The exported screenshot remains legible as a static image and matches the repo-owned Excalidraw source."
    - "Docs tests fail if the architecture section or diagram asset references disappear."
  artifacts:
    - path: "docs/architecture/system-diagram.png"
      provides: "README-facing screenshot export of the Excalidraw system diagram"
      contains: "PNG"
    - path: "README.md"
      provides: "System Architecture section with embedded diagram and truthful orientation text"
      contains: "## System Architecture"
    - path: "tests/ops/test_operational_docs.py"
      provides: "Drift coverage for architecture section and diagram asset references"
      contains: "system-diagram.png"
  key_links:
    - from: "docs/architecture/system-diagram.excalidraw"
      to: "docs/architecture/system-diagram.png"
      via: "PNG export of the same source asset"
      pattern: "system-diagram"
    - from: "README.md"
      to: "docs/architecture/system-diagram.png"
      via: "relative markdown image reference in the architecture section"
      pattern: "docs/architecture/system-diagram.png"
    - from: "tests/ops/test_operational_docs.py"
      to: "README.md"
      via: "assertions for section heading, image path, and live-runtime wording"
      pattern: "System Architecture|broker_prestaged|system-diagram.png"
---

<objective>
Export the README-facing architecture screenshot, wire it into `README.md`, and add drift guards so the diagram remains visible and truthful from the repo entry point.

Purpose: The diagram only improves operator orientation if the screenshot is easy to find immediately and the docs test suite prevents future removal or stale wording.
Output: A checked-in PNG export, a README architecture section, pytest coverage for asset and section drift, and one final human legibility check.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-system-diagram-and-readme-architecture-capture/12-RESEARCH.md
@.planning/phases/12-system-diagram-and-readme-architecture-capture/12-VALIDATION.md
@docs/architecture/system-diagram-contract.md
@docs/architecture/system-diagram.excalidraw
@README.md
@tests/ops/test_operational_docs.py
@app/api/routes.py
@app/workflows/engine.py
@app/ops/dry_run.py

<interfaces>
From tests/ops/test_operational_docs.py:
```python
README_COMMANDS = [
    "python -m app.ops.dry_run",
    "./scripts/cron-install.sh",
]

README_CHECKLIST_LINES = [
    "## Dry Run",
    "## Go-Live Checklist",
    "## Acceptance Verification",
]
```

From README.md:
```markdown
# investor

## Setup
...
## Dry Run
...
```

From docs/architecture/system-diagram-contract.md:
```text
Operator
Automation
Application Runtime
External Services
Storage
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing docs drift checks for the architecture section and diagram assets</name>
  <files>tests/ops/test_operational_docs.py, README.md, docs/architecture/system-diagram.png, docs/architecture/system-diagram.excalidraw</files>
  <read_first>tests/ops/test_operational_docs.py, README.md, docs/architecture/system-diagram-contract.md, docs/architecture/system-diagram.excalidraw</read_first>
  <behavior>
    - Test 1: README contains a `## System Architecture` section with a relative reference to `docs/architecture/system-diagram.png`.
    - Test 2: Both the editable `.excalidraw` source and the exported `.png` screenshot must exist in `docs/architecture/`.
    - Test 3: README architecture copy states that the current runtime ends at `broker_prestaged`, not direct order submission.
  </behavior>
  <action>Extend `tests/ops/test_operational_docs.py` with new failing assertions before implementing the README or PNG changes. Add a test that requires `README.md` to contain `## System Architecture`, the relative image path `docs/architecture/system-diagram.png`, and a truthful orientation line stating the implemented approval path ends at `broker_prestaged`. Add a second test that requires both `docs/architecture/system-diagram.excalidraw` and `docs/architecture/system-diagram.png` to exist. Do not add the PNG export or README section in this task; leave the repo red on purpose so the next task proves the integration.</action>
  <acceptance_criteria>
    - `rg -n "System Architecture|system-diagram.png|broker_prestaged|system-diagram.excalidraw" tests/ops/test_operational_docs.py` returns the new architecture assertions.
    - `python -m pytest tests/ops/test_operational_docs.py -q` fails before implementation because the README section and PNG export do not exist yet.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>Docs drift coverage exists for the README architecture section and both diagram asset files, and it fails before the assets are added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export the README screenshot and integrate the truthful architecture section</name>
  <files>docs/architecture/system-diagram.png, README.md, tests/ops/test_operational_docs.py</files>
  <read_first>docs/architecture/system-diagram-contract.md, docs/architecture/system-diagram.excalidraw, README.md, tests/ops/test_operational_docs.py</read_first>
  <behavior>
    - Test 1: The exported PNG exists beside the Excalidraw source and is the screenshot used by README.
    - Test 2: README shows the diagram near the top with short orientation copy about triggers, approval, storage, and the `broker_prestaged` boundary.
    - Test 3: The updated docs test passes once the PNG and README references are in place.
  </behavior>
  <action>Export `docs/architecture/system-diagram.excalidraw` to `docs/architecture/system-diagram.png` using the same diagram source from Plan 12-01; do not create a second hand-maintained image. Update `README.md` near the top to add a `## System Architecture` section that embeds the PNG with a relative markdown image reference and 1-2 short orientation paragraphs. The copy must stay truthful to the live runtime: show manual and scheduled triggers, memo/approval flow, durable records, and operator proof paths, while explicitly stating that the current approved path ends at `broker_prestaged` rather than direct order submission. Keep the section concise and operator-facing. Update any supporting constants in `tests/ops/test_operational_docs.py` so the architecture section is part of the normal doc drift suite.</action>
  <acceptance_criteria>
    - `test -f docs/architecture/system-diagram.png` succeeds.
    - `rg -n "## System Architecture|!\\[.*\\]\\(docs/architecture/system-diagram.png\\)|broker_prestaged|python -m app.ops.dry_run" README.md` returns the section heading, image embed, truthful boundary, and proof-path language.
    - `python -m pytest tests/ops/test_operational_docs.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>The README now exposes the architecture diagram from the repo entry point, the screenshot comes from the checked-in Excalidraw source, and docs drift coverage passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manually verify README screenshot legibility and truthful runtime scope</name>
  <action>Preview the rendered README after the PNG export lands and confirm the diagram remains readable at normal markdown scale, with distinct lane spacing and a truthful `broker_prestaged` terminal boundary. If the screenshot is dense, overlapping, or implies direct order submission, send the execution back to adjust the Excalidraw layout or README copy before closing the plan.</action>
  <what-built>README-visible architecture screenshot and truthful architecture section wired to the repo-owned Excalidraw source</what-built>
  <how-to-verify>
    1. Open `README.md` in a Markdown preview that renders local images.
    2. Confirm the architecture image is visible without scrolling deep into the file and that the diagram text is readable at normal preview scale.
    3. Check that the layout feels deliberately spaced: lane headers are distinct, boxes are not overlapping, and the screenshot does not look like a dense center blob.
    4. Confirm the rendered relationships are all visible and truthful: manual trigger and scheduled trigger flow into the FastAPI app, the approval callback flows into the approval path, runtime connects to Quiver, the OpenAI-compatible LLM, and SMTP memo delivery, runtime connects to durable records, and the approved path ends at `broker_prestaged` with broker artifacts and Alpaca shown as the current-state touchpoints.
    5. Confirm the copy and diagram do not present direct order submission or LangGraph as current behavior.
  </how-to-verify>
  <verify>
    <automated>python -m pytest tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>The automated docs checks are green and a human has approved the screenshot as legible, intentionally spaced, and truthful to the current runtime boundary.</done>
  <resume-signal>Type "approved" if the screenshot is legible, or describe the specific readability issue to fix.</resume-signal>
</task>

</tasks>

<verification>
Run `python -m pytest tests/ops/test_operational_docs.py -q`. Then preview `README.md` and verify the diagram is legible at normal markdown scale, shows the required trigger, approval, runtime, external-service, and storage relationships, and the architecture text ends at `broker_prestaged`.
</verification>

<success_criteria>
Phase 12 makes the architecture visible from the repo entry point: the README embeds a legible system-diagram screenshot derived from the repo-owned Excalidraw source, and the docs tests prevent the section or assets from drifting away.
</success_criteria>

<output>
After completion, create `.planning/phases/12-system-diagram-and-readme-architecture-capture/12-system-diagram-and-readme-architecture-capture-02-SUMMARY.md`
</output>

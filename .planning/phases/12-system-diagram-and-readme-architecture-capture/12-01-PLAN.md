---
phase: 12-system-diagram-and-readme-architecture-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/system-diagram-contract.md
  - docs/architecture/system-diagram.excalidraw
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
must_haves:
  truths:
    - "The repo owns an editable architecture-diagram source that matches the implemented runtime from trigger through `broker_prestaged`."
    - "Operator touchpoints, automation entrypoints, application/runtime components, external services, and durable storage are all visible in one diagram."
    - "The diagram layout uses deliberate lane spacing and grouping instead of a dense central cluster."
  artifacts:
    - path: "docs/architecture/system-diagram-contract.md"
      provides: "Truthful component inventory, lane layout, proof-path callouts, and scope boundary for the architecture diagram"
      contains: "broker_prestaged"
    - path: "docs/architecture/system-diagram.excalidraw"
      provides: "Editable system diagram source with grouped lanes and labeled runtime relationships"
      contains: "WorkflowEngine"
  key_links:
    - from: "app/api/routes.py"
      to: "docs/architecture/system-diagram-contract.md"
      via: "route inventory for manual trigger, scheduled trigger, and approval callback"
      pattern: "/runs/trigger|/runs/trigger/scheduled|/approval/\\{token\\}"
    - from: "app/workflows/engine.py"
      to: "docs/architecture/system-diagram.excalidraw"
      via: "approved workflow terminal state label"
      pattern: "broker_prestaged"
    - from: "docs/architecture/system-diagram-contract.md"
      to: "docs/architecture/system-diagram.excalidraw"
      via: "shared lane headers and component labels"
      pattern: "Operator|Automation|Application Runtime|External Services|Storage"
---

<objective>
Create the source-of-truth architecture contract for Phase 12 and turn it into a repo-owned Excalidraw diagram that reflects the live runtime, not the stale LangGraph-era design.

Purpose: The README screenshot in the next plan is only trustworthy if the editable source first locks the implemented component list, lane layout, and runtime boundary at `broker_prestaged`.
Output: A diagram contract markdown file plus an editable Excalidraw system diagram under `docs/architecture/`.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-system-diagram-and-readme-architecture-capture/12-RESEARCH.md
@.planning/phases/12-system-diagram-and-readme-architecture-capture/12-VALIDATION.md
@.planning/phases/03-scheduling-and-email-delivery/03-03-SUMMARY.md
@.planning/phases/05-operational-readiness/05-operational-readiness-02-SUMMARY.md
@README.md
@app/main.py
@app/api/routes.py
@app/workflows/engine.py
@app/graph/workflow.py
@app/db/models.py
@app/ops/dry_run.py
@scripts/cron-install.sh
@scripts/cron-trigger.sh
@tests/ops/test_operational_docs.py
@tests/ops/test_dry_run.py

<interfaces>
From app/api/routes.py:
```python
@router.post("/runs/trigger", status_code=202)
def trigger_run(request: Request) -> dict[str, str]:
    ...

@router.post("/runs/trigger/scheduled")
def trigger_scheduled_run(...) -> dict[str, object]:
    ...

@router.get("/approval/{token}")
def review_token(token: str, request: Request) -> dict:
    ...
```

From app/workflows/engine.py:
```python
result = WorkflowResult(
    status="broker_prestaged",
    current_step="broker_prestaged",
    state_payload={...},
)
```

From app/db/models.py:
```python
class RunRecord(Base): ...
class RecommendationRecord(Base): ...
class ApprovalEventRecord(Base): ...
class StateTransitionRecord(Base): ...
class BrokerArtifactRecord(Base): ...
```

From app/ops/dry_run.py:
```python
DRY_RUN_COMMAND = "python -m app.ops.dry_run"
assert payload["approval_status"] == "broker_prestaged"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the live architecture inventory and layout contract</name>
  <files>docs/architecture/system-diagram-contract.md</files>
  <read_first>.planning/phases/12-system-diagram-and-readme-architecture-capture/12-RESEARCH.md, README.md, app/main.py, app/api/routes.py, app/workflows/engine.py, app/graph/workflow.py, app/db/models.py, app/ops/dry_run.py, scripts/cron-install.sh, scripts/cron-trigger.sh, tests/ops/test_operational_docs.py, tests/ops/test_dry_run.py</read_first>
  <acceptance_criteria>
    - `rg -n "Operator|Automation|Application Runtime|External Services|Storage" docs/architecture/system-diagram-contract.md` returns the five lane headers in order.
    - `rg -n "/runs/trigger|/runs/trigger/scheduled|/approval/\\{token\\}|WorkflowEngine|ResearchNode|compose_report_email|BrokerPrestageService|broker_prestaged" docs/architecture/system-diagram-contract.md` returns the required runtime boxes and scope boundary.
    - `rg -n "python -m app.ops.dry_run|./scripts/cron-install.sh|./scripts/cron-trigger.sh|Quiver|OpenAI-compatible LLM|SMTP|Alpaca|runs|recommendations|approval_events|state_transitions|broker_artifacts" docs/architecture/system-diagram-contract.md` returns the proof paths, external services, and persisted records.
    - `rg -n "Manual trigger -> FastAPI app|Scheduled trigger -> FastAPI app|Approval callback -> approval service|WorkflowEngine -> Quiver|WorkflowEngine -> OpenAI-compatible LLM|WorkflowEngine -> SMTP memo|WorkflowEngine -> runs/recommendations/approval_events/state_transitions|broker_prestaged -> broker_artifacts -> Alpaca" docs/architecture/system-diagram-contract.md` returns the exact relationship inventory the diagram must render.
  </acceptance_criteria>
  <action>Create `docs/architecture/system-diagram-contract.md` as the inventory and layout source for the visual asset. Organize it into five explicit lanes in this order: `Operator`, `Automation`, `Application Runtime`, `External Services`, and `Storage`. Under each lane, list the exact boxes to draw and the arrows between them using the live runtime only: manual trigger, scheduled trigger, approval callback, `FastAPI app`, `WorkflowEngine`, `ResearchNode`, report/email composition, approval service, broker prestage, Quiver, OpenAI-compatible LLM, SMTP, Alpaca, and the durable SQLAlchemy-backed records. Add an explicit `Required Edges` section that enumerates the exact relationships the final diagram must show, including `Manual trigger -> FastAPI app`, `Scheduled trigger -> FastAPI app`, `Approval callback -> approval service`, `WorkflowEngine -> Quiver`, `WorkflowEngine -> OpenAI-compatible LLM`, `WorkflowEngine -> SMTP memo`, `WorkflowEngine -> runs/recommendations/approval_events/state_transitions`, and `broker_prestaged -> broker_artifacts -> Alpaca`. State explicitly that the truthful approved-path terminal state is `broker_prestaged` and that direct order submission is not part of the current diagram. Call out the proof paths `python -m app.ops.dry_run`, `./scripts/cron-install.sh`, and `./scripts/cron-trigger.sh`. Do not mention LangGraph, checkpointing, or submitted orders as current behavior.</action>
  <verify>
    <automated>rg -n "Operator|Automation|Application Runtime|External Services|Storage|/runs/trigger|/runs/trigger/scheduled|/approval/\\{token\\}|WorkflowEngine|ResearchNode|compose_report_email|BrokerPrestageService|broker_prestaged|python -m app.ops.dry_run|./scripts/cron-install.sh|./scripts/cron-trigger.sh|Quiver|OpenAI-compatible LLM|SMTP|Alpaca|runs|recommendations|approval_events|state_transitions|broker_artifacts|Manual trigger -> FastAPI app|Scheduled trigger -> FastAPI app|Approval callback -> approval service|WorkflowEngine -> Quiver|WorkflowEngine -> OpenAI-compatible LLM|WorkflowEngine -> SMTP memo|WorkflowEngine -> runs/recommendations/approval_events/state_transitions|broker_prestaged -> broker_artifacts -> Alpaca" docs/architecture/system-diagram-contract.md</automated>
  </verify>
  <done>The contract file describes the exact live architecture, lane layout, and scope boundary needed to draw the system diagram without consulting stale design docs.</done>
</task>

<task type="auto">
  <name>Task 2: Build the editable Excalidraw system diagram from the contract</name>
  <files>docs/architecture/system-diagram.excalidraw, docs/architecture/system-diagram-contract.md</files>
  <read_first>docs/architecture/system-diagram-contract.md, .planning/phases/12-system-diagram-and-readme-architecture-capture/12-RESEARCH.md</read_first>
  <acceptance_criteria>
    - `test -f docs/architecture/system-diagram.excalidraw` succeeds.
    - `rg -n "Operator|Automation|Application Runtime|External Services|Storage|Manual trigger|Scheduled trigger|Approval callback|WorkflowEngine|ResearchNode|SMTP memo|broker_prestaged|runs|recommendations|approval_events|state_transitions|broker_artifacts" docs/architecture/system-diagram.excalidraw` returns the major lane and component labels.
    - `rg -n "python -m app.ops.dry_run|./scripts/cron-install.sh|./scripts/cron-trigger.sh" docs/architecture/system-diagram.excalidraw` returns the proof-path callouts or legend labels.
    - `rg -n "Manual trigger -> FastAPI app|Scheduled trigger -> FastAPI app|Approval callback -> approval service|WorkflowEngine -> Quiver|WorkflowEngine -> OpenAI-compatible LLM|WorkflowEngine -> SMTP memo|WorkflowEngine -> runs/recommendations/approval_events/state_transitions|broker_prestaged -> broker_artifacts -> Alpaca" docs/architecture/system-diagram-contract.md` returns the relationship inventory the Excalidraw source must implement.
  </acceptance_criteria>
  <action>Create `docs/architecture/system-diagram.excalidraw` as the editable source asset. Use the lane order and labels from the contract exactly, with deliberate horizontal or vertical spacing so every lane can be inspected without overlapping boxes or crossing-arrow clutter. Include separate ingress arrows for manual trigger, scheduled trigger, and approval callback before they merge into the shared application runtime. Label the approved path so it ends at `broker_prestaged`, then connect persisted broker artifacts and Alpaca as the final current-state touchpoints. Add small proof-path callouts for dry run and cron scripts, and keep the visual language explicitly current-state rather than speculative future-state. If the contract needs wording tweaks to keep the source and spec aligned, update both files in the same task.</action>
  <verify>
    <automated>rg -n "Operator|Automation|Application Runtime|External Services|Storage|Manual trigger|Scheduled trigger|Approval callback|WorkflowEngine|ResearchNode|SMTP memo|broker_prestaged|runs|recommendations|approval_events|state_transitions|broker_artifacts|python -m app.ops.dry_run|./scripts/cron-install.sh|./scripts/cron-trigger.sh|Manual trigger -> FastAPI app|Scheduled trigger -> FastAPI app|Approval callback -> approval service|WorkflowEngine -> Quiver|WorkflowEngine -> OpenAI-compatible LLM|WorkflowEngine -> SMTP memo|WorkflowEngine -> runs/recommendations/approval_events/state_transitions|broker_prestaged -> broker_artifacts -> Alpaca" docs/architecture/system-diagram.excalidraw docs/architecture/system-diagram-contract.md</automated>
  </verify>
  <done>The repo now has an editable Excalidraw architecture diagram whose labels and layout contract match the implemented runtime and remain maintainable for future edits.</done>
</task>

</tasks>

<verification>
Run `rg -n "broker_prestaged|/runs/trigger|/runs/trigger/scheduled|/approval/\\{token\\}|WorkflowEngine|ResearchNode|SMTP memo|runs|recommendations|approval_events|state_transitions|broker_artifacts|Manual trigger -> FastAPI app|Scheduled trigger -> FastAPI app|Approval callback -> approval service|WorkflowEngine -> Quiver|WorkflowEngine -> OpenAI-compatible LLM|WorkflowEngine -> SMTP memo|WorkflowEngine -> runs/recommendations/approval_events/state_transitions|broker_prestaged -> broker_artifacts -> Alpaca" docs/architecture/system-diagram-contract.md docs/architecture/system-diagram.excalidraw`. Confirm there is no current-state label for submitted orders or LangGraph.
</verification>

<success_criteria>
Phase 12 has a truthful editable source asset: the architecture inventory is locked to live code and rendered as a deliberately spaced Excalidraw diagram that covers operator, automation, runtime, external, and storage surfaces through `broker_prestaged`.
</success_criteria>

<output>
After completion, create `.planning/phases/12-system-diagram-and-readme-architecture-capture/12-system-diagram-and-readme-architecture-capture-01-SUMMARY.md`
</output>

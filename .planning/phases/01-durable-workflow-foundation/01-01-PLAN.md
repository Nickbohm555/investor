---
phase: 01-durable-workflow-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - app/db/models.py
  - app/db/session.py
  - app/db/migrations/versions/0001_create_run_tables.py
  - app/repositories/__init__.py
  - app/repositories/run_repository.py
  - app/services/run_service.py
  - tests/services/test_persistence.py
autonomous: true
requirements:
  - RUNT-01
  - RUNT-02
must_haves:
  truths:
    - "Triggering a run creates a durable run record with a stable thread identifier before any approval wait state is returned."
    - "Run records, recommendation records, approval events, and state transitions exist in Postgres-backed tables rather than process-local memory."
    - "A service layer owns run-state writes so later invoke/resume code can use one persisted source of truth."
  artifacts:
    - path: "app/repositories/run_repository.py"
      provides: "CRUD and transition logging for runs, recommendations, approval events, and thread identifiers"
    - path: "app/services/run_service.py"
      provides: "Transactional service methods for create-run and persisted status transitions"
    - path: "app/db/models.py"
      provides: "Durable runtime models including run/thread state and audit fields"
    - path: "app/db/migrations/versions/0001_create_run_tables.py"
      provides: "Schema matching the runtime durability tables"
  key_links:
    - from: "app/services/run_service.py"
      to: "app/repositories/run_repository.py"
      via: "transactional service methods"
      pattern: "RunRepository"
    - from: "app/repositories/run_repository.py"
      to: "app/db/models.py"
      via: "SQLAlchemy ORM writes"
      pattern: "RunRecord|RecommendationRecord|ApprovalEventRecord|StateTransitionRecord"
    - from: "app/db/session.py"
      to: "app/services/run_service.py"
      via: "sessionmaker factory injection"
      pattern: "sessionmaker|Session"
---

<objective>
Create the persistence foundation for durable runs so later plans can invoke and resume the workflow against Postgres-backed state instead of `app.state.workflow_store`.

Purpose: Establish the restart-safe source of truth required by RUNT-01 and RUNT-02.
Output: Updated runtime schema, repository/service layer, and persistence tests that exercise the durable records.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-durable-workflow-foundation/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@app/db/models.py
@app/db/session.py
@app/config.py
@tests/services/test_persistence.py

<interfaces>
From `app/db/models.py`:
```python
class RunRecord(Base):
    run_id: Mapped[str]
    status: Mapped[str]
    created_at: Mapped[datetime]

class RecommendationRecord(Base):
    run_id: Mapped[str]
    ticker: Mapped[str]
    action: Mapped[str]
    rationale: Mapped[str]
```

From `app/db/session.py`:
```python
def get_engine(database_url: str | None = None)
def get_session_factory(database_url: str | None = None) -> sessionmaker[Session]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the runtime persistence schema for durable run state</name>
  <read_first>
    app/db/models.py
    app/db/migrations/versions/0001_create_run_tables.py
    app/config.py
    .planning/phases/01-durable-workflow-foundation/01-RESEARCH.md
  </read_first>
  <files>app/db/models.py, app/db/migrations/versions/0001_create_run_tables.py, app/config.py</files>
  <behavior>
    - Test 1: A `RunRecord` can store `run_id`, `thread_id`, `status`, `trigger_source`, `approval_status`, and `current_step`.
    - Test 2: Recommendation, approval-event, and transition rows reference the persisted `run_id`.
    - Test 3: The settings object exposes `database_url`, `langgraph_checkpointer_url`, and `approval_token_ttl_seconds`.
  </behavior>
  <action>Update `app/db/models.py` per RUNT-01 and RUNT-02 so `RunRecord` includes `thread_id` as a non-null `String(128)` unique column, `trigger_source` as non-null `String(32)`, `approval_status` as non-null `String(32)` defaulting to `"pending"`, `current_step` as non-null `String(64)`, and `updated_at` as non-null `DateTime`. Add `created_at` columns to `RecommendationRecord`, `ApprovalEventRecord`, and `StateTransitionRecord`; add `token_id` unique on `ApprovalEventRecord`; add `occurred_at` to approvals/transitions; add `reason` nullable `Text` on transitions. Update `app/db/migrations/versions/0001_create_run_tables.py` to create those exact columns and constraints in the initial migration instead of leaving the schema drift for later. Update `app/config.py` so `Settings` defines `langgraph_checkpointer_url: str | None = None` and `approval_token_ttl_seconds: int = 900` alongside the existing `database_url`, with the same `INVESTOR_` env prefix. Do not introduce new config loaders or split settings into multiple modules in this plan.</action>
  <acceptance_criteria>
    - `rg -n "thread_id|approval_status|current_step|updated_at" app/db/models.py` returns matches for `RunRecord`.
    - `rg -n "token_id|occurred_at|reason" app/db/models.py` returns matches in the approval/transition models.
    - `rg -n "langgraph_checkpointer_url|approval_token_ttl_seconds" app/config.py` returns both setting names.
    - `rg -n "thread_id|approval_status|current_step|updated_at|occurred_at|reason" app/db/migrations/versions/0001_create_run_tables.py` returns all added schema fields.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/services/test_persistence.py -q</automated>
  </verify>
  <done>The runtime models and initial Alembic migration define the durable run, recommendation, approval-event, and transition schema required for persisted workflow state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add repository and service contracts for durable run lifecycle writes</name>
  <read_first>
    app/db/session.py
    app/db/models.py
    tests/services/test_persistence.py
    .planning/phases/01-durable-workflow-foundation/01-RESEARCH.md
  </read_first>
  <files>app/db/session.py, app/repositories/__init__.py, app/repositories/run_repository.py, app/services/run_service.py, tests/services/test_persistence.py</files>
  <behavior>
    - Test 1: Creating a run persists a row with the supplied `run_id`, `thread_id`, `status`, and `current_step`.
    - Test 2: Recording recommendations and a transition stores child rows tied to the same run.
    - Test 3: Recording an approval event with the same `token_id` twice is rejected or safely deduplicated by the persistence layer.
  </behavior>
  <action>Create `app/repositories/run_repository.py` with a `RunRepository` class exposing concrete methods `create_run(...)`, `get_run(run_id: str)`, `list_recommendations(run_id: str)`, `replace_recommendations(run_id: str, recommendations: list[Recommendation])`, `record_transition(run_id: str, from_status: str, to_status: str, reason: str | None = None)`, and `record_approval_event(run_id: str, decision: str, token_id: str)`. Create `app/services/run_service.py` with a `RunService` class that accepts a SQLAlchemy `sessionmaker[Session]`, opens `with session_factory.begin() as session`, and wraps those repository calls into `create_pending_run(...)`, `store_recommendations(...)`, and `mark_status(...)`. Update `app/db/session.py` only as needed to make the session factory injectable without rebuilding settings on every call; keep `create_engine(..., future=True)` and `expire_on_commit=False`. Extend `tests/services/test_persistence.py` to cover the new service/repository behavior against SQLite memory using `Base.metadata.create_all(engine)`. Do not wire FastAPI routes or LangGraph in this plan.</action>
  <acceptance_criteria>
    - `test -f app/repositories/run_repository.py` succeeds.
    - `test -f app/services/run_service.py` succeeds.
    - `rg -n "class RunRepository|def create_run|def record_transition|def record_approval_event" app/repositories/run_repository.py` returns all methods.
    - `rg -n "class RunService|def create_pending_run|def store_recommendations|def mark_status" app/services/run_service.py` returns all methods.
    - `rg -n "create_pending_run|record_approval_event|replace_recommendations" tests/services/test_persistence.py` returns the new coverage.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/services/test_persistence.py -q</automated>
  </verify>
  <done>The repo has a transaction-owning persistence layer that can create durable runs, store recommendations, and log approval/transition events for later invoke/resume logic.</done>
</task>

</tasks>

<verification>
Run `pytest tests/services/test_persistence.py -q` and confirm the persistence layer passes against the updated schema. Spot-check the created files with `rg` to confirm the exact repository/service methods and schema columns exist.
</verification>

<success_criteria>
Phase 1 persistence foundation is ready when the codebase has durable runtime models with thread identifiers and audit fields, and a repository/service layer can create and update persisted runs without relying on FastAPI app state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-durable-workflow-foundation/01-01-SUMMARY.md`
</output>

---
phase: 01-durable-workflow-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/main.py
  - app/api/routes.py
  - app/graph/workflow.py
  - app/graph/runtime.py
  - app/services/tokens.py
  - app/services/run_service.py
  - app/schemas/workflow.py
  - tests/api/test_routes.py
  - tests/graph/test_workflow.py
autonomous: true
requirements:
  - RUNT-01
  - RUNT-02
  - RUNT-03
must_haves:
  truths:
    - "Triggering a run persists the run/thread state and returns a run identifier that can be resumed after restart."
    - "Approval requests validate the signed token, consult persisted run state, and resume the same durable workflow instance."
    - "Invalid, expired, stale, or duplicate approval attempts return deliberate 4xx API responses instead of unhandled 500s."
  artifacts:
    - path: "app/graph/runtime.py"
      provides: "LangGraph checkpointer setup and durable invoke/resume adapter"
    - path: "app/api/routes.py"
      provides: "Trigger and approval endpoints backed by persisted runtime services"
    - path: "app/main.py"
      provides: "Application composition root with durable runtime services instead of `workflow_store`"
    - path: "app/services/tokens.py"
      provides: "Token payload parsing rich enough to detect decision and token identity for duplicate checks"
  key_links:
    - from: "app/api/routes.py"
      to: "app/services/run_service.py"
      via: "trigger and approval handlers"
      pattern: "RunService"
    - from: "app/api/routes.py"
      to: "app/graph/runtime.py"
      via: "invoke/resume using persisted thread_id"
      pattern: "thread_id|resume"
    - from: "app/services/tokens.py"
      to: "app/api/routes.py"
      via: "domain errors mapped to HTTPException/custom handlers"
      pattern: "InvalidApprovalTokenError|ExpiredApprovalTokenError"
---

<objective>
Replace process-local invoke/resume behavior with a durable runtime path that uses persisted run state, real signed approval links, and explicit API error mapping.

Purpose: Deliver the user-visible restart-safe trigger/approval behavior required by RUNT-01 and RUNT-03 while consuming the persistence layer from Plan 01.
Output: Durable runtime wiring, signed-token approval flow, and route/workflow tests covering the new contracts.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-durable-workflow-foundation/01-RESEARCH.md
@.planning/phases/01-durable-workflow-foundation/01-01-PLAN.md
@app/main.py
@app/api/routes.py
@app/graph/workflow.py
@app/services/tokens.py
@app/schemas/workflow.py
@tests/api/test_routes.py
@tests/graph/test_workflow.py

<interfaces>
From `app/services/tokens.py`:
```python
class InvalidApprovalTokenError(ValueError): ...
class ExpiredApprovalTokenError(InvalidApprovalTokenError): ...

@dataclass(frozen=True)
class ApprovalTokenPayload:
    run_id: str
    decision: str

def sign_approval_token(run_id: str, decision: str, secret: str, ttl_seconds: int) -> str
def verify_approval_token(token: str, secret: str, ttl_seconds: int) -> ApprovalTokenPayload
```

From `app/graph/workflow.py`:
```python
def compile_workflow(research_node) -> CompiledInvestorWorkflow
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the durable workflow runtime and persisted invoke/resume service path</name>
  <read_first>
    app/main.py
    app/api/routes.py
    app/graph/workflow.py
    app/services/run_service.py
    app/db/session.py
    .planning/phases/01-durable-workflow-foundation/01-RESEARCH.md
  </read_first>
  <files>app/main.py, app/api/routes.py, app/graph/workflow.py, app/graph/runtime.py, app/services/run_service.py, app/schemas/workflow.py, tests/graph/test_workflow.py</files>
  <behavior>
    - Test 1: Triggering a run calls persistence first, using one `run_id` and one `thread_id` for the durable workflow instance.
    - Test 2: Resuming with an approval decision uses the same `thread_id` and updates the persisted run status instead of touching `app.state.workflow_store`.
    - Test 3: Workflow-generated email payloads use signed approval and rejection links instead of `http://localhost:8000/approval/{run_id}:approve`.
  </behavior>
  <action>Create `app/graph/runtime.py` as the durable runtime seam. It should define a concrete `InvestorRuntime` (or equivalently named class) that compiles the workflow with a LangGraph checkpointer when `langgraph` is installed, but also exposes an injectable interface for tests so this plan can verify invoke/resume behavior without requiring live Postgres in every test. Update `app/services/run_service.py` to add orchestrator-facing methods that create a pending run with `status="triggered"`, `approval_status="pending"`, `current_step="research"`, and a generated `thread_id`, then persist recommendations and transitions as the workflow progresses through `"research"`, `"awaiting_human_review"`, `"approved"`, `"rejected"`, and `"completed"`. Update `app/graph/workflow.py` so it no longer hard-codes `http://localhost:8000/approval/{run_id}:approve`; instead, compose email data from signed approval and rejection tokens using settings-provided TTL and base URL inputs supplied by the runtime/service layer. Update `app/main.py` to attach durable dependencies such as `settings`, `session_factory`, `run_service`, and the durable runtime object on `app.state`; remove `app.state.workflow_store = {}` completely. Update `app/api/routes.py` so `POST /runs/trigger` creates the persisted run before invoking the workflow and `GET /approval/{token}` loads the persisted run and resumes by `thread_id` rather than by cached Python object. Update `app/schemas/workflow.py` only as needed to add typed payloads for persisted runtime state or approval responses that replace ad hoc dict shape assumptions. Keep the existing `StaticLLM` test stub pattern unless a smaller injected fixture is clearer.</action>
  <acceptance_criteria>
    - `test -f app/graph/runtime.py` succeeds.
    - `rg -n "workflow_store" app/main.py app/api/routes.py` returns no matches.
    - `rg -n "thread_id|awaiting_human_review|approved|completed" app/services/run_service.py` returns persisted status handling.
    - `rg -n "sign_approval_token|approval_url|rejection_url" app/graph/workflow.py app/graph/runtime.py app/api/routes.py` returns signed-link wiring.
    - `rg -n "thread_id|resume" tests/graph/test_workflow.py` returns durable resume assertions.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/graph/test_workflow.py tests/api/test_routes.py -q</automated>
  </verify>
  <done>The app no longer depends on in-memory paused workflow state, and the trigger/approval path uses persisted run/thread data plus signed links for invoke/resume behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map approval-domain failures to explicit HTTP responses and persisted audit writes</name>
  <read_first>
    app/api/routes.py
    app/services/tokens.py
    app/services/run_service.py
    tests/api/test_routes.py
    .planning/phases/01-durable-workflow-foundation/01-RESEARCH.md
  </read_first>
  <files>app/api/routes.py, app/services/tokens.py, app/services/run_service.py, tests/api/test_routes.py</files>
  <behavior>
    - Test 1: Invalid tokens return HTTP 400 with `{"detail": "Approval token invalid"}`.
    - Test 2: Expired tokens return HTTP 410 with `{"detail": "Approval token expired"}`.
    - Test 3: Unknown, stale, or duplicate approvals return stable 404 or 409 responses instead of 500s, and the persisted approval event log records accepted attempts.
  </behavior>
  <action>Extend `app/services/tokens.py` so `ApprovalTokenPayload` includes a stable `token_id` derived from the signed payload or serializer output, and ensure `verify_approval_token(...)` returns that `token_id` together with `run_id` and `decision`. In `app/services/run_service.py`, add domain-level exceptions for `RunNotFound`, `StaleApproval`, and `DuplicateApproval`, plus a method that validates the persisted run state before resume: reject missing runs, reject runs whose `status` is already `"completed"` or `"rejected"`, reject approval events whose `token_id` already exists, and record valid approval events and state transitions in the same transaction. In `app/api/routes.py`, catch `InvalidApprovalTokenError` as HTTP 400, `ExpiredApprovalTokenError` as HTTP 410, `RunNotFound` as HTTP 404, and `StaleApproval`/`DuplicateApproval` as HTTP 409 with exact `detail` strings matching the exception messages. Keep the route response body shape to `{"status": "...", "run_id": "..."}` or the durable workflow payload, but do not allow framework-default 500s for these cases.</action>
  <acceptance_criteria>
    - `rg -n "token_id" app/services/tokens.py` returns a field on `ApprovalTokenPayload` and logic in `verify_approval_token`.
    - `rg -n "RunNotFound|StaleApproval|DuplicateApproval" app/services/run_service.py` returns the new exception classes.
    - `rg -n "status_code=400|status_code=404|status_code=409|status_code=410" app/api/routes.py` returns explicit HTTP mappings.
    - `rg -n "Approval token invalid|Approval token expired" tests/api/test_routes.py` returns exact response assertions.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/api/test_routes.py -q</automated>
  </verify>
  <done>Approval failures are explicit API contracts, and accepted approvals create persisted audit entries before resuming the durable workflow.</done>
</task>

</tasks>

<verification>
Run `pytest tests/graph/test_workflow.py tests/api/test_routes.py -q`. Confirm `rg -n "workflow_store"` shows no usage and `rg -n "status_code=400|status_code=409|status_code=410" app/api/routes.py` shows the explicit response mapping.
</verification>

<success_criteria>
Phase 1 runtime wiring is complete when trigger and approval behavior use persisted run/thread state, signed approval links, and explicit 4xx semantics for invalid or stale approval attempts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-durable-workflow-foundation/01-02-SUMMARY.md`
</output>

---
phase: 01-durable-workflow-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - tests/conftest.py
  - tests/api/test_routes.py
  - tests/graph/test_workflow.py
  - tests/services/test_persistence.py
  - tests/integration/test_durable_workflow.py
  - README.md
autonomous: true
requirements:
  - RUNT-01
  - RUNT-02
  - RUNT-03
must_haves:
  truths:
    - "An automated test can trigger a run, recreate the app, and complete approval on the same persisted run."
    - "Automated coverage proves recommendations, approval events, and transitions are stored durably for the run."
    - "Automated coverage proves invalid, expired, stale, and duplicate approvals return the intended API responses."
  artifacts:
    - path: "tests/integration/test_durable_workflow.py"
      provides: "Restart-safe end-to-end coverage of trigger then approval"
    - path: "tests/api/test_routes.py"
      provides: "Approval error-contract coverage for 400, 404, 409, and 410 responses"
    - path: "tests/services/test_persistence.py"
      provides: "Persistence assertions for runs, recommendations, approvals, and transitions"
  key_links:
    - from: "tests/integration/test_durable_workflow.py"
      to: "app/main.py"
      via: "create_app twice against the same database"
      pattern: "create_app"
    - from: "tests/api/test_routes.py"
      to: "app/api/routes.py"
      via: "TestClient approval calls"
      pattern: "/approval/"
    - from: "tests/services/test_persistence.py"
      to: "app/services/run_service.py"
      via: "service-level audit assertions"
      pattern: "RunService"
---

<objective>
Add the restart-safe and error-contract tests that prove the durable workflow foundation works under real application restarts and approval edge cases.

Purpose: Close the verification gap for RUNT-01, RUNT-02, and RUNT-03 so Phase 1 is defended by automated tests rather than happy-path-only units.
Output: Integration and service tests for restart behavior, persisted audit records, and approval failure semantics, plus updated verification docs if needed.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-durable-workflow-foundation/01-RESEARCH.md
@.planning/phases/01-durable-workflow-foundation/01-01-PLAN.md
@.planning/phases/01-durable-workflow-foundation/01-02-PLAN.md
@.planning/codebase/TESTING.md
@tests/conftest.py
@tests/api/test_routes.py
@tests/graph/test_workflow.py
@tests/services/test_persistence.py
@README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add restart-safe integration coverage for persisted invoke and approval</name>
  <read_first>
    tests/conftest.py
    tests/api/test_routes.py
    tests/graph/test_workflow.py
    app/main.py
    app/api/routes.py
    app/services/run_service.py
  </read_first>
  <files>tests/conftest.py, tests/integration/test_durable_workflow.py, tests/api/test_routes.py</files>
  <behavior>
    - Test 1: The test triggers a run using one app instance, destroys that instance, creates a second app against the same database, and approves the same run successfully.
    - Test 2: The persisted database rows include one run, at least one recommendation, one approval event, and matching state transitions for the approved run.
    - Test 3: Duplicate approval on the same token returns HTTP 409 after the first approval succeeds.
  </behavior>
  <action>Create `tests/integration/test_durable_workflow.py` and use a file-backed SQLite database URL such as `sqlite+pysqlite:///{tmp_path}/investor.db` so two `create_app()` calls can share the same persisted state without needing a containerized database. Update `tests/conftest.py` so tests can override `INVESTOR_DATABASE_URL`, `INVESTOR_LANGGRAPH_CHECKPOINTER_URL`, and `INVESTOR_APPROVAL_TOKEN_TTL_SECONDS` per test without leaking cached settings between app instances. Add integration assertions that call `POST /runs/trigger`, capture the returned `run_id`, generate or extract a signed approval token, create a fresh app/TestClient, call `GET /approval/{token}`, and then inspect the database or API-visible state to confirm the persisted run/recommendation/approval/transition records exist. Extend `tests/api/test_routes.py` with a duplicate-approval test that performs the callback twice and asserts the second response is HTTP 409. Do not weaken existing assertions into generic status-code checks; keep exact state assertions.</action>
  <acceptance_criteria>
    - `test -f tests/integration/test_durable_workflow.py` succeeds.
    - `rg -n "create_app\\(\\).*create_app\\(" tests/integration/test_durable_workflow.py` or equivalent multiple app construction pattern returns matches.
    - `rg -n "approval_events|state_transitions|recommendations" tests/integration/test_durable_workflow.py` returns persisted-record assertions.
    - `rg -n "409" tests/api/test_routes.py` returns duplicate-approval coverage.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/integration/test_durable_workflow.py tests/api/test_routes.py -q</automated>
  </verify>
  <done>The test suite proves that approval can resume the same persisted run after app recreation and that duplicate approvals are explicitly rejected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close persistence and failure-mode coverage gaps in the service and verification docs</name>
  <read_first>
    tests/services/test_persistence.py
    tests/api/test_routes.py
    README.md
    .planning/codebase/TESTING.md
    app/services/run_service.py
  </read_first>
  <files>tests/services/test_persistence.py, tests/api/test_routes.py, README.md</files>
  <behavior>
    - Test 1: Service tests assert run creation, recommendation storage, approval-event storage, and transition logging in one persisted lifecycle.
    - Test 2: API tests assert 400 for invalid token, 410 for expired token, 404 for unknown run, and 409 for stale or duplicate approvals.
    - Test 3: README verification commands mention the new restart-safe integration command rather than only `pytest -v`.
  </behavior>
  <action>Expand `tests/services/test_persistence.py` so one test exercises a full lifecycle through `RunService`: create a pending run, store recommendations, record an approval event, and transition to completed, then assert the database contains exactly the expected rows and statuses. Fill any remaining approval-error gaps in `tests/api/test_routes.py` so the suite checks exact `detail` strings for invalid token, expired token, missing run, stale decision, and duplicate token paths. Update `README.md` verification text to include the concrete fast command `pytest tests/services/test_persistence.py tests/api/test_routes.py tests/integration/test_durable_workflow.py -q` and mention that restart-safe approval is now part of the documented verification flow. Do not add broad operational docs for later phases; keep this change limited to Phase 1 verification coverage.</action>
  <acceptance_criteria>
    - `rg -n "approval event|transition|completed" tests/services/test_persistence.py` or exact lifecycle test names returns expanded lifecycle coverage.
    - `rg -n "Approval token invalid|Approval token expired|409|404" tests/api/test_routes.py` returns exact failure assertions.
    - `rg -n "tests/integration/test_durable_workflow.py" README.md` returns the new verification command.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/services/test_persistence.py tests/api/test_routes.py tests/integration/test_durable_workflow.py -q</automated>
  </verify>
  <done>The automated suite and README now prove and document the durable workflow, persisted audit trail, and explicit approval failure contracts required to finish Phase 1.</done>
</task>

</tasks>

<verification>
Run `pytest tests/services/test_persistence.py tests/api/test_routes.py tests/integration/test_durable_workflow.py -q`. Confirm the new integration test exists and the README includes the same command verbatim.
</verification>

<success_criteria>
Phase 1 verification is complete when the suite recreates the app across trigger and approval, checks durable audit rows, and enforces the 4xx approval-response contracts described in the roadmap.
</success_criteria>

<output>
After completion, create `.planning/phases/01-durable-workflow-foundation/01-03-SUMMARY.md`
</output>

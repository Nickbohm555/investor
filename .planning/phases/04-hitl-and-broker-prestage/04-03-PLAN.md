---
phase: 04-hitl-and-broker-prestage
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - app/main.py
  - app/api/routes.py
  - app/graph/workflow.py
  - app/services/approvals.py
  - tests/conftest.py
  - tests/api/test_routes.py
  - tests/integration/test_broker_prestage.py
autonomous: true
requirements:
  - HITL-01
  - HITL-02
  - BRKR-01
  - BRKR-02
  - BRKR-03
must_haves:
  truths:
    - "A full approve flow produces broker artifacts linked to the originating run and recommendation set."
    - "A reject flow leaves the run finalized with no broker artifacts."
    - "Duplicate or invalid approval attempts remain explicit failures even after broker-prestage wiring is added."
  artifacts:
    - path: "tests/integration/test_broker_prestage.py"
      provides: "End-to-end proof of approval-to-artifact and reject-without-artifact behavior."
    - path: "tests/conftest.py"
      provides: "Shared persisted-run, recommendation, and mocked Alpaca fixtures for restart-safe Phase 4 tests."
    - path: "tests/api/test_routes.py"
      provides: "Route-level assertions for 4xx approval semantics and broker-linked success payloads."
  key_links:
    - from: "app/services/approvals.py"
      to: "app/services/broker_prestage.py"
      via: "approved decision path invokes broker prestage while reject bypasses the service"
      pattern: "prestage_approved_recommendations|decision == \"reject\""
    - from: "tests/integration/test_broker_prestage.py"
      to: "app/db/models.py"
      via: "integration test reads persisted broker artifact rows linked to run and recommendation IDs"
      pattern: "BrokerArtifactRecord|recommendation_id|run_id"
    - from: "tests/api/test_routes.py"
      to: "app/services/approvals.py"
      via: "route tests assert approve/reject/duplicate/invalid HTTP semantics"
      pattern: "409|410|404|200"
---

<objective>
Prove the complete Phase 4 path by wiring the approval route to broker prestage and adding end-to-end tests for approve, reject, duplicate, and invalid scenarios.

Purpose: Close the loop from human decision to safe broker artifact creation with automated evidence.
Output: Shared Phase 4 test fixtures, route assertions, and approval-to-broker integration coverage.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-hitl-and-broker-prestage/04-RESEARCH.md
@.planning/phases/04-hitl-and-broker-prestage/04-01-PLAN.md
@.planning/phases/04-hitl-and-broker-prestage/04-02-PLAN.md
@app/main.py
@app/api/routes.py
@tests/conftest.py
@tests/api/test_routes.py

<interfaces>
Expected from 04-01:
```python
def apply_review_decision(payload: ApprovalTokenPayload, token_id: str) -> dict: ...
```

Expected from 04-02:
```python
def prestage_approved_recommendations(run_id: str, recommendation_ids: list[int], broker_mode: str) -> list[BrokerArtifact]:
    ...
```

Route contract to preserve:
```python
@router.get("/approval/{token}")
def review_token(token: str, request: Request) -> dict:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared persisted-run fixtures and route-level Phase 4 assertions</name>
  <files>tests/conftest.py, tests/api/test_routes.py</files>
  <read_first>.planning/phases/04-hitl-and-broker-prestage/04-01-PLAN.md, .planning/phases/04-hitl-and-broker-prestage/04-02-PLAN.md, tests/conftest.py, tests/api/test_routes.py, app/api/routes.py, app/main.py</read_first>
  <behavior>
    - Test 1: route approval success returns a `broker_prestaged` payload for the original persisted run.
    - Test 2: duplicate approval returns `409`.
    - Test 3: stale approval returns `410` and missing run returns `404`.
  </behavior>
  <action>Extend `tests/conftest.py` with shared fixtures for a persisted run containing a stable `thread_id`, persisted recommendation rows, and mocked Alpaca account/asset responses that can survive app recreation between trigger and approval. Update `tests/api/test_routes.py` so route coverage no longer constructs only happy-path in-memory flows: add assertions for `409`, `410`, and `404`, and assert the success payload still references the original `run_id` while exposing the explicit post-approval status `broker_prestaged`.</action>
  <acceptance_criteria>
    - `rg -n "thread_id|persisted_run|mock_alpaca_account|mock_alpaca_asset" tests/conftest.py` returns shared Phase 4 fixture hooks.
    - `rg -n "409|410|404" tests/api/test_routes.py` returns explicit route-status assertions.
    - `rg -n "broker_prestaged|run_id" tests/api/test_routes.py` returns assertions that the success path stays tied to the originating run and uses the explicit success status.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/api/test_routes.py -q</automated>
  </verify>
  <done>The test harness can recreate the app between trigger and approval, and the route-level suite proves the explicit Phase 4 approval semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire approve into broker prestage and add end-to-end approval-to-broker integration coverage</name>
  <files>tests/integration/test_broker_prestage.py, app/api/routes.py, app/main.py, app/graph/workflow.py, app/services/approvals.py</files>
  <read_first>.planning/phases/04-hitl-and-broker-prestage/04-01-PLAN.md, .planning/phases/04-hitl-and-broker-prestage/04-02-PLAN.md, tests/conftest.py, app/api/routes.py, app/main.py, app/graph/workflow.py, app/services/approvals.py, tests/integration/test_hitl_resume.py, tests/services/test_broker_policy.py</read_first>
  <behavior>
    - Test 1: approving a run creates one broker artifact per approved recommendation, each linked to the source run and recommendation IDs.
    - Test 2: rejecting a run creates zero broker artifacts.
    - Test 3: invalid buying power or non-tradable assets block the approve path before artifact persistence.
  </behavior>
  <action>Create `tests/integration/test_broker_prestage.py` with exact tests `test_approved_run_creates_broker_artifacts`, `test_broker_artifact_links_to_run_and_recommendations`, `test_reject_path_creates_no_broker_artifacts`, and `test_invalid_broker_policy_blocks_artifact_creation`. Update the runtime execution path in `app/services/approvals.py` or `app/graph/workflow.py` so an approved decision invokes `prestage_approved_recommendations(run_id=..., recommendation_ids=..., broker_mode=...)` after resuming the persisted thread, and so the reject path returns `rejected` without reaching the prestage service. If `app/main.py` still needs composition hooks to inject the broker-prestage service or repository-backed dependencies, add them there instead of embedding construction logic into `app/api/routes.py`. The integration test must recreate the app or dependency container between the initial run and the approval callback so the flow proves restart-safe persisted execution rather than an in-memory continuation.</action>
  <acceptance_criteria>
    - `rg -n "test_approved_run_creates_broker_artifacts|test_broker_artifact_links_to_run_and_recommendations|test_reject_path_creates_no_broker_artifacts|test_invalid_broker_policy_blocks_artifact_creation" tests/integration/test_broker_prestage.py` returns all four end-to-end tests.
    - `rg -n "BrokerArtifactRecord|recommendation_id|run_id|client_order_id" tests/integration/test_broker_prestage.py` returns traceability assertions.
    - `rg -n "prestage_approved_recommendations|decision == \"reject\"|broker_prestaged" app/services/approvals.py app/graph/workflow.py tests/integration/test_broker_prestage.py` returns the explicit approve wiring, reject bypass, and success status contract.
    - `rg -n "create_app|broker_prestage|prestage_approved_recommendations" app/main.py app/api/routes.py app/services/approvals.py tests/integration/test_broker_prestage.py` returns the composition and integration hooks that connect approval to prestage.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/integration/test_hitl_resume.py tests/integration/test_broker_prestage.py tests/api/test_routes.py -q</automated>
  </verify>
  <done>The repo has end-to-end automated proof that approve creates traceable broker artifacts, reject creates none, and invalid broker conditions block the path cleanly.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/integration/test_hitl_resume.py tests/integration/test_broker_prestage.py tests/api/test_routes.py -q`, then run `python -m pytest -q` for the wave gate. The integration coverage must recreate the app or dependency container between trigger and approval to prove restart-safe behavior.
</verification>

<success_criteria>
Automated tests prove the full approval-to-broker-prestage flow, including reject safety, duplicate/invalid approval semantics, policy blocking, and traceability from broker artifacts back to the originating run and recommendation set.
</success_criteria>

<output>
After completion, create `.planning/phases/04-hitl-and-broker-prestage/04-hitl-and-broker-prestage-03-SUMMARY.md`
</output>

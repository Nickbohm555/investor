---
phase: 04-hitl-and-broker-prestage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/approvals.py
  - app/repositories/runs.py
  - app/graph/workflow.py
  - app/api/routes.py
  - tests/integration/test_hitl_resume.py
autonomous: true
requirements:
  - HITL-01
  - HITL-02
must_haves:
  truths:
    - "Approval resumes the exact persisted run thread instead of creating a new workflow instance."
    - "Reject finalizes the run without creating any broker artifact rows."
    - "Duplicate, stale, missing, or already-finalized approvals return explicit 4xx responses."
  artifacts:
    - path: "app/services/approvals.py"
      provides: "Idempotent approval state-machine service for approve and reject decisions."
    - path: "app/graph/workflow.py"
      provides: "Approval branch that resumes with the persisted thread_id and sends reject to a terminal state."
    - path: "tests/integration/test_hitl_resume.py"
      provides: "Automated proof that approve/reject paths are restart-safe and side-effect-safe."
  key_links:
    - from: "app/api/routes.py"
      to: "app/services/approvals.py"
      via: "approval callback delegates token payload and current run lookup to approval service"
      pattern: "apply_review_decision|resume_persisted_run"
    - from: "app/services/approvals.py"
      to: "app/graph/workflow.py"
      via: "resume uses persisted thread_id with Command(resume=...)"
      pattern: "thread_id|Command\\(resume="
    - from: "app/services/approvals.py"
      to: "tests/integration/test_hitl_resume.py"
      via: "approve/reject/duplicate/stale tests assert service-visible states"
      pattern: "test_approval_resumes_same_thread|test_reject_finalizes_without_broker_side_effects"
---

<objective>
Make human review decisions durable and explicit so approval resumes the same persisted workflow instance while rejection safely finalizes the run.

Purpose: Satisfy the HITL boundary before any broker prestage logic runs.
Output: An approval-state machine service, workflow resume/reject branches, and restart-safe approval integration coverage.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-hitl-and-broker-prestage/04-RESEARCH.md
@app/api/routes.py
@app/graph/workflow.py
@app/services/tokens.py
@app/db/models.py
@tests/api/test_routes.py
@tests/graph/test_workflow.py

<interfaces>
From app/services/tokens.py:
```python
@dataclass(frozen=True)
class ApprovalTokenPayload:
    run_id: str
    decision: str

def verify_approval_token(token: str, secret: str, ttl_seconds: int) -> ApprovalTokenPayload:
    ...
```

From app/graph/workflow.py:
```python
class CompiledInvestorWorkflow:
    def invoke(self, state: dict) -> dict: ...
    def resume(self, state: dict, decision: str) -> dict: ...

def compile_workflow(research_node) -> CompiledInvestorWorkflow:
    ...
```

From app/db/models.py:
```python
class RunRecord(Base):
    run_id: Mapped[str]
    status: Mapped[str]

class ApprovalEventRecord(Base):
    run_id: Mapped[str]
    decision: Mapped[str]
    token_id: Mapped[str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the durable approval-decision service and failing HITL coverage</name>
  <files>app/services/approvals.py, app/repositories/runs.py, tests/integration/test_hitl_resume.py</files>
  <read_first>.planning/phases/04-hitl-and-broker-prestage/04-RESEARCH.md, app/services/tokens.py, app/api/routes.py, app/graph/workflow.py, app/db/models.py, tests/api/test_routes.py, tests/graph/test_workflow.py</read_first>
  <behavior>
    - Test 1: `approve` resumes the persisted workflow using the same `thread_id` stored on the run record.
    - Test 2: `reject` transitions the run to `rejected` and leaves broker artifact persistence untouched.
    - Test 3: duplicate approval returns an explicit conflict-style error instead of mutating the run again.
    - Test 4: stale, missing, or already-finalized runs return explicit 4xx-mapped errors.
  </behavior>
  <action>Create `app/services/approvals.py` with explicit domain errors `RunNotAwaitingReviewError`, `DuplicateApprovalError`, `StaleApprovalError`, and `MissingRunError`. Implement a service entry point named `apply_review_decision(payload: ApprovalTokenPayload, token_id: str) -> dict` that loads the persisted run through `app/repositories/runs.py`, requires `run.status == "awaiting_review"`, records a single approval event keyed by `run_id` plus `token_id`, and then either: (a) for `decision == "approve"`, transitions the run through `resuming` and resumes the workflow with the persisted `thread_id`; or (b) for `decision == "reject"`, transitions directly to `rejected` and returns a terminal payload without invoking any broker-prestage service. Add `tests/integration/test_hitl_resume.py` first and make it cover `test_approval_resumes_same_thread`, `test_reject_finalizes_without_broker_side_effects`, `test_duplicate_approval_returns_explicit_error`, and `test_stale_or_missing_run_returns_explicit_error` before the service is implemented.</action>
  <acceptance_criteria>
    - `rg -n "class RunNotAwaitingReviewError|class DuplicateApprovalError|class StaleApprovalError|class MissingRunError" app/services/approvals.py` returns all four error classes.
    - `rg -n "def apply_review_decision\\(payload: ApprovalTokenPayload, token_id: str\\)" app/services/approvals.py` returns exactly one service entry point.
    - `rg -n "test_approval_resumes_same_thread|test_reject_finalizes_without_broker_side_effects|test_duplicate_approval_returns_explicit_error|test_stale_or_missing_run_returns_explicit_error" tests/integration/test_hitl_resume.py` returns all four tests.
    - `rg -n "awaiting_review|resuming|rejected" app/services/approvals.py tests/integration/test_hitl_resume.py` returns the exact status strings used by the state machine.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/integration/test_hitl_resume.py -q</automated>
  </verify>
  <done>The approval service enforces one durable decision per run, reject never creates broker side effects, and the dedicated HITL integration tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the approval route and workflow resume branch to the durable service</name>
  <files>app/api/routes.py, app/graph/workflow.py, app/services/approvals.py, tests/api/test_routes.py, tests/graph/test_workflow.py</files>
  <read_first>.planning/phases/04-hitl-and-broker-prestage/04-RESEARCH.md, app/api/routes.py, app/graph/workflow.py, app/services/approvals.py, app/services/tokens.py, tests/integration/test_hitl_resume.py, tests/api/test_routes.py, tests/graph/test_workflow.py</read_first>
  <behavior>
    - Test 1: `GET /approval/{token}` delegates to the approval service and maps domain failures to stable 4xx responses.
    - Test 2: approval resumes the persisted thread instead of reading `request.app.state.workflow_store`.
    - Test 3: rejection returns a terminal rejected payload with no broker-prestage call path.
  </behavior>
  <action>Update `app/api/routes.py` to stop reading or mutating `request.app.state.workflow_store`; the route must verify the signed token, derive a deterministic `token_id` from the signed token string, call `apply_review_decision(...)`, and map `MissingRunError` to `404`, `StaleApprovalError` to `410`, and `DuplicateApprovalError` or `RunNotAwaitingReviewError` to `409`. Update `app/graph/workflow.py` so the approval pause point expects a later `Command(resume={"decision": "approve"})` or `Command(resume={"decision": "reject"})`, sends approve to the broker-prestage node, and sends reject to a terminal finalize node with status `rejected`. Add failing assertions in `tests/api/test_routes.py` for the exact `404`, `409`, and `410` responses, and add a workflow test in `tests/graph/test_workflow.py` that proves the approve branch emits the broker-prestage transition while the reject branch terminates as `rejected`. Keep the approval URLs on the signed-token contract; do not leave raw `run_id:decision` URLs in the workflow layer.</action>
  <acceptance_criteria>
    - `rg -n "workflow_store" app/api/routes.py app/graph/workflow.py` returns no matches.
    - `rg -n "DuplicateApprovalError|RunNotAwaitingReviewError|StaleApprovalError|MissingRunError" app/api/routes.py` returns explicit error handling references.
    - `rg -n "Command\\(resume=\\{\"decision\": \"approve\"\\}\\)|Command\\(resume=\\{\"decision\": \"reject\"\\}\\)" app/graph/workflow.py` returns both resume branches.
    - `rg -n "run_id:approve|run_id:reject|approval/\\{run_id\\}" app/graph/workflow.py` returns no matches.
    - `rg -n "404|409|410" tests/api/test_routes.py` returns explicit approval error assertions.
    - `rg -n "broker_prestage|rejected" tests/graph/test_workflow.py` returns approve-branch and reject-branch assertions.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/api/test_routes.py tests/graph/test_workflow.py tests/integration/test_hitl_resume.py -q</automated>
  </verify>
  <done>The HTTP approval endpoint, workflow resume branch, and domain error semantics are wired to the durable HITL service with no in-memory approval-state fallback.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/api/test_routes.py tests/graph/test_workflow.py tests/integration/test_hitl_resume.py -q` after the final task. The route must no longer reference `workflow_store`, and approve/reject behavior must be covered by dedicated integration tests.
</verification>

<success_criteria>
Approval and rejection decisions operate against persisted state only, resume the same workflow thread by `thread_id`, and expose stable explicit error responses for duplicate, stale, missing, and already-finalized decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-hitl-and-broker-prestage/04-hitl-and-broker-prestage-01-SUMMARY.md`
</output>

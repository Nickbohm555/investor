---
phase: 04-hitl-and-broker-prestage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/db/models.py
  - app/db/migrations/versions/0002_create_broker_artifacts.py
  - app/schemas/broker.py
  - app/repositories/broker_artifacts.py
  - app/services/broker_policy.py
  - app/services/broker_prestage.py
  - app/tools/alpaca.py
  - tests/services/test_persistence.py
  - tests/services/test_broker_policy.py
autonomous: true
requirements:
  - BRKR-01
  - BRKR-02
  - BRKR-03
must_haves:
  truths:
    - "Approved recommendations are converted into persisted Alpaca-ready draft artifacts rather than submitted orders."
    - "Buying power, tradability, supported order shape, and selected broker mode are validated before prestage."
    - "Every broker artifact can be traced back to the originating run and recommendation rows."
  artifacts:
    - path: "app/services/broker_policy.py"
      provides: "Deterministic buying-power, asset, order-shape, and mode validation."
    - path: "app/services/broker_prestage.py"
      provides: "Draft-order artifact creation with deterministic client_order_id values."
    - path: "app/db/models.py"
      provides: "Persisted broker artifact ORM record linked to runs and recommendations."
  key_links:
    - from: "app/services/broker_prestage.py"
      to: "app/services/broker_policy.py"
      via: "prestage validates each proposal before artifact persistence"
      pattern: "validate_order_proposal|BrokerPolicyError"
    - from: "app/services/broker_prestage.py"
      to: "app/repositories/broker_artifacts.py"
      via: "validated order proposals are persisted as app-owned artifacts"
      pattern: "create_artifact|client_order_id"
    - from: "app/tools/alpaca.py"
      to: "app/services/broker_policy.py"
      via: "account and asset lookups supply buying_power and tradable/fractionable flags"
      pattern: "get_account|get_asset"
---

<objective>
Build the safe broker-prestage layer that turns approved recommendations into Alpaca-ready draft-order artifacts after deterministic policy validation.

Purpose: Meet the broker-safety requirements without crossing the live-order submission boundary.
Output: Broker artifact persistence, broker-policy validation, expanded Alpaca adapter methods, and policy-focused tests.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-hitl-and-broker-prestage/04-RESEARCH.md
@app/db/models.py
@app/tools/alpaca.py
@app/schemas/workflow.py
@tests/tools/test_clients.py

<interfaces>
From app/schemas/workflow.py:
```python
class Recommendation(BaseModel):
    ticker: str
    action: str
    conviction_score: float
    rationale: str
```

From app/tools/alpaca.py:
```python
class AlpacaClient:
    def __init__(self, base_url: str, api_key: str, transport: httpx.BaseTransport | None = None):
        ...

    def get_buying_power(self) -> Decimal:
        ...
```

From app/db/models.py:
```python
class RecommendationRecord(Base):
    id: Mapped[int]
    run_id: Mapped[str]
    ticker: Mapped[str]
    action: Mapped[str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define broker artifact persistence and typed draft-order contracts</name>
  <files>app/db/models.py, app/db/migrations/versions/0002_create_broker_artifacts.py, app/schemas/broker.py, app/repositories/broker_artifacts.py, tests/services/test_persistence.py</files>
  <read_first>.planning/phases/04-hitl-and-broker-prestage/04-RESEARCH.md, app/db/models.py, app/db/migrations/versions/0001_create_run_tables.py, app/schemas/workflow.py, tests/services/test_persistence.py</read_first>
  <behavior>
    - Test 1: a broker artifact row links one approved recommendation to one run with a deterministic `client_order_id`.
    - Test 2: the artifact captures broker mode and the exact policy snapshot used to approve prestage.
    - Test 3: the draft-order schema exposes only safe v1 fields for long-only equity buys.
  </behavior>
  <action>Extend `app/db/models.py` with a `BrokerArtifactRecord` mapped to table name `broker_artifacts` containing exact columns `id`, `run_id`, `recommendation_id`, `broker_mode`, `symbol`, `side`, `order_type`, `time_in_force`, `qty`, `notional`, `client_order_id`, `status`, `policy_snapshot_json`, and `created_at`. Create `app/db/migrations/versions/0002_create_broker_artifacts.py` to add that table with foreign keys to `runs.run_id` and `recommendations.id`, plus a unique constraint on `client_order_id`. Create `app/schemas/broker.py` with typed DTOs `BrokerMode`, `BrokerPolicySnapshot`, `OrderProposal`, and `BrokerArtifact`. In `app/repositories/broker_artifacts.py`, add a repository method `create_artifact(proposal: OrderProposal, snapshot: BrokerPolicySnapshot) -> BrokerArtifactRecord` that persists app-owned draft artifacts only; do not call Alpaca order-submission endpoints in this task. Add failing coverage in `tests/services/test_persistence.py` that asserts one broker artifact row links `run_id` to `recommendation_id`, preserves `broker_mode`, stores `policy_snapshot_json`, and enforces deterministic `client_order_id` uniqueness before implementing the persistence layer.</action>
  <acceptance_criteria>
    - `rg -n "class BrokerArtifactRecord|__tablename__ = \"broker_artifacts\"" app/db/models.py` returns the ORM model and table name.
    - `rg -n "client_order_id|policy_snapshot_json|recommendation_id|broker_mode" app/db/migrations/versions/0002_create_broker_artifacts.py` returns all required migration columns.
    - `rg -n "class BrokerMode|class BrokerPolicySnapshot|class OrderProposal|class BrokerArtifact" app/schemas/broker.py` returns all four broker DTOs.
    - `rg -n "def create_artifact\\(proposal: OrderProposal, snapshot: BrokerPolicySnapshot\\)" app/repositories/broker_artifacts.py` returns the persistence method.
    - `rg -n "client_order_id|broker_mode|policy_snapshot_json|recommendation_id|run_id" tests/services/test_persistence.py` returns explicit broker-artifact persistence assertions.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_persistence.py -q</automated>
  </verify>
  <done>The repo has a concrete persisted artifact model and typed broker contracts that can carry approved recommendations into a safe draft-order layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Alpaca policy validation and draft-order prestage services</name>
  <files>app/services/broker_policy.py, app/services/broker_prestage.py, app/tools/alpaca.py, tests/services/test_broker_policy.py</files>
  <read_first>.planning/phases/04-hitl-and-broker-prestage/04-RESEARCH.md, app/tools/alpaca.py, app/schemas/broker.py, app/repositories/broker_artifacts.py, tests/tools/test_clients.py</read_first>
  <behavior>
    - Test 1: unsupported broker mode, blocked account, or insufficient buying power prevents artifact creation.
    - Test 2: non-tradable or non-fractionable assets fail validation before persistence.
    - Test 3: supported `buy` `market` `day` proposals produce deterministic `client_order_id` values and persisted artifacts.
  </behavior>
  <action>Expand `app/tools/alpaca.py` to add `get_account() -> dict` and `get_asset(symbol: str) -> dict` methods while keeping HTTP access behind the adapter edge. Create `app/services/broker_policy.py` with `BrokerPolicyError` plus validator functions that enforce these exact v1 rules: `broker_mode` must be either `paper` or `live`; `paper` mode uses base URL `https://paper-api.alpaca.markets`; `live` mode uses `https://api.alpaca.markets`; `side` must equal `buy`; `order_type` must equal `market`; `time_in_force` must equal `day`; the asset must have `tradable == True`; notional sizing requires `fractionable == True`; and account `buying_power` must cover the proposed notional or estimated qty cost. Create `app/services/broker_prestage.py` with `prestage_approved_recommendations(run_id: str, recommendation_ids: list[int], broker_mode: str) -> list[BrokerArtifact]` that builds `client_order_id` values in the format `{run_id}-{recommendation_id}-{broker_mode}`, records a `policy_snapshot_json` for each artifact, persists through `app/repositories/broker_artifacts.py`, and never calls `POST /v2/orders`. Add `tests/services/test_broker_policy.py` to cover both accepted and rejected paths.</action>
  <acceptance_criteria>
    - `rg -n "def get_account\\(|def get_asset\\(" app/tools/alpaca.py` returns both Alpaca adapter methods.
    - `rg -n "paper-api\\.alpaca\\.markets|api\\.alpaca\\.markets|buy|market|day" app/services/broker_policy.py` returns the exact allowed mode and order-shape values.
    - `rg -n "def prestage_approved_recommendations\\(run_id: str, recommendation_ids: list\\[int\\], broker_mode: str\\)" app/services/broker_prestage.py` returns the prestage entry point.
    - `rg -n "client_order_id = f\"\\{run_id\\}-\\{recommendation_id\\}-\\{broker_mode\\}\"|POST /v2/orders" app/services/broker_prestage.py` shows deterministic IDs and no order-submission call path.
    - `rg -n "test_rejects_unsupported_mode|test_rejects_non_tradable_asset|test_rejects_insufficient_buying_power|test_prestages_supported_market_buy" tests/services/test_broker_policy.py` returns all four policy tests.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_broker_policy.py tests/tools/test_clients.py -q</automated>
  </verify>
  <done>The broker prestage layer creates only validated, traceable, app-owned draft artifacts and blocks invalid broker conditions before persistence.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/services/test_broker_policy.py tests/tools/test_clients.py -q` after the final task. Search the prestage service for any `POST /v2/orders` or direct submission path and treat that as a failure.
</verification>

<success_criteria>
The codebase contains a traceable broker artifact model, deterministic prestage service, and explicit policy checks for mode, buying power, tradability, and supported order shape before any artifact is created.
</success_criteria>

<output>
After completion, create `.planning/phases/04-hitl-and-broker-prestage/04-hitl-and-broker-prestage-02-SUMMARY.md`
</output>

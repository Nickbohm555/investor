---
phase: 17-harden-live-run-approval-observability-and-replay-operations
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - app/runtime.py
  - app/tools/quiver.py
  - app/agents/research.py
  - app/services/mail_provider.py
  - app/services/approvals.py
  - app/services/broker_prestage.py
  - app/workflows/engine.py
  - tests/integration/test_run_observability.py
autonomous: true
requirements:
  - OPS-01
must_haves:
  truths:
    - "A live run records stage-specific Quiver, LLM, SMTP, approval, and broker events against the same `run_id`."
    - "Success and failure paths persist exact provider-stage taxonomy instead of collapsing into one generic run failure."
    - "Logs contain the same event identifiers and stage names that operators can later query from Postgres."
  artifacts:
    - "tests/integration/test_run_observability.py :: integration coverage for provider-stage event creation and log correlation"
    - "app/tools/quiver.py :: `quiver.fetch` event writes for success and failure"
    - "app/agents/research.py :: `llm.complete` event writes tied to the active `run_id`"
    - "app/services/mail_provider.py :: `smtp.send` event writes tied to the active `run_id`"
    - "app/services/approvals.py :: `approval.verify` and `approval.apply` event writes"
    - "app/services/broker_prestage.py` and `app/workflows/engine.py` :: `broker.prestage` and `broker.submit` event writes"
  key_links:
    - "app/runtime.py -> app/services/operation_events.py :: runtime composes one event service and injects it into Quiver, research, mail, approval, and broker seams"
    - "app/agents/research.py -> app/services/operation_events.py :: LLM completion writes carry the same `run_id` as the surrounding workflow"
    - "app/workflows/engine.py -> app/services/operation_events.py :: execution confirmation records broker submission outcomes with the same durable event ids seen in logs"
---

<objective>
Instrument the live runtime so every important provider boundary writes a typed operation-event row and emits a correlated log line.

Purpose: The event table from Plan 01 only becomes useful once the real Quiver, LLM, SMTP, approval, and broker seams write into it with stable stage names.
Output: Runtime instrumentation plus integration coverage that proves logs and persisted event rows agree on provider-stage outcomes.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-RESEARCH.md
@.planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-VALIDATION.md
@.planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-01-PLAN.md
@app/runtime.py
@app/tools/quiver.py
@app/agents/research.py
@app/services/mail_provider.py
@app/services/approvals.py
@app/services/broker_prestage.py
@app/workflows/engine.py
@tests/conftest.py

<interfaces>
From app/runtime.py:
```python
class AppRuntime:
    def create_quiver_client(self) -> QuiverClient:
        ...
```

From app/services/mail_provider.py:
```python
class MailProvider(Protocol):
    def send(self, subject: str, text_body: str, html_body: str, recipient: str) -> None:
        ...
```

From app/workflows/engine.py:
```python
def start_run(self, *, run_id: str, quiver_client: object, baseline_report: dict | None = None) -> dict:
def advance_run(self, *, run_id: str, event: str) -> dict:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing integration coverage for provider-stage observability</name>
  <files>tests/integration/test_run_observability.py, app/runtime.py, app/tools/quiver.py, app/agents/research.py, app/services/mail_provider.py, app/services/approvals.py, app/services/broker_prestage.py, app/workflows/engine.py</files>
  <read_first>tests/conftest.py, app/runtime.py, app/tools/quiver.py, app/agents/research.py, app/services/mail_provider.py, app/services/approvals.py, app/services/broker_prestage.py, app/workflows/engine.py, app/services/operation_events.py, .planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-RESEARCH.md</read_first>
  <acceptance_criteria>
    - `rg -n "test_quiver_failure_records_quiver_fetch_event_with_http_status|test_scheduled_success_records_llm_and_smtp_events_for_same_run|test_approval_and_broker_paths_record_approval_and_broker_events" tests/integration/test_run_observability.py` returns all three integration tests.
    - `rg -n "quiver.fetch|llm.complete|smtp.send|approval.verify|approval.apply|broker.prestage|broker.submit" tests/integration/test_run_observability.py` returns the exact stage taxonomy assertions.
    - `rg -n "operation_event run_id=|event_id=" tests/integration/test_run_observability.py` returns the log-correlation assertions.
  </acceptance_criteria>
  <action>Create `tests/integration/test_run_observability.py` with three failing tests named exactly `test_quiver_failure_records_quiver_fetch_event_with_http_status`, `test_scheduled_success_records_llm_and_smtp_events_for_same_run`, and `test_approval_and_broker_paths_record_approval_and_broker_events`. In `test_quiver_failure_records_quiver_fetch_event_with_http_status`, build the app with an `httpx.MockTransport` that returns `401` for `/beta/live/congresstrading`, post once to `/runs/trigger/scheduled`, and assert an `operation_events` row exists for the created `run_id` with `stage="quiver.fetch"`, `provider="quiver"`, `outcome="failure"`, `http_status=401`, and `error_code="HTTPStatusError"`. In `test_scheduled_success_records_llm_and_smtp_events_for_same_run`, use the existing dry-run doubles from `tests/conftest.py`, trigger one scheduled run, and assert at least one `llm.complete` event with `provider="openai"` and one `smtp.send` event with `provider="smtp"` exist for the same `run_id`, both with `outcome="success"`. In `test_approval_and_broker_paths_record_approval_and_broker_events`, run scheduled trigger -> approval callback -> `/runs/{run_id}/execute` and assert persisted events exist with exact stage/provider pairs `approval.verify/approval`, `approval.apply/approval`, `broker.prestage/alpaca`, and `broker.submit/alpaca`. Capture logs with `caplog` and require the exact prefix `operation_event run_id=` plus `event_id=` in at least one log line for each test. Keep the suite red until the runtime instrumentation exists.</action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/integration/test_run_observability.py -q</automated>
  </verify>
  <done>The repo has red integration tests that pin the exact provider-stage taxonomy and log-correlation contract for live-run observability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument the runtime boundaries to persist correlated operation events</name>
  <files>app/runtime.py, app/tools/quiver.py, app/agents/research.py, app/services/mail_provider.py, app/services/approvals.py, app/services/broker_prestage.py, app/workflows/engine.py, tests/integration/test_run_observability.py</files>
  <read_first>app/runtime.py, tests/integration/test_run_observability.py, app/services/operation_events.py, app/tools/quiver.py, app/agents/research.py, app/services/mail_provider.py, app/services/approvals.py, app/services/broker_prestage.py, app/workflows/engine.py</read_first>
  <acceptance_criteria>
    - `rg -n "quiver.fetch|llm.complete|smtp.send|approval.verify|approval.apply|broker.prestage|broker.submit" app/tools/quiver.py app/agents/research.py app/services/mail_provider.py app/services/approvals.py app/services/broker_prestage.py app/workflows/engine.py` returns all exact stage names.
    - `rg -n "operation_event run_id=%s stage=%s provider=%s outcome=%s event_id=%s" app/tools/quiver.py app/agents/research.py app/services/mail_provider.py app/services/approvals.py app/services/broker_prestage.py app/workflows/engine.py` returns the shared correlated log format.
    - `rg -n "OperationEventService|create_quiver_client\\(self, run_id: str\\)|send\\(self, subject: str, text_body: str, html_body: str, recipient: str, run_id: str \\| None = None\\)" app/runtime.py app/tools/quiver.py app/services/mail_provider.py` returns the runtime injection and run-aware mail surface.
    - `PYTHONPATH=. pytest tests/integration/test_run_observability.py -q` exits successfully.
  </acceptance_criteria>
  <action>Update `app/runtime.py` so `build_runtime(...)` constructs one `OperationEventService` from the current settings secrets and injects it everywhere Phase 17 needs it. Change `AppRuntime.create_quiver_client` to the exact signature `create_quiver_client(self, run_id: str) -> QuiverClient`, and update both trigger routes to call it with the active `run_id`. In `app/tools/quiver.py`, add optional constructor arguments `run_id: str | None = None` and `operation_event_service: object | None = None`; inside `_get`, record `stage="quiver.fetch"`, `provider="quiver"`, `outcome="success"` or `outcome="failure"`, the exact request path in `detail`, and `response.status_code` when available, then log `operation_event run_id=%s stage=%s provider=%s outcome=%s event_id=%s`. In `app/agents/research.py`, wrap both the legacy `invoke(...)` path and the tool-capable loop path so each LLM completion writes `stage="llm.complete"`, `provider="openai"`, and `outcome="success"` or `outcome="failure"` for the active `run_id`; include `tool_choice=auto` and `parallel_tool_calls=False` in the success detail for tool calls. In `app/services/mail_provider.py`, change the protocol and implementation signature to `send(self, subject: str, text_body: str, html_body: str, recipient: str, run_id: str | None = None) -> None`, and record `stage="smtp.send"`, `provider="smtp"`, `outcome="success"` on the existing `memo_delivery result=sent` path and `outcome="failure"` on exception. In `app/services/approvals.py`, record `approval.verify` immediately after token uniqueness/run-state checks pass and `approval.apply` after `workflow_engine.advance_run(...)` succeeds. In `app/services/broker_prestage.py`, record one `broker.prestage` success or failure event per `run_id` after the prestage loop completes or raises. In `app/workflows/engine.py`, record one `broker.submit` event for each execution confirmation submission success or failure. Do not rename the existing route statuses like `started`, `duplicate`, `awaiting_review`, or `broker_prestaged`; this plan adds observability, not a new workflow vocabulary.</action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/integration/test_run_observability.py -q</automated>
  </verify>
  <done>The live runtime now emits durable, run-specific operation events and correlated logs for Quiver, LLM, SMTP, approval, broker prestage, and broker submission boundaries.</done>
</task>

</tasks>

<verification>
Run `PYTHONPATH=. pytest tests/integration/test_run_observability.py -q`. Then run `rg -n "quiver.fetch|llm.complete|smtp.send|approval.verify|approval.apply|broker.prestage|broker.submit" app tools app/services app/workflows`.
</verification>

<success_criteria>
Operators can inspect one run and tell whether it failed in Quiver fetch, model completion, SMTP delivery, approval processing, broker prestage, or broker submission, using the same stage names in both logs and persisted rows.
</success_criteria>

<output>
After completion, create `.planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-harden-live-run-approval-observability-and-replay-operations-02-SUMMARY.md`
</output>

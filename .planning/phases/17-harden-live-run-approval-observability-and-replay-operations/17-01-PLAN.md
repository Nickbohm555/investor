---
phase: 17-harden-live-run-approval-observability-and-replay-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/db/models.py
  - app/db/migrations/versions/0005_phase17_operation_events.py
  - app/repositories/operation_events.py
  - app/services/operation_events.py
  - tests/services/test_operation_events.py
autonomous: true
requirements:
  - OPS-01
must_haves:
  truths:
    - "Provider-stage outcomes can be stored as first-class rows instead of disappearing into free-form logs or `runs.state_payload`."
    - "An operation-event write keeps the exact stage, provider, outcome, and optional failure metadata for one run."
    - "Sensitive values are redacted before durable event detail is persisted."
  artifacts:
    - "app/db/models.py :: `OperationEventRecord` ORM model linked to `runs.run_id`"
    - "app/db/migrations/versions/0005_phase17_operation_events.py :: migration creating the `operation_events` table"
    - "app/repositories/operation_events.py :: repository helpers for create/list operations"
    - "app/services/operation_events.py :: typed write/list service plus secret-redaction helper"
    - "tests/services/test_operation_events.py :: service-level coverage for persistence and masking"
  key_links:
    - "app/services/operation_events.py -> app/repositories/operation_events.py :: service delegates durable writes to a repository instead of issuing ad hoc SQL"
    - "app/repositories/operation_events.py -> app/db/models.py :: repository persists `OperationEventRecord` rows tied to `run_id`"
    - "app/db/migrations/versions/0005_phase17_operation_events.py -> app/db/models.py :: migration and ORM stay aligned on column names and nullability"
---

<objective>
Create the durable operation-event ledger and masking helper that every later observability and replay surface will depend on.

Purpose: Phase 17 cannot distinguish Quiver, LLM, SMTP, approval, and broker failures until the repo has one typed persistence seam for provider-stage events.
Output: An `operation_events` table, repository/service helpers, and tests that lock the stage/provider/outcome/redaction contract.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-RESEARCH.md
@.planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-VALIDATION.md
@app/db/models.py
@app/repositories/runs.py
@app/services/run_service.py
@app/config.py
@tests/conftest.py

<interfaces>
From app/db/models.py:
```python
class RunRecord(Base):
    run_id: Mapped[str] = mapped_column(String(64), primary_key=True)
    replay_of_run_id: Mapped[Optional[str]] = mapped_column(String(64), nullable=True)
    state_payload: Mapped[Optional[dict]] = mapped_column(JSON, nullable=True)
```

From app/repositories/runs.py:
```python
class RunsRepository:
    def get_run(self, run_id: str) -> Optional[RunRecord]:
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing operation-event persistence and redaction tests</name>
  <files>tests/services/test_operation_events.py, app/repositories/operation_events.py, app/services/operation_events.py</files>
  <read_first>tests/conftest.py, app/db/models.py, app/repositories/runs.py, app/services/run_service.py, app/config.py, .planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-RESEARCH.md</read_first>
  <acceptance_criteria>
    - `rg -n "test_record_operation_event_persists_stage_provider_outcome_and_codes|test_record_operation_event_masks_secret_values_in_detail|test_list_run_events_returns_newest_first" tests/services/test_operation_events.py` returns all three test names.
    - `rg -n "OperationEventService|record_event|list_run_events|\\*\\*\\*redacted\\*\\*\\*" tests/services/test_operation_events.py` returns the planned service API and masking assertions.
    - `rg -n "def record_event\\(|def list_run_events\\(" app/services/operation_events.py` returns the exact helper signatures the tests target.
  </acceptance_criteria>
  <action>Create `tests/services/test_operation_events.py` with three failing tests named exactly `test_record_operation_event_persists_stage_provider_outcome_and_codes`, `test_record_operation_event_masks_secret_values_in_detail`, and `test_list_run_events_returns_newest_first`. In those tests, require a new `OperationEventService` in `app/services/operation_events.py` with method signatures `record_event(*, run_id: str, stage: str, provider: str, outcome: str, error_code: str | None = None, http_status: int | None = None, detail: str | None = None, trace_id: str | None = None) -> OperationEventRecord` and `list_run_events(run_id: str) -> list[OperationEventRecord]`. Make the persistence assertions use these exact values: `stage="quiver.fetch"`, `provider="quiver"`, `outcome="failure"`, `error_code="HTTPStatusError"`, `http_status=401`, and `trace_id="trace-quiver-401"`. Make the masking test persist a detail string containing `smtp-password`, `change-me-scheduled-trigger`, and `replace-with-openai-api-key`, and require the stored `detail` column to replace each sensitive value with the exact token `***redacted***`. Make the list test require descending `id` order for events on the same `run_id`. Leave `app/repositories/operation_events.py` and `app/services/operation_events.py` as red stubs until Task 2; do not add runtime instrumentation in this task.</action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/services/test_operation_events.py -q</automated>
  </verify>
  <done>The repo has red-first tests that lock the exact operation-event service API, persisted fields, and secret-masking contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the operation-event model, migration, repository, and service</name>
  <files>app/db/models.py, app/db/migrations/versions/0005_phase17_operation_events.py, app/repositories/operation_events.py, app/services/operation_events.py, tests/services/test_operation_events.py</files>
  <read_first>app/db/models.py, tests/services/test_operation_events.py, app/db/migrations/versions/0004_phase6_workflow_engine_schema.py, app/repositories/runs.py, app/config.py</read_first>
  <acceptance_criteria>
    - `rg -n "class OperationEventRecord|__tablename__ = \"operation_events\"|stage: Mapped\\[str\\]|provider: Mapped\\[str\\]|outcome: Mapped\\[str\\]|error_code: Mapped\\[Optional\\[str\\]\\]|http_status: Mapped\\[Optional\\[int\\]\\]|detail: Mapped\\[Optional\\[str\\]\\]|trace_id: Mapped\\[Optional\\[str\\]\\]" app/db/models.py` returns the new ORM fields.
    - `rg -n "create_table\\(\"operation_events\"|ForeignKeyConstraint\\(\\[\"run_id\"\\], \\[\"runs.run_id\"\\]\\)|sa.Column\\(\"stage\"|sa.Column\\(\"provider\"|sa.Column\\(\"outcome\"|sa.Column\\(\"http_status\"" app/db/migrations/versions/0005_phase17_operation_events.py` returns the migration contract.
    - `rg -n "class OperationEventsRepository|def create_event\\(|def list_run_events\\(" app/repositories/operation_events.py` returns the repository API.
    - `rg -n "\\*\\*\\*redacted\\*\\*\\*|def _mask_sensitive_values\\(" app/services/operation_events.py` returns the concrete masking implementation.
    - `PYTHONPATH=. pytest tests/services/test_operation_events.py -q` exits successfully.
  </acceptance_criteria>
  <action>In `app/db/models.py`, add a new ORM class `OperationEventRecord` with `__tablename__ = "operation_events"` and these exact columns: `id` integer primary key autoincrement, `run_id` foreign key to `runs.run_id` and `nullable=False`, `stage` `String(64)` `nullable=False`, `provider` `String(32)` `nullable=False`, `outcome` `String(32)` `nullable=False`, `error_code` `String(128)` nullable, `http_status` integer nullable, `detail` `Text` nullable, `trace_id` `String(128)` nullable, and `created_at` `DateTime` default `datetime.utcnow` `nullable=False`. Create `app/db/migrations/versions/0005_phase17_operation_events.py` that creates the `operation_events` table with that exact schema and drops it on downgrade. Create `app/repositories/operation_events.py` with class `OperationEventsRepository` exposing `create_event(...)` and `list_run_events(run_id: str) -> list[OperationEventRecord]`, with `list_run_events` ordered by `OperationEventRecord.id.desc()`. Create `app/services/operation_events.py` with class `OperationEventService(session_factory, secrets: Sequence[str])`; implement `_mask_sensitive_values(detail: str | None) -> str | None` so every non-empty secret value is replaced by `***redacted***` before persistence. Seed the default secret list from the current settings contract values that already exist in `app/config.py`: `app_secret`, `smtp_password`, `scheduled_trigger_token`, `execution_trigger_token`, `quiver_api_key`, `openai_api_key`, and `alpaca_api_key`. Keep this plan limited to the persistence seam only; do not modify Quiver, LLM, SMTP, approval, broker, routes, or replay logic yet.</action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/services/test_operation_events.py -q</automated>
  </verify>
  <done>The repo has a durable `operation_events` table and service with exact typed fields and centralized secret masking, ready for later runtime instrumentation.</done>
</task>

</tasks>

<verification>
Run `PYTHONPATH=. pytest tests/services/test_operation_events.py -q`. Then run `rg -n "operation_events|OperationEventRecord|\\*\\*\\*redacted\\*\\*\\*" app/db/models.py app/db/migrations/versions/0005_phase17_operation_events.py app/services/operation_events.py`.
</verification>

<success_criteria>
Phase 17 has a single typed operation-event persistence seam that later plans can write to for Quiver, LLM, SMTP, approval, and broker boundaries without inventing provider-specific storage formats.
</success_criteria>

<output>
After completion, create `.planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-harden-live-run-approval-observability-and-replay-operations-01-SUMMARY.md`
</output>

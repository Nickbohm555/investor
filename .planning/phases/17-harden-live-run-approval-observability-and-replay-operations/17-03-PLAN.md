---
phase: 17-harden-live-run-approval-observability-and-replay-operations
plan: 03
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
files_modified:
  - app/api/routes.py
  - app/ops/live_run_ops.py
  - app/repositories/runs.py
  - app/services/run_service.py
  - app/services/replay.py
  - app/workflows/engine.py
  - tests/integration/test_replay_ops.py
  - tests/ops/test_live_run_ops.py
  - tests/ops/test_live_run_safety.py
  - .planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-LIVE-RUN-OPERATIONS.md
autonomous: true
requirements:
  - OPS-02
  - OPS-03
must_haves:
  truths:
    - "An operator can inspect one run, replay it as an evidence-only linked run, or re-drive research as a new linked run without mutating the original row."
    - "Replay and re-drive default to `allow_email=False`, `allow_approval_links=False`, and `allow_broker=False`."
    - "Preflight and runbook safety checks can detect unsafe callback hosts and mask secrets before operators iterate on live credentials."
  artifacts:
    - "app/services/replay.py :: service that creates linked replay and re-drive runs"
    - "app/ops/live_run_ops.py :: repo-owned CLI for `preflight`, `inspect-run`, `replay-run`, and `redrive-run`"
    - "app/api/routes.py :: `/approval/probe` endpoint for public-host reachability checks"
    - "tests/integration/test_replay_ops.py :: linked-run and side-effect-suppression coverage"
    - "tests/ops/test_live_run_ops.py :: CLI contract coverage for inspect/replay/redrive"
    - "tests/ops/test_live_run_safety.py :: safety checks for redaction and unsafe hosts"
    - ".planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-LIVE-RUN-OPERATIONS.md :: exact operator runbook for inspect/replay/redrive and callback probing"
  key_links:
    - "app/services/replay.py -> app/repositories/runs.py :: linked runs set `replay_of_run_id` instead of mutating the source run"
    - "app/ops/live_run_ops.py -> app/services/replay.py :: CLI uses the repo-owned replay service instead of shelling into raw SQL or notebooks"
    - "app/ops/live_run_ops.py -> app/api/routes.py :: preflight probes `/approval/probe` on `INVESTOR_EXTERNAL_BASE_URL` before live replay or re-drive"
---

<objective>
Add the operator-facing inspect/replay/redrive surface, plus the safety checks and runbook needed to use it safely with live credentials.

Purpose: Phase 17 is not complete until replay and re-drive exist as repo-owned, side-effect-guarded tools rather than ad hoc manual debugging steps.
Output: A linked-run replay service, a CLI with safe defaults, a public approval probe endpoint, safety tests, and a runbook.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-RESEARCH.md
@.planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-VALIDATION.md
@.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-01-PLAN.md
@app/api/routes.py
@app/repositories/runs.py
@app/services/run_service.py
@app/workflows/engine.py
@app/config.py
@tests/conftest.py

<interfaces>
From app/db/models.py:
```python
class RunRecord(Base):
    replay_of_run_id: Mapped[Optional[str]] = mapped_column(String(64), nullable=True)
```

From app/workflows/engine.py:
```python
def start_run(self, *, run_id: str, quiver_client: object, baseline_report: dict | None = None) -> dict:
```

From app/api/routes.py:
```python
@router.post("/runs/trigger/scheduled")
def trigger_scheduled_run(...):
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing replay, CLI, and safety coverage</name>
  <files>tests/integration/test_replay_ops.py, tests/ops/test_live_run_ops.py, tests/ops/test_live_run_safety.py, app/ops/live_run_ops.py, app/services/replay.py, .planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-LIVE-RUN-OPERATIONS.md</files>
  <read_first>tests/conftest.py, app/api/routes.py, app/repositories/runs.py, app/services/run_service.py, app/workflows/engine.py, app/config.py, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-01-PLAN.md, .planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-RESEARCH.md</read_first>
  <acceptance_criteria>
    - `rg -n "test_replay_run_creates_linked_run_with_copied_evidence_and_no_side_effects|test_redrive_run_creates_linked_run_and_keeps_email_and_broker_disabled_by_default" tests/integration/test_replay_ops.py` returns both replay integration tests.
    - `rg -n "test_inspect_run_outputs_status_replay_lineage_and_event_rows|test_replay_run_cli_emits_linked_run_json|test_redrive_run_cli_requires_explicit_opt_in_flags_for_side_effects" tests/ops/test_live_run_ops.py` returns all three CLI tests.
    - `rg -n "test_preflight_blocks_localhost_external_base_url|test_preflight_masks_secret_values_in_output|test_live_run_operations_runbook_lists_probe_inspect_replay_and_redrive_commands" tests/ops/test_live_run_safety.py` returns all three safety tests.
    - `rg -n "def main\\(argv: list\\[str\\] \\| None = None\\) -> int|def replay_run\\(|def redrive_run\\(" app/ops/live_run_ops.py app/services/replay.py` returns the targeted CLI and service entrypoints.
  </acceptance_criteria>
  <action>Create `tests/integration/test_replay_ops.py` with failing tests named exactly `test_replay_run_creates_linked_run_with_copied_evidence_and_no_side_effects` and `test_redrive_run_creates_linked_run_and_keeps_email_and_broker_disabled_by_default`. In the replay test, seed one completed run whose `state_payload` contains `evidence_bundles`, `research_trace`, `research_stop_reason`, `research_tool_call_count`, `investigated_tickers`, `finalized_outcome`, `strategic_report`, and `recommendations`, then require a new linked run with `trigger_source="replay"`, `replay_of_run_id=<source_run_id>`, `status="replayed"`, `current_step="replayed"`, `approval_status="replayed"`, `state_payload["replay_mode"]="evidence_only"`, and all three side-effect flags set to `False`. In the redrive test, require a new linked run with `trigger_source="redrive"`, `replay_of_run_id=<source_run_id>`, `state_payload["replay_mode"]="redrive"`, `allow_email=False`, `allow_approval_links=False`, and `allow_broker=False`, plus no new SMTP sends and no new broker artifacts by default. Create `tests/ops/test_live_run_ops.py` with failing tests named exactly `test_inspect_run_outputs_status_replay_lineage_and_event_rows`, `test_replay_run_cli_emits_linked_run_json`, and `test_redrive_run_cli_requires_explicit_opt_in_flags_for_side_effects`; pin the inspect JSON to the exact top-level keys `run_id`, `status`, `current_step`, `approval_status`, `replay_of_run_id`, and `events`, and require each event row to expose `event_id`, `stage`, `provider`, `outcome`, `http_status`, and `error_code`. Create `tests/ops/test_live_run_safety.py` with failing tests named exactly `test_preflight_blocks_localhost_external_base_url`, `test_preflight_masks_secret_values_in_output`, and `test_live_run_operations_runbook_lists_probe_inspect_replay_and_redrive_commands`. Require `preflight` output to include the exact booleans `public_base_url_safe`, `approval_probe_reachable`, and the exact fields `approval_probe_url`, `approval_probe_status_code`, `scheduled_trigger_url`, and `masked_settings`; treat `localhost`, `127.0.0.1`, `0.0.0.0`, and any host ending in `.local` as unsafe for `public_base_url_safe`. Require the runbook file `.planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-LIVE-RUN-OPERATIONS.md` to contain the exact headings `## Preflight`, `## Inspect Run`, `## Replay Run`, `## Re-drive Run`, and `## Failure Markers`, plus the exact commands `python -m app.ops.live_run_ops preflight`, `python -m app.ops.live_run_ops inspect-run --run-id`, `python -m app.ops.live_run_ops replay-run --run-id`, and `python -m app.ops.live_run_ops redrive-run --run-id`.</action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/integration/test_replay_ops.py tests/ops/test_live_run_ops.py tests/ops/test_live_run_safety.py -q</automated>
  </verify>
  <done>The repo has red tests that lock replay lineage, CLI JSON output, unsafe-host blocking, secret masking, and the live-ops runbook contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement linked replay and re-drive services, CLI commands, and safety checks</name>
  <files>app/api/routes.py, app/ops/live_run_ops.py, app/repositories/runs.py, app/services/run_service.py, app/services/replay.py, app/workflows/engine.py, tests/integration/test_replay_ops.py, tests/ops/test_live_run_ops.py, tests/ops/test_live_run_safety.py, .planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-LIVE-RUN-OPERATIONS.md</files>
  <read_first>app/api/routes.py, tests/integration/test_replay_ops.py, tests/ops/test_live_run_ops.py, tests/ops/test_live_run_safety.py, app/repositories/runs.py, app/services/run_service.py, app/workflows/engine.py, app/services/operation_events.py, app/config.py</read_first>
  <acceptance_criteria>
    - `rg -n "@router.get\\(\"/approval/probe\"\\)" app/api/routes.py` returns the probe route.
    - `rg -n "trigger_source=\"replay\"|trigger_source=\"redrive\"|status=\"replayed\"|current_step=\"replayed\"|approval_status=\"replayed\"|replay_mode" app/services/replay.py app/services/run_service.py` returns the linked-run vocabulary and payload markers.
    - `rg -n "add_parser\\(\"preflight\"\\)|add_parser\\(\"inspect-run\"\\)|add_parser\\(\"replay-run\"\\)|add_parser\\(\"redrive-run\"\\)" app/ops/live_run_ops.py` returns all four CLI commands.
    - `rg -n "allow_email=False|allow_approval_links=False|allow_broker=False|public_base_url_safe|approval_probe_reachable|\\*\\*\\*redacted\\*\\*\\*" app/ops/live_run_ops.py .planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-LIVE-RUN-OPERATIONS.md` returns the safe-defaults and masking contract.
    - `PYTHONPATH=. pytest tests/integration/test_replay_ops.py tests/ops/test_live_run_ops.py tests/ops/test_live_run_safety.py -q` exits successfully.
  </acceptance_criteria>
  <action>Extend `app/repositories/runs.py` and `app/services/run_service.py` so pending-run creation can accept `replay_of_run_id: str | None = None` and persist it on `RunRecord`. Create `app/services/replay.py` with class `ReplayService(session_factory, workflow_engine, run_service, create_quiver_client, operation_event_service)` exposing two exact methods: `replay_run(source_run_id: str) -> dict[str, object]` and `redrive_run(source_run_id: str, *, allow_email: bool = False, allow_approval_links: bool = False, allow_broker: bool = False) -> dict[str, object]`. Implement `replay_run` to create a new run id `run-<8 hex chars>`, set `trigger_source="replay"`, `status="replayed"`, `current_step="replayed"`, `approval_status="replayed"`, `replay_of_run_id=<source_run_id>`, and copy these exact source payload keys when present: `evidence_bundles`, `research_trace`, `research_stop_reason`, `research_tool_call_count`, `investigated_tickers`, `finalized_outcome`, `strategic_report`, `recommendations`, and `baseline_run_id`. Persist `state_payload["replay_mode"]="evidence_only"`, `state_payload["source_run_id"]=source_run_id`, and the three side-effect flags all set to `False`. Implement `redrive_run` to create a new run with `trigger_source="redrive"`, `replay_of_run_id=<source_run_id>`, and payload markers `replay_mode="redrive"`, `source_run_id=<source_run_id>`, plus the three side-effect flags. Extend `WorkflowEngine.start_run` to accept exact keyword flags `allow_email: bool = True` and `allow_approval_links: bool = True`; when both are `False`, still build `evidence_bundles`, research output, and `strategic_report`, but skip SMTP send and set `email_body=None`, `approval_url_disabled=True`, and `replay_mode` markers in persisted state. Add `GET /approval/probe` to `app/api/routes.py` returning HTTP 200 JSON `{"status":"probe","external_base_url": runtime.settings.external_base_url}` so the public callback host can be verified safely before a live run. Create `app/ops/live_run_ops.py` as an argparse CLI with subcommands `preflight`, `inspect-run`, `replay-run`, and `redrive-run`; `preflight` must output JSON with `public_base_url_safe`, `approval_probe_url`, `approval_probe_status_code`, `approval_probe_reachable`, `scheduled_trigger_url`, and `masked_settings`, and it must redact current secret values to `***redacted***`; `inspect-run` must return the exact top-level keys from Task 1 plus an `events` array sourced from `operation_events`; `replay-run` and `redrive-run` must emit the new linked run JSON returned by `ReplayService`. Write `.planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-LIVE-RUN-OPERATIONS.md` with the exact headings from Task 1, list the exact CLI commands, state that `replay-run` and `redrive-run` default to `allow_email=False`, `allow_approval_links=False`, and `allow_broker=False`, and include `scheduled_trigger result=failure`, `memo_delivery result=failure`, and `public_base_url_safe=false` under `## Failure Markers`. Do not mutate source runs in place and do not allow replay or re-drive to send email or create broker artifacts unless an explicit flag is passed.</action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/integration/test_replay_ops.py tests/ops/test_live_run_ops.py tests/ops/test_live_run_safety.py -q</automated>
  </verify>
  <done>The repo exposes safe, linked replay and re-drive tooling with a public approval probe, masked preflight output, and a runbook that operators can follow without shell improvisation.</done>
</task>

</tasks>

<verification>
Run `PYTHONPATH=. pytest tests/integration/test_replay_ops.py tests/ops/test_live_run_ops.py tests/ops/test_live_run_safety.py -q`. Then run `python -m app.ops.live_run_ops --help` and confirm the command list is exactly `preflight`, `inspect-run`, `replay-run`, and `redrive-run`.
</verification>

<success_criteria>
Phase 17 has one repo-owned live-ops surface for preflight, inspect, replay, and re-drive, and that surface preserves the source run, creates linked runs, blocks unsafe callback hosts, and keeps email and broker side effects disabled by default.
</success_criteria>

<output>
After completion, create `.planning/phases/17-harden-live-run-approval-observability-and-replay-operations/17-harden-live-run-approval-observability-and-replay-operations-03-SUMMARY.md`
</output>

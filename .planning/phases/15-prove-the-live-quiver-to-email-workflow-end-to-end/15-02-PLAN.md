---
phase: 15-prove-the-live-quiver-to-email-workflow-end-to-end
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - README.md
  - tests/ops/test_operational_docs.py
  - .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md
  - .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md
autonomous: true
requirements:
  - LQE-03
must_haves:
  truths:
    - "Operators can follow one repo-owned runbook for the live proof, including preflight, runtime startup, scheduled trigger, inbox verification, approval callback, and persisted-state inspection."
    - "The repo records the exact fields that must be captured from a real proof run instead of leaving the evidence in transient terminal output or inbox state."
    - "README and docs tests point to the same Phase 15 live-proof workflow, failure modes, and remaining manual steps."
  artifacts:
    - "README.md :: links the live-proof workflow from the operator docs"
    - ".planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md :: exact commands, reachability guidance, failure markers, and recovery steps"
    - ".planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md :: structured proof record template for the actual run"
    - "tests/ops/test_operational_docs.py :: drift coverage for the README and runbook live-proof contract"
  key_links:
    - "README.md -> 15-LIVE-PROOF-RUNBOOK.md :: top-level operator docs point to the phase-specific runbook instead of duplicating it"
    - "15-LIVE-PROOF-RUNBOOK.md -> app/ops/live_proof.py :: the runbook uses only repo-owned commands and the new live-proof CLI"
    - "15-LIVE-PROOF-RESULT.md -> 15-03 execution :: the live run has one expected place to record run_id, inbox proof, callback outcome, and persisted-state evidence"
---

<objective>
Document the exact Phase 15 operator workflow and create the proof record artifacts so the live run can be executed and audited without relying on memory or undocumented local setup.

Purpose: Turn the research guidance and new live-proof CLI into one locked runbook plus one result template before the real operator-visible proof happens.
Output: README linkage, a phase-local runbook, a structured proof-result template, and docs drift tests.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-RESEARCH.md
@.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-VALIDATION.md
@.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-01-PLAN.md
@.planning/phases/13-replace-host-cron-scripts-with-a-docker-native-scheduler/13-replace-host-cron-scripts-with-a-docker-native-scheduler-03-SUMMARY.md
@README.md
@tests/ops/test_operational_docs.py

<interfaces>
From app/ops/live_proof.py (from Plan 15-01):
```python
python -m app.ops.live_proof preflight
python -m app.ops.live_proof trigger-scheduled
python -m app.ops.live_proof inspect-run --run-id <run_id>
```

From README.md:
```markdown
## Go-Live Checklist
- SMTP credentials send to the real operator inbox
- INVESTOR_EXTERNAL_BASE_URL resolves to the public approval host
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add docs drift coverage for the Phase 15 live-proof workflow and artifacts</name>
  <files>tests/ops/test_operational_docs.py, README.md, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md</files>
  <read_first>tests/ops/test_operational_docs.py, README.md, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-RESEARCH.md, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-VALIDATION.md, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-01-PLAN.md</read_first>
  <behavior>
    - Test 1: README points operators to the Phase 15 runbook and mentions the live-proof CLI commands instead of inventing manual glue.
    - Test 2: the runbook contains exact sections for public base URL reachability, preflight, runtime startup, scheduled trigger, inbox verification, approval callback, persisted-state inspection, observed log capture, and failure markers.
    - Test 3: the result template contains exact fields for `run_id`, memo delivery, approval callback, persisted callback evidence, observed live-run logs, final persisted status, and remaining manual follow-up.
  </behavior>
  <action>Extend `tests/ops/test_operational_docs.py` with failing tests named exactly `test_readme_links_phase_15_live_proof_runbook`, `test_phase_15_runbook_covers_reachability_preflight_and_persisted_state_checks`, and `test_phase_15_result_template_requires_operator_visible_proof_fields`. Require `README.md` to contain the exact string `See \`.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md\` for the real-credential live proof workflow.`. Require the runbook file to contain these exact markdown headings: `## Reachability`, `## Preflight`, `## Start The Runtime`, `## Trigger The Live Run`, `## Inbox Verification`, `## Approval Callback`, `## Persisted State Verification`, `## Observed Logs`, and `## Failure Markers`. Require the runbook to mention the exact commands `python -m app.ops.live_proof preflight`, `python -m app.ops.live_proof trigger-scheduled`, `python -m app.ops.live_proof inspect-run --run-id`, `docker compose up -d --build`, and `docker compose logs -f migrate app`. Require the result template file to contain the exact field labels `run_id:`, `scheduled_trigger_status:`, `approval_probe_url:`, `approval_probe_status_code:`, `memo_delivered_to:`, `approval_link_host:`, `approval_callback_status:`, `current_step:`, `approval_status:`, `scheduled_trigger_log_line:`, `memo_delivery_log_line:`, `final_run_status:`, `approval_event_count:`, `state_transition_count:`, `broker_artifact_count:`, and `remaining_manual_steps:`. Leave README and the two phase files absent or unchanged until Task 2 so these new tests fail first.</action>
  <acceptance_criteria>
    - `rg -n "test_readme_links_phase_15_live_proof_runbook|test_phase_15_runbook_covers_reachability_preflight_and_persisted_state_checks|test_phase_15_result_template_requires_operator_visible_proof_fields" tests/ops/test_operational_docs.py` returns all three docs tests.
    - `rg -n "## Reachability|## Preflight|## Start The Runtime|## Trigger The Live Run|## Inbox Verification|## Approval Callback|## Persisted State Verification|## Observed Logs|## Failure Markers" tests/ops/test_operational_docs.py` returns the exact heading assertions.
    - `rg -n "run_id:|scheduled_trigger_status:|approval_probe_url:|approval_probe_status_code:|memo_delivered_to:|approval_link_host:|approval_callback_status:|current_step:|approval_status:|scheduled_trigger_log_line:|memo_delivery_log_line:|final_run_status:|approval_event_count:|state_transition_count:|broker_artifact_count:|remaining_manual_steps:" tests/ops/test_operational_docs.py` returns the result-template field assertions.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>The Phase 15 documentation contract is pinned by automated tests before the runbook and template are written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the live-proof runbook, result template, and README entrypoint</name>
  <files>README.md, tests/ops/test_operational_docs.py, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md</files>
  <read_first>README.md, tests/ops/test_operational_docs.py, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-RESEARCH.md, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-VALIDATION.md, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-01-PLAN.md</read_first>
  <behavior>
    - Test 1: operators can follow the runbook from environment readiness through real delivery and callback verification using only repo-owned commands plus one inbox click.
    - Test 2: reachability guidance makes `INVESTOR_EXTERNAL_BASE_URL` explicit and records whether the proof uses a port remap, reverse proxy, or tunnel.
    - Test 3: the result template captures all operator-visible evidence, persisted callback evidence, observed live-run log lines, and remaining manual steps from the live proof.
  </behavior>
  <action>Update `README.md` by adding one sentence to the `Go-Live Checklist` or adjacent operator section with the exact text `See \`.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md\` for the real-credential live proof workflow.`. Create `.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md` with the exact headings required by Task 1 and write concrete steps under each heading. In `## Reachability`, require recording the exact public URL used for `INVESTOR_EXTERNAL_BASE_URL`, whether the proof depends on a host-port remap, reverse proxy, or tunnel, and the expected success condition for the pre-send approval probe at `<INVESTOR_EXTERNAL_BASE_URL>/approval/probe`. In `## Preflight`, require `python -m app.ops.live_proof preflight` and interpretation guidance for Quiver, LLM, SMTP, and the approval-boundary `reachability_check`; require the runbook to state that a non-reachable probe blocks `trigger-scheduled`. In `## Start The Runtime`, require `docker compose up -d --build` and `docker compose logs -f migrate app`. In `## Trigger The Live Run`, require `python -m app.ops.live_proof trigger-scheduled`, state that only a returned `status` of `started` counts as the passing path, and instruct the operator to copy the returned `run_id` into `.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md`. In `## Inbox Verification` and `## Approval Callback`, require checking delivery to `INVESTOR_DAILY_MEMO_TO_EMAIL`, confirming the approval link host matches `INVESTOR_EXTERNAL_BASE_URL`, and clicking the live approval link. In `## Persisted State Verification`, require `python -m app.ops.live_proof inspect-run --run-id <run_id>` and specify that successful proof expects populated `current_step`, `approval_status`, at least one approval event, and state transitions recorded for that run. In `## Observed Logs`, require capturing the exact observed log lines for scheduled-trigger and memo-delivery outcomes from `docker compose logs --no-color app`. In `## Failure Markers`, list the exact log strings `scheduled_trigger result=failure` and `memo_delivery result=failure` plus one bullet for non-working public callback reachability. Create `.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md` as a fill-in template using the exact field labels from Task 1, with placeholder values `<pending>` on each line. Do not create a second runtime path or standalone proof script in the docs; every command must point back to Docker Compose, `app.ops.live_proof`, or the delivered approval link.</action>
  <acceptance_criteria>
    - `rg -n "See `.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md` for the real-credential live proof workflow\\." README.md` returns the exact README linkage sentence.
    - `rg -n "## Reachability|## Preflight|## Start The Runtime|## Trigger The Live Run|## Inbox Verification|## Approval Callback|## Persisted State Verification|## Observed Logs|## Failure Markers" .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md` returns all required headings.
    - `rg -n "python -m app\\.ops\\.live_proof preflight|python -m app\\.ops\\.live_proof trigger-scheduled|python -m app\\.ops\\.live_proof inspect-run --run-id|docker compose up -d --build|docker compose logs -f migrate app|docker compose logs --no-color app|approval/probe|status` of `started`|scheduled_trigger result=failure|memo_delivery result=failure" .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md` returns all exact commands and failure markers.
    - `rg -n "^run_id: <pending>$|^scheduled_trigger_status: <pending>$|^approval_probe_url: <pending>$|^approval_probe_status_code: <pending>$|^memo_delivered_to: <pending>$|^approval_link_host: <pending>$|^approval_callback_status: <pending>$|^current_step: <pending>$|^approval_status: <pending>$|^scheduled_trigger_log_line: <pending>$|^memo_delivery_log_line: <pending>$|^final_run_status: <pending>$|^approval_event_count: <pending>$|^state_transition_count: <pending>$|^broker_artifact_count: <pending>$|^remaining_manual_steps: <pending>$" .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md` returns all template fields.
    - `PYTHONPATH=. pytest tests/ops/test_operational_docs.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>The repo now contains one exact Phase 15 runbook and one structured result template, both locked by docs drift tests.</done>
</task>

</tasks>

<verification>
Run `PYTHONPATH=. pytest tests/ops/test_operational_docs.py -q`. Then read the Phase 15 runbook and confirm every operator step uses only Docker Compose, `python -m app.ops.live_proof`, or the delivered approval link.
</verification>

<success_criteria>
Phase 15 has a documented live-proof workflow with no undocumented glue, and the eventual live-run evidence has a predefined place to capture the approval-boundary reachability probe, observed scheduled-trigger and memo-delivery logs, and the persisted callback fields `current_step` and `approval_status`.
</success_criteria>

<output>
After completion, create `.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-prove-the-live-quiver-to-email-workflow-end-to-end-02-SUMMARY.md`
</output>

---
phase: 15-prove-the-live-quiver-to-email-workflow-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/ops/live_proof.py
  - tests/ops/test_live_proof.py
autonomous: true
requirements:
  - LQE-01
  - LQE-02
must_haves:
  truths:
    - "The repo exposes one operator-facing CLI surface that can preflight the real Quiver, OpenAI-compatible, SMTP, and scheduled-trigger dependencies without inventing a second workflow path."
    - "A triggered live run can be identified by run ID and inspected from persisted database state through a repo-owned command instead of ad hoc SQL spelunking."
    - "The live-proof helper uses the same scheduled trigger route and runtime settings contract that the shipped app already uses."
  artifacts:
    - "app/ops/live_proof.py :: repo-owned CLI for preflight, scheduled trigger, and persisted run inspection"
    - "tests/ops/test_live_proof.py :: regression coverage for the helper command output and runtime seam usage"
  key_links:
    - "app/ops/live_proof.py -> app/config.py :: helper reads the existing INVESTOR_* settings contract instead of shadow config"
    - "app/ops/live_proof.py -> app/api/routes.py :: trigger command calls POST /runs/trigger/scheduled with the configured token rather than invoking workflow internals directly"
    - "app/ops/live_proof.py -> app/db/models.py :: inspect command reports persisted runs, approval events, state transitions, and broker artifacts by run_id"
---

<objective>
Add repo-owned live-proof tooling that exercises the shipped scheduled-trigger and persistence seams with real credentials, so Phase 15 execution can be driven and audited without undocumented shell glue.

Purpose: Close the Wave 0 gaps from research by standardizing preflight, run triggering, and persisted-state inspection before the manual inbox approval step.
Output: A single `python -m app.ops.live_proof ...` CLI and tests that pin its contract.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-RESEARCH.md
@.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-VALIDATION.md
@.planning/phases/13-replace-host-cron-scripts-with-a-docker-native-scheduler/13-replace-host-cron-scripts-with-a-docker-native-scheduler-03-SUMMARY.md
@app/config.py
@app/api/routes.py
@app/services/mail_provider.py
@app/services/research_llm.py
@app/tools/quiver.py
@app/db/models.py
@ops/scheduler/trigger-scheduled.sh

<interfaces>
From app/api/routes.py:
```python
@router.post("/runs/trigger/scheduled")
def trigger_scheduled_run(
    request: Request,
    response: Response,
    scheduled_trigger: Optional[str] = Header(default=None, alias="X-Investor-Scheduled-Trigger"),
) -> dict[str, object]:
```

From app/services/research_llm.py:
```python
def complete_with_tools(
    self,
    *,
    messages: list[dict[str, object]],
    tools: list[dict[str, object]],
    tool_choice: str = "auto",
    parallel_tool_calls: bool = False,
) -> dict[str, object]:
```

From app/tools/quiver.py:
```python
def get_live_congress_trading(self, ticker: str | None = None) -> list[CongressionalTrade]:
def get_live_insider_trading(self, ticker: str | None = None) -> list[InsiderTrade]:
def get_live_government_contracts(self, ticker: str | None = None) -> list[GovernmentContractAward]:
def get_live_lobbying(self, ticker: str | None = None) -> list[LobbyingDisclosure]:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing ops coverage for the live-proof helper contract</name>
  <files>tests/ops/test_live_proof.py, app/ops/live_proof.py</files>
  <read_first>tests/ops/test_operational_docs.py, app/config.py, app/api/routes.py, app/services/mail_provider.py, app/services/research_llm.py, app/tools/quiver.py, ops/scheduler/trigger-scheduled.sh, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `preflight` reports exact Quiver endpoint checks, one OpenAI-compatible tool-calling check, SMTP settings visibility, the configured external base URL, and a concrete approval-boundary reachability probe in machine-readable JSON.
    - Test 2: `trigger-scheduled` calls the existing scheduled route with `X-Investor-Scheduled-Trigger` and returns the run payload JSON without touching workflow internals.
    - Test 3: `inspect-run` reports persisted run status, approval event count, state transition count, and broker artifact count for a specific run id.
  </behavior>
  <action>Create `tests/ops/test_live_proof.py` with failing tests named exactly `test_preflight_reports_quiver_llm_smtp_and_external_url_checks`, `test_trigger_scheduled_posts_to_configured_route_with_repo_token_header`, and `test_inspect_run_reports_persisted_status_and_related_counts`. In those tests, require a new module `app/ops/live_proof.py` to expose a CLI entrypoint `main(argv: list[str] | None = None) -> int` plus helper functions `_run_preflight(settings)`, `_trigger_scheduled(settings)`, and `_inspect_run(settings, run_id)`. Pin the preflight JSON to these top-level keys: `quiver_checks`, `llm_check`, `smtp_check`, `external_base_url`, and `reachability_check`. Require `quiver_checks` to contain exactly `congresstrading`, `insiders`, `govcontracts`, and `lobbying`. Require `llm_check["tool_choice"] == "auto"` and `llm_check["path"] == "/chat/completions"`. Require `reachability_check` to contain exactly `approval_probe_url`, `status_code`, and `reachable`, with `approval_probe_url` built from `INVESTOR_EXTERNAL_BASE_URL` plus `/approval/probe` and `reachable` treated as true only when the probe returns an app-originated HTTP response instead of DNS/connect/TLS failure. Require `trigger-scheduled` to POST to `settings.schedule_trigger_url` with header `X-Investor-Scheduled-Trigger: <settings.scheduled_trigger_token>` and to print the raw route JSON including `status`, `run_id`, `schedule_key`, and `duplicate`. Require `inspect-run` to return JSON containing exactly `run_id`, `status`, `current_step`, `approval_status`, `approval_event_count`, `state_transition_count`, and `broker_artifact_count`. Keep all tests transport-backed or SQLite-backed; do not hit the network in the test suite.</action>
  <acceptance_criteria>
    - `rg -n "test_preflight_reports_quiver_llm_smtp_and_external_url_checks|test_trigger_scheduled_posts_to_configured_route_with_repo_token_header|test_inspect_run_reports_persisted_status_and_related_counts" tests/ops/test_live_proof.py` returns all three failing-contract tests.
    - `rg -n "def main\\(argv: list\\[str\\] \\| None = None\\) -> int|def _run_preflight\\(settings\\)|def _trigger_scheduled\\(settings\\)|def _inspect_run\\(settings, run_id\\)" app/ops/live_proof.py` returns the planned helper surface.
    - `rg -n "quiver_checks|llm_check|smtp_check|external_base_url|reachability_check|approval_probe_url|status_code|reachable|congresstrading|insiders|govcontracts|lobbying" tests/ops/test_live_proof.py` returns the required JSON contract assertions.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest tests/ops/test_live_proof.py -q</automated>
  </verify>
  <done>The live-proof helper contract is pinned by automated tests before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the repo-owned live-proof CLI against the existing runtime seams</name>
  <files>app/ops/live_proof.py, tests/ops/test_live_proof.py</files>
  <read_first>app/ops/live_proof.py, tests/ops/test_live_proof.py, app/config.py, app/services/mail_provider.py, app/services/research_llm.py, app/tools/quiver.py, app/db/models.py, app/db/session.py, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-RESEARCH.md</read_first>
  <behavior>
    - Test 1: preflight checks the exact Quiver endpoints the repo uses, verifies the OpenAI-compatible `/chat/completions` tool-call shape, surfaces SMTP connection settings, echoes the configured public base URL, and proves that `INVESTOR_EXTERNAL_BASE_URL` reaches the approval-path boundary before any live trigger attempt.
    - Test 2: trigger-scheduled reuses the real scheduled route contract and returns the same payload operators would get from the API.
    - Test 3: inspect-run summarizes persisted database state for approval and follow-up verification without requiring raw SQL.
  </behavior>
  <action>Implement `app/ops/live_proof.py` as an argparse-driven CLI with subcommands `preflight`, `trigger-scheduled`, and `inspect-run`. Make `preflight` instantiate `Settings()` through `get_settings()`, query the four Quiver methods with ticker `NVDA`, and record one JSON object per endpoint with keys `path`, `status`, and `row_count`. Make the LLM preflight instantiate `HttpResearchLLM` and call `complete_with_tools(...)` with one user message `\"Use one Quiver tool call to inspect NVDA and stop.\"`, one tool definition named exactly `get_live_congress_trading`, `tool_choice=\"auto\"`, and `parallel_tool_calls=False`; emit `llm_check` keys `path`, `tool_choice`, `parallel_tool_calls`, and `has_tool_calls`. Reuse the same STARTTLS/login rules as `SmtpMailProvider` for `smtp_check`, but stop at connect/login and emit `host`, `port`, and `uses_starttls` instead of sending a separate proof email. Add a concrete reachability probe that performs an HTTP GET to `<INVESTOR_EXTERNAL_BASE_URL>/approval/probe` before any live trigger, records `approval_probe_url`, `status_code`, and `reachable`, and treats any app-originated HTTP response such as `404` or `405` as success because it proves the public host reaches the approval-path boundary while DNS/connect/TLS failures remain blocking. Implement `trigger-scheduled` with `httpx.post(settings.schedule_trigger_url, headers={\"X-Investor-Scheduled-Trigger\": settings.scheduled_trigger_token})` and print the JSON response exactly as returned by the route. Implement `inspect-run` by opening a SQLAlchemy session from `get_session_factory(settings.database_url)` and returning the run row plus counts from `approval_events`, `state_transitions`, and `broker_artifacts` for the requested `run_id`. Do not call `workflow_engine.start_run()` or `approval_service.apply_review_decision()` directly anywhere in this module; the helper must stay route- and persistence-oriented, per the phase goal.</action>
  <acceptance_criteria>
    - `rg -n "add_parser\\(\"preflight\"\\)|add_parser\\(\"trigger-scheduled\"\\)|add_parser\\(\"inspect-run\"\\)" app/ops/live_proof.py` returns all three CLI commands.
    - `rg -n "\"/beta/live/congresstrading\"|\"/beta/live/insiders\"|\"/beta/live/govcontracts\"|\"/beta/live/lobbying\"" app/ops/live_proof.py` returns the exact Quiver endpoint checks.
    - `rg -n "\"Use one Quiver tool call to inspect NVDA and stop\\.\"|tool_choice=\"auto\"|parallel_tool_calls=False|\"/chat/completions\"" app/ops/live_proof.py` returns the exact LLM preflight contract.
    - `rg -n "approval/probe|reachability_check|status_code|reachable" app/ops/live_proof.py` returns the concrete external-base-url reachability probe.
    - `rg -n "X-Investor-Scheduled-Trigger" app/ops/live_proof.py` returns the scheduled route header usage.
    - `rg -n "approval_event_count|state_transition_count|broker_artifact_count" app/ops/live_proof.py` returns the persisted-state summary fields.
    - `PYTHONPATH=. pytest tests/ops/test_live_proof.py -q` exits successfully.
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest tests/ops/test_live_proof.py -q</automated>
  </verify>
  <done>The repo has a tested live-proof CLI that preflights dependencies, triggers the shipped scheduled route, and inspects persisted run state by run id.</done>
</task>

</tasks>

<verification>
Run `PYTHONPATH=. pytest tests/ops/test_live_proof.py -q`. Then run `python -m app.ops.live_proof --help` and confirm the help output exposes exactly `preflight`, `trigger-scheduled`, and `inspect-run`.
</verification>

<success_criteria>
Phase 15 has one repo-owned automation surface for live preflight, route-based triggering, and persisted-state inspection, with no proof-only workflow path added, and that preflight now includes a blocking approval-boundary reachability probe for `INVESTOR_EXTERNAL_BASE_URL` before any live trigger attempt.
</success_criteria>

<output>
After completion, create `.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-prove-the-live-quiver-to-email-workflow-end-to-end-01-SUMMARY.md`
</output>

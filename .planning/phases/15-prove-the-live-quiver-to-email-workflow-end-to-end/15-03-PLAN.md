---
phase: 15-prove-the-live-quiver-to-email-workflow-end-to-end
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified:
  - .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md
autonomous: false
requirements:
  - LQE-01
  - LQE-02
  - LQE-03
user_setup:
  - service: quiver
    why: "Authenticated live data access for the real proof run"
    env_vars:
      - name: INVESTOR_QUIVER_API_KEY
        source: "Quiver account or operator secret store"
  - service: openai-compatible-llm
    why: "Live research synthesis through the shipped /v1 chat completions contract"
    env_vars:
      - name: INVESTOR_OPENAI_API_KEY
        source: "Model provider dashboard or operator secret store"
      - name: INVESTOR_OPENAI_BASE_URL
        source: "Provider API docs or operator-managed gateway"
      - name: INVESTOR_OPENAI_MODEL
        source: "Provider dashboard or operator config"
  - service: smtp
    why: "Real memo delivery to the operator inbox"
    env_vars:
      - name: INVESTOR_SMTP_HOST
        source: "SMTP provider dashboard"
      - name: INVESTOR_SMTP_PORT
        source: "SMTP provider docs"
      - name: INVESTOR_SMTP_USERNAME
        source: "SMTP provider dashboard"
      - name: INVESTOR_SMTP_PASSWORD
        source: "SMTP provider dashboard"
      - name: INVESTOR_SMTP_FROM_EMAIL
        source: "SMTP provider verified sender identity"
      - name: INVESTOR_DAILY_MEMO_TO_EMAIL
        source: "Target operator inbox for the live proof"
  - service: public-callback-host
    why: "The delivered approval link must resolve back to this running app"
    env_vars:
      - name: INVESTOR_EXTERNAL_BASE_URL
        source: "Operator-managed tunnel, reverse proxy, or reachable host URL"
must_haves:
  truths:
    - "One real scheduled-trigger run uses live Quiver data, the configured OpenAI-compatible model, and the SMTP provider to send a real memo to the operator inbox."
    - "The delivered approval link resolves against the configured external base URL and advances the persisted run state for the same run id."
    - "The proof result artifact captures the observed run id, delivery outcome, callback outcome, persisted counts, and any remaining manual cleanup or blockers."
  artifacts:
    - ".planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md :: completed proof record with observed outputs"
  key_links:
    - "python -m app.ops.live_proof trigger-scheduled -> POST /runs/trigger/scheduled :: the real proof begins on the shipped route"
    - "Delivered inbox memo -> GET /approval/{token} :: operator click exercises the public callback boundary"
    - "python -m app.ops.live_proof inspect-run --run-id <run_id> -> runs/approval_events/state_transitions :: persisted state confirms the approval callback reached the expected run"
---

<objective>
Run the real operator-visible proof, pause for the unavoidable inbox approval step, and capture the resulting persisted state in the phase artifact.

Purpose: Satisfy the Phase 15 goal with one documented live run instead of stopping at tooling or docs.
Output: A completed `15-LIVE-PROOF-RESULT.md` proving the live run, memo delivery, approval callback, and persisted-state outcome.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-RESEARCH.md
@.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-VALIDATION.md
@.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md
@.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md
@.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-01-PLAN.md
@.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-02-PLAN.md

<interfaces>
From .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md:
```text
run_id: <pending>
scheduled_trigger_status: <pending>
memo_delivered_to: <pending>
approval_link_host: <pending>
approval_callback_status: <pending>
final_run_status: <pending>
approval_event_count: <pending>
state_transition_count: <pending>
broker_artifact_count: <pending>
remaining_manual_steps: <pending>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run preflight and trigger the live scheduled path with the configured runtime</name>
  <files>.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md</files>
  <read_first>.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md, app/ops/live_proof.py, docker-compose.yml, README.md</read_first>
  <action>Follow the Phase 15 runbook exactly. Start the runtime with `docker compose up -d --build`, confirm the app and migrate logs are healthy with `docker compose logs -f migrate app`, then run `python -m app.ops.live_proof preflight` and `python -m app.ops.live_proof trigger-scheduled`. Capture the returned `run_id`, scheduled-trigger payload status, configured recipient inbox, and approval-link host into `15-LIVE-PROOF-RESULT.md`. If preflight fails for Quiver, LLM, SMTP, or reachability, record the exact failing section and stop instead of inventing a fallback path. Do not call workflow internals or inject doubles; this task must use the live configured runtime path only.</action>
  <acceptance_criteria>
    - `rg -n "^run_id: run-" .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md` returns the recorded live run id.
    - `rg -n "^scheduled_trigger_status: (started|duplicate|failed-preflight|blocked)$" .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md` returns a non-pending trigger status.
    - `rg -n "^approval_link_host: https?://" .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md` returns the recorded approval host.
  </acceptance_criteria>
  <verify>
    <automated>python -m app.ops.live_proof preflight && python -m app.ops.live_proof trigger-scheduled</automated>
  </verify>
  <done>The live runtime has been started, the repo-owned preflight has run, and the result artifact records the triggered run id plus the operator-visible delivery target and approval host.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Verify the delivered memo in the operator inbox and click the real approval link</name>
  <files>.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md</files>
  <read_first>.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md</read_first>
  <action>Open the inbox for the configured `INVESTOR_DAILY_MEMO_TO_EMAIL` address, confirm the live memo for the recorded `run_id` arrived, inspect the approval link host, and click the approval link once. If the memo does not arrive or the page errors, record the exact failure details in `15-LIVE-PROOF-RESULT.md` and stop. This is the only human-only step in the phase; do not substitute a local direct HTTP call for the inbox-delivered link.</action>
  <acceptance_criteria>
    - `15-LIVE-PROOF-RESULT.md` contains a non-pending `memo_delivered_to:` value matching the target inbox or an explicit failure note.
    - `15-LIVE-PROOF-RESULT.md` contains a non-pending `approval_callback_status:` value describing the click outcome.
    - `15-LIVE-PROOF-RESULT.md` contains the observed approval-link host copied from the delivered memo.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "^memo_delivered_to: |^approval_callback_status: |^approval_link_host: " .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md</automated>
  </verify>
  <what-built>The live run was triggered and a result artifact is ready to record inbox delivery and approval-link behavior.</what-built>
  <how-to-verify>
    1. Open the target inbox listed in `memo_delivered_to`.
    2. Find the memo associated with the recorded `run_id`.
    3. Confirm the approval link host matches `approval_link_host`.
    4. Click the approval link once and note the exact browser outcome.
    5. Update `15-LIVE-PROOF-RESULT.md` with the observed delivery and callback results.
  </how-to-verify>
  <resume-signal>Type "approved" after the inbox step is complete, or paste the exact failure details that were recorded.</resume-signal>
  <done>The result artifact records whether the live memo reached the operator inbox and whether the delivered approval link succeeded or failed when clicked from that inbox.</done>
</task>

<task type="auto">
  <name>Task 3: Inspect persisted approval state and finalize the live-proof record</name>
  <files>.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md</files>
  <read_first>.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md, .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RUNBOOK.md, app/ops/live_proof.py</read_first>
  <action>Extract the recorded run id from `15-LIVE-PROOF-RESULT.md`, run `python -m app.ops.live_proof inspect-run --run-id <run_id>`, and copy the persisted `status`, `approval_status`, `approval_event_count`, `state_transition_count`, and `broker_artifact_count` into the result artifact. If the approval callback succeeded, the recorded counts must show at least one approval event and at least one state transition for the same run. If the callback failed, record the exact persisted mismatch or absence instead of patching the database. Finish the template by writing any remaining cleanup or manual follow-up into `remaining_manual_steps:`.</action>
  <acceptance_criteria>
    - `rg -n "^final_run_status: " .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md` returns a non-pending final status.
    - `rg -n "^approval_event_count: [0-9]+$|^state_transition_count: [0-9]+$|^broker_artifact_count: [0-9]+$" .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md` returns persisted numeric counts.
    - `rg -n "^remaining_manual_steps: " .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md` returns a non-pending closeout note.
  </acceptance_criteria>
  <verify>
    <automated>RUN_ID=$(rg -o "run-[A-Za-z0-9-]+" .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md -m1); python -m app.ops.live_proof inspect-run --run-id "$RUN_ID"</automated>
  </verify>
  <done>The Phase 15 result artifact records the observed live delivery, approval callback outcome, and persisted-state evidence for the same run id.</done>
</task>

</tasks>

<verification>
1. Run `python -m app.ops.live_proof preflight`.
2. Run `python -m app.ops.live_proof trigger-scheduled`.
3. Complete the blocking inbox approval checkpoint.
4. Run `RUN_ID=$(rg -o "run-[A-Za-z0-9-]+" .planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-LIVE-PROOF-RESULT.md -m1); python -m app.ops.live_proof inspect-run --run-id "$RUN_ID"`.
5. Confirm `15-LIVE-PROOF-RESULT.md` contains non-pending values for all proof fields.
</verification>

<success_criteria>
Phase 15 is complete only when one live run is documented end to end: the repo triggered it through the shipped scheduled path, the operator received the real memo, the delivered approval link was exercised, and the persisted run record reflects the observed callback outcome.
</success_criteria>

<output>
After completion, create `.planning/phases/15-prove-the-live-quiver-to-email-workflow-end-to-end/15-prove-the-live-quiver-to-email-workflow-end-to-end-03-SUMMARY.md`
</output>

---
phase: 05-operational-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - app/main.py
  - app/ops/readiness.py
  - app/ops/dry_run.py
  - tests/ops/test_readiness.py
  - tests/ops/test_dry_run.py
  - tests/api/test_routes.py
autonomous: true
requirements:
  - OPER-01
  - OPER-02
must_haves:
  truths:
    - "App startup refuses to serve requests when required env vars are missing, placeholder, or mode-inconsistent."
    - "Operator can run one repo-owned dry-run command that triggers the workflow, captures the approval link, follows it automatically, and ends at broker_prestaged."
    - "Readiness and dry-run failures surface exact operator-facing diagnostics instead of silent fallback behavior."
  artifacts:
    - "app/ops/readiness.py :: startup validation helpers that aggregate readiness errors and probe the configured database/session path"
    - "app/main.py :: FastAPI lifespan gate that calls readiness before the app starts serving"
    - "app/ops/dry_run.py :: deterministic CLI entrypoint that runs scheduled trigger -> email capture -> approval callback -> broker artifact verification"
    - "tests/ops/test_readiness.py :: startup and diagnostic coverage for missing, placeholder, and inconsistent env"
    - "tests/ops/test_dry_run.py :: automated proof that the dry-run command reaches broker_prestaged without manual steps"
  key_links:
    - "app/main.py -> app/ops/readiness.py :: lifespan calls assert_startup_readiness(settings, session_factory) before yielding the FastAPI app"
    - "app/ops/dry_run.py -> app/api/routes.py :: the dry-run posts to /runs/trigger/scheduled with X-Investor-Scheduled-Trigger and then GETs the captured /approval/{token} URL"
    - "app/ops/dry_run.py -> tests/ops/test_dry_run.py :: stdout JSON and broker artifact assertions prove the command exercised trigger, memo, approval, and prestage"
---

<objective>
Build the executable operational boundary for the repo: startup must fail fast on bad configuration, and one deterministic dry-run command must prove the end-to-end workflow without hidden manual steps.

Purpose: Close the code/runtime portion of the “fill vars and run” gap before documentation is finalized.
Output: A readiness module, FastAPI startup gate, dry-run CLI, and focused operational test coverage.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-operational-readiness/05-RESEARCH.md
@.planning/phases/04-hitl-and-broker-prestage/04-03-SUMMARY.md
@app/config.py
@app/main.py
@app/api/routes.py
@app/graph/runtime.py
@app/tools/quiver.py
@tests/conftest.py
@tests/api/test_routes.py

<interfaces>
From app/config.py:
```python
class Settings(BaseSettings):
    app_secret: str
    database_url: str
    smtp_host: str
    smtp_password: str
    external_base_url: str
    scheduled_trigger_token: str
    quiver_api_key: str
    broker_mode: Literal["paper", "live"]
    alpaca_base_url: str
    alpaca_api_key: str
    langgraph_checkpointer_url: Optional[str]
    approval_token_ttl_seconds: int
```

From app/main.py:
```python
def create_app(
    *,
    settings=None,
    session_factory=None,
    runtime=None,
    research_node=None,
    quiver_transport=None,
    mail_provider=None,
    alpaca_client_factory=None,
    broker_prestage_service=None,
) -> FastAPI:
    ...
```

From app/api/routes.py:
```python
@router.post("/runs/trigger/scheduled")
def trigger_scheduled_run(...) -> dict[str, object]:
    ...

@router.get("/approval/{token}")
def review_token(token: str, request: Request) -> dict:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing readiness and dry-run coverage around the exact operational contract</name>
  <files>tests/ops/test_readiness.py, tests/ops/test_dry_run.py, tests/api/test_routes.py, app/ops/readiness.py, app/ops/dry_run.py</files>
  <read_first>.planning/phases/05-operational-readiness/05-RESEARCH.md, app/config.py, app/main.py, app/api/routes.py, app/graph/runtime.py, tests/conftest.py, tests/api/test_routes.py, tests/integration/test_broker_prestage.py</read_first>
  <behavior>
    - Test 1: startup fails before serving when env values are missing or still set to placeholder defaults.
    - Test 2: startup fails when `INVESTOR_BROKER_MODE=live` and `INVESTOR_ALPACA_BASE_URL=https://paper-api.alpaca.markets`.
    - Test 3: startup succeeds with explicit non-placeholder test values and `/health` still returns `{"status": "ok"}`.
    - Test 4: `python -m app.ops.dry_run` prints a JSON summary containing `trigger_status`, `approval_status`, `run_id`, `approval_url`, and `artifact_count`, with `approval_status == "broker_prestaged"`.
    - Test 5: the dry-run path drives `POST /runs/trigger/scheduled` using `X-Investor-Scheduled-Trigger`, extracts the signed approval URL from the captured memo text, follows it automatically, and persists broker artifacts without manual input.
  </behavior>
  <action>Create `tests/ops/test_readiness.py` first with explicit failing tests named `test_create_app_rejects_placeholder_operational_values`, `test_create_app_rejects_live_mode_with_paper_alpaca_base_url`, and `test_create_app_accepts_explicit_non_placeholder_values`. Make the placeholder case assert the raised error text contains these exact diagnostics: `INVESTOR_APP_SECRET must not equal change-me`, `INVESTOR_SMTP_PASSWORD must not equal change-me`, `INVESTOR_SCHEDULED_TRIGGER_TOKEN must not equal change-me-scheduled-trigger`, `INVESTOR_QUIVER_API_KEY must not equal secret`, `INVESTOR_ALPACA_API_KEY must not equal secret`, `INVESTOR_SMTP_HOST must not equal smtp.example.com`, and `INVESTOR_EXTERNAL_BASE_URL must not equal https://investor.example.com`. Create `tests/ops/test_dry_run.py` with failing tests named `test_dry_run_executes_scheduled_trigger_through_approval_and_prestage` and `test_dry_run_prints_operator_summary_json`; the command under test must be exactly `python -m app.ops.dry_run`. The dry-run assertions must require stdout JSON keys `trigger_status`, `approval_status`, `run_id`, `approval_url`, `artifact_count`, and `log_lines`, with `trigger_status == "started"`, `approval_status == "broker_prestaged"`, and `artifact_count >= 1`. Add an API-level proof in `tests/api/test_routes.py` that startup-gated apps still return `{"status": "ok"}` from `/health` after readiness passes. Create skeleton modules `app/ops/readiness.py` and `app/ops/dry_run.py` exporting the exact entry points `collect_readiness_errors(settings: Settings) -> list[str]`, `assert_startup_readiness(settings: Settings, session_factory) -> None`, and `main() -> int` so the new tests have concrete import targets before implementation.</action>
  <acceptance_criteria>
    - `rg -n "test_create_app_rejects_placeholder_operational_values|test_create_app_rejects_live_mode_with_paper_alpaca_base_url|test_create_app_accepts_explicit_non_placeholder_values" tests/ops/test_readiness.py` returns all three readiness tests.
    - `rg -n "INVESTOR_APP_SECRET must not equal change-me|INVESTOR_SMTP_PASSWORD must not equal change-me|INVESTOR_SCHEDULED_TRIGGER_TOKEN must not equal change-me-scheduled-trigger|INVESTOR_QUIVER_API_KEY must not equal secret|INVESTOR_ALPACA_API_KEY must not equal secret|INVESTOR_SMTP_HOST must not equal smtp.example.com|INVESTOR_EXTERNAL_BASE_URL must not equal https://investor.example.com" tests/ops/test_readiness.py` returns every exact diagnostic string.
    - `rg -n "test_dry_run_executes_scheduled_trigger_through_approval_and_prestage|test_dry_run_prints_operator_summary_json|python -m app\\.ops\\.dry_run" tests/ops/test_dry_run.py` returns both dry-run tests and the exact command.
    - `rg -n "trigger_status|approval_status|run_id|approval_url|artifact_count|log_lines" tests/ops/test_dry_run.py` returns all required JSON summary keys.
    - `rg -n "def collect_readiness_errors\\(settings: Settings\\) -> list\\[str\\]|def assert_startup_readiness\\(settings: Settings, session_factory\\) -> None|def main\\(\\) -> int" app/ops/readiness.py app/ops/dry_run.py` returns the exact contract entry points.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ops/test_readiness.py tests/ops/test_dry_run.py tests/api/test_routes.py -q</automated>
  </verify>
  <done>The repo contains failing tests and explicit entry-point contracts for startup readiness and the deterministic dry-run path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the FastAPI readiness gate and deterministic dry-run command</name>
  <files>app/config.py, app/main.py, app/ops/readiness.py, app/ops/dry_run.py, tests/ops/test_readiness.py, tests/ops/test_dry_run.py, tests/api/test_routes.py</files>
  <read_first>.planning/phases/05-operational-readiness/05-RESEARCH.md, app/config.py, app/main.py, app/api/routes.py, app/graph/runtime.py, app/services/approvals.py, app/db/models.py, tests/ops/test_readiness.py, tests/ops/test_dry_run.py, tests/conftest.py</read_first>
  <behavior>
    - Test 1: `create_app()` raises one aggregated readiness exception before app startup when any required operational env is missing, placeholder, or mode-inconsistent.
    - Test 2: successful app startup uses FastAPI lifespan, keeps `/health` reachable, and stops relying on silent placeholder defaults for go-live-only values.
    - Test 3: `python -m app.ops.dry_run` creates a deterministic local app instance, invokes the scheduled trigger route, captures the memo, follows the signed approval link, and proves at least one broker artifact row exists.
  </behavior>
  <action>Implement `app/ops/readiness.py` with an exception class `OperationalReadinessError(ValueError)` whose message joins one error per line, plus `collect_readiness_errors(settings: Settings) -> list[str]` and `assert_startup_readiness(settings: Settings, session_factory) -> None`. `collect_readiness_errors(...)` must reject these exact placeholder/default pairs unless explicitly overridden: `INVESTOR_APP_SECRET=change-me`, `INVESTOR_SMTP_HOST=smtp.example.com`, `INVESTOR_SMTP_PASSWORD=change-me`, `INVESTOR_SMTP_FROM_EMAIL=investor@example.com`, `INVESTOR_DAILY_MEMO_TO_EMAIL=operator@example.com`, `INVESTOR_EXTERNAL_BASE_URL=https://investor.example.com`, `INVESTOR_SCHEDULED_TRIGGER_TOKEN=change-me-scheduled-trigger`, `INVESTOR_QUIVER_BASE_URL=https://example.test`, `INVESTOR_QUIVER_API_KEY=secret`, `INVESTOR_ALPACA_API_KEY=secret`, and `INVESTOR_DATABASE_URL=sqlite+pysqlite:///./investor.db`. Keep the existing broker-mode validator in `app/config.py`, but remove or tighten operational defaults so these values are no longer treated as valid runtime configuration. `assert_startup_readiness(...)` must call `collect_readiness_errors(...)`, then open a session from the provided `session_factory` and execute `SELECT 1` through SQLAlchemy to prove the configured database is reachable; any collected or probe failure must raise `OperationalReadinessError` before startup continues. Update `app/main.py` to replace the plain `FastAPI(title=settings.app_name)` construction with a lifespan context that stores `settings`, `session_factory`, and the existing app state fields, but calls `assert_startup_readiness(settings, session_factory)` before `yield`; do not leave readiness checks buried in `/health`. Implement `app/ops/dry_run.py` as a module runnable with `python -m app.ops.dry_run` that: (1) creates a temporary SQLite database file and sets test-safe env overrides for all required operational vars; (2) builds the app with a `MailProviderSpy`, a deterministic `ResearchNode`/LLM output equivalent to the existing single-candidate NVDA path, and an `httpx.MockTransport` that returns Quiver payloads for `/beta/live/congresstrading`, `/beta/live/insiders`, `/beta/live/govcontracts`, and `/beta/live/lobbying`; (3) posts to `/runs/trigger/scheduled` with header `X-Investor-Scheduled-Trigger`; (4) extracts the signed approval URL from the captured memo text by splitting on `Approve: `; (5) calls that URL through the same `TestClient`; (6) queries persisted `BrokerArtifactRecord` rows for the returned `run_id`; and (7) prints one JSON object to stdout with exact keys `trigger_status`, `approval_status`, `run_id`, `approval_url`, `artifact_count`, and `log_lines`. Keep the dry-run deterministic and local-only; do not require real SMTP, Quiver, Alpaca, cron, or an already-running server.</action>
  <acceptance_criteria>
    - `rg -n "class OperationalReadinessError|def collect_readiness_errors\\(settings: Settings\\) -> list\\[str\\]|def assert_startup_readiness\\(settings: Settings, session_factory\\) -> None" app/ops/readiness.py` returns the readiness error type and both helper functions.
    - `rg -n "INVESTOR_APP_SECRET=change-me|INVESTOR_SMTP_HOST=smtp.example.com|INVESTOR_SMTP_PASSWORD=change-me|INVESTOR_SMTP_FROM_EMAIL=investor@example.com|INVESTOR_DAILY_MEMO_TO_EMAIL=operator@example.com|INVESTOR_EXTERNAL_BASE_URL=https://investor.example.com|INVESTOR_SCHEDULED_TRIGGER_TOKEN=change-me-scheduled-trigger|INVESTOR_QUIVER_BASE_URL=https://example.test|INVESTOR_QUIVER_API_KEY=secret|INVESTOR_ALPACA_API_KEY=secret|INVESTOR_DATABASE_URL=sqlite\\+pysqlite:///\\./investor\\.db" app/ops/readiness.py` returns every rejected placeholder value.
    - `rg -n "asynccontextmanager|assert_startup_readiness\\(settings, session_factory\\)|FastAPI\\(title=settings.app_name, lifespan=lifespan\\)" app/main.py` returns the lifespan-managed startup gate.
    - `rg -n "python -m app\\.ops\\.dry_run|X-Investor-Scheduled-Trigger|Approve: |trigger_status|approval_status|artifact_count" app/ops/dry_run.py` returns the exact command surface, header name, memo extraction marker, and JSON keys.
    - `rg -n "sqlite\\+pysqlite:///|MockTransport|BrokerArtifactRecord|/runs/trigger/scheduled" app/ops/dry_run.py` returns the local deterministic harness pieces and scheduled trigger path.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ops/test_readiness.py tests/ops/test_dry_run.py tests/api/test_routes.py tests/integration/test_broker_prestage.py -q</automated>
  </verify>
  <done>Startup blocks on incomplete or inconsistent configuration, the deterministic dry-run command completes the trigger-to-prestage path locally, and the new operational tests pass.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/ops/test_readiness.py tests/ops/test_dry_run.py tests/api/test_routes.py tests/integration/test_broker_prestage.py -q`. Then run `python -m app.ops.dry_run` and confirm the emitted JSON contains `approval_status` set to `broker_prestaged` and `artifact_count` greater than or equal to `1`.
</verification>

<success_criteria>
Phase 5 has an executable operational runtime contract: the app refuses bad env configuration at startup and one repo-owned command can prove the full daily workflow locally without relying on hidden manual approval, live network calls, or an already-running server.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-readiness/05-operational-readiness-01-SUMMARY.md`
</output>

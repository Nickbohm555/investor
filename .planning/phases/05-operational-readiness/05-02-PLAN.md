---
phase: 05-operational-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - README.md
  - .env.example
  - tests/ops/test_operational_docs.py
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - OPER-02
  - OPER-03
must_haves:
  truths:
    - "README tells the operator exactly how to bootstrap, migrate, dry-run, serve, install cron, and verify the app using repo-owned commands."
    - "`.env.example` contains every runtime variable needed for local cron, SMTP, Quiver, approval links, and Alpaca paper prestage."
    - "Documentation drift is caught by automated tests instead of manual spot checks."
  artifacts:
    - "README.md :: env-ready setup guide, dry-run instructions, go-live checklist, and acceptance verification commands"
    - ".env.example :: complete operational variable template for Phase 5 go-live"
    - "tests/ops/test_operational_docs.py :: doc assertions that lock README commands and env keys to the implemented runtime surface"
  key_links:
    - "README.md -> app/ops/dry_run.py :: README documents `python -m app.ops.dry_run` as the no-hidden-manual-work proof path"
    - ".env.example -> app/config.py :: every documented `INVESTOR_*` key matches a real settings field used by runtime startup"
    - "tests/ops/test_operational_docs.py -> README.md/.env.example :: automated assertions fail when commands or required env keys drift"
---

<objective>
Finalize the operator-facing contract so the repo’s docs and env template match the implemented readiness gate and dry-run flow exactly.

Purpose: Satisfy the documentation and handoff portion of operational readiness after the runtime surface exists.
Output: A complete `.env.example`, an env-ready README, automated doc drift tests, and updated roadmap plan references.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-operational-readiness/05-RESEARCH.md
@.planning/phases/05-operational-readiness/05-01-PLAN.md
@README.md
@.env.example
@app/config.py

<interfaces>
From app/config.py:
```python
class Settings(BaseSettings):
    app_name: str
    app_env: str
    app_secret: str
    database_url: str
    smtp_host: str
    smtp_port: int
    smtp_username: str
    smtp_password: str
    smtp_from_email: str
    daily_memo_to_email: str
    external_base_url: str
    schedule_cron_expression: str
    schedule_trigger_url: str
    scheduled_trigger_token: str
    cron_log_path: str
    quiver_base_url: str
    quiver_api_key: str
    broker_mode: Literal["paper", "live"]
    alpaca_base_url: str
    alpaca_api_key: str
    langgraph_checkpointer_url: Optional[str]
    approval_token_ttl_seconds: int
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add doc-proof tests for the final env template and operator command surface</name>
  <files>tests/ops/test_operational_docs.py, README.md, .env.example</files>
  <read_first>.planning/phases/05-operational-readiness/05-RESEARCH.md, .planning/phases/05-operational-readiness/05-01-PLAN.md, README.md, .env.example, app/config.py</read_first>
  <behavior>
    - Test 1: `.env.example` contains every required `INVESTOR_*` variable used by runtime startup and dry-run.
    - Test 2: `README.md` documents the exact bootstrap, migration, dry-run, serve, cron, and verification commands implemented in the repo.
    - Test 3: README contains a go-live checklist covering local cron, SMTP credentials, Quiver API access, external approval URL host, and Alpaca paper mode.
  </behavior>
  <action>Create `tests/ops/test_operational_docs.py` first with failing tests named `test_env_example_lists_all_required_operational_variables`, `test_readme_documents_repo_owned_bootstrap_and_dry_run_commands`, and `test_readme_contains_go_live_checklist_sections`. Make the env-file test require these exact keys in `.env.example`: `INVESTOR_APP_NAME`, `INVESTOR_APP_ENV`, `INVESTOR_APP_SECRET`, `INVESTOR_DATABASE_URL`, `INVESTOR_SMTP_HOST`, `INVESTOR_SMTP_PORT`, `INVESTOR_SMTP_USERNAME`, `INVESTOR_SMTP_PASSWORD`, `INVESTOR_SMTP_FROM_EMAIL`, `INVESTOR_DAILY_MEMO_TO_EMAIL`, `INVESTOR_EXTERNAL_BASE_URL`, `INVESTOR_SCHEDULE_CRON_EXPRESSION`, `INVESTOR_SCHEDULE_TRIGGER_URL`, `INVESTOR_SCHEDULED_TRIGGER_TOKEN`, `INVESTOR_CRON_LOG_PATH`, `INVESTOR_QUIVER_BASE_URL`, `INVESTOR_QUIVER_API_KEY`, `INVESTOR_BROKER_MODE`, `INVESTOR_ALPACA_BASE_URL`, `INVESTOR_ALPACA_API_KEY`, `INVESTOR_LANGGRAPH_CHECKPOINTER_URL`, and `INVESTOR_APPROVAL_TOKEN_TTL_SECONDS`. Make the README-command test require these exact command strings: `pip install -e .`, `cp .env.example .env`, `docker compose up -d postgres`, `alembic upgrade head`, `python -m app.ops.dry_run`, `uvicorn app.main:app --reload`, `./scripts/cron-install.sh`, `./scripts/cron-status.sh`, `./scripts/cron-trigger.sh`, `./scripts/cron-remove.sh`, and `python -m pytest tests/ops/test_readiness.py tests/ops/test_dry_run.py tests/ops/test_operational_docs.py -q`. Make the checklist test require the README headings `## Dry Run`, `## Go-Live Checklist`, and `## Acceptance Verification`, plus the exact checklist lines `- SMTP credentials send to the real operator inbox`, `- Quiver API key and base URL point to the intended account`, `- INVESTOR_EXTERNAL_BASE_URL resolves to the public approval host`, `- Alpaca paper mode uses https://paper-api.alpaca.markets`, and `- Cron is installed with ./scripts/cron-install.sh and verified with ./scripts/cron-status.sh`.</action>
  <acceptance_criteria>
    - `rg -n "test_env_example_lists_all_required_operational_variables|test_readme_documents_repo_owned_bootstrap_and_dry_run_commands|test_readme_contains_go_live_checklist_sections" tests/ops/test_operational_docs.py` returns all three documentation tests.
    - `rg -n "INVESTOR_APP_NAME|INVESTOR_APP_ENV|INVESTOR_APP_SECRET|INVESTOR_DATABASE_URL|INVESTOR_SMTP_HOST|INVESTOR_SMTP_PORT|INVESTOR_SMTP_USERNAME|INVESTOR_SMTP_PASSWORD|INVESTOR_SMTP_FROM_EMAIL|INVESTOR_DAILY_MEMO_TO_EMAIL|INVESTOR_EXTERNAL_BASE_URL|INVESTOR_SCHEDULE_CRON_EXPRESSION|INVESTOR_SCHEDULE_TRIGGER_URL|INVESTOR_SCHEDULED_TRIGGER_TOKEN|INVESTOR_CRON_LOG_PATH|INVESTOR_QUIVER_BASE_URL|INVESTOR_QUIVER_API_KEY|INVESTOR_BROKER_MODE|INVESTOR_ALPACA_BASE_URL|INVESTOR_ALPACA_API_KEY|INVESTOR_LANGGRAPH_CHECKPOINTER_URL|INVESTOR_APPROVAL_TOKEN_TTL_SECONDS" tests/ops/test_operational_docs.py` returns every required env key.
    - `rg -n "pip install -e \\.|cp \\.env\\.example \\.env|docker compose up -d postgres|alembic upgrade head|python -m app\\.ops\\.dry_run|uvicorn app\\.main:app --reload|\\./scripts/cron-install\\.sh|\\./scripts/cron-status\\.sh|\\./scripts/cron-trigger\\.sh|\\./scripts/cron-remove\\.sh|python -m pytest tests/ops/test_readiness\\.py tests/ops/test_dry_run\\.py tests/ops/test_operational_docs\\.py -q" tests/ops/test_operational_docs.py` returns all exact operator commands.
    - `rg -n "## Dry Run|## Go-Live Checklist|## Acceptance Verification|SMTP credentials send to the real operator inbox|Quiver API key and base URL point to the intended account|INVESTOR_EXTERNAL_BASE_URL resolves to the public approval host|Alpaca paper mode uses https://paper-api.alpaca.markets|Cron is installed with \\./scripts/cron-install\\.sh and verified with \\./scripts/cron-status\\.sh" tests/ops/test_operational_docs.py` returns the required headings and checklist lines.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>The repo has failing doc-proof coverage that locks the final env template and operator command surface to exact strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite the README and env template to make fill-vars-and-run the final setup step</name>
  <files>README.md, .env.example, tests/ops/test_operational_docs.py, .planning/ROADMAP.md</files>
  <read_first>.planning/phases/05-operational-readiness/05-RESEARCH.md, .planning/phases/05-operational-readiness/05-01-PLAN.md, README.md, .env.example, app/config.py, tests/ops/test_operational_docs.py</read_first>
  <behavior>
    - Test 1: `.env.example` enumerates every operational variable with phase-accurate defaults or placeholders matching the implemented readiness gate.
    - Test 2: README walks the operator through bootstrap, migrations, dry-run, serving, cron install/remove, and acceptance verification using repo-owned commands only.
    - Test 3: ROADMAP Phase 5 plan list reflects the final plan filenames and objectives actually created in this planning step.
  </behavior>
  <action>Update `.env.example` so it contains one line for each required runtime variable in this exact set and order: `INVESTOR_APP_NAME=investor`, `INVESTOR_APP_ENV=development`, `INVESTOR_APP_SECRET=replace-with-long-random-secret`, `INVESTOR_DATABASE_URL=postgresql://investor:investor@localhost:5432/investor`, `INVESTOR_SMTP_HOST=smtp.example.com`, `INVESTOR_SMTP_PORT=587`, `INVESTOR_SMTP_USERNAME=investor-user`, `INVESTOR_SMTP_PASSWORD=replace-with-smtp-password`, `INVESTOR_SMTP_FROM_EMAIL=investor@example.com`, `INVESTOR_DAILY_MEMO_TO_EMAIL=operator@example.com`, `INVESTOR_EXTERNAL_BASE_URL=https://investor.example.com`, `INVESTOR_SCHEDULE_CRON_EXPRESSION=30 8 * * 1-5`, `INVESTOR_SCHEDULE_TRIGGER_URL=http://127.0.0.1:8000/runs/trigger/scheduled`, `INVESTOR_SCHEDULED_TRIGGER_TOKEN=replace-with-scheduled-trigger-token`, `INVESTOR_CRON_LOG_PATH=logs/cron/daily-trigger.log`, `INVESTOR_QUIVER_BASE_URL=https://api.quiverquant.com`, `INVESTOR_QUIVER_API_KEY=replace-with-quiver-api-key`, `INVESTOR_BROKER_MODE=paper`, `INVESTOR_ALPACA_BASE_URL=https://paper-api.alpaca.markets`, `INVESTOR_ALPACA_API_KEY=replace-with-alpaca-api-key`, `INVESTOR_LANGGRAPH_CHECKPOINTER_URL=postgresql://investor:investor@localhost:5432/investor`, and `INVESTOR_APPROVAL_TOKEN_TTL_SECONDS=900`. Rewrite `README.md` so it includes these exact sections and command blocks: `## Setup`, `## Dry Run`, `## Run The Service`, `## Cron Operations`, `## Go-Live Checklist`, and `## Acceptance Verification`. In `## Setup`, include the exact commands `pip install -e .`, `cp .env.example .env`, `docker compose up -d postgres`, and `alembic upgrade head`. In `## Dry Run`, document `python -m app.ops.dry_run` as the canonical no-hidden-manual-work proof path and state that it runs scheduled trigger, memo generation, approval callback, and broker prestage locally. In `## Run The Service`, include `uvicorn app.main:app --reload`. In `## Cron Operations`, include install, status, trigger, and remove commands exactly. In `## Acceptance Verification`, include the exact pytest command `python -m pytest tests/ops/test_readiness.py tests/ops/test_dry_run.py tests/ops/test_operational_docs.py -q` plus one full-workflow command example `python -m app.ops.dry_run`. In `## Go-Live Checklist`, include these exact bullet lines: `- SMTP credentials send to the real operator inbox`, `- Quiver API key and base URL point to the intended account`, `- INVESTOR_EXTERNAL_BASE_URL resolves to the public approval host`, `- Alpaca paper mode uses https://paper-api.alpaca.markets`, and `- Cron is installed with ./scripts/cron-install.sh and verified with ./scripts/cron-status.sh`. Update `.planning/ROADMAP.md` Phase 5 plan list to the exact entries `- [ ] 05-01-PLAN.md — Add startup readiness gating and a deterministic dry-run command` and `- [ ] 05-02-PLAN.md — Lock docs and env templates to the final operator workflow`.</action>
  <acceptance_criteria>
    - `rg -n "^INVESTOR_APP_NAME=investor$|^INVESTOR_APP_ENV=development$|^INVESTOR_APP_SECRET=replace-with-long-random-secret$|^INVESTOR_DATABASE_URL=postgresql://investor:investor@localhost:5432/investor$|^INVESTOR_SMTP_HOST=smtp.example.com$|^INVESTOR_SMTP_PORT=587$|^INVESTOR_SMTP_USERNAME=investor-user$|^INVESTOR_SMTP_PASSWORD=replace-with-smtp-password$|^INVESTOR_SMTP_FROM_EMAIL=investor@example.com$|^INVESTOR_DAILY_MEMO_TO_EMAIL=operator@example.com$|^INVESTOR_EXTERNAL_BASE_URL=https://investor.example.com$|^INVESTOR_SCHEDULE_CRON_EXPRESSION=30 8 \\* \\* 1-5$|^INVESTOR_SCHEDULE_TRIGGER_URL=http://127.0.0.1:8000/runs/trigger/scheduled$|^INVESTOR_SCHEDULED_TRIGGER_TOKEN=replace-with-scheduled-trigger-token$|^INVESTOR_CRON_LOG_PATH=logs/cron/daily-trigger.log$|^INVESTOR_QUIVER_BASE_URL=https://api.quiverquant.com$|^INVESTOR_QUIVER_API_KEY=replace-with-quiver-api-key$|^INVESTOR_BROKER_MODE=paper$|^INVESTOR_ALPACA_BASE_URL=https://paper-api.alpaca.markets$|^INVESTOR_ALPACA_API_KEY=replace-with-alpaca-api-key$|^INVESTOR_LANGGRAPH_CHECKPOINTER_URL=postgresql://investor:investor@localhost:5432/investor$|^INVESTOR_APPROVAL_TOKEN_TTL_SECONDS=900$" .env.example` returns every required env line.
    - `rg -n "^## Setup$|^## Dry Run$|^## Run The Service$|^## Cron Operations$|^## Go-Live Checklist$|^## Acceptance Verification$|pip install -e \\.|cp \\.env\\.example \\.env|docker compose up -d postgres|alembic upgrade head|python -m app\\.ops\\.dry_run|uvicorn app\\.main:app --reload|\\./scripts/cron-install\\.sh|\\./scripts/cron-status\\.sh|\\./scripts/cron-trigger\\.sh|\\./scripts/cron-remove\\.sh|python -m pytest tests/ops/test_readiness\\.py tests/ops/test_dry_run\\.py tests/ops/test_operational_docs\\.py -q" README.md` returns every required section heading and command.
    - `rg -n "SMTP credentials send to the real operator inbox|Quiver API key and base URL point to the intended account|INVESTOR_EXTERNAL_BASE_URL resolves to the public approval host|Alpaca paper mode uses https://paper-api.alpaca.markets|Cron is installed with \\./scripts/cron-install\\.sh and verified with \\./scripts/cron-status\\.sh" README.md` returns all required checklist bullets.
    - `rg -n "05-01-PLAN.md — Add startup readiness gating and a deterministic dry-run command|05-02-PLAN.md — Lock docs and env templates to the final operator workflow" .planning/ROADMAP.md` returns both final Phase 5 plan entries.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ops/test_operational_docs.py -q</automated>
  </verify>
  <done>The docs and env template exactly match the implemented operational workflow, README drift is test-covered, and the roadmap points to the final Phase 5 plans.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/ops/test_operational_docs.py -q`. Then manually inspect `README.md` and `.env.example` only to confirm the command order follows setup -> dry run -> service -> cron -> checklist -> acceptance verification with no missing runtime variables.
</verification>

<success_criteria>
The repository’s operator documentation is now an exact executable contract: an operator can fill `.env`, run the documented bootstrap commands, prove the end-to-end workflow with `python -m app.ops.dry_run`, and follow a concrete go-live checklist without guessing missing steps.
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-readiness/05-operational-readiness-02-SUMMARY.md`
</output>

---
phase: 02-quiver-research-and-ranking
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - app/tools/quiver.py
  - app/services/quiver_normalize.py
  - app/services/ranking.py
  - app/services/email.py
  - app/graph/workflow.py
  - app/api/routes.py
  - tests/services/test_ranking.py
  - tests/services/test_email.py
  - tests/graph/test_workflow.py
  - tests/api/test_routes.py
autonomous: true
requirements:
  - RSCH-01
  - RSCH-03
  - RSCH-04
must_haves:
  truths:
    - "Candidates are ranked and pruned deterministically by conviction, evidence count, duplicate ticker collapse, broker eligibility, and stable ticker tie-breaks."
    - "Weak or conflicting evidence downgrades the daily result to a watchlist or no-action branch instead of forcing candidates."
    - "The workflow email body clearly renders ranked candidates, watchlist items, or no-action reasons."
  artifacts:
    - path: "app/services/ranking.py"
      provides: "Deterministic ranking, pruning, and watchlist/no-action downgrade logic"
      contains: "def finalize_research_outcome"
    - path: "app/graph/workflow.py"
      provides: "Workflow integration of evidence input, ranked output, and branch-aware email composition"
      contains: "finalize_research_outcome"
    - path: "app/services/email.py"
      provides: "Email rendering for candidate, watchlist, and no-action outcomes"
      contains: "Watchlist"
  key_links:
    - from: "app/graph/workflow.py"
      to: "app/services/ranking.py"
      via: "Workflow finalizes research output before composing email"
      pattern: "finalize_research_outcome"
    - from: "app/services/email.py"
      to: "app/schemas/research.py"
      via: "Email rendering branches on structured outcome type"
      pattern: "candidates|watchlist|no_action"
---

<objective>
Implement deterministic ranking, pruning, and no-action/watchlist behavior in the workflow.

Purpose: Satisfy the runtime portion of `RSCH-01` plus `RSCH-03` and `RSCH-04` by wiring multi-signal Quiver querying into the live workflow, keeping ranking decisions in Python, and ensuring delivery behavior is stable and branch-aware.
Output: A ranking service, runtime workflow and API integration for finalized outcomes, and email rendering/tests for candidates, watchlist, and no-action paths.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-quiver-research-and-ranking/02-RESEARCH.md
@.planning/phases/02-quiver-research-and-ranking/02-02-PLAN.md
@app/api/routes.py
@app/graph/workflow.py
@app/services/email.py
@app/services/risk.py
@tests/api/test_routes.py
@tests/graph/test_workflow.py
@tests/services/test_email.py

<interfaces>
From 02-02 plan output:
```python
class CandidateRecommendation(BaseModel):
    ticker: str
    action: str
    conviction_score: float
    supporting_evidence: list[str]
    opposing_evidence: list[str]
    risk_notes: list[str]
    source_summary: list[str]
    broker_eligible: bool


ResearchOutcome = Annotated[
    CandidateOutcome | WatchlistOutcome | NoActionOutcome,
    Field(discriminator="outcome"),
]
```

From app/services/email.py:
```python
def compose_recommendation_email(
    run_id: str,
    recommendations: list,
    approval_url: str,
    rejection_url: str,
) -> RecommendationEmail:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic ranking and downgrade rules for research outcomes</name>
  <files>app/services/ranking.py
tests/services/test_ranking.py</files>
  <read_first>.planning/REQUIREMENTS.md
.planning/phases/02-quiver-research-and-ranking/02-RESEARCH.md
app/services/risk.py
app/schemas/research.py</read_first>
  <behavior>
    - Test 1: Duplicate ticker candidates collapse to one entry and the remaining entry is the highest-ranked version.
    - Test 2: Candidates are sorted by `conviction_score` descending, `len(supporting_evidence)` descending, `len(opposing_evidence)` ascending, then `ticker` ascending.
    - Test 3: `finalize_research_outcome()` returns `WatchlistOutcome` when at least one candidate survives deterministic pruning, fewer than `max_ideas` survive, and every surviving candidate has either `conviction_score < 0.75` or `len(supporting_evidence) <= len(opposing_evidence) + 1`.
    - Test 4: `finalize_research_outcome()` returns `NoActionOutcome` when zero candidates survive pruning and the reasons list includes `No candidates survived deterministic pruning.`.
  </behavior>
  <action>Create `app/services/ranking.py` with `rank_candidates`, `dedupe_candidates_by_ticker`, and `finalize_research_outcome`. `finalize_research_outcome` must accept `outcome: ResearchOutcome`, `minimum_conviction: float = 0.6`, `max_ideas: int = 3`, `allowed_conflict_gap: int = 0`, and `watchlist_conviction_ceiling: float = 0.75`. For `CandidateOutcome`, drop any recommendation whose `conviction_score` is below `0.6`, whose `broker_eligible` field is `False`, or whose `len(opposing_evidence)` is greater than `len(supporting_evidence) + allowed_conflict_gap`. Deduplicate by uppercase `ticker`, keep the strongest candidate per ticker, then sort with this exact tuple key: `(-conviction_score, -len(supporting_evidence), len(opposing_evidence), ticker)`. If zero candidates survive, return `NoActionOutcome` with reasons that include the exact phrase `No candidates survived deterministic pruning.`. If at least one candidate survives but fewer than `max_ideas` survive and every surviving candidate satisfies either `conviction_score < watchlist_conviction_ceiling` or `len(supporting_evidence) <= len(opposing_evidence) + 1`, return `WatchlistOutcome` with `summary="Signals remain mixed after deterministic pruning."` and preserve each surviving item in `items`. Otherwise return `CandidateOutcome` with the ranked surviving recommendations truncated to `max_ideas`.</action>
  <acceptance_criteria>- `rg -n \"def rank_candidates|def dedupe_candidates_by_ticker|def finalize_research_outcome\" app/services/ranking.py` returns all three functions.
- `rg -n \"0\\.6|0\\.75|max_ideas|allowed_conflict_gap|watchlist_conviction_ceiling|No candidates survived deterministic pruning\\.|Signals remain mixed after deterministic pruning\\.\" app/services/ranking.py tests/services/test_ranking.py` returns the exact thresholds and downgrade literals.
- `rg -n \"-len\\(item.supporting_evidence\\)|len\\(item.opposing_evidence\\)|item.ticker\" app/services/ranking.py` returns the stable sort tuple components.
- `pytest tests/services/test_ranking.py -q` exits with code 0.</acceptance_criteria>
  <verify>
    <automated>pytest tests/services/test_ranking.py -q</automated>
  </verify>
  <done>Phase 2 has a deterministic Python ranking layer that prunes weak, duplicate, conflicting, and broker-ineligible ideas and downgrades empty results safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire finalized research outcomes into the workflow and branch-aware email rendering</name>
  <files>app/services/email.py
app/graph/workflow.py
tests/services/test_email.py
tests/graph/test_workflow.py</files>
  <read_first>.planning/phases/02-quiver-research-and-ranking/02-RESEARCH.md
app/graph/workflow.py
app/services/email.py
app/services/ranking.py
app/schemas/research.py
tests/graph/test_workflow.py
tests/services/test_email.py</read_first>
  <behavior>
    - Test 1: Workflow invoke calls `QuiverClient.get_live_congress_trading()`, `get_live_insider_trading()`, `get_live_government_contracts()`, and `get_live_lobbying()`, passes those results into `build_ticker_evidence_bundles(congress=..., insiders=..., gov_contracts=..., lobbying=...)`, stores the returned `evidence_bundles` on state, and then calls the research node with `run_id`, `evidence_bundles`, and `account_context`.
    - Test 2: Candidate emails render a `Ranked Candidates` section with ticker order matching deterministic ranking.
    - Test 3: Watchlist and no-action outcomes render `Watchlist` or `No Action` headings and preserve summary/reason text in the email body.
    - Test 4: `resume(..., decision="approve")` still calls `build_alpaca_handoff` with `state["recommendations"]`, where `recommendations` equals the finalized candidate list for `CandidateOutcome` and `[]` for `WatchlistOutcome` or `NoActionOutcome`.
    - Test 5: `POST /runs/trigger` still returns `202`, constructs `compile_workflow(research_node=ResearchNode(llm=StaticLLM()), quiver_client=QuiverClient(...))`, and stores paused state that includes `evidence_bundles` plus a discriminated `finalized_outcome`.
  </behavior>
  <action>Update `app/graph/workflow.py` so `CompiledInvestorWorkflow.__init__` accepts `research_node`, `quiver_client`, and an optional `evidence_builder` defaulting to `build_ticker_evidence_bundles`. In `invoke`, fetch Quiver rows by calling `self._quiver_client.get_live_congress_trading()`, `get_live_insider_trading()`, `get_live_government_contracts()`, and `get_live_lobbying()`, then build `evidence_bundles = self._evidence_builder(congress=congress_rows, insiders=insider_rows, gov_contracts=gov_contract_rows, lobbying=lobbying_rows)`. Pass `run_id=state["run_id"]`, `evidence_bundles=evidence_bundles`, and `account_context=state` into the research node, then pass the structured result through `finalize_research_outcome`. Store both `evidence_bundles` and `finalized_outcome` on workflow state, and also store `recommendations` as `finalized_outcome.recommendations` for `CandidateOutcome` or `[]` for `WatchlistOutcome` and `NoActionOutcome` so `resume()` continues to call `build_alpaca_handoff(state["run_id"], state["recommendations"])` without breaking later phases. Update `app/api/routes.py` so `trigger_run` constructs `QuiverClient` with the app settings values already used for runtime configuration, passes that instance into `compile_workflow(...)`, and changes `StaticLLM.invoke` to return one valid discriminated JSON branch such as `{\"outcome\":\"candidates\",\"recommendations\":[{\"ticker\":\"NVDA\",\"action\":\"buy\",\"conviction_score\":0.81,\"supporting_evidence\":[\"Congress buy\"],\"opposing_evidence\":[],\"risk_notes\":[\"Volatile\"],\"source_summary\":[\"Congress and insider signals aligned\"],\"broker_eligible\":true}]}` so the trigger path matches the Phase 2 schema. Update `app/services/email.py` so `compose_recommendation_email` accepts `outcome: ResearchOutcome` instead of a bare recommendation list and renders three exact section headings depending on branch: `Ranked Candidates`, `Watchlist`, and `No Action`. Candidate rows must include ticker, action, conviction, and joined `supporting_evidence` / `risk_notes`; watchlist items must render `summary`; no-action bodies must render each reason on its own line. Extend workflow, email, and API tests with a stub `QuiverClient` and a stub `evidence_builder` so they assert the four fetch calls, `evidence_bundles` state, branch-specific headings, ranked ticker order, `POST /runs/trigger` returning `202`, and approval resume/handoff behavior for candidate and non-candidate branches.</action>
  <acceptance_criteria>- `rg -n \"finalize_research_outcome|evidence_bundles|finalized_outcome\" app/graph/workflow.py` returns the new workflow integration points.
- `rg -n \"get_live_congress_trading|get_live_insider_trading|get_live_government_contracts|get_live_lobbying|build_ticker_evidence_bundles|state\\[\\\"recommendations\\\"\\]\" app/graph/workflow.py tests/graph/test_workflow.py` returns the evidence-ingestion and handoff-preservation points.
- `rg -n \"QuiverClient|StaticLLM|outcome\\\":\\\"candidates\\\"|POST /runs/trigger|/runs/trigger\" app/api/routes.py tests/api/test_routes.py` returns the route/runtime wiring and discriminated fixture shape.
- `rg -n \"Ranked Candidates|Watchlist|No Action\" app/services/email.py tests/services/test_email.py tests/graph/test_workflow.py` returns all three exact heading strings.
- `rg -n \"compose_recommendation_email\\(|outcome: ResearchOutcome\" app/services/email.py` shows the branch-aware email signature.
- `pytest tests/services/test_email.py tests/graph/test_workflow.py tests/api/test_routes.py -q` exits with code 0.</acceptance_criteria>
  <verify>
    <automated>pytest tests/services/test_email.py tests/graph/test_workflow.py tests/api/test_routes.py -q</automated>
  </verify>
  <done>The workflow finalizes structured research output deterministically and the email layer renders candidates, watchlist, and no-action branches explicitly.</done>
</task>

</tasks>

<verification>
Run `pytest tests/services/test_ranking.py tests/services/test_email.py tests/graph/test_workflow.py tests/api/test_routes.py -q` and confirm the workflow now queries Quiver before synthesis, stores finalized outcomes, and keeps the trigger path passing across `candidates`, `watchlist`, and `no_action`.
</verification>

<success_criteria>
`RSCH-01`, `RSCH-03`, and `RSCH-04` are covered by runtime Quiver querying before synthesis, deterministic Python ranking/pruning logic, and explicit workflow/email/API branches that avoid forced candidates when evidence is weak or conflicting.
</success_criteria>

<output>
After completion, create `.planning/phases/02-quiver-research-and-ranking/02-03-SUMMARY.md`
</output>

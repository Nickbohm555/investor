---
phase: 02-quiver-research-and-ranking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/quiver.py
  - app/tools/quiver.py
  - app/services/quiver_normalize.py
  - tests/tools/test_clients.py
  - tests/services/test_quiver_normalize.py
autonomous: true
requirements:
  - RSCH-01
must_haves:
  truths:
    - "The research layer can fetch congressional trading, insider trading, government contracts, and lobbying data through typed Quiver client methods."
    - "Raw Quiver payloads are normalized into per-ticker evidence bundles before any LLM synthesis step."
  artifacts:
    - path: "app/schemas/quiver.py"
      provides: "Typed Quiver endpoint models and canonical signal bundle models"
      contains: "class TickerEvidenceBundle"
    - path: "app/tools/quiver.py"
      provides: "Typed Quiver client methods for four live datasets"
      contains: "def get_live_lobbying"
    - path: "app/services/quiver_normalize.py"
      provides: "Deterministic normalization from dataset rows to canonical evidence bundles"
      contains: "def build_ticker_evidence_bundles"
  key_links:
    - from: "app/tools/quiver.py"
      to: "app/schemas/quiver.py"
      via: "Pydantic validation of each endpoint response"
      pattern: "TypeAdapter|validate_python|TickerEvidenceBundle"
    - from: "app/services/quiver_normalize.py"
      to: "app/schemas/quiver.py"
      via: "Normalization output uses canonical signal models"
      pattern: "SignalRecord|TickerEvidenceBundle"
---

<objective>
Create the typed Quiver ingestion and normalization foundation for Phase 2.

Purpose: Satisfy `RSCH-01` with concrete multi-signal inputs before any prompt or ranking work.
Output: Quiver dataset schemas, four typed client methods, and deterministic per-ticker evidence bundles with tests.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-quiver-research-and-ranking/02-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@app/tools/quiver.py
@tests/tools/test_clients.py

<interfaces>
From app/tools/quiver.py:
```python
class CongressTrade(BaseModel):
    ticker: str
    transaction: str


class QuiverClient:
    def __init__(self, base_url: str, api_key: str, transport: httpx.BaseTransport | None = None):
        ...

    def get_congress_trades(self) -> list[CongressTrade]:
        ...
```

From 02-RESEARCH.md:
```python
class SignalRecord(BaseModel):
    signal_type: Literal["congress", "insider", "gov_contract", "lobbying"]
    ticker: str
    observed_at: datetime
    direction: Literal["positive", "negative", "mixed", "neutral"]
    magnitude_note: str
    source_note: str


class TickerEvidenceBundle(BaseModel):
    ticker: str
    supporting_signals: list[SignalRecord]
    contradictory_signals: list[SignalRecord]
    source_summary: list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build typed Quiver dataset contracts and client methods</name>
  <files>app/schemas/quiver.py
app/tools/quiver.py
tests/tools/test_clients.py</files>
  <read_first>.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/02-quiver-research-and-ranking/02-RESEARCH.md
app/tools/quiver.py
tests/tools/test_clients.py</read_first>
  <behavior>
    - Test 1: `QuiverClient.get_live_congress_trading()` validates `/beta/live/congresstrading` responses into a typed list.
    - Test 2: `QuiverClient.get_live_insider_trading()`, `get_live_government_contracts()`, and `get_live_lobbying()` each send the `X-API-Key` header and validate their JSON payloads.
    - Test 3: Optional `ticker` filtering is passed as a query parameter only when a ticker argument is provided.
  </behavior>
  <action>Create `app/schemas/quiver.py` with raw endpoint models for `CongressionalTrade`, `InsiderTrade`, `GovernmentContractAward`, and `LobbyingDisclosure`, plus shared type aliases for `signal_type` and `direction`. Update `app/tools/quiver.py` to replace the current single `get_congress_trades()` method with four explicit methods named `get_live_congress_trading`, `get_live_insider_trading`, `get_live_government_contracts`, and `get_live_lobbying`. Each method must call these exact paths: `/beta/live/congresstrading`, `/beta/live/insiders`, `/beta/live/govcontracts`, and `/beta/live/lobbying`. Use `TypeAdapter(list[ModelName]).validate_python(response.json())` for parsing instead of manual dict indexing. Extend `tests/tools/test_clients.py` to cover all four methods, assert the request path, assert the `X-API-Key` header value `secret`, and assert `params={"ticker": "NVDA"}` appears only for calls that pass `ticker=\"NVDA\"`.</action>
  <acceptance_criteria>- `rg -n \"class CongressionalTrade|class InsiderTrade|class GovernmentContractAward|class LobbyingDisclosure\" app/schemas/quiver.py` returns four model definitions.
- `rg -n \"def get_live_congress_trading|def get_live_insider_trading|def get_live_government_contracts|def get_live_lobbying\" app/tools/quiver.py` returns four method definitions.
- `rg -n \"/beta/live/congresstrading|/beta/live/insiders|/beta/live/govcontracts|/beta/live/lobbying\" app/tools/quiver.py tests/tools/test_clients.py` returns all four endpoint strings.
- `pytest tests/tools/test_clients.py -q` exits with code 0.</acceptance_criteria>
  <verify>
    <automated>pytest tests/tools/test_clients.py -q</automated>
  </verify>
  <done>The Quiver adapter exposes four typed live dataset methods and the client tests prove request shape and response parsing for each dataset.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize Quiver rows into canonical ticker evidence bundles</name>
  <files>app/schemas/quiver.py
app/services/quiver_normalize.py
tests/services/test_quiver_normalize.py</files>
  <read_first>.planning/REQUIREMENTS.md
.planning/phases/02-quiver-research-and-ranking/02-RESEARCH.md
app/schemas/quiver.py
app/services/risk.py</read_first>
  <behavior>
    - Test 1: Mixed raw rows for one ticker collapse into one `TickerEvidenceBundle` keyed by canonical uppercase ticker.
    - Test 2: Insider and congress purchase-like rows land in `supporting_signals`, while sale-like or explicitly adverse rows land in `contradictory_signals`.
    - Test 3: Contracts and lobbying rows produce `source_summary` lines and `direction` values without being treated as unconditional buy signals.
  </behavior>
  <action>Extend `app/schemas/quiver.py` with canonical normalization models `SignalRecord` and `TickerEvidenceBundle`. Create `app/services/quiver_normalize.py` exporting `normalize_congressional_trades`, `normalize_insider_trades`, `normalize_government_contracts`, `normalize_lobbying`, and `build_ticker_evidence_bundles`. `build_ticker_evidence_bundles` must accept four keyword arguments named `congress`, `insiders`, `gov_contracts`, and `lobbying`, normalize all rows, uppercase tickers, merge rows per ticker, sort bundles by `ticker`, and preserve separate `supporting_signals`, `contradictory_signals`, and `source_summary` lists. Encode these dataset rules directly: congressional `Purchase` is `positive`, congressional `Sale` is `negative`, insider `Buy` is `positive`, insider `Sell` is `negative`, government contracts default to `neutral` unless the row explicitly marks cancellation/termination, and lobbying defaults to `mixed` with a contextual `source_note`.</action>
  <acceptance_criteria>- `rg -n \"class SignalRecord|class TickerEvidenceBundle\" app/schemas/quiver.py` returns both canonical model names.
- `rg -n \"def normalize_congressional_trades|def normalize_insider_trades|def normalize_government_contracts|def normalize_lobbying|def build_ticker_evidence_bundles\" app/services/quiver_normalize.py` returns five functions.
- `rg -n \"Purchase|Sale|Buy|Sell|neutral|mixed\" app/services/quiver_normalize.py` returns the explicit direction-mapping literals.
- `pytest tests/services/test_quiver_normalize.py -q` exits with code 0.</acceptance_criteria>
  <verify>
    <automated>pytest tests/services/test_quiver_normalize.py -q</automated>
  </verify>
  <done>Phase 2 has a deterministic evidence-bundle layer that turns Quiver rows into canonical per-ticker signals with test coverage for direction and deduping behavior.</done>
</task>

</tasks>

<verification>
Run `pytest tests/tools/test_clients.py tests/services/test_quiver_normalize.py -q` and confirm the codebase now has one typed Quiver schema module and one normalization module for the four required datasets.
</verification>

<success_criteria>
`RSCH-01` is covered by code artifacts that fetch the four required Quiver datasets and convert them into canonical evidence bundles before any prompt generation or ranking logic runs.
</success_criteria>

<output>
After completion, create `.planning/phases/02-quiver-research-and-ranking/02-01-SUMMARY.md`
</output>

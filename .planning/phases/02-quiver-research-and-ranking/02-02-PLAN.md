---
phase: 02-quiver-research-and-ranking
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - app/schemas/research.py
  - app/schemas/workflow.py
  - app/agents/research.py
  - app/services/research_prompt.py
  - tests/graph/test_research_node.py
  - tests/services/test_research_prompt.py
autonomous: true
requirements:
  - RSCH-02
must_haves:
  truths:
    - "Each recommendation branch carries supporting evidence, opposing evidence, risk notes, and a concise source summary in validated schema fields."
    - "The research node accepts normalized evidence bundles and returns one explicit outcome type: candidates, watchlist, or no_action."
  artifacts:
    - path: "app/schemas/research.py"
      provides: "Discriminated recommendation outcome schema with evidence-rich recommendation models"
      contains: "ResearchOutcome = Annotated["
    - path: "app/services/research_prompt.py"
      provides: "Prompt assembly from canonical ticker evidence bundles"
      contains: "def build_research_prompt_payload"
    - path: "app/agents/research.py"
      provides: "Research node parsing validated structured outcomes"
      contains: "TypeAdapter(ResearchOutcome)"
  key_links:
    - from: "app/services/research_prompt.py"
      to: "app/schemas/quiver.py"
      via: "Prompt payload consumes TickerEvidenceBundle inputs"
      pattern: "TickerEvidenceBundle"
    - from: "app/agents/research.py"
      to: "app/schemas/research.py"
      via: "LLM response is parsed into discriminated output model"
      pattern: "ResearchOutcome|CandidateOutcome|WatchlistOutcome|NoActionOutcome"
---

<objective>
Create the evidence-rich prompt and structured output contract for research synthesis.

Purpose: Satisfy `RSCH-02` by making every synthesis result explicit, validated, and evidence-dense before ranking logic runs.
Output: A dedicated research schema module, a prompt builder that consumes normalized evidence, and a research node that validates discriminated outcomes.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-quiver-research-and-ranking/02-RESEARCH.md
@.planning/phases/02-quiver-research-and-ranking/02-01-PLAN.md
@app/agents/research.py
@app/schemas/workflow.py
@tests/graph/test_research_node.py

<interfaces>
From 02-01 plan output:
```python
class TickerEvidenceBundle(BaseModel):
    ticker: str
    supporting_signals: list[SignalRecord]
    contradictory_signals: list[SignalRecord]
    source_summary: list[str]
```

From app/agents/research.py:
```python
class InvokableLLM(Protocol):
    def invoke(self, payload: dict[str, str]) -> str:
        ...


class ResearchNode:
    def __init__(self, llm: InvokableLLM):
        ...

    def run(self, account_context: dict[str, str]) -> ResearchResult:
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define discriminated research outcome schemas with explicit evidence fields</name>
  <files>app/schemas/research.py
app/schemas/workflow.py
tests/graph/test_research_node.py</files>
  <read_first>.planning/REQUIREMENTS.md
.planning/phases/02-quiver-research-and-ranking/02-RESEARCH.md
app/schemas/workflow.py
app/agents/research.py
tests/graph/test_research_node.py</read_first>
  <behavior>
    - Test 1: A JSON response with `{\"outcome\": \"candidates\"}` parses into `CandidateOutcome`.
    - Test 2: A JSON response with `{\"outcome\": \"watchlist\"}` parses into `WatchlistOutcome`.
    - Test 3: A JSON response with `{\"outcome\": \"no_action\"}` parses into `NoActionOutcome`.
  </behavior>
  <action>Create `app/schemas/research.py` and move Phase 2 research contracts into it. Define `CandidateRecommendation` with these exact fields: `ticker`, `action`, `conviction_score`, `supporting_evidence`, `opposing_evidence`, `risk_notes`, `source_summary`, and `broker_eligible`. Define `CandidateOutcome`, `WatchlistOutcome`, and `NoActionOutcome`, then define `ResearchOutcome` as an `Annotated[...]` discriminated union on the `outcome` field. Update `app/schemas/workflow.py` so it keeps workflow/email-facing models and re-exports or aliases the new research models instead of owning the old flat `ResearchResult`. Replace the JSON fixture in `tests/graph/test_research_node.py` so it asserts parsed `outcome` values and evidence arrays, not only `ticker`.</action>
  <acceptance_criteria>- `rg -n \"class CandidateRecommendation|class CandidateOutcome|class WatchlistOutcome|class NoActionOutcome\" app/schemas/research.py` returns all four class names.
- `rg -n \"ResearchOutcome = Annotated\\[\" app/schemas/research.py` returns the discriminated union definition.
- `rg -n \"supporting_evidence|opposing_evidence|risk_notes|source_summary|broker_eligible\" app/schemas/research.py tests/graph/test_research_node.py` returns all five field names.
- `pytest tests/graph/test_research_node.py -q` exits with code 0.</acceptance_criteria>
  <verify>
    <automated>pytest tests/graph/test_research_node.py -q</automated>
  </verify>
  <done>The codebase has a validated discriminated research outcome model that makes evidence, counter-evidence, risks, and source summaries first-class schema fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the research prompt payload and parse structured outcomes in the research node</name>
  <files>app/agents/research.py
app/services/research_prompt.py
tests/graph/test_research_node.py
tests/services/test_research_prompt.py</files>
  <read_first>.planning/phases/02-quiver-research-and-ranking/02-RESEARCH.md
app/agents/research.py
app/schemas/research.py
app/schemas/quiver.py</read_first>
  <behavior>
    - Test 1: `build_research_prompt_payload()` returns a dict with `system` and `user` keys that explicitly mention `candidates`, `watchlist`, and `no_action`.
    - Test 2: `ResearchNode.run()` passes a payload built from `TickerEvidenceBundle` input to the LLM and parses the JSON with `TypeAdapter(ResearchOutcome)`.
    - Test 3: Invalid outcome shapes raise a validation error instead of silently returning partial data.
  </behavior>
  <action>Create `app/services/research_prompt.py` exporting `build_research_prompt_payload(run_id: str, evidence_bundles: list[TickerEvidenceBundle], account_context: dict[str, str]) -> dict[str, str]`. The payload must contain a `system` prompt that instructs the model to return JSON only and a `user` prompt that includes, for each ticker, supporting signals, contradictory signals, and source summaries from the normalized bundles. The prompt must instruct the model to choose exactly one `outcome` value from `candidates`, `watchlist`, or `no_action`, and to populate `supporting_evidence`, `opposing_evidence`, `risk_notes`, and `source_summary` arrays on every recommendation-like item. Update `app/agents/research.py` so `ResearchNode.__init__` accepts an optional `prompt_builder` defaulting to `build_research_prompt_payload`, and change `run` to accept `run_id`, `evidence_bundles`, and `account_context`, call the prompt builder, invoke the LLM with that payload, and parse the JSON with `TypeAdapter(ResearchOutcome).validate_json(response)`.</action>
  <acceptance_criteria>- `rg -n \"def build_research_prompt_payload\" app/services/research_prompt.py` returns the function definition.
- `rg -n \"candidates|watchlist|no_action|JSON only|supporting_evidence|opposing_evidence|risk_notes|source_summary\" app/services/research_prompt.py` returns all required prompt/output literals.
- `rg -n \"prompt_builder|TypeAdapter\\(ResearchOutcome\\)|validate_json\" app/agents/research.py` returns the new parsing and injection points.
- `pytest tests/graph/test_research_node.py tests/services/test_research_prompt.py -q` exits with code 0.</acceptance_criteria>
  <verify>
    <automated>pytest tests/graph/test_research_node.py tests/services/test_research_prompt.py -q</automated>
  </verify>
  <done>The research node consumes normalized evidence bundles, builds an explicit prompt contract, and returns one validated outcome branch with evidence-rich fields.</done>
</task>

</tasks>

<verification>
Run `pytest tests/graph/test_research_node.py tests/services/test_research_prompt.py -q` and confirm the research layer now validates one of three explicit outcome branches with evidence arrays in every recommendation-like item.
</verification>

<success_criteria>
`RSCH-02` is covered by schema and agent code that force supporting evidence, contradictory evidence, risk notes, and source summaries into validated research outputs instead of free-form rationale strings.
</success_criteria>

<output>
After completion, create `.planning/phases/02-quiver-research-and-ranking/02-02-SUMMARY.md`
</output>

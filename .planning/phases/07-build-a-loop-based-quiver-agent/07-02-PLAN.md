---
phase: 07-build-a-loop-based-quiver-agent
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - app/agents/quiver_loop.py
  - app/agents/research.py
  - app/graph/workflow.py
  - tests/agents/test_quiver_loop.py
  - tests/graph/test_research_node.py
  - tests/graph/test_workflow.py
  - tests/api/test_routes.py
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03
must_haves:
  truths:
    - "One research run can make multiple Quiver follow-up calls after the deterministic seed pass, with explicit budgets and stop reasons."
    - "Paused workflow state contains a structured research trace that explains why the agent inspected another ticker or dataset."
    - "Ranking, memo rendering, approval, and broker handoff still consume the same finalized `ResearchOutcome` and candidate recommendation shapes."
  artifacts:
    - path: "app/agents/quiver_loop.py"
      provides: "App-owned bounded serial Quiver loop executor"
      contains: "class QuiverLoopAgent"
    - path: "app/agents/research.py"
      provides: "Backward-compatible ResearchNode plus trace-aware execution method"
      contains: "def run_with_trace"
    - path: "app/graph/workflow.py"
      provides: "Workflow integration that persists research trace metadata alongside finalized outcomes"
      contains: "research_trace"
  key_links:
    - from: "app/agents/quiver_loop.py"
      to: "app/tools/quiver.py"
      via: "Loop follow-up calls reuse existing ticker-scoped Quiver client methods"
      pattern: "get_live_congress_trading|get_live_insider_trading|get_live_government_contracts|get_live_lobbying"
    - from: "app/graph/workflow.py"
      to: "app/services/ranking.py"
      via: "Workflow still finalizes the agent's validated ResearchOutcome through deterministic Python ranking"
      pattern: "finalize_research_outcome"
---

<objective>
Implement the bounded Quiver loop and wire its trace into the runtime without breaking downstream contracts.

Purpose: Deliver the actual Phase 7 agent behavior while keeping the existing ranking, email, approval, and broker-prestage pipeline stable.
Output: A `QuiverLoopAgent`, a trace-aware `ResearchNode`, and workflow/API tests proving persisted trace metadata plus backward-compatible finalized outputs.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-build-a-loop-based-quiver-agent/07-RESEARCH.md
@.planning/phases/02-quiver-research-and-ranking/02-03-SUMMARY.md
@.planning/phases/04-hitl-and-broker-prestage/04-03-SUMMARY.md
@app/agents/research.py
@app/graph/workflow.py
@app/tools/quiver.py
@app/services/ranking.py
@tests/graph/test_research_node.py
@tests/graph/test_workflow.py
@tests/api/test_routes.py

<interfaces>
From 07-01 planned output:
```python
class ResearchAgentBudget(BaseModel):
    max_steps: int
    max_tool_calls: int
    max_seed_tickers: int


class ResearchExecutionResult(BaseModel):
    outcome: ResearchOutcome
    trace: list[AgentTraceStep]
    stop_reason: str
    tool_call_count: int
    investigated_tickers: list[str]
```

From app/graph/workflow.py:
```python
class CompiledInvestorWorkflow:
    def invoke(self, state: dict) -> dict:
        ...

    def resume(self, state: dict, decision: str) -> dict:
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the bounded Quiver loop with deterministic seed-and-follow-up behavior</name>
  <files>app/agents/quiver_loop.py
tests/agents/test_quiver_loop.py</files>
  <behavior>
    - Test 1: The loop starts from the existing deterministic seed evidence bundles, limits the initial shortlist to `max_seed_tickers`, and never re-runs untickered global scans inside the loop.
    - Test 2: Each follow-up turn executes exactly one ticker-scoped Quiver lookup using an existing `QuiverClient.get_live_*` method with `ticker=` set.
    - Test 3: The loop returns a typed `ResearchExecutionResult` with explicit `stop_reason`, `tool_call_count`, and `investigated_tickers`, whether it stops on final answer, empty ticker set, or budget exhaustion.
  </behavior>
  <action>Create `app/agents/quiver_loop.py` with `QuiverLoopAgent`. The agent must accept the tool-capable LLM from 07-01, a `ResearchAgentBudget`, and an optional shortlist selection strategy that defaults to the strongest seed tickers from the normalized evidence bundles. Implement the loop as app-owned Python, not prompt-only pseudo-reasoning: bootstrap prompt/messages from the new prompt builders, call `complete_with_tools(...)`, validate the assistant response into a typed decision, execute exactly one ticker-scoped Quiver call per turn, append an `AgentTraceStep`, and repeat until a final answer or stop condition. Use only the existing typed Quiver client methods with `ticker=` arguments for follow-ups. Do not move pruning, broker eligibility, watchlist downgrade, or memo logic into this module; the output of the loop must still be a raw `ResearchOutcome` awaiting `finalize_research_outcome()`.</action>
  <verify>
    <automated>pytest tests/agents/test_quiver_loop.py -q</automated>
  </verify>
  <done>The repo has an app-owned loop executor that can perform multiple bounded Quiver follow-up calls and emit a typed trace plus a validated `ResearchOutcome` candidate/watchlist/no-action branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire trace-aware loop execution into ResearchNode, workflow state, and persisted trigger payloads</name>
  <files>app/agents/research.py
app/graph/workflow.py
tests/graph/test_research_node.py
tests/graph/test_workflow.py
tests/api/test_routes.py</files>
  <behavior>
    - Test 1: `ResearchNode.run_with_trace()` accepts `quiver_client` and returns `ResearchExecutionResult`, while `run()` remains backward-compatible and returns only `ResearchOutcome`.
    - Test 2: Workflow invoke stores `research_trace`, `research_stop_reason`, `research_tool_call_count`, and `investigated_tickers` on the paused state alongside `finalized_outcome` and `recommendations`.
    - Test 3: The trigger route persists those new trace fields in the run `state_payload`, and approval/handoff still operate on candidate recommendations exactly as before.
  </behavior>
  <action>Update `app/agents/research.py` so `ResearchNode` grows a `run_with_trace(run_id, evidence_bundles, account_context, quiver_client) -> ResearchExecutionResult` method that delegates to `QuiverLoopAgent`, while the existing `run(...) -> ResearchOutcome` method returns `run_with_trace(...).outcome` for backward compatibility. Update `app/graph/workflow.py` to call `run_with_trace`, keep `finalize_research_outcome(execution.outcome)` in place, and persist the trace metadata fields onto workflow state before the review pause. Extend workflow and route tests so the paused/persisted payload proves multiple tool calls, explicit stop reason metadata, and unchanged `recommendations` behavior for candidate vs watchlist/no-action branches. Per the user constraint and Phase 6 direction, wire this through app-owned runtime seams only; do not add new LangGraph-specific names, checkpoint payloads, or replay abstractions.</action>
  <verify>
    <automated>pytest tests/graph/test_research_node.py tests/graph/test_workflow.py tests/api/test_routes.py -q</automated>
  </verify>
  <done>Phase 7 runtime behavior is live: the workflow uses a bounded loop-based Quiver agent, persists structured trace metadata, and still hands a backward-compatible finalized outcome into ranking, email, approval, and broker handoff.</done>
</task>

</tasks>

<verification>
Run `pytest tests/agents/test_quiver_loop.py tests/graph/test_research_node.py tests/graph/test_workflow.py tests/api/test_routes.py -q` and confirm the paused trigger payload now records multi-step investigation trace data while the finalized outcome contract remains unchanged downstream.
</verification>

<success_criteria>
`AGENT-01`, `AGENT-02`, and `AGENT-03` are satisfied in runtime code: the app can loop over Quiver follow-ups within budget, persist why it investigated further, and still feed the existing ranking/email/approval pipeline with a valid `ResearchOutcome`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-build-a-loop-based-quiver-agent/07-02-SUMMARY.md`
</output>

---
phase: 07-build-a-loop-based-quiver-agent
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - app/config.py
  - app/main.py
  - app/ops/readiness.py
  - app/ops/dry_run.py
  - tests/ops/test_readiness.py
  - tests/ops/test_dry_run.py
  - tests/services/test_research_llm.py
autonomous: true
requirements:
  - AGENT-01
  - AGENT-03
must_haves:
  truths:
    - "Normal runtime composition instantiates the loop agent through app-owned settings and composition seams, not hidden defaults or graph/checkpointer concepts."
    - "Startup and dry-run verification catch invalid loop budgets or broken tool-calling runtime paths before an operator relies on the app."
    - "The repo-owned dry run still reaches broker prestage and now proves the loop agent made bounded follow-up calls before finalizing."
    - "Readiness fails with a clear operator-facing diagnostic when the configured OpenAI-compatible provider lacks required tool-calling capability for the loop path."
  artifacts:
    - path: "app/config.py"
      provides: "Env-backed loop-agent budget settings"
      contains: "research_agent_max_steps"
    - path: "app/ops/readiness.py"
      provides: "Loop-agent operational validation and provider-capability gating"
      contains: "assert_startup_readiness"
    - path: "app/ops/dry_run.py"
      provides: "Deterministic local proof of loop-agent execution"
      contains: "research_tool_call_count"
    - path: "tests/services/test_research_llm.py"
      provides: "Capability-probe and unsupported-provider fallback coverage"
      contains: "test_provider_capability_probe"
  key_links:
    - from: "app/main.py"
      to: "app/agents/research.py"
      via: "App composition injects loop-agent settings into the research node"
      pattern: "research_agent_max_steps|research_agent_max_tool_calls|research_agent_max_seed_tickers"
    - from: "app/ops/dry_run.py"
      to: "app/graph/workflow.py"
      via: "Dry run exercises the same scheduled trigger and approval route while asserting loop metadata"
      pattern: "research_tool_call_count|research_stop_reason|investigated_tickers"
    - from: "app/ops/readiness.py"
      to: "app/services/research_llm.py"
      via: "Startup verifies the configured provider supports the minimum tool-calling contract required by the loop agent, or fails loudly with a tested fallback diagnostic"
      pattern: "complete_with_tools|provider_capability_missing"
---

<objective>
Finish the Phase 7 operational surface so loop-agent behavior is configured, validated, and regression-tested through the repo-owned dry run.

Purpose: Keep the app env-ready after the research cutover by making the loop settings explicit and proving the new agent path through the existing operator workflow.
Output: Env-backed loop budgets, readiness checks, and dry-run/tests that confirm the loop agent still lands in the same downstream pipeline.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-build-a-loop-based-quiver-agent/07-RESEARCH.md
@.planning/phases/05-operational-readiness/05-operational-readiness-01-SUMMARY.md
@app/config.py
@app/main.py
@app/ops/readiness.py
@app/ops/dry_run.py
@tests/ops/test_readiness.py
@tests/ops/test_dry_run.py
@tests/services/test_research_llm.py

<interfaces>
From app/config.py:
```python
class Settings(BaseSettings):
    openai_api_key: str
    openai_base_url: str
    openai_model: str
    approval_token_ttl_seconds: int
```

From 07-02 planned output:
```python
class ResearchExecutionResult(BaseModel):
    outcome: ResearchOutcome
    trace: list[AgentTraceStep]
    stop_reason: str
    tool_call_count: int
    investigated_tickers: list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add env-backed loop-agent settings plus provider-capability validation to normal app composition</name>
  <files>app/config.py
app/main.py
app/ops/readiness.py
tests/ops/test_readiness.py
tests/services/test_research_llm.py</files>
  <behavior>
    - Test 1: `Settings` exposes positive integer loop controls for `research_agent_max_steps`, `research_agent_max_tool_calls`, and `research_agent_max_seed_tickers`.
    - Test 2: readiness rejects zero or negative loop budgets with explicit diagnostics instead of allowing an unusable runtime configuration.
    - Test 3: readiness or startup capability checks fail with an explicit operator-facing error when the configured `openai_base_url` path cannot satisfy tool-calling requirements for `complete_with_tools()`.
    - Test 4: `create_app()` composes the Phase 7 research path from settings through app-owned seams and still avoids hidden Quiver/LLM defaults.
  </behavior>
  <action>Extend `app/config.py` with env-backed integer settings named `research_agent_max_steps`, `research_agent_max_tool_calls`, and `research_agent_max_seed_tickers`. Update `app/main.py` to instantiate the Phase 7 `ResearchNode` / loop-agent path from those settings using the existing explicit composition seam from Phase 5. Update `app/ops/readiness.py` and `tests/ops/test_readiness.py` so startup blocks when any of the new budget values are less than 1, and add a provider-capability probe or equivalent startup validation that verifies the configured `openai_base_url` supports the minimum `complete_with_tools()` contract required by Phase 7. Extend `tests/services/test_research_llm.py` or adjacent composition coverage to assert both the supported-provider path and the unsupported-provider diagnostic/fallback path. Keep the implementation app-owned; do not surface or depend on LangGraph-era config names.</action>
  <verify>
    <automated>pytest tests/ops/test_readiness.py tests/services/test_research_llm.py -q</automated>
  </verify>
  <done>Loop-agent budgets are operator-configurable, invalid values or unsupported providers fail fast at startup, and normal app composition wires the new research path through explicit app-owned settings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update the deterministic dry run to prove multi-step loop execution and downstream compatibility</name>
  <files>app/ops/dry_run.py
tests/ops/test_dry_run.py</files>
  <behavior>
    - Test 1: `python -m app.ops.dry_run` still completes scheduled trigger through approval and broker prestage locally.
    - Test 2: The emitted JSON now includes `research_tool_call_count`, `research_stop_reason`, and `investigated_tickers`, proving the loop agent executed bounded follow-ups before finalization.
    - Test 3: The dry-run path remains deterministic and local-only by injecting Quiver and research doubles instead of relying on live external services.
  </behavior>
  <action>Update `app/ops/dry_run.py` and `tests/ops/test_dry_run.py` so the dry-run harness exercises the real loop-agent workflow shape rather than a one-shot research response. Use explicit local doubles for Quiver and the tool-calling research adapter, make the loop take at least two research turns before finalizing, and include `research_tool_call_count`, `research_stop_reason`, and `investigated_tickers` in the printed JSON summary alongside the existing trigger/approval/artifact fields. The dry run must keep proving the same downstream contract: candidate/watchlist/no-action output still flows through ranking, email, approval, and broker prestage without manual work.</action>
  <verify>
    <automated>pytest tests/ops/test_dry_run.py -q</automated>
  </verify>
  <done>The repo-owned dry run proves the new loop-agent path end to end and exposes operator-readable loop metadata without breaking the existing trigger-to-prestage proof.</done>
</task>

</tasks>

<verification>
Run `pytest tests/ops/test_readiness.py tests/services/test_research_llm.py tests/ops/test_dry_run.py -q`. Then run `python -m app.ops.dry_run` and confirm the JSON output still ends at `broker_prestaged` while including the Phase 7 loop metadata fields.
</verification>

<success_criteria>
The loop-agent cutover remains operationally safe: normal app startup uses explicit loop settings, bad loop budgets fail fast, and the dry-run command proves a bounded multi-step research run still lands in the existing approval and broker-prestage flow.
</success_criteria>

<output>
After completion, create `.planning/phases/07-build-a-loop-based-quiver-agent/07-03-SUMMARY.md`
</output>

---
phase: 07-build-a-loop-based-quiver-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/research_agent.py
  - app/services/research_prompt.py
  - app/services/research_llm.py
  - tests/services/test_research_prompt.py
  - tests/services/test_research_llm.py
autonomous: true
requirements:
  - AGENT-01
must_haves:
  truths:
    - "Research uses an app-owned serial tool loop with explicit max-step and max-tool-call budgets instead of a single prompt pass."
    - "Each assistant turn resolves to one typed tool request, a typed final answer, or a typed stop reason."
    - "The tool-calling seam stays provider-portable by extending the existing OpenAI-compatible HTTP adapter rather than introducing a new agent framework."
  artifacts:
    - path: "app/schemas/research_agent.py"
      provides: "Loop budget, decision, and trace contracts"
      contains: "class AgentTraceStep"
    - path: "app/services/research_prompt.py"
      provides: "Loop-system, seed-brief, and final-synthesis prompt builders"
      contains: "def build_quiver_loop_system_prompt"
    - path: "app/services/research_llm.py"
      provides: "OpenAI-compatible serial tool-calling adapter"
      contains: "def complete_with_tools"
  key_links:
    - from: "app/services/research_prompt.py"
      to: "app/schemas/quiver.py"
      via: "Prompt builders summarize canonical TickerEvidenceBundle inputs"
      pattern: "TickerEvidenceBundle"
    - from: "app/services/research_llm.py"
      to: "app/schemas/research_agent.py"
      via: "Tool-enabled turns are parsed into typed decision contracts"
      pattern: "ToolCallRequest|FinalDecision|StopDecision"
---

<objective>
Define the loop-agent contracts and provider-portable tool-calling seam for Phase 7.

Purpose: Establish the app-owned interfaces that later plans will implement against, without deepening LangGraph-era coupling.
Output: Typed loop-agent schemas, loop/final prompt builders, and a tool-capable `HttpResearchLLM` interface with focused tests.
</objective>

<execution_context>
@/Users/nickbohm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/nickbohm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-build-a-loop-based-quiver-agent/07-RESEARCH.md
@.planning/phases/02-quiver-research-and-ranking/02-02-SUMMARY.md
@.planning/phases/05-operational-readiness/05-operational-readiness-01-SUMMARY.md
@app/agents/research.py
@app/services/research_prompt.py
@app/services/research_llm.py
@tests/services/test_research_prompt.py
@tests/services/test_research_llm.py

<interfaces>
From app/agents/research.py:
```python
class InvokableLLM(Protocol):
    def invoke(self, payload: dict[str, str]) -> str:
        ...


class ResearchNode:
    def run(
        self,
        run_id: str,
        evidence_bundles: List[TickerEvidenceBundle],
        account_context: dict[str, str],
    ) -> ResearchOutcome:
        ...
```

From app/schemas/research.py:
```python
ResearchOutcome = Annotated[
    Union[CandidateOutcome, WatchlistOutcome, NoActionOutcome],
    Field(discriminator="outcome"),
]
```

From app/services/research_llm.py:
```python
class HttpResearchLLM:
    def invoke(self, payload: dict[str, str]) -> str:
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define loop-agent schemas and prompt builders against the existing ResearchOutcome contract</name>
  <files>app/schemas/research_agent.py
app/services/research_prompt.py
tests/services/test_research_prompt.py</files>
  <behavior>
    - Test 1: `build_quiver_loop_system_prompt()` names the allowed tool set, enforces serial one-tool-at-a-time behavior, and states explicit stop conditions and budget limits.
    - Test 2: `build_seed_research_brief()` summarizes canonical `TickerEvidenceBundle` inputs into a bounded shortlist the agent can investigate next.
    - Test 3: `build_final_research_payload()` still instructs the model to emit exactly one valid `ResearchOutcome` branch with evidence arrays so downstream ranking and email code stay unchanged.
  </behavior>
  <action>Create `app/schemas/research_agent.py` with typed loop contracts: `ResearchAgentBudget`, `ToolCallRequest`, `ToolDecision`, `FinalDecision`, `StopDecision`, `AgentDecision`, `AgentTraceStep`, and `ResearchExecutionResult`. `ResearchExecutionResult` must carry `outcome: ResearchOutcome`, `trace: list[AgentTraceStep]`, `stop_reason`, `tool_call_count`, and `investigated_tickers`. Update `app/services/research_prompt.py` to add `build_quiver_loop_system_prompt`, `build_seed_research_brief`, and `build_final_research_payload`. Keep `build_research_prompt_payload` as a backward-compatible wrapper around the new final-synthesis payload so existing callers do not break mid-phase. Per the Phase 7 research and Phase 6 direction, all prompt builders must describe app-owned loop behavior and must not mention LangGraph, checkpoints, or replay semantics.</action>
  <verify>
    <automated>pytest tests/services/test_research_prompt.py -q</automated>
  </verify>
  <done>The repo has a typed loop-agent contract and prompt surface that can drive a bounded serial Quiver investigation while preserving the existing `ResearchOutcome` final shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the existing HTTP research adapter for serial tool-calling turns</name>
  <files>app/services/research_llm.py
tests/services/test_research_llm.py</files>
  <behavior>
    - Test 1: `HttpResearchLLM.complete_with_tools()` posts an OpenAI-compatible `/chat/completions` payload containing `tools`, `tool_choice=\"auto\"`, and `parallel_tool_calls=false`.
    - Test 2: The method returns the assistant message payload with `tool_calls` preserved so the app-owned loop can validate and execute one request at a time.
    - Test 3: The existing plain `invoke()` path remains valid for final JSON-only turns and the Phase 5 dry-run harness.
  </behavior>
  <action>Update `app/services/research_llm.py` to keep the current `invoke(payload)` interface for simple JSON-only calls, but add `complete_with_tools(*, messages: list[dict[str, object]], tools: list[dict[str, object]], tool_choice: str = "auto", parallel_tool_calls: bool = False) -> dict[str, object]`. Use the same `httpx.Client`, base URL, and bearer-auth seam already established in Phase 5. Do not adopt the OpenAI SDK or any third-party agent package. The implementation must POST raw JSON to `/chat/completions`, preserve provider portability through the existing OpenAI-compatible interface, and leave decision parsing to the app-owned schema layer from Task 1.</action>
  <verify>
    <automated>pytest tests/services/test_research_llm.py -q</automated>
  </verify>
  <done>The research adapter can handle both final JSON synthesis turns and serial tool-calling turns through the same provider-portable HTTP seam.</done>
</task>

</tasks>

<verification>
Run `pytest tests/services/test_research_prompt.py tests/services/test_research_llm.py -q` and confirm the repo now has typed loop-agent contracts plus a tool-capable `HttpResearchLLM` without changing the `ResearchOutcome` final schema.
</verification>

<success_criteria>
`AGENT-01` is satisfied at the interface level: the codebase has explicit bounded-loop contracts, explicit stop/budget concepts, and a tool-calling LLM seam ready for app-owned execution.
</success_criteria>

<output>
After completion, create `.planning/phases/07-build-a-loop-based-quiver-agent/07-01-SUMMARY.md`
</output>
